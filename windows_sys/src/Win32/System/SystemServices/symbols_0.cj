// Generated by windows_bindgen. DO NOT EDIT.

package windows_sys.Win32.System.SystemServices

import windows_polyfill as windows_polyfill

@When[Windows_Win32_Graphics_Gdi == "on"]
import windows_sys.Win32.Graphics.Gdi as Windows_Win32_Graphics_Gdi
@When[Windows_Win32_System_Com == "on"]
import windows_sys.Win32.System.Com as Windows_Win32_System_Com

// Windows.Win32.System.SystemServices.RTL_UMS_SCHEDULER_REASON (synthetic:Windows_Win32_System_SystemServices_RTL_UMS_SCHEDULER_REASON)
@When[Windows_Win32_System_SystemServices == "on"]
public type RTL_UMS_SCHEDULER_REASON = Int32
@When[Windows_Win32_System_SystemServices == "on"]
public let RTL_UMS_SCHEDULER_REASON_UmsSchedulerStartup: RTL_UMS_SCHEDULER_REASON = 0
@When[Windows_Win32_System_SystemServices == "on"]
public let RTL_UMS_SCHEDULER_REASON_UmsSchedulerThreadBlocked: RTL_UMS_SCHEDULER_REASON = 1
@When[Windows_Win32_System_SystemServices == "on"]
public let RTL_UMS_SCHEDULER_REASON_UmsSchedulerThreadYield: RTL_UMS_SCHEDULER_REASON = 2

// Windows.Win32.System.SystemServices.remoteMETAFILEPICT (synthetic:Windows_Win32_System_SystemServices_remoteMETAFILEPICT)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct remoteMETAFILEPICT {
    public var mm: Int32 = 0i32
    public var xExt: Int32 = 0i32
    public var yExt: Int32 = 0i32
    public var hMF: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.System.SystemServices.userBITMAP (synthetic:Windows_Win32_System_SystemServices_userBITMAP)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userBITMAP {
    public var bmType: Int32 = 0i32
    public var bmWidth: Int32 = 0i32
    public var bmHeight: Int32 = 0i32
    public var bmWidthBytes: Int32 = 0i32
    public var bmPlanes: UInt16 = 0u16
    public var bmBitsPixel: UInt16 = 0u16
    public var cbSize: UInt32 = 0u32
    public var pBuffer: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.System.SystemServices.userHBITMAP (synthetic:Windows_Win32_System_SystemServices_userHBITMAP)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHBITMAP {
    public var fContext: Int32 = 0i32
    public var u: userHBITMAP__u_e__Struct = userHBITMAP__u_e__Struct()
}

// Windows.Win32.System.SystemServices.userHBITMAP._u_e__Struct (synthetic:Windows_Win32_System_SystemServices_userHBITMAP__u_e__Struct)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHBITMAP__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.System.SystemServices.userHENHMETAFILE (synthetic:Windows_Win32_System_SystemServices_userHENHMETAFILE)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHENHMETAFILE {
    public var fContext: Int32 = 0i32
    public var u: userHENHMETAFILE__u_e__Struct = userHENHMETAFILE__u_e__Struct()
}

// Windows.Win32.System.SystemServices.userHENHMETAFILE._u_e__Struct (synthetic:Windows_Win32_System_SystemServices_userHENHMETAFILE__u_e__Struct)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHENHMETAFILE__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.System.SystemServices.userHGLOBAL (synthetic:Windows_Win32_System_SystemServices_userHGLOBAL)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHGLOBAL {
    public var fContext: Int32 = 0i32
    public var u: userHGLOBAL__u_e__Struct = userHGLOBAL__u_e__Struct()
}

// Windows.Win32.System.SystemServices.userHGLOBAL._u_e__Struct (synthetic:Windows_Win32_System_SystemServices_userHGLOBAL__u_e__Struct)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHGLOBAL__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.System.SystemServices.userHMETAFILE (synthetic:Windows_Win32_System_SystemServices_userHMETAFILE)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHMETAFILE {
    public var fContext: Int32 = 0i32
    public var u: userHMETAFILE__u_e__Struct = userHMETAFILE__u_e__Struct()
}

// Windows.Win32.System.SystemServices.userHMETAFILE._u_e__Struct (synthetic:Windows_Win32_System_SystemServices_userHMETAFILE__u_e__Struct)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHMETAFILE__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.System.SystemServices.userHMETAFILEPICT (synthetic:Windows_Win32_System_SystemServices_userHMETAFILEPICT)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHMETAFILEPICT {
    public var fContext: Int32 = 0i32
    public var u: userHMETAFILEPICT__u_e__Struct = userHMETAFILEPICT__u_e__Struct()
}

// Windows.Win32.System.SystemServices.userHMETAFILEPICT._u_e__Struct (synthetic:Windows_Win32_System_SystemServices_userHMETAFILEPICT__u_e__Struct)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHMETAFILEPICT__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.System.SystemServices.userHPALETTE (synthetic:Windows_Win32_System_SystemServices_userHPALETTE)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHPALETTE {
    public var fContext: Int32 = 0i32
    public var u: userHPALETTE__u_e__Struct = userHPALETTE__u_e__Struct()
}

// Windows.Win32.System.SystemServices.userHPALETTE._u_e__Struct (synthetic:Windows_Win32_System_SystemServices_userHPALETTE__u_e__Struct)
@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHPALETTE__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
}
