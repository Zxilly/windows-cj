// Generated by windows_bindgen. DO NOT EDIT.

package windows_sys.Win32.System.Variant

import windows_polyfill as windows_polyfill

@When[Windows_Win32_Foundation == "on"]
import windows_sys.Win32.Foundation as Windows_Win32_Foundation
@When[Windows_Win32_System_Com == "on"]
import windows_sys.Win32.System.Com as Windows_Win32_System_Com
@When[Windows_Win32_System_Ole == "on"]
import windows_sys.Win32.System.Ole as Windows_Win32_System_Ole

// Windows.Win32.System.Variant.VARENUM (synthetic:Windows_Win32_System_Variant_VARENUM)
@When[Windows_Win32_System_Variant == "on"]
public type VARENUM = UInt16
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_EMPTY: VARENUM = 0
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_NULL: VARENUM = 1
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_I2: VARENUM = 2
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_I4: VARENUM = 3
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_R4: VARENUM = 4
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_R8: VARENUM = 5
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_CY: VARENUM = 6
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_DATE: VARENUM = 7
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_BSTR: VARENUM = 8
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_DISPATCH: VARENUM = 9
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_ERROR: VARENUM = 10
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_BOOL: VARENUM = 11
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_VARIANT: VARENUM = 12
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_UNKNOWN: VARENUM = 13
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_DECIMAL: VARENUM = 14
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_I1: VARENUM = 16
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_UI1: VARENUM = 17
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_UI2: VARENUM = 18
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_UI4: VARENUM = 19
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_I8: VARENUM = 20
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_UI8: VARENUM = 21
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_INT: VARENUM = 22
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_UINT: VARENUM = 23
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_VOID: VARENUM = 24
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_HRESULT: VARENUM = 25
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_PTR: VARENUM = 26
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_SAFEARRAY: VARENUM = 27
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_CARRAY: VARENUM = 28
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_USERDEFINED: VARENUM = 29
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_LPSTR: VARENUM = 30
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_LPWSTR: VARENUM = 31
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_RECORD: VARENUM = 36
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_INT_PTR: VARENUM = 37
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_UINT_PTR: VARENUM = 38
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_FILETIME: VARENUM = 64
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_BLOB: VARENUM = 65
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_STREAM: VARENUM = 66
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_STORAGE: VARENUM = 67
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_STREAMED_OBJECT: VARENUM = 68
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_STORED_OBJECT: VARENUM = 69
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_BLOB_OBJECT: VARENUM = 70
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_CF: VARENUM = 71
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_CLSID: VARENUM = 72
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_VERSIONED_STREAM: VARENUM = 73
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_BSTR_BLOB: VARENUM = 4095
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_VECTOR: VARENUM = 4096
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_ARRAY: VARENUM = 8192
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_BYREF: VARENUM = 16384
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_RESERVED: VARENUM = 32768
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_ILLEGAL: VARENUM = 65535
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_ILLEGALMASKED: VARENUM = 4095
@When[Windows_Win32_System_Variant == "on"]
public let VARENUM_VT_TYPEMASK: VARENUM = 4095

// Windows.Win32.System.Variant.VARIANT (synthetic:Windows_Win32_System_Variant_VARIANT)
@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARIANT {
    public var Anonymous: VARIANT__Anonymous_e__Union = VARIANT__Anonymous_e__Union()
}

// Windows.Win32.System.Variant.VARIANT._Anonymous_e__Union (synthetic:Windows_Win32_System_Variant_VARIANT__Anonymous_e__Union)
@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARIANT__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)

    public mut prop Anonymous: VARIANT__Anonymous_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop decVal: Windows_Win32_Foundation.DECIMAL {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Windows_Win32_Foundation.DECIMAL>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Windows_Win32_Foundation.DECIMAL>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.System.Variant.VARIANT._Anonymous_e__Union._Anonymous_e__Struct (synthetic:Windows_Win32_System_Variant_VARIANT__Anonymous_e__Union__Anonymous_e__Struct)
@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct {
    public var vt: UInt16 = 0u16
    public var wReserved1: UInt16 = 0u16
    public var wReserved2: UInt16 = 0u16
    public var wReserved3: UInt16 = 0u16
    public var Anonymous: VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union = VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union()
}

// Windows.Win32.System.Variant.VARIANT._Anonymous_e__Union._Anonymous_e__Struct._Anonymous_e__Union (synthetic:Windows_Win32_System_Variant_VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union)
@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop llVal: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop lVal: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop bVal: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt8(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt8(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop iVal: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fltVal: Float32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadFloat32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteFloat32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dblVal: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadFloat64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteFloat64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop boolVal: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop __OBSOLETE__VARIANT_BOOL: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop scode: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cyVal: Windows_Win32_System_Com.CY {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Windows_Win32_System_Com.CY>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Windows_Win32_System_Com.CY>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop date: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadFloat64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteFloat64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop bstrVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop punkVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pdispVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop parray: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pbVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop piVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop plVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pllVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pfltVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pdblVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pboolVal: CPointer<Int16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop __OBSOLETE__VARIANT_PBOOL: CPointer<Int16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pscode: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pcyVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pdate: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pbstrVal: CPointer<CPointer<Unit>> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ppunkVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ppdispVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pparray: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pvarVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop byref: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cVal: Int8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt8(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt8(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uiVal: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ulVal: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ullVal: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop intVal: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uintVal: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pdecVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pcVal: CPointer<UInt8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop puiVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pulVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pullVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pintVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop puintVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Anonymous: VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.System.Variant.VARIANT._Anonymous_e__Union._Anonymous_e__Struct._Anonymous_e__Union._Anonymous_e__Struct (synthetic:Windows_Win32_System_Variant_VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct)
@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct {
    public var pvRecord: CPointer<Unit> = CPointer<Unit>()
    public var pRecInfo: CPointer<Unit> = CPointer<Unit>()
}
