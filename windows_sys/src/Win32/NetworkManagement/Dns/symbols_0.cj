// Generated by windows_bindgen. DO NOT EDIT.

package windows_sys.Win32.NetworkManagement.Dns

import windows_polyfill as windows_polyfill

@When[Windows_Win32_Foundation == "on"]
import windows_sys.Win32.Foundation as Windows_Win32_Foundation

// Windows.Win32.NetworkManagement.Dns.DNS_AAAA_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_AAAA_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_AAAA_DATA {
    public var Ip6Address: IP6_ADDRESS = IP6_ADDRESS()
}

// Windows.Win32.NetworkManagement.Dns.DNS_ADDR (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_ADDR)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_ADDR {
    public var MaxSa: VArray<Int8, $32> = VArray<Int8, $32>(repeat: 0i8)
    public var Data: DNS_ADDR__Data_e__Union = DNS_ADDR__Data_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_ADDR._Data_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_ADDR__Data_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_ADDR__Data_e__Union {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    public mut prop DnsAddrUserDword: VArray<UInt32, $8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt32, $8>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt32, $8>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_ADDR_ARRAY (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_ADDR_ARRAY)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_ADDR_ARRAY {
    public var MaxCount: UInt32 = 0u32
    public var AddrCount: UInt32 = 0u32
    public var Tag: UInt32 = 0u32
    public var Family: UInt16 = 0u16
    public var WordReserved: UInt16 = 0u16
    public var Flags: UInt32 = 0u32
    public var MatchFlag: UInt32 = 0u32
    public var Reserved1: UInt32 = 0u32
    public var Reserved2: UInt32 = 0u32
    public var AddrArray: VArray<DNS_ADDR, $1> = VArray<DNS_ADDR, $1>(repeat: DNS_ADDR())
}

// Windows.Win32.NetworkManagement.Dns.DNS_APPLICATION_SETTINGS (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_APPLICATION_SETTINGS)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_APPLICATION_SETTINGS {
    public var Version: UInt32 = 0u32
    public var Flags: UInt64 = 0u64
}

// Windows.Win32.NetworkManagement.Dns.DNS_ATMA_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_ATMA_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_ATMA_DATA {
    public var AddressType: UInt8 = 0u8
    public var Address: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_A_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_A_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_A_DATA {
    public var IpAddress: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_CHARSET (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CHARSET)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_CHARSET {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CHARSET_DnsCharSetUnknown = DNS_CHARSET(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CHARSET_DnsCharSetUnicode = DNS_CHARSET(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CHARSET_DnsCharSetUtf8 = DNS_CHARSET(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CHARSET_DnsCharSetAnsi = DNS_CHARSET(3)

// Windows.Win32.NetworkManagement.Dns.DNS_CONFIG_TYPE (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONFIG_TYPE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_CONFIG_TYPE {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigPrimaryDomainName_W = DNS_CONFIG_TYPE(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigPrimaryDomainName_A = DNS_CONFIG_TYPE(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigPrimaryDomainName_UTF8 = DNS_CONFIG_TYPE(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigAdapterDomainName_W = DNS_CONFIG_TYPE(3)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigAdapterDomainName_A = DNS_CONFIG_TYPE(4)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigAdapterDomainName_UTF8 = DNS_CONFIG_TYPE(5)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigDnsServerList = DNS_CONFIG_TYPE(6)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigSearchList = DNS_CONFIG_TYPE(7)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigAdapterInfo = DNS_CONFIG_TYPE(8)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigPrimaryHostNameRegistrationEnabled = DNS_CONFIG_TYPE(9)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigAdapterHostNameRegistrationEnabled = DNS_CONFIG_TYPE(10)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigAddressRegistrationMaxCount = DNS_CONFIG_TYPE(11)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigHostName_W = DNS_CONFIG_TYPE(12)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigHostName_A = DNS_CONFIG_TYPE(13)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigHostName_UTF8 = DNS_CONFIG_TYPE(14)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigFullHostName_W = DNS_CONFIG_TYPE(15)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigFullHostName_A = DNS_CONFIG_TYPE(16)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigFullHostName_UTF8 = DNS_CONFIG_TYPE(17)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONFIG_TYPE_DnsConfigNameServer = DNS_CONFIG_TYPE(18)

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_IFINDEX_ENTRY (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_IFINDEX_ENTRY)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_IFINDEX_ENTRY {
    public var pwszConnectionName: CPointer<UInt16> = CPointer<UInt16>()
    public var dwIfIndex: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_IFINDEX_LIST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_IFINDEX_LIST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_IFINDEX_LIST {
    public var pConnectionIfIndexEntries: CPointer<Unit> = CPointer<Unit>()
    public var nEntries: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_NAME (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_NAME)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_NAME {
    public var wszName: VArray<UInt16, $65> = VArray<UInt16, $65>(repeat: 0u16)
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_NAME_LIST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_NAME_LIST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_NAME_LIST {
    public var cNames: UInt32 = 0u32
    public var pNames: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_POLICY_ENTRY (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_POLICY_ENTRY)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_POLICY_ENTRY {
    public var pwszHost: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszAppId: CPointer<UInt16> = CPointer<UInt16>()
    public var cbAppSid: UInt32 = 0u32
    public var pbAppSid: CPointer<Unit> = CPointer<Unit>()
    public var nConnections: UInt32 = 0u32
    public var ppwszConnections: CPointer<CPointer<UInt16>> = CPointer<CPointer<UInt16>>()
    public var dwPolicyEntryFlags: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_POLICY_ENTRY_LIST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_POLICY_ENTRY_LIST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_POLICY_ENTRY_LIST {
    public var pPolicyEntries: CPointer<Unit> = CPointer<Unit>()
    public var nEntries: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_POLICY_TAG (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_POLICY_TAG)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_CONNECTION_POLICY_TAG {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_POLICY_TAG_TAG_DNS_CONNECTION_POLICY_TAG_DEFAULT = DNS_CONNECTION_POLICY_TAG(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_POLICY_TAG_TAG_DNS_CONNECTION_POLICY_TAG_CONNECTION_MANAGER = DNS_CONNECTION_POLICY_TAG(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_POLICY_TAG_TAG_DNS_CONNECTION_POLICY_TAG_WWWPT = DNS_CONNECTION_POLICY_TAG(2)

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_ELEMENT (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_ELEMENT)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_ELEMENT {
    public var Type: Int32 = 0i32
    public var Info: DNS_CONNECTION_PROXY_INFO = DNS_CONNECTION_PROXY_INFO()
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_INFO (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_INFO)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO {
    public var Version: UInt32 = 0u32
    public var pwszFriendlyName: CPointer<UInt16> = CPointer<UInt16>()
    public var Flags: UInt32 = 0u32
    public var Switch: Int32 = 0i32
    public var Anonymous: DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union = DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_INFO._Anonymous_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)

    public mut prop Config: DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Script: DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_INFO._Anonymous_e__Union._DNS_CONNECTION_PROXY_INFO_CONFIG (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG {
    public var pwszServer: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszUsername: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszPassword: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszException: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszExtraInfo: CPointer<UInt16> = CPointer<UInt16>()
    public var Port: UInt16 = 0u16
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_INFO._Anonymous_e__Union._DNS_CONNECTION_PROXY_INFO_SCRIPT (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT {
    public var pwszScript: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszUsername: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszPassword: CPointer<UInt16> = CPointer<UInt16>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_INFO_EX (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_INFO_EX)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO_EX {
    public var ProxyInfo: DNS_CONNECTION_PROXY_INFO = DNS_CONNECTION_PROXY_INFO()
    public var dwInterfaceIndex: UInt32 = 0u32
    public var pwszConnectionName: CPointer<UInt16> = CPointer<UInt16>()
    public var fDirectConfiguration: Int32 = 0i32
    public var hConnection: Windows_Win32_Foundation.HANDLE = Windows_Win32_Foundation.HANDLE()
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_INFO_SWITCH (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_INFO_SWITCH)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_CONNECTION_PROXY_INFO_SWITCH {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_PROXY_INFO_SWITCH_DNS_CONNECTION_PROXY_INFO_SWITCH_CONFIG = DNS_CONNECTION_PROXY_INFO_SWITCH(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_PROXY_INFO_SWITCH_DNS_CONNECTION_PROXY_INFO_SWITCH_SCRIPT = DNS_CONNECTION_PROXY_INFO_SWITCH(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_PROXY_INFO_SWITCH_DNS_CONNECTION_PROXY_INFO_SWITCH_WPAD = DNS_CONNECTION_PROXY_INFO_SWITCH(2)

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_LIST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_LIST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_LIST {
    public var cProxies: UInt32 = 0u32
    public var pProxies: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_CONNECTION_PROXY_TYPE (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CONNECTION_PROXY_TYPE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_CONNECTION_PROXY_TYPE {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_PROXY_TYPE_DNS_CONNECTION_PROXY_TYPE_NULL = DNS_CONNECTION_PROXY_TYPE(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_PROXY_TYPE_DNS_CONNECTION_PROXY_TYPE_HTTP = DNS_CONNECTION_PROXY_TYPE(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_PROXY_TYPE_DNS_CONNECTION_PROXY_TYPE_WAP = DNS_CONNECTION_PROXY_TYPE(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_PROXY_TYPE_DNS_CONNECTION_PROXY_TYPE_SOCKS4 = DNS_CONNECTION_PROXY_TYPE(4)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_CONNECTION_PROXY_TYPE_DNS_CONNECTION_PROXY_TYPE_SOCKS5 = DNS_CONNECTION_PROXY_TYPE(5)

// Windows.Win32.NetworkManagement.Dns.DNS_CUSTOM_SERVER (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CUSTOM_SERVER)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CUSTOM_SERVER {
    public var dwServerType: UInt32 = 0u32
    public var ullFlags: UInt64 = 0u64
    public var Anonymous1: DNS_CUSTOM_SERVER__Anonymous1_e__Union = DNS_CUSTOM_SERVER__Anonymous1_e__Union()
    public var Anonymous2: DNS_CUSTOM_SERVER__Anonymous2_e__Union = DNS_CUSTOM_SERVER__Anonymous2_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_CUSTOM_SERVER._Anonymous1_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CUSTOM_SERVER__Anonymous1_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CUSTOM_SERVER__Anonymous1_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop pwszTemplate: CPointer<UInt16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pwszHostname: CPointer<UInt16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_CUSTOM_SERVER._Anonymous2_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_CUSTOM_SERVER__Anonymous2_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_CUSTOM_SERVER__Anonymous2_e__Union {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    public mut prop MaxSa: VArray<Int8, $32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<Int8, $32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<Int8, $32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_DHCID_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_DHCID_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_DHCID_DATA {
    public var dwByteCount: UInt32 = 0u32
    public var DHCID: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_DS_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_DS_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_DS_DATA {
    public var wKeyTag: UInt16 = 0u16
    public var chAlgorithm: UInt8 = 0u8
    public var chDigestType: UInt8 = 0u8
    public var wDigestLength: UInt16 = 0u16
    public var wPad: UInt16 = 0u16
    public var Digest: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_FREE_TYPE (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_FREE_TYPE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_FREE_TYPE {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_FREE_TYPE_DnsFreeFlat = DNS_FREE_TYPE(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_FREE_TYPE_DnsFreeRecordList = DNS_FREE_TYPE(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_FREE_TYPE_DnsFreeParsedMessageFields = DNS_FREE_TYPE(2)

// Windows.Win32.NetworkManagement.Dns.DNS_HEADER (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_HEADER)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_HEADER {
    public var Xid: UInt16 = 0u16
    public var _bitfield1: UInt8 = 0u8

    public mut prop RecursionDesired: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield1, 0, 1)
        }
        set(val) {
            _bitfield1 = windows_polyfill.bitfieldWriteUInt8(_bitfield1, 0, 1, val)
        }
    }

    public mut prop Truncation: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield1, 1, 1)
        }
        set(val) {
            _bitfield1 = windows_polyfill.bitfieldWriteUInt8(_bitfield1, 1, 1, val)
        }
    }

    public mut prop Authoritative: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield1, 2, 1)
        }
        set(val) {
            _bitfield1 = windows_polyfill.bitfieldWriteUInt8(_bitfield1, 2, 1, val)
        }
    }

    public mut prop Opcode: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield1, 3, 4)
        }
        set(val) {
            _bitfield1 = windows_polyfill.bitfieldWriteUInt8(_bitfield1, 3, 4, val)
        }
    }

    public mut prop IsResponse: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield1, 7, 1)
        }
        set(val) {
            _bitfield1 = windows_polyfill.bitfieldWriteUInt8(_bitfield1, 7, 1, val)
        }
    }
    public var _bitfield2: UInt8 = 0u8

    public mut prop ResponseCode: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield2, 0, 4)
        }
        set(val) {
            _bitfield2 = windows_polyfill.bitfieldWriteUInt8(_bitfield2, 0, 4, val)
        }
    }

    public mut prop CheckingDisabled: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield2, 4, 1)
        }
        set(val) {
            _bitfield2 = windows_polyfill.bitfieldWriteUInt8(_bitfield2, 4, 1, val)
        }
    }

    public mut prop AuthenticatedData: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield2, 5, 1)
        }
        set(val) {
            _bitfield2 = windows_polyfill.bitfieldWriteUInt8(_bitfield2, 5, 1, val)
        }
    }

    public mut prop Reserved: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield2, 6, 1)
        }
        set(val) {
            _bitfield2 = windows_polyfill.bitfieldWriteUInt8(_bitfield2, 6, 1, val)
        }
    }

    public mut prop RecursionAvailable: UInt8 {
        get() {
            return windows_polyfill.bitfieldReadUInt8(_bitfield2, 7, 1)
        }
        set(val) {
            _bitfield2 = windows_polyfill.bitfieldWriteUInt8(_bitfield2, 7, 1, val)
        }
    }
    public var QuestionCount: UInt16 = 0u16
    public var AnswerCount: UInt16 = 0u16
    public var NameServerCount: UInt16 = 0u16
    public var AdditionalCount: UInt16 = 0u16
}

// Windows.Win32.NetworkManagement.Dns.DNS_HEADER_EXT (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_HEADER_EXT)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_HEADER_EXT {
    public var _bitfield: UInt16 = 0u16

    public mut prop Reserved: UInt16 {
        get() {
            return windows_polyfill.bitfieldReadUInt16(_bitfield, 0, 15)
        }
        set(val) {
            _bitfield = windows_polyfill.bitfieldWriteUInt16(_bitfield, 0, 15, val)
        }
    }

    public mut prop DnssecOk: UInt16 {
        get() {
            return windows_polyfill.bitfieldReadUInt16(_bitfield, 15, 1)
        }
        set(val) {
            _bitfield = windows_polyfill.bitfieldWriteUInt16(_bitfield, 15, 1, val)
        }
    }
    public var chRcode: UInt8 = 0u8
    public var chVersion: UInt8 = 0u8
}

// Windows.Win32.NetworkManagement.Dns.DNS_KEY_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_KEY_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_KEY_DATA {
    public var wFlags: UInt16 = 0u16
    public var chProtocol: UInt8 = 0u8
    public var chAlgorithm: UInt8 = 0u8
    public var wKeyLength: UInt16 = 0u16
    public var wPad: UInt16 = 0u16
    public var Key: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_LOC_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_LOC_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_LOC_DATA {
    public var wVersion: UInt16 = 0u16
    public var wSize: UInt16 = 0u16
    public var wHorPrec: UInt16 = 0u16
    public var wVerPrec: UInt16 = 0u16
    public var dwLatitude: UInt32 = 0u32
    public var dwLongitude: UInt32 = 0u32
    public var dwAltitude: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_MESSAGE_BUFFER (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_MESSAGE_BUFFER)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_MESSAGE_BUFFER {
    public var MessageHead: DNS_HEADER = DNS_HEADER()
    public var MessageBody: VArray<Int8, $1> = VArray<Int8, $1>(repeat: 0i8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_MINFO_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_MINFO_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_MINFO_DATAA {
    public var pNameMailbox: CPointer<UInt8> = CPointer<UInt8>()
    public var pNameErrorsMailbox: CPointer<UInt8> = CPointer<UInt8>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_MINFO_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_MINFO_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_MINFO_DATAW {
    public var pNameMailbox: CPointer<UInt16> = CPointer<UInt16>()
    public var pNameErrorsMailbox: CPointer<UInt16> = CPointer<UInt16>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_MX_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_MX_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_MX_DATAA {
    public var pNameExchange: CPointer<UInt8> = CPointer<UInt8>()
    public var wPreference: UInt16 = 0u16
    public var Pad: UInt16 = 0u16
}

// Windows.Win32.NetworkManagement.Dns.DNS_MX_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_MX_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_MX_DATAW {
    public var pNameExchange: CPointer<UInt16> = CPointer<UInt16>()
    public var wPreference: UInt16 = 0u16
    public var Pad: UInt16 = 0u16
}

// Windows.Win32.NetworkManagement.Dns.DNS_NAME_FORMAT (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NAME_FORMAT)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_NAME_FORMAT {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_NAME_FORMAT_DnsNameDomain = DNS_NAME_FORMAT(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_NAME_FORMAT_DnsNameDomainLabel = DNS_NAME_FORMAT(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_NAME_FORMAT_DnsNameHostnameFull = DNS_NAME_FORMAT(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_NAME_FORMAT_DnsNameHostnameLabel = DNS_NAME_FORMAT(3)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_NAME_FORMAT_DnsNameWildcard = DNS_NAME_FORMAT(4)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_NAME_FORMAT_DnsNameSrvRecord = DNS_NAME_FORMAT(5)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_NAME_FORMAT_DnsNameValidateTld = DNS_NAME_FORMAT(6)

// Windows.Win32.NetworkManagement.Dns.DNS_NAPTR_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NAPTR_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NAPTR_DATAA {
    public var wOrder: UInt16 = 0u16
    public var wPreference: UInt16 = 0u16
    public var pFlags: CPointer<UInt8> = CPointer<UInt8>()
    public var pService: CPointer<UInt8> = CPointer<UInt8>()
    public var pRegularExpression: CPointer<UInt8> = CPointer<UInt8>()
    public var pReplacement: CPointer<UInt8> = CPointer<UInt8>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_NAPTR_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NAPTR_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NAPTR_DATAW {
    public var wOrder: UInt16 = 0u16
    public var wPreference: UInt16 = 0u16
    public var pFlags: CPointer<UInt16> = CPointer<UInt16>()
    public var pService: CPointer<UInt16> = CPointer<UInt16>()
    public var pRegularExpression: CPointer<UInt16> = CPointer<UInt16>()
    public var pReplacement: CPointer<UInt16> = CPointer<UInt16>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_NSEC3PARAM_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NSEC3PARAM_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NSEC3PARAM_DATA {
    public var chAlgorithm: UInt8 = 0u8
    public var bFlags: UInt8 = 0u8
    public var wIterations: UInt16 = 0u16
    public var bSaltLength: UInt8 = 0u8
    public var bPad: VArray<UInt8, $3> = VArray<UInt8, $3>(repeat: 0u8)
    public var pbSalt: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_NSEC3_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NSEC3_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NSEC3_DATA {
    public var chAlgorithm: UInt8 = 0u8
    public var bFlags: UInt8 = 0u8
    public var wIterations: UInt16 = 0u16
    public var bSaltLength: UInt8 = 0u8
    public var bHashLength: UInt8 = 0u8
    public var wTypeBitMapsLength: UInt16 = 0u16
    public var chData: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_NSEC_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NSEC_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NSEC_DATAA {
    public var pNextDomainName: CPointer<UInt8> = CPointer<UInt8>()
    public var wTypeBitMapsLength: UInt16 = 0u16
    public var wPad: UInt16 = 0u16
    public var TypeBitMaps: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_NSEC_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NSEC_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NSEC_DATAW {
    public var pNextDomainName: CPointer<UInt16> = CPointer<UInt16>()
    public var wTypeBitMapsLength: UInt16 = 0u16
    public var wPad: UInt16 = 0u16
    public var TypeBitMaps: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_NULL_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NULL_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NULL_DATA {
    public var dwByteCount: UInt32 = 0u32
    public var Data: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_NXT_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NXT_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NXT_DATAA {
    public var pNameNext: CPointer<UInt8> = CPointer<UInt8>()
    public var wNumTypes: UInt16 = 0u16
    public var wTypes: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0u16)
}

// Windows.Win32.NetworkManagement.Dns.DNS_NXT_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_NXT_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_NXT_DATAW {
    public var pNameNext: CPointer<UInt16> = CPointer<UInt16>()
    public var wNumTypes: UInt16 = 0u16
    public var wTypes: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0u16)
}

// Windows.Win32.NetworkManagement.Dns.DNS_OPT_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_OPT_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_OPT_DATA {
    public var wDataLength: UInt16 = 0u16
    public var wPad: UInt16 = 0u16
    public var Data: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_PROXY_COMPLETION_ROUTINE (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_PROXY_COMPLETION_ROUTINE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public type DNS_PROXY_COMPLETION_ROUTINE = CFunc<(CPointer<Unit>, Int32) -> Unit>

// Windows.Win32.NetworkManagement.Dns.DNS_PROXY_INFORMATION (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_PROXY_INFORMATION)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_PROXY_INFORMATION {
    public var version: UInt32 = 0u32
    public var proxyInformationType: Int32 = 0i32
    public var proxyName: CPointer<UInt16> = CPointer<UInt16>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_PROXY_INFORMATION_TYPE (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_PROXY_INFORMATION_TYPE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_PROXY_INFORMATION_TYPE {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_PROXY_INFORMATION_TYPE_DNS_PROXY_INFORMATION_DIRECT = DNS_PROXY_INFORMATION_TYPE(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_PROXY_INFORMATION_TYPE_DNS_PROXY_INFORMATION_DEFAULT_SETTINGS = DNS_PROXY_INFORMATION_TYPE(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_PROXY_INFORMATION_TYPE_DNS_PROXY_INFORMATION_PROXY_NAME = DNS_PROXY_INFORMATION_TYPE(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_PROXY_INFORMATION_TYPE_DNS_PROXY_INFORMATION_DOES_NOT_EXIST = DNS_PROXY_INFORMATION_TYPE(3)

// Windows.Win32.NetworkManagement.Dns.DNS_PTR_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_PTR_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_PTR_DATAA {
    public var pNameHost: CPointer<UInt8> = CPointer<UInt8>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_PTR_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_PTR_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_PTR_DATAW {
    public var pNameHost: CPointer<UInt16> = CPointer<UInt16>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_CANCEL (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_CANCEL)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_CANCEL {
    public var Reserved: VArray<Int8, $32> = VArray<Int8, $32>(repeat: 0i8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_OPTIONS (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_OPTIONS)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_QUERY_OPTIONS {
    public let value: UInt32
    public const init() {
        this.value = 0
    }
    public const init(value: UInt32) {
        this.value = value
    }
    public func contains(other: DNS_QUERY_OPTIONS): Bool {
        return (value & other.value) == other.value
    }
    public operator func |(other: DNS_QUERY_OPTIONS): DNS_QUERY_OPTIONS {
        return DNS_QUERY_OPTIONS(value | other.value)
    }
    public operator func &(other: DNS_QUERY_OPTIONS): DNS_QUERY_OPTIONS {
        return DNS_QUERY_OPTIONS(value & other.value)
    }
    public operator func ^(other: DNS_QUERY_OPTIONS): DNS_QUERY_OPTIONS {
        return DNS_QUERY_OPTIONS(value ^ other.value)
    }
    public operator func !(): DNS_QUERY_OPTIONS {
        return DNS_QUERY_OPTIONS(!value)
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_PARSE_ALL_RECORDS = DNS_QUERY_OPTIONS(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_STANDARD = DNS_QUERY_OPTIONS(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_ACCEPT_TRUNCATED_RESPONSE = DNS_QUERY_OPTIONS(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_USE_TCP_ONLY = DNS_QUERY_OPTIONS(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_NO_RECURSION = DNS_QUERY_OPTIONS(4)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_BYPASS_CACHE = DNS_QUERY_OPTIONS(8)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_NO_WIRE_QUERY = DNS_QUERY_OPTIONS(16)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_NO_LOCAL_NAME = DNS_QUERY_OPTIONS(32)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_NO_HOSTS_FILE = DNS_QUERY_OPTIONS(64)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_NO_NETBT = DNS_QUERY_OPTIONS(128)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_WIRE_ONLY = DNS_QUERY_OPTIONS(256)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_RETURN_MESSAGE = DNS_QUERY_OPTIONS(512)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_MULTICAST_ONLY = DNS_QUERY_OPTIONS(1024)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_NO_MULTICAST = DNS_QUERY_OPTIONS(2048)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_TREAT_AS_FQDN = DNS_QUERY_OPTIONS(4096)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_ADDRCONFIG = DNS_QUERY_OPTIONS(8192)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_DUAL_ADDR = DNS_QUERY_OPTIONS(16384)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_DONT_RESET_TTL_VALUES = DNS_QUERY_OPTIONS(1048576)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_DISABLE_IDN_ENCODING = DNS_QUERY_OPTIONS(2097152)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_APPEND_MULTILABEL = DNS_QUERY_OPTIONS(8388608)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_DNSSEC_OK = DNS_QUERY_OPTIONS(16777216)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_DNSSEC_CHECKING_DISABLED = DNS_QUERY_OPTIONS(33554432)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_DNSSEC_REQUIRED = DNS_QUERY_OPTIONS(67108864)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_RESERVED = DNS_QUERY_OPTIONS(4026531840)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_CACHE_ONLY = DNS_QUERY_OPTIONS(16)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_REQUEST_VERSION1 = DNS_QUERY_OPTIONS(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_REQUEST_VERSION2 = DNS_QUERY_OPTIONS(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_RESULTS_VERSION1 = DNS_QUERY_OPTIONS(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_REQUEST_VERSION3 = DNS_QUERY_OPTIONS(3)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_RAW_RESULTS_VERSION1 = DNS_QUERY_OPTIONS(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_RAW_REQUEST_VERSION1 = DNS_QUERY_OPTIONS(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_QUERY_OPTIONS_DNS_QUERY_RAW_OPTION_BEST_EFFORT_PARSE = DNS_QUERY_OPTIONS(1)

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_RAW_CANCEL (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_RAW_CANCEL)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_RAW_CANCEL {
    public var reserved: VArray<Int8, $32> = VArray<Int8, $32>(repeat: 0i8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_RAW_COMPLETION_ROUTINE (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_RAW_COMPLETION_ROUTINE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public type DNS_QUERY_RAW_COMPLETION_ROUTINE = CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit>

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_RAW_REQUEST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_RAW_REQUEST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_RAW_REQUEST {
    public var version: UInt32 = 0u32
    public var resultsVersion: UInt32 = 0u32
    public var dnsQueryRawSize: UInt32 = 0u32
    public var dnsQueryRaw: CPointer<Unit> = CPointer<Unit>()
    public var dnsQueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var dnsQueryType: UInt16 = 0u16
    public var queryOptions: UInt64 = 0u64
    public var interfaceIndex: UInt32 = 0u32
    public var queryCompletionCallback: CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit> = CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit>(CPointer<Unit>())
    public var queryContext: CPointer<Unit> = CPointer<Unit>()
    public var queryRawOptions: UInt64 = 0u64
    public var customServersSize: UInt32 = 0u32
    public var customServers: CPointer<Unit> = CPointer<Unit>()
    public var protocol: UInt32 = 0u32
    public var Anonymous: DNS_QUERY_RAW_REQUEST__Anonymous_e__Union = DNS_QUERY_RAW_REQUEST__Anonymous_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_RAW_REQUEST._Anonymous_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_RAW_REQUEST__Anonymous_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_RAW_REQUEST__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    public mut prop maxSa: VArray<Int8, $32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<Int8, $32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<Int8, $32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_RAW_RESULT (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_RAW_RESULT)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_RAW_RESULT {
    public var version: UInt32 = 0u32
    public var queryStatus: Int32 = 0i32
    public var queryOptions: UInt64 = 0u64
    public var queryRawOptions: UInt64 = 0u64
    public var responseFlags: UInt64 = 0u64
    public var queryRawResponseSize: UInt32 = 0u32
    public var queryRawResponse: CPointer<Unit> = CPointer<Unit>()
    public var queryRecords: CPointer<Unit> = CPointer<Unit>()
    public var protocol: UInt32 = 0u32
    public var Anonymous: DNS_QUERY_RAW_RESULT__Anonymous_e__Union = DNS_QUERY_RAW_RESULT__Anonymous_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_RAW_RESULT._Anonymous_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_RAW_RESULT__Anonymous_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_RAW_RESULT__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    public mut prop maxSa: VArray<Int8, $32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<Int8, $32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<Int8, $32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_REQUEST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_REQUEST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_REQUEST {
    public var Version: UInt32 = 0u32
    public var QueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var QueryType: UInt16 = 0u16
    public var QueryOptions: UInt64 = 0u64
    public var pDnsServerList: CPointer<Unit> = CPointer<Unit>()
    public var InterfaceIndex: UInt32 = 0u32
    public var pQueryCompletionCallback: CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit> = CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit>(CPointer<Unit>())
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_REQUEST3 (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_REQUEST3)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_REQUEST3 {
    public var Version: UInt32 = 0u32
    public var QueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var QueryType: UInt16 = 0u16
    public var QueryOptions: UInt64 = 0u64
    public var pDnsServerList: CPointer<Unit> = CPointer<Unit>()
    public var InterfaceIndex: UInt32 = 0u32
    public var pQueryCompletionCallback: CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit> = CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit>(CPointer<Unit>())
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public var IsNetworkQueryRequired: Int32 = 0i32
    public var RequiredNetworkIndex: UInt32 = 0u32
    public var cCustomServers: UInt32 = 0u32
    public var pCustomServers: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_QUERY_RESULT (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_QUERY_RESULT)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_QUERY_RESULT {
    public var Version: UInt32 = 0u32
    public var QueryStatus: Int32 = 0i32
    public var QueryOptions: UInt64 = 0u64
    public var pQueryRecords: CPointer<Unit> = CPointer<Unit>()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORDA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORDA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORDA {
    public var pNext: CPointer<Unit> = CPointer<Unit>()
    public var pName: CPointer<UInt8> = CPointer<UInt8>()
    public var wType: UInt16 = 0u16
    public var wDataLength: UInt16 = 0u16
    public var Flags: DNS_RECORDA__Flags_e__Union = DNS_RECORDA__Flags_e__Union()
    public var dwTtl: UInt32 = 0u32
    public var dwReserved: UInt32 = 0u32
    public var Data: DNS_RECORDA__Data_e__Union = DNS_RECORDA__Data_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORDA._Data_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORDA__Data_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORDA__Data_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop A: DNS_A_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_A_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_A_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop SOA: DNS_SOA_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SOA_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SOA_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Soa: DNS_SOA_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SOA_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SOA_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop PTR: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Ptr: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NS: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Ns: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop CNAME: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Cname: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop DNAME: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Dname: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MB: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mb: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MD: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Md: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MF: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mf: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MG: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mg: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MR: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mr: DNS_PTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MINFO: DNS_MINFO_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Minfo: DNS_MINFO_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop RP: DNS_MINFO_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Rp: DNS_MINFO_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MX: DNS_MX_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mx: DNS_MX_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop AFSDB: DNS_MX_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Afsdb: DNS_MX_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop RT: DNS_MX_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Rt: DNS_MX_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop HINFO: DNS_TXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Hinfo: DNS_TXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ISDN: DNS_TXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Isdn: DNS_TXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop TXT: DNS_TXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Txt: DNS_TXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop X25: DNS_TXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Null: DNS_NULL_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NULL_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NULL_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop WKS: DNS_WKS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WKS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WKS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Wks: DNS_WKS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WKS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WKS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop AAAA: DNS_AAAA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_AAAA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_AAAA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop KEY: DNS_KEY_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Key: DNS_KEY_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop SIG: DNS_SIG_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Sig: DNS_SIG_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ATMA: DNS_ATMA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_ATMA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_ATMA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Atma: DNS_ATMA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_ATMA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_ATMA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NXT: DNS_NXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nxt: DNS_NXT_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NXT_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NXT_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop SRV: DNS_SRV_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SRV_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SRV_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Srv: DNS_SRV_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SRV_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SRV_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NAPTR: DNS_NAPTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NAPTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NAPTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Naptr: DNS_NAPTR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NAPTR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NAPTR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop OPT: DNS_OPT_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Opt: DNS_OPT_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop DS: DNS_DS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Ds: DNS_DS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop RRSIG: DNS_SIG_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Rrsig: DNS_SIG_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NSEC: DNS_NSEC_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nsec: DNS_NSEC_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop DNSKEY: DNS_KEY_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Dnskey: DNS_KEY_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop TKEY: DNS_TKEY_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TKEY_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TKEY_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Tkey: DNS_TKEY_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TKEY_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TKEY_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop TSIG: DNS_TSIG_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TSIG_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TSIG_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Tsig: DNS_TSIG_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TSIG_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TSIG_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop WINS: DNS_WINS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Wins: DNS_WINS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop WINSR: DNS_WINSR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop WinsR: DNS_WINSR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NBSTAT: DNS_WINSR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nbstat: DNS_WINSR_DATAA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop DHCID: DNS_DHCID_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DHCID_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DHCID_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NSEC3: DNS_NSEC3_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nsec3: DNS_NSEC3_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NSEC3PARAM: DNS_NSEC3PARAM_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nsec3Param: DNS_NSEC3PARAM_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop TLSA: DNS_TLSA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TLSA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TLSA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Tlsa: DNS_TLSA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TLSA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TLSA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop SVCB: DNS_SVCB_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SVCB_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SVCB_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Svcb: DNS_SVCB_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SVCB_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SVCB_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop UNKNOWN: DNS_UNKNOWN_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Unknown: DNS_UNKNOWN_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pDataPtr: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORDA._Flags_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORDA__Flags_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORDA__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0u32)

    public mut prop DW: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop S: DNS_RECORD_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_RECORD_FLAGS>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_RECORD_FLAGS>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORDW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORDW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORDW {
    public var pNext: CPointer<Unit> = CPointer<Unit>()
    public var pName: CPointer<UInt16> = CPointer<UInt16>()
    public var wType: UInt16 = 0u16
    public var wDataLength: UInt16 = 0u16
    public var Flags: DNS_RECORDW__Flags_e__Union = DNS_RECORDW__Flags_e__Union()
    public var dwTtl: UInt32 = 0u32
    public var dwReserved: UInt32 = 0u32
    public var Data: DNS_RECORDW__Data_e__Union = DNS_RECORDW__Data_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORDW._Data_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORDW__Data_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORDW__Data_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop A: DNS_A_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_A_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_A_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop SOA: DNS_SOA_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SOA_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SOA_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Soa: DNS_SOA_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SOA_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SOA_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop PTR: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Ptr: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NS: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Ns: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop CNAME: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Cname: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop DNAME: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Dname: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MB: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mb: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MD: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Md: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MF: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mf: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MG: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mg: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MR: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mr: DNS_PTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_PTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MINFO: DNS_MINFO_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Minfo: DNS_MINFO_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop RP: DNS_MINFO_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Rp: DNS_MINFO_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MINFO_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop MX: DNS_MX_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Mx: DNS_MX_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop AFSDB: DNS_MX_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Afsdb: DNS_MX_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop RT: DNS_MX_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Rt: DNS_MX_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_MX_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop HINFO: DNS_TXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Hinfo: DNS_TXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ISDN: DNS_TXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Isdn: DNS_TXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop TXT: DNS_TXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Txt: DNS_TXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop X25: DNS_TXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Null: DNS_NULL_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NULL_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NULL_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop WKS: DNS_WKS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WKS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WKS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Wks: DNS_WKS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WKS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WKS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop AAAA: DNS_AAAA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_AAAA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_AAAA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop KEY: DNS_KEY_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Key: DNS_KEY_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop SIG: DNS_SIG_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Sig: DNS_SIG_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ATMA: DNS_ATMA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_ATMA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_ATMA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Atma: DNS_ATMA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_ATMA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_ATMA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NXT: DNS_NXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nxt: DNS_NXT_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NXT_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NXT_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop SRV: DNS_SRV_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SRV_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SRV_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Srv: DNS_SRV_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SRV_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SRV_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NAPTR: DNS_NAPTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NAPTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NAPTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Naptr: DNS_NAPTR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NAPTR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NAPTR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop OPT: DNS_OPT_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Opt: DNS_OPT_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop DS: DNS_DS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Ds: DNS_DS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop RRSIG: DNS_SIG_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Rrsig: DNS_SIG_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SIG_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NSEC: DNS_NSEC_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nsec: DNS_NSEC_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop DNSKEY: DNS_KEY_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Dnskey: DNS_KEY_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_KEY_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop TKEY: DNS_TKEY_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TKEY_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TKEY_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Tkey: DNS_TKEY_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TKEY_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TKEY_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop TSIG: DNS_TSIG_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TSIG_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TSIG_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Tsig: DNS_TSIG_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TSIG_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TSIG_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop WINS: DNS_WINS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Wins: DNS_WINS_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINS_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINS_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop WINSR: DNS_WINSR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop WinsR: DNS_WINSR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NBSTAT: DNS_WINSR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nbstat: DNS_WINSR_DATAW {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAW>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_WINSR_DATAW>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop DHCID: DNS_DHCID_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DHCID_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_DHCID_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NSEC3: DNS_NSEC3_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nsec3: DNS_NSEC3_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop NSEC3PARAM: DNS_NSEC3PARAM_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Nsec3Param: DNS_NSEC3PARAM_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop TLSA: DNS_TLSA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TLSA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TLSA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Tlsa: DNS_TLSA_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TLSA_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_TLSA_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop SVCB: DNS_SVCB_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SVCB_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SVCB_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Svcb: DNS_SVCB_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SVCB_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_SVCB_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop UNKNOWN: DNS_UNKNOWN_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Unknown: DNS_UNKNOWN_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pDataPtr: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORDW._Flags_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORDW__Flags_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORDW__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0u32)

    public mut prop DW: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop S: DNS_RECORD_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_RECORD_FLAGS>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_RECORD_FLAGS>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORD_FLAGS (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORD_FLAGS)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORD_FLAGS {
    public var _bitfield: UInt32 = 0u32

    public mut prop Section: UInt32 {
        get() {
            return windows_polyfill.bitfieldReadUInt32(_bitfield, 0, 2)
        }
        set(val) {
            _bitfield = windows_polyfill.bitfieldWriteUInt32(_bitfield, 0, 2, val)
        }
    }

    public mut prop Delete: UInt32 {
        get() {
            return windows_polyfill.bitfieldReadUInt32(_bitfield, 2, 1)
        }
        set(val) {
            _bitfield = windows_polyfill.bitfieldWriteUInt32(_bitfield, 2, 1, val)
        }
    }

    public mut prop CharSet: UInt32 {
        get() {
            return windows_polyfill.bitfieldReadUInt32(_bitfield, 3, 2)
        }
        set(val) {
            _bitfield = windows_polyfill.bitfieldWriteUInt32(_bitfield, 3, 2, val)
        }
    }

    public mut prop Unused: UInt32 {
        get() {
            return windows_polyfill.bitfieldReadUInt32(_bitfield, 5, 3)
        }
        set(val) {
            _bitfield = windows_polyfill.bitfieldWriteUInt32(_bitfield, 5, 3, val)
        }
    }

    public mut prop Reserved: UInt32 {
        get() {
            return windows_polyfill.bitfieldReadUInt32(_bitfield, 8, 24)
        }
        set(val) {
            _bitfield = windows_polyfill.bitfieldWriteUInt32(_bitfield, 8, 24, val)
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORD_OPTW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORD_OPTW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORD_OPTW {
    public var pNext: CPointer<Unit> = CPointer<Unit>()
    public var pName: CPointer<UInt16> = CPointer<UInt16>()
    public var wType: UInt16 = 0u16
    public var wDataLength: UInt16 = 0u16
    public var Flags: DNS_RECORD_OPTW__Flags_e__Union = DNS_RECORD_OPTW__Flags_e__Union()
    public var ExtHeader: DNS_HEADER_EXT = DNS_HEADER_EXT()
    public var wPayloadSize: UInt16 = 0u16
    public var wReserved: UInt16 = 0u16
    public var Data: DNS_RECORD_OPTW__Data_e__Union = DNS_RECORD_OPTW__Data_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORD_OPTW._Data_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORD_OPTW__Data_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORD_OPTW__Data_e__Union {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)

    public mut prop OPT: DNS_OPT_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Opt: DNS_OPT_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_RECORD_OPTW._Flags_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RECORD_OPTW__Flags_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RECORD_OPTW__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0u32)

    public mut prop DW: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop S: DNS_RECORD_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_RECORD_FLAGS>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_RECORD_FLAGS>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_RRSET (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_RRSET)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_RRSET {
    public var pFirstRR: CPointer<Unit> = CPointer<Unit>()
    public var pLastRR: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_SECTION (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SECTION)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_SECTION {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SECTION_DnsSectionQuestion = DNS_SECTION(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SECTION_DnsSectionAnswer = DNS_SECTION(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SECTION_DnsSectionAuthority = DNS_SECTION(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SECTION_DnsSectionAddtional = DNS_SECTION(3)

// Windows.Win32.NetworkManagement.Dns.DNS_SERVICE_BROWSE_REQUEST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SERVICE_BROWSE_REQUEST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SERVICE_BROWSE_REQUEST {
    public var Version: UInt32 = 0u32
    public var InterfaceIndex: UInt32 = 0u32
    public var QueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var Anonymous: DNS_SERVICE_BROWSE_REQUEST__Anonymous_e__Union = DNS_SERVICE_BROWSE_REQUEST__Anonymous_e__Union()
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_SERVICE_BROWSE_REQUEST._Anonymous_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SERVICE_BROWSE_REQUEST__Anonymous_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SERVICE_BROWSE_REQUEST__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop pBrowseCallback: CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pBrowseCallbackV2: CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_SERVICE_CANCEL (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SERVICE_CANCEL)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SERVICE_CANCEL {
    public var reserved: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_SERVICE_INSTANCE (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SERVICE_INSTANCE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SERVICE_INSTANCE {
    public var pszInstanceName: CPointer<UInt16> = CPointer<UInt16>()
    public var pszHostName: CPointer<UInt16> = CPointer<UInt16>()
    public var ip4Address: CPointer<Unit> = CPointer<Unit>()
    public var ip6Address: CPointer<Unit> = CPointer<Unit>()
    public var wPort: UInt16 = 0u16
    public var wPriority: UInt16 = 0u16
    public var wWeight: UInt16 = 0u16
    public var dwPropertyCount: UInt32 = 0u32
    public var keys: CPointer<CPointer<UInt16>> = CPointer<CPointer<UInt16>>()
    public var values: CPointer<CPointer<UInt16>> = CPointer<CPointer<UInt16>>()
    public var dwInterfaceIndex: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_SERVICE_REGISTER_REQUEST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SERVICE_REGISTER_REQUEST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SERVICE_REGISTER_REQUEST {
    public var Version: UInt32 = 0u32
    public var InterfaceIndex: UInt32 = 0u32
    public var pServiceInstance: CPointer<Unit> = CPointer<Unit>()
    public var pRegisterCompletionCallback: CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit> = CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit>(CPointer<Unit>())
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public var hCredentials: Windows_Win32_Foundation.HANDLE = Windows_Win32_Foundation.HANDLE()
    public var unicastEnabled: Int32 = 0i32
}

// Windows.Win32.NetworkManagement.Dns.DNS_SERVICE_RESOLVE_REQUEST (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SERVICE_RESOLVE_REQUEST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SERVICE_RESOLVE_REQUEST {
    public var Version: UInt32 = 0u32
    public var InterfaceIndex: UInt32 = 0u32
    public var QueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var pResolveCompletionCallback: CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit> = CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit>(CPointer<Unit>())
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_SIG_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SIG_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SIG_DATAA {
    public var wTypeCovered: UInt16 = 0u16
    public var chAlgorithm: UInt8 = 0u8
    public var chLabelCount: UInt8 = 0u8
    public var dwOriginalTtl: UInt32 = 0u32
    public var dwExpiration: UInt32 = 0u32
    public var dwTimeSigned: UInt32 = 0u32
    public var wKeyTag: UInt16 = 0u16
    public var wSignatureLength: UInt16 = 0u16
    public var pNameSigner: CPointer<UInt8> = CPointer<UInt8>()
    public var Signature: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_SIG_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SIG_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SIG_DATAW {
    public var wTypeCovered: UInt16 = 0u16
    public var chAlgorithm: UInt8 = 0u8
    public var chLabelCount: UInt8 = 0u8
    public var dwOriginalTtl: UInt32 = 0u32
    public var dwExpiration: UInt32 = 0u32
    public var dwTimeSigned: UInt32 = 0u32
    public var wKeyTag: UInt16 = 0u16
    public var wSignatureLength: UInt16 = 0u16
    public var pNameSigner: CPointer<UInt16> = CPointer<UInt16>()
    public var Signature: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_SOA_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SOA_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SOA_DATAA {
    public var pNamePrimaryServer: CPointer<UInt8> = CPointer<UInt8>()
    public var pNameAdministrator: CPointer<UInt8> = CPointer<UInt8>()
    public var dwSerialNo: UInt32 = 0u32
    public var dwRefresh: UInt32 = 0u32
    public var dwRetry: UInt32 = 0u32
    public var dwExpire: UInt32 = 0u32
    public var dwDefaultTtl: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_SOA_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SOA_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SOA_DATAW {
    public var pNamePrimaryServer: CPointer<UInt16> = CPointer<UInt16>()
    public var pNameAdministrator: CPointer<UInt16> = CPointer<UInt16>()
    public var dwSerialNo: UInt32 = 0u32
    public var dwRefresh: UInt32 = 0u32
    public var dwRetry: UInt32 = 0u32
    public var dwExpire: UInt32 = 0u32
    public var dwDefaultTtl: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.DNS_SRV_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SRV_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SRV_DATAA {
    public var pNameTarget: CPointer<UInt8> = CPointer<UInt8>()
    public var wPriority: UInt16 = 0u16
    public var wWeight: UInt16 = 0u16
    public var wPort: UInt16 = 0u16
    public var Pad: UInt16 = 0u16
}

// Windows.Win32.NetworkManagement.Dns.DNS_SRV_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SRV_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SRV_DATAW {
    public var pNameTarget: CPointer<UInt16> = CPointer<UInt16>()
    public var wPriority: UInt16 = 0u16
    public var wWeight: UInt16 = 0u16
    public var wPort: UInt16 = 0u16
    public var Pad: UInt16 = 0u16
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_DATA {
    public var wSvcPriority: UInt16 = 0u16
    public var pszTargetName: CPointer<UInt8> = CPointer<UInt8>()
    public var cSvcParams: UInt16 = 0u16
    public var pSvcParams: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_PARAM {
    public var wSvcParamKey: UInt16 = 0u16
    public var Anonymous: DNS_SVCB_PARAM__Anonymous_e__Union = DNS_SVCB_PARAM__Anonymous_e__Union()
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM._Anonymous_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM__Anonymous_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_PARAM__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0u64)

    public mut prop pIpv4Hints: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pIpv6Hints: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pMandatory: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pAlpn: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop wPort: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pUnknown: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pszDohPath: CPointer<UInt8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pReserved: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM_ALPN (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM_ALPN)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_PARAM_ALPN {
    public var cIds: UInt16 = 0u16
    public var rgIds: VArray<DNS_SVCB_PARAM_ALPN_ID, $1> = VArray<DNS_SVCB_PARAM_ALPN_ID, $1>(repeat: DNS_SVCB_PARAM_ALPN_ID())
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM_ALPN_ID (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM_ALPN_ID)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_PARAM_ALPN_ID {
    public var cBytes: UInt8 = 0u8
    public var pbId: CPointer<Unit> = CPointer<Unit>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM_IPV4 (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM_IPV4)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_PARAM_IPV4 {
    public var cIps: UInt16 = 0u16
    public var rgIps: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0u32)
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM_IPV6 (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM_IPV6)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_PARAM_IPV6 {
    public var cIps: UInt16 = 0u16
    public var rgIps: VArray<IP6_ADDRESS, $1> = VArray<IP6_ADDRESS, $1>(repeat: IP6_ADDRESS())
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM_MANDATORY (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM_MANDATORY)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_PARAM_MANDATORY {
    public var cMandatoryKeys: UInt16 = 0u16
    public var rgwMandatoryKeys: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0u16)
}

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM_TYPE (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM_TYPE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public struct DNS_SVCB_PARAM_TYPE {
    public let value: Int32
    public const init() {
        this.value = 0
    }
    public const init(value: Int32) {
        this.value = value
    }
}
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamMandatory = DNS_SVCB_PARAM_TYPE(0)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamAlpn = DNS_SVCB_PARAM_TYPE(1)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamNoDefaultAlpn = DNS_SVCB_PARAM_TYPE(2)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamPort = DNS_SVCB_PARAM_TYPE(3)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamIpv4Hint = DNS_SVCB_PARAM_TYPE(4)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamEch = DNS_SVCB_PARAM_TYPE(5)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamIpv6Hint = DNS_SVCB_PARAM_TYPE(6)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamDohPath = DNS_SVCB_PARAM_TYPE(7)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public const DNS_SVCB_PARAM_TYPE_DnsSvcbParamDohPathOpenDns = DNS_SVCB_PARAM_TYPE(65432)

// Windows.Win32.NetworkManagement.Dns.DNS_SVCB_PARAM_UNKNOWN (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_SVCB_PARAM_UNKNOWN)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_SVCB_PARAM_UNKNOWN {
    public var cBytes: UInt16 = 0u16
    public var pbSvcParamValue: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_TKEY_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_TKEY_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_TKEY_DATAA {
    public var pNameAlgorithm: CPointer<UInt8> = CPointer<UInt8>()
    public var pAlgorithmPacket: CPointer<Unit> = CPointer<Unit>()
    public var pKey: CPointer<Unit> = CPointer<Unit>()
    public var pOtherData: CPointer<Unit> = CPointer<Unit>()
    public var dwCreateTime: UInt32 = 0u32
    public var dwExpireTime: UInt32 = 0u32
    public var wMode: UInt16 = 0u16
    public var wError: UInt16 = 0u16
    public var wKeyLength: UInt16 = 0u16
    public var wOtherLength: UInt16 = 0u16
    public var cAlgNameLength: UInt8 = 0u8
    public var bPacketPointers: Int32 = 0i32
}

// Windows.Win32.NetworkManagement.Dns.DNS_TKEY_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_TKEY_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_TKEY_DATAW {
    public var pNameAlgorithm: CPointer<UInt16> = CPointer<UInt16>()
    public var pAlgorithmPacket: CPointer<Unit> = CPointer<Unit>()
    public var pKey: CPointer<Unit> = CPointer<Unit>()
    public var pOtherData: CPointer<Unit> = CPointer<Unit>()
    public var dwCreateTime: UInt32 = 0u32
    public var dwExpireTime: UInt32 = 0u32
    public var wMode: UInt16 = 0u16
    public var wError: UInt16 = 0u16
    public var wKeyLength: UInt16 = 0u16
    public var wOtherLength: UInt16 = 0u16
    public var cAlgNameLength: UInt8 = 0u8
    public var bPacketPointers: Int32 = 0i32
}

// Windows.Win32.NetworkManagement.Dns.DNS_TLSA_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_TLSA_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_TLSA_DATA {
    public var bCertUsage: UInt8 = 0u8
    public var bSelector: UInt8 = 0u8
    public var bMatchingType: UInt8 = 0u8
    public var bCertificateAssociationDataLength: UInt16 = 0u16
    public var bPad: VArray<UInt8, $3> = VArray<UInt8, $3>(repeat: 0u8)
    public var bCertificateAssociationData: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_TSIG_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_TSIG_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_TSIG_DATAA {
    public var pNameAlgorithm: CPointer<UInt8> = CPointer<UInt8>()
    public var pAlgorithmPacket: CPointer<Unit> = CPointer<Unit>()
    public var pSignature: CPointer<Unit> = CPointer<Unit>()
    public var pOtherData: CPointer<Unit> = CPointer<Unit>()
    public var i64CreateTime: Int64 = 0i64
    public var wFudgeTime: UInt16 = 0u16
    public var wOriginalXid: UInt16 = 0u16
    public var wError: UInt16 = 0u16
    public var wSigLength: UInt16 = 0u16
    public var wOtherLength: UInt16 = 0u16
    public var cAlgNameLength: UInt8 = 0u8
    public var bPacketPointers: Int32 = 0i32
}

// Windows.Win32.NetworkManagement.Dns.DNS_TSIG_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_TSIG_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_TSIG_DATAW {
    public var pNameAlgorithm: CPointer<UInt16> = CPointer<UInt16>()
    public var pAlgorithmPacket: CPointer<Unit> = CPointer<Unit>()
    public var pSignature: CPointer<Unit> = CPointer<Unit>()
    public var pOtherData: CPointer<Unit> = CPointer<Unit>()
    public var i64CreateTime: Int64 = 0i64
    public var wFudgeTime: UInt16 = 0u16
    public var wOriginalXid: UInt16 = 0u16
    public var wError: UInt16 = 0u16
    public var wSigLength: UInt16 = 0u16
    public var wOtherLength: UInt16 = 0u16
    public var cAlgNameLength: UInt8 = 0u8
    public var bPacketPointers: Int32 = 0i32
}

// Windows.Win32.NetworkManagement.Dns.DNS_TXT_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_TXT_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_TXT_DATAA {
    public var dwStringCount: UInt32 = 0u32
    public var pStringArray: VArray<CPointer<UInt8>, $1> = VArray<CPointer<UInt8>, $1>(repeat: CPointer<UInt8>())
}

// Windows.Win32.NetworkManagement.Dns.DNS_TXT_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_TXT_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_TXT_DATAW {
    public var dwStringCount: UInt32 = 0u32
    public var pStringArray: VArray<CPointer<UInt16>, $1> = VArray<CPointer<UInt16>, $1>(repeat: CPointer<UInt16>())
}

// Windows.Win32.NetworkManagement.Dns.DNS_UNKNOWN_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_UNKNOWN_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_UNKNOWN_DATA {
    public var dwByteCount: UInt32 = 0u32
    public var bData: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.DNS_WINSR_DATAA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_WINSR_DATAA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_WINSR_DATAA {
    public var dwMappingFlag: UInt32 = 0u32
    public var dwLookupTimeout: UInt32 = 0u32
    public var dwCacheTimeout: UInt32 = 0u32
    public var pNameResultDomain: CPointer<UInt8> = CPointer<UInt8>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_WINSR_DATAW (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_WINSR_DATAW)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_WINSR_DATAW {
    public var dwMappingFlag: UInt32 = 0u32
    public var dwLookupTimeout: UInt32 = 0u32
    public var dwCacheTimeout: UInt32 = 0u32
    public var pNameResultDomain: CPointer<UInt16> = CPointer<UInt16>()
}

// Windows.Win32.NetworkManagement.Dns.DNS_WINS_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_WINS_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_WINS_DATA {
    public var dwMappingFlag: UInt32 = 0u32
    public var dwLookupTimeout: UInt32 = 0u32
    public var dwCacheTimeout: UInt32 = 0u32
    public var cWinsServerCount: UInt32 = 0u32
    public var WinsServers: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0u32)
}

// Windows.Win32.NetworkManagement.Dns.DNS_WIRE_QUESTION (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_WIRE_QUESTION)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_WIRE_QUESTION {
    public var QuestionType: UInt16 = 0u16
    public var QuestionClass: UInt16 = 0u16
}

// Windows.Win32.NetworkManagement.Dns.DNS_WIRE_RECORD (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_WIRE_RECORD)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_WIRE_RECORD {
    public var RecordType: UInt16 = 0u16
    public var RecordClass: UInt16 = 0u16
    public var TimeToLive: UInt32 = 0u32
    public var DataLength: UInt16 = 0u16
}

// Windows.Win32.NetworkManagement.Dns.DNS_WKS_DATA (synthetic:Windows_Win32_NetworkManagement_Dns_DNS_WKS_DATA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct DNS_WKS_DATA {
    public var IpAddress: UInt32 = 0u32
    public var chProtocol: UInt8 = 0u8
    public var BitMask: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)
}

// Windows.Win32.NetworkManagement.Dns.IP4_ARRAY (synthetic:Windows_Win32_NetworkManagement_Dns_IP4_ARRAY)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct IP4_ARRAY {
    public var AddrCount: UInt32 = 0u32
    public var AddrArray: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0u32)
}

// Windows.Win32.NetworkManagement.Dns.IP6_ADDRESS (synthetic:Windows_Win32_NetworkManagement_Dns_IP6_ADDRESS)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct IP6_ADDRESS {
    public var _raw: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0u64)

    public mut prop IP6Qword: VArray<UInt64, $2> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt64, $2>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt64, $2>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop IP6Dword: VArray<UInt32, $4> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt32, $4>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt32, $4>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop IP6Word: VArray<UInt16, $8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt16, $8>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt16, $8>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop IP6Byte: VArray<UInt8, $16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt8, $16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt8, $16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns.MDNS_QUERY_HANDLE (synthetic:Windows_Win32_NetworkManagement_Dns_MDNS_QUERY_HANDLE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct MDNS_QUERY_HANDLE {
    public var nameBuf: VArray<UInt16, $256> = VArray<UInt16, $256>(repeat: 0u16)
    public var wType: UInt16 = 0u16
    public var pSubscription: CPointer<Unit> = CPointer<Unit>()
    public var pWnfCallbackParams: CPointer<Unit> = CPointer<Unit>()
    public var stateNameData: VArray<UInt32, $2> = VArray<UInt32, $2>(repeat: 0u32)
}

// Windows.Win32.NetworkManagement.Dns.MDNS_QUERY_REQUEST (synthetic:Windows_Win32_NetworkManagement_Dns_MDNS_QUERY_REQUEST)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct MDNS_QUERY_REQUEST {
    public var Version: UInt32 = 0u32
    public var ulRefCount: UInt32 = 0u32
    public var Query: CPointer<UInt16> = CPointer<UInt16>()
    public var QueryType: UInt16 = 0u16
    public var QueryOptions: UInt64 = 0u64
    public var InterfaceIndex: UInt32 = 0u32
    public var pQueryCallback: CFunc<(CPointer<Unit>, CPointer<Unit>, CPointer<Unit>) -> Unit> = CFunc<(CPointer<Unit>, CPointer<Unit>, CPointer<Unit>) -> Unit>(CPointer<Unit>())
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public var fAnswerReceived: Int32 = 0i32
    public var ulResendCount: UInt32 = 0u32
}

// Windows.Win32.NetworkManagement.Dns.PDNS_QUERY_COMPLETION_ROUTINE (synthetic:Windows_Win32_NetworkManagement_Dns_PDNS_QUERY_COMPLETION_ROUTINE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public type PDNS_QUERY_COMPLETION_ROUTINE = CFunc<(CPointer<Unit>, CPointer<Unit>) -> Unit>

// Windows.Win32.NetworkManagement.Dns.PDNS_SERVICE_BROWSE_CALLBACK (synthetic:Windows_Win32_NetworkManagement_Dns_PDNS_SERVICE_BROWSE_CALLBACK)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public type PDNS_SERVICE_BROWSE_CALLBACK = CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit>

// Windows.Win32.NetworkManagement.Dns.PDNS_SERVICE_REGISTER_COMPLETE (synthetic:Windows_Win32_NetworkManagement_Dns_PDNS_SERVICE_REGISTER_COMPLETE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public type PDNS_SERVICE_REGISTER_COMPLETE = CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit>

// Windows.Win32.NetworkManagement.Dns.PDNS_SERVICE_RESOLVE_COMPLETE (synthetic:Windows_Win32_NetworkManagement_Dns_PDNS_SERVICE_RESOLVE_COMPLETE)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public type PDNS_SERVICE_RESOLVE_COMPLETE = CFunc<(UInt32, CPointer<Unit>, CPointer<Unit>) -> Unit>

// Windows.Win32.NetworkManagement.Dns.PMDNS_QUERY_CALLBACK (synthetic:Windows_Win32_NetworkManagement_Dns_PMDNS_QUERY_CALLBACK)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
public type PMDNS_QUERY_CALLBACK = CFunc<(CPointer<Unit>, CPointer<Unit>, CPointer<Unit>) -> Unit>

// Windows.Win32.NetworkManagement.Dns._DnsRecordOptA (synthetic:Windows_Win32_NetworkManagement_Dns__DnsRecordOptA)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct _DnsRecordOptA {
    public var pNext: CPointer<Unit> = CPointer<Unit>()
    public var pName: CPointer<UInt8> = CPointer<UInt8>()
    public var wType: UInt16 = 0u16
    public var wDataLength: UInt16 = 0u16
    public var Flags: _DnsRecordOptA__Flags_e__Union = _DnsRecordOptA__Flags_e__Union()
    public var ExtHeader: DNS_HEADER_EXT = DNS_HEADER_EXT()
    public var wPayloadSize: UInt16 = 0u16
    public var wReserved: UInt16 = 0u16
    public var Data: _DnsRecordOptA__Data_e__Union = _DnsRecordOptA__Data_e__Union()
}

// Windows.Win32.NetworkManagement.Dns._DnsRecordOptA._Data_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns__DnsRecordOptA__Data_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct _DnsRecordOptA__Data_e__Union {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)

    public mut prop OPT: DNS_OPT_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop Opt: DNS_OPT_DATA {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_OPT_DATA>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}

// Windows.Win32.NetworkManagement.Dns._DnsRecordOptA._Flags_e__Union (synthetic:Windows_Win32_NetworkManagement_Dns__DnsRecordOptA__Flags_e__Union)
@When[Windows_Win32_NetworkManagement_Dns == "on"]
@C
public struct _DnsRecordOptA__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0u32)

    public mut prop DW: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return windows_polyfill.unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                windows_polyfill.unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop S: DNS_RECORD_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_RECORD_FLAGS>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DNS_RECORD_FLAGS>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
}
