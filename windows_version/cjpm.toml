[package]
  name = "windows_version"
  version = "0.1.0"
  description = "Windows version query helpers for Cangjie"
  output-type = "static"
  cjc-version = "1.1.0"
  compile-option = "-Woff unused"

[dependencies]
  windows_common = { path = "../windows_common" }
  windows_strings = { path = "../windows_strings" }
