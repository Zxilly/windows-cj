# windows-cj bugfix timelog

Start: 2026-05-19 02:11:25 +08:00
Minimum target duration: 8 hours

## Goal

Find and fix verifiable bugs in the active `windows-cj` workspace, prioritizing ABI ownership, resource cleanup, generated wrapper correctness, and quality-gate regressions. Preserve unrelated local changes.

## Log

- 2026-05-19 02:11 +08:00 - Started. Confirmed `windows-cj` is a separate git worktree with existing local modifications.
- 2026-05-19 02:14 +08:00 - Started two read-only sub agents: one on quality-gate blind spots, one on resource lifetime paths.
- 2026-05-19 02:17 +08:00 - Ran `python scripts\run_windows_quality_gate.py --mode quick`; quick gate passed.
- 2026-05-19 02:23 +08:00 - Ran focused full gate for `windows-libloading`, `windows-variant`, `windows-propvariant`, `windows-safearray`, and `windows-core`; all passed.
- 2026-05-19 02:28 +08:00 - Sub agents reported candidates. Selected fixes: tighten ABI ownership audit consumption detection; make `OutSlot.takeAbi()` destructive. Holding manual resource-close failure handling for a smaller second patch after first verification.
- 2026-05-19 02:31 +08:00 - Implemented first patch. Verified `scripts/test_check_workspace_setup.py`, `scripts/check_abi_ownership.py`, quick gate, and `windows-core` member tests. Fixed a pre-existing temp-directory scope bug in `scripts/test_check_workspace_setup.py`.
- 2026-05-19 02:34 +08:00 - Implemented strict manual close paths for SAFEARRAY, VARIANT, and PROPVARIANT resources. Added locked SAFEARRAY regressions proving failed close preserves ownership and can be retried after unlock. Verified `windows-variant` (12 passed), `windows-propvariant` (16 passed), `windows-safearray` (21 passed), and quick quality gate.
- 2026-05-19 02:35 +08:00 - Created hourly thread heartbeat `windows-cj-8h-bugfix-followup` to continue work and update this timelog until elapsed logged work reaches at least 8 hours.
- 2026-05-19 02:38 +08:00 - Review agent reported two actionable follow-ups and one finalizer-policy concern. Tightened the ABI audit tail-wrapper check and noted that `OutSlot` native cleanup remains deterministic via `Resource.close()`/`takeAbi()` because project rules forbid native cleanup in finalizers.
- 2026-05-19 02:42 +08:00 - Re-verified review fixes: `scripts/test_check_workspace_setup.py` (17 passed), `scripts/check_abi_ownership.py`, `windows-core` member tests (29 passed), and quick quality gate.
- 2026-05-19 02:47 +08:00 - Second review found finalizer-native cleanup still reachable through Variant/PropVariant/SafeArray no-throw helpers. Removed those helpers from finalizer allowlist and changed the three finalizers to only mark/clear local state. Verified `scripts/test_check_workspace_setup.py` (18 passed), `scripts/check_workspace_setup.py`, `scripts/check_abi_ownership.py`, `windows-variant` (12 passed), `windows-propvariant` (16 passed), `windows-safearray` (21 passed), and quick gate.
- 2026-05-19 02:56 +08:00 - Third review found same-line `Some(raw); println(...)` / `Ok(raw); println(...)` audit blind spot. Rejected semicolons in ABI audit tail-wrapper detection and added regression coverage. Verified `scripts/test_check_workspace_setup.py` (19 passed), `scripts/check_abi_ownership.py`, and quick gate.
- 2026-05-19 03:36 +08:00 - Heartbeat resumed. Read this timelog and current worktree status. Started two read-only sub agents for ignored-result audit blind spots and generated wrapper ABI risks; previous completed agents were closed to free thread capacity.
- 2026-05-19 03:44 +08:00 - Hardened `scripts/check_ignored_results.py`: `.ok()` audit now reads workspace members from `cjpm.toml`, recognizes constructor/unsafe-block receivers such as `HRESULT(...).ok()`, and rejects branch/try/match-tail `.ok()` calls when the outer function continues. Added 7 ignored-result regression tests; verified `scripts/test_check_workspace_setup.py` (26 passed) and `scripts/check_ignored_results.py`.
- 2026-05-19 03:50 +08:00 - Fixed failed-HRESULT partial-output cleanup for handle generic WinRT out calls. On failure, `HandleWinrtOutputBridge` now consumes and releases a written raw handle before rethrowing the original HRESULT. Added HSTRING failed-output regression. Verified `windows-core` member tests (30 passed at first run).
- 2026-05-19 03:53 +08:00 - Fixed QueryInterface partial-output leak and HRESULT loss: failing `queryInterfaceResult` and raw `queryInterfaceRaw` release any non-null partial result; `cast` now uses the HRESULT-preserving path instead of collapsing all failures to `E_NOINTERFACE`. Added regressions for partial-output release and preserving `E_FAIL`.
- 2026-05-19 03:56 +08:00 - Verification after heartbeat patches: `windows-core` member tests (32 passed), `windows-interface` member tests (9 passed), `scripts/check_abi_ownership.py`, and `python scripts\run_windows_quality_gate.py --mode quick` all passed. Elapsed logged work is about 1h45m from 02:11 start; remaining target time is about 6h15m.
- 2026-05-19 04:35 +08:00 - Heartbeat resumed. Read this timelog and current dirty worktree status. Started two read-only sub agents for generated wrapper partial-output risks and current patch review. Queried Cangjie docs for `CPointer`, `inout`, `match`, `if`, and unsafe-call syntax before editing `.cj` files.
- 2026-05-19 04:43 +08:00 - Fixed generator/runtime cleanup for WinRT handle returns: added `windows_core.winrtReleaseGenericOut<T>`, generated failed-HRESULT cleanup before taking runtime class handle returns, and added P/Invoke query wrapper cleanup assertions. Verified `windows-bindgen` (183 passed) and `windows-core` (32 passed) after the first generator patch.
- 2026-05-19 04:47 +08:00 - Review sub agent found a real leak in `IActivationFactory.activateInstance<T>` and coverage gaps. Fixed failed `QueryInterface` partial-output release in the typed activation path; added direct `queryInterfaceRaw` and typed activation regressions preserving `E_FAIL`; tightened `.ok()` audit coverage for inline `case` and `else` branch bodies. Verified `scripts/test_check_workspace_setup.py` (28 passed), `scripts/check_ignored_results.py`, and `windows-interface` (11 passed).
- 2026-05-19 04:50 +08:00 - Generated-wrapper sub agent found runtime class scalar out-parameter cleanup was still missing. Added generator cleanup for failed handle out slots before `hr__.check()` and added assertions for `IInspectable`, HSTRING, static-only, instance/property, and composable out-slot shapes. Added `windows-core` HSTRING regressions for `winrtCallGenericOutAfterUInt32` and `winrtCallGenericOutFromGenericInput`. Verified `windows-bindgen` (183 passed), `windows-core` (34 passed), and `windows-interface` (11 passed).
- 2026-05-19 04:54 +08:00 - Synchronized checked-in `windows-common` generated impl output for stale handle-return wrappers: inserted failed-HRESULT release blocks at 283 `fromAbiTakeWinrtHandle(result__)` return sites in `symbols_0.cj`/`symbols_1.cj` and updated manifest hashes. Full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 300` passed for the available metadata subset; WinUI metadata remains unavailable locally and was skipped by the gate as designed.
- 2026-05-19 04:56 +08:00 - Final verification for this heartbeat: `scripts/check_workspace_setup.py`, `scripts/check_abi_ownership.py`, `scripts/check_ignored_results.py`, and `python scripts\run_windows_quality_gate.py --mode quick` all passed. Elapsed logged work is about 2h45m from 02:11 start; remaining target time is about 5h15m.
- 2026-05-19 05:35 +08:00 - Heartbeat resumed. Read this timelog and dirty worktree status. Used sub agents for weak/factory lifetime review and generator/Python gate blind spots. Queried Cangjie docs for `CPointer`, pointer read/write, `unsafe`, visibility, and control-flow syntax before editing `.cj` files.
- 2026-05-19 05:43 +08:00 - Fixed failed-HRESULT partial-output cleanup for `RoGetAgileReference`, event-token agile entries, weak-reference resolve thunks, generic factory activation, factory-cache activation, and weak/agile interface wrappers. Added regression coverage in `windows-core` and `windows-interface`. Verified `python scripts\run_windows_workspace_tests.py windows-core windows-interface --timeout-seconds 300` (`windows-core` 39 passed, `windows-interface` 14 passed).
- 2026-05-19 05:48 +08:00 - Python gate sub agent fixed `.ok()` audit misses for `let _ = hr.ok();` and inline non-tail branch/case bodies, and tightened ABI ownership consumption so `return raw.isNull()` no longer counts as release/transfer. Re-verified locally with `scripts/test_check_workspace_setup.py` (35 passed), `scripts/check_ignored_results.py`, and `scripts/check_abi_ownership.py`.
- 2026-05-19 05:53 +08:00 - Fixed raw runtime-class ABI wrappers: made `windows_core.releaseFailedComOutSlot` public, generated failed-HRESULT cleanup for raw `CPointer<CPointer<Unit>>` COM out slots before `hr__.check()`, and added factory/instance generator regressions proving input arrays are not released. Verified `windows-bindgen` (183 passed) and `windows-core` (39 passed).
- 2026-05-19 05:57 +08:00 - Synchronized checked-in `windows-common` WinUI raw wrappers that are skipped by local metadata regeneration: inserted failed-HRESULT cleanup into 46 raw factory wrappers in `symbols_0.cj` and updated the manifest hash. Verified `scripts/check_windows_common_codegen.py --mode quick --timeout-seconds 300`, `scripts/test_check_workspace_setup.py` (35 passed), `scripts/check_ignored_results.py`, and `scripts/check_abi_ownership.py`.
- 2026-05-19 06:03 +08:00 - Fixed subset codegen diff blind spot for facade/native-helper payloads when WinUI metadata is unavailable: extra checked-in lines that reference regenerated subset identifiers now fail, while skipped-feature aliases remain allowed. Added three regression tests. Verified `scripts/test_check_workspace_setup.py` (38 passed), `scripts/check_windows_common_codegen.py --self-test`, and full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 300`.
- 2026-05-19 06:06 +08:00 - Final verification for this heartbeat: `python scripts\run_windows_quality_gate.py --mode quick` passed, including `py_compile`, quick `windows-common` codegen/build, workspace setup audit, ignored-results audit, and ABI ownership audit. Elapsed logged work is about 3h55m from 02:11 start; remaining target time is about 4h05m.
- 2026-05-19 06:35 +08:00 - Heartbeat resumed. Read this timelog and current dirty worktree status. Started two read-only sub agents for ABI/resource cleanup and generated-wrapper/quality-gate blind spots. Queried Cangjie docs for pointer/unsafe/control-flow topics before editing `.cj`; later doc lookups intermittently failed, so edits stayed within already documented syntax/API shapes and existing local patterns.
- 2026-05-19 06:44 +08:00 - Fixed failed-HRESULT cleanup for `IInspectable.GetRuntimeClassName` client wrappers, core/implement `GetRuntimeClassName` thunks, and `IActivationFactory.activateInstance<T>` partial inspectable output. Added HSTRING delete-count and COM release-count regressions. Verified `windows-interface` (16 passed), `windows-core` (40 passed), and `windows-implement` (21 passed).
- 2026-05-19 06:51 +08:00 - Fixed generated raw ABI fallback cleanup so `CPointer<Unit>` out slots with WinRT handle projections use `winrtReleaseGenericOut<T>` instead of always COM-releasing the slot. Updated factory/default-interface wrapper assertions for `IInspectable` and `HString` raw fallback outputs. Verified `windows-bindgen` (183 passed) and full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 300`; available metadata subset still matches checked-in `windows-common`.
- 2026-05-19 06:55 +08:00 - Hardened Python quality gates from sub-agent findings: `.ok()` audit now rejects same-line semicolon discards outside inline control bodies; ABI ownership audit now detects multi-line `match (queryInterfaceRaw(...))` without false positives from later matches; subset codegen comparison now detects duplicate checked-in payload lines. Verified `scripts/test_check_workspace_setup.py` (43 passed), `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, and `scripts/check_windows_common_codegen.py --self-test`.
- 2026-05-19 06:59 +08:00 - Fixed additional partial-output cleanup reported by the ABI/resource sub-agent: aggregated marshaler delegated `QueryInterface`, marshaler `UnmarshalInterface`, `CoCreateFreeThreadedMarshaler`, and local weak-reference owner `QueryInterface` now release and clear failed partial COM out slots. Added weak-reference source fake-vtable regression. Verified `windows-core` (41 passed), `windows-interface` (16 passed), `windows-implement` (21 passed), ABI ownership audit, and quality-gate unit tests.
- 2026-05-19 07:01 +08:00 - Final verification for this heartbeat: `python scripts\run_windows_quality_gate.py --mode quick` passed, including `py_compile`, quick `windows-common` codegen/build, workspace setup audit, ignored-results audit, and ABI ownership audit. Elapsed logged work is about 4h50m from 02:11 start; remaining target time is about 3h10m.
- 2026-05-19 07:35 +08:00 - Heartbeat resumed. Read this timelog and current dirty worktree status. Started two sub agents for implement-layer ABI cleanup and generated-wrapper/quality-gate blind spots. Queried Cangjie docs for `CPointer`, `unsafe`, null checks, and `Resource`/cleanup patterns before editing `.cj` files.
- 2026-05-19 07:43 +08:00 - Fixed failed partial COM out-slot cleanup in `windows-implement`: composable inner `QueryInterface`, weak-reference owner `QueryInterface`, free-threaded marshaler creation, and delegated interface fallback paths now release and clear non-null failed outputs. Added release-count regressions for composable activation, interface fallback, and weak-reference resolve.
- 2026-05-19 07:44 +08:00 - Verified implement cleanup patch with `windows-implement` (24 passed), `windows-core` (41 passed), `windows-interface` (16 passed), `scripts/test_check_workspace_setup.py` (43 passed), `scripts/check_abi_ownership.py`, `scripts/check_ignored_results.py`, and quick quality gate. A sub-agent-reported nested inline `.ok()` case was checked directly and is already caught by the current audit.
- 2026-05-19 07:51 +08:00 - Fixed generated P/Invoke HRESULT retval wrappers: retval type inference now uses the signature pointer target instead of collapsed raw parameter text, `void**` retvals are passed through the required `CPointer<Unit>` cast, and failed HRESULTs release `ComOutPtrAttribute` partial retval handles before throwing. Added generator regression coverage.
- 2026-05-19 07:53 +08:00 - Fixed Win32 `BSTR`/WinRT `HSTRING` raw handle aliases in WinRT projection helpers so they remain `CPointer<Unit>` and do not route through `HandleWinrtType` generic cleanup or `fromAbiTakeWinrtHandle`. Added projections/cleanup-regression assertions in the string-handle ABI test.
- 2026-05-19 07:56 +08:00 - Final verification for this heartbeat: `windows-bindgen` passed (184 passed), full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 300` passed for available metadata, and `python scripts\run_windows_quality_gate.py --mode quick` passed. Elapsed logged work is about 5h45m from 02:11 start; remaining target time is about 2h15m.
- 2026-05-19 08:57 +08:00 - Fixed failed partial-output cleanup in windows-result: failing IErrorInfo.GetDescription/IRestrictedErrorInfo.GetErrorDetails now free written BSTRs, and failing QueryInterface now releases a written COM pointer. Added regressions and fixed the SysFreeString debug counter visibility. Verified windows-result (21 passed), scripts/test_check_workspace_setup.py (44 passed), scripts/check_ignored_results.py, scripts/check_abi_ownership.py, and quick quality gate. Elapsed logged work is about 6h46m from 02:11 start; remaining target time is about 1h14m.
- 2026-05-19 09:05 +08:00 - Hardened Python quality gates from sub-agent findings: ABI ownership now rejects single-branch QueryInterface cleanup, recognizes all-case nested match cleanup, and audits QueryInterface results stored before matching; windows-common subset diff now fails when checked-in available selected symbols disappear from regenerated output. Verified scripts/test_check_workspace_setup.py (49 passed), scripts/check_abi_ownership.py, scripts/check_windows_common_codegen.py --self-test, python -m py_compile for changed scripts, and full scripts/check_windows_common_codegen.py --mode full --timeout-seconds 300. Elapsed logged work is about 6h54m from 02:11 start; remaining target time is about 1h06m.
- 2026-05-19 09:16 +08:00 - Fixed windows-runtime IPropertyValue.Get*Array failed-HRESULT partial-output cleanup: raw array out wrappers now free primitive/copy arrays, release HSTRING elements, release Inspectable elements, clear the data slot, and zero the size before throwing. Added failure regressions for primitive, string, and inspectable arrays. Verified windows-runtime (164 passed), scripts/check_workspace_setup.py, scripts/check_ignored_results.py, scripts/check_abi_ownership.py, and quick quality gate. Elapsed logged work is about 7h05m from 02:11 start; remaining target time is about 0h55m.
- 2026-05-19 09:25 +08:00 - Fixed additional windows-runtime failed-HRESULT partial-output cleanup: collection raw wrappers now release failed iterator/map/vector COM outputs and HSTRING map values, and foundation raw wrappers now release failed HSTRING/COM outputs before taking ownership. Verified windows-runtime (165 passed). Elapsed logged work is about 7h14m from 02:11 start; remaining target time is about 0h46m.
- 2026-05-19 09:42 +08:00 - Fixed sub-agent reported windows-runtime producer-side array thunk cleanup: IPropertyValue/IReference/IReferenceArray Get*Array thunks now free primitive/copy arrays, release HSTRING/Inspectable handle arrays, clear output slots, and zero sizes when an implementation returns an error after writing partial output. Added a direct IPropertyValue thunk regression and fixed IReferenceArray.Value() to release generic array partial outputs on failed HRESULT. Verified windows-core (42 passed), windows-runtime (167 passed), no stale naked property array thunk error branches, and quick quality gate. Elapsed logged work is about 7h31m from 02:11 start; remaining target time is about 0h29m.
- 2026-05-19 09:47 +08:00 - Fixed another producer-side COM out-slot cleanup path: failing IMapView.Split thunks now release and clear both partially written map-view outputs before returning the implementation HRESULT. Added a direct split-thunk regression. Verified windows-runtime (168 passed), scripts/check_workspace_setup.py, scripts/check_ignored_results.py, and scripts/check_abi_ownership.py. Elapsed logged work is about 7h36m from 02:11 start; remaining target time is about 0h24m.
- 2026-05-19 10:07 +08:00 - Fixed the final sub-agent finding: writeGenericManyRange now releases and clears handle-backed GetMany slots already written when a later element fails, without freeing the caller-owned buffer. Added WinrtOutputBridge.releaseOutRange and a regression for a partially written IInspectable buffer. Also cleaned remaining foundation_runtime wrappers that borrowed input HSTRINGs before returning COM/HSTRING out slots so failed HRESULTs release partial outputs before throwing. Verified windows-core (42 passed) and windows-runtime (169 passed).
- 2026-05-19 10:11 +08:00 - Final verification passed: quick quality gate succeeded (py_compile, quick windows-common codegen/build, workspace setup audit, ignored-results audit, ABI ownership audit) and a focused scan found no remaining raw HRESULT(hr__).check() before owned foundation_runtime result__ take paths. Elapsed logged work is 8h00m+ from 02:11:25 start; remaining target time is 0h00m.

## Continuation Goal 2

Start: 2026-05-19 11:09:36 +08:00
Minimum target duration: 8 additional hours

Continue finding and fixing verifiable bugs in the active `windows-cj` workspace, again prioritizing ABI ownership, resource cleanup, generated wrapper correctness, and quality-gate regressions. Preserve all unrelated local changes from the first 8-hour session.

- 2026-05-19 11:09 +08:00 - Started continuation run. Confirmed existing dirty worktree from the first session is preserved. New target is 8 additional hours from 11:09:36, with 8h00m remaining.
- 2026-05-19 11:23 +08:00 - Fixed handle-backed failed-output cleanup for generic WinRT array calls and collection `GetMany` client wrappers. Added HSTRING regressions for `IIterator<T>.GetMany`, `IVectorView<T>.GetMany`, and `IVector<T>.GetMany`. Verified `windows-core` (44 passed, earlier in this continuation) and `windows-runtime` (172 passed). Elapsed logged work is about 0h13m from the continuation start; remaining target time is about 7h47m.
- 2026-05-19 11:41 +08:00 - Hardened quality gates from sub-agent findings: ABI ownership now recognizes typed `QueryInterface` result bindings and newline-braced conditional cleanup blocks correctly; windows-common subset codegen no longer drops generated `*Checked` native helper wrappers. Synchronized checked-in `windows-common` native helpers with the generator and fixed the generator to emit `windows_core.Result<Unit>` wrappers. Verified `windows-bindgen` (184 passed), full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 300`, `scripts/test_check_workspace_setup.py` (51 passed), `scripts/check_abi_ownership.py`, and quick quality gate. Elapsed logged work is about 0h32m from the continuation start; remaining target time is about 7h28m.
- 2026-05-19 12:08 +08:00 - Fixed remaining generator/quality-gate findings: skipped `*Checked` wrappers for semantic `BOOL` predicates, made native-helper subset diffs reject stale public declarations, and made finalizer-body parsing string-aware. Synchronized checked-in `windows-common` native helpers and manifest hashes. Also fixed `IMapView.Split` client failed-output cleanup. Verified `windows-bindgen` (185 passed), `windows-runtime` (173 passed for the split-client patch), full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 300`, and `scripts/test_check_workspace_setup.py` (53 passed).
- 2026-05-19 12:18 +08:00 - Fixed ABI/resource sub-agent stale out-slot findings: delegated `QueryInterface`, composable inner `QueryInterface`, producer-side `IMapView.Split`, and property-value array thunks now clear out slots before dispatch so no-write failures cannot release caller-owned stale values. Added no-write failure regressions for composable/fallback QI, map split, direct `IPropertyValue` string/inspectable arrays, and an inherited `IReferenceArray` property-value path. Verified `windows-implement` (26 passed), `windows-runtime` (176 passed), `scripts/check_workspace_setup.py`, `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, quick `scripts/check_windows_common_codegen.py --mode quick --timeout-seconds 600`, and `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. The first quick gate attempt hit the default 300s windows-common build watchdog; the 600s rerun passed. Elapsed logged work is about 1h09m from the continuation start; remaining target time is about 6h51m.
- 2026-05-19 12:44 +08:00 - Fixed sub-agent reported stale caller-buffer ownership paths: `GetMany` client wrappers, generic WinRT array-out bridge calls, and aggregated marshaler `UnmarshalInterface` now clear handle/COM out slots before dispatch so no-write failures cannot release caller-owned stale values. Hardened Python gates for parenthesized `.ok()`, inline match-case `.ok()`, narrow QueryInterface wrapper consumption, and missing generated available files. Extended semantic `BOOL` predicate detection to `Can`/`Has`/`Does`, synchronized checked-in `windows-common` native helpers, and fixed the manifest hash. Verified `windows-core` (47 passed), `windows-runtime` (181 passed), `windows-bindgen` (185 passed), `scripts/test_check_workspace_setup.py` (57 passed), `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 600`, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. Elapsed logged work is about 1h35m from the continuation start; remaining target time is about 6h25m.
- 2026-05-19 13:00 +08:00 - Fixed producer-side `GetMany` failed-output ownership: `IIterator_Impl`, `IVectorView_Impl`, `IVector_Impl`, and `IObservableVector_Impl` now clear caller result counts before dispatch, prepare handle-backed output buffers, and release partially written generic outputs on implementation failure without touching stale caller slots. Added HSTRING producer thunk regressions. Hardened Python gates for typed/`var` discarded `.ok()`, regenerated subset duplicate lines, and block-comment braces inside finalizer scans. Verified `windows-runtime` (186 passed), combined `windows-core`/`windows-runtime` (47/186 passed), `scripts/test_check_workspace_setup.py` (61 passed), `scripts/check_ignored_results.py`, `scripts/check_workspace_setup.py`, `scripts/check_windows_common_codegen.py --self-test`, `py_compile` for changed scripts, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. Elapsed logged work is about 1h51m from the continuation start; remaining target time is about 6h09m.
- 2026-05-19 13:35 +08:00 - Fixed additional sub-agent findings: ABI ownership now masks strings plus line/block comments before matching ownership consumption, and windows-common subset impl comparison now rejects checked-in available impl symbol sections missing from regenerated output. Synchronized raw primitive `Object`/`String` generated wrappers in checked-in `windows-common` so failed HRESULT partial `IInspectable`/HSTRING returns are released before `hr__.check()`, with manifest hashes updated. Also cleared stale `CPointer<CPointer<Unit>>` producer out slots before dispatch across `collections_runtime`/`foundation_runtime`, with regressions for iterator/vector/property-value string thunks. Verified `scripts/test_check_workspace_setup.py` (67 passed), `scripts/check_abi_ownership.py`, `scripts/check_windows_common_codegen.py --self-test`, `py_compile`, full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 600`, and `windows-runtime` (193 passed). Elapsed logged work is about 2h26m from the continuation start; remaining target time is about 5h34m.
- 2026-05-19 13:50 +08:00 - Fixed Pasteur's remaining generator/gate findings: generated runtime scalar `OutSlot<CPointer<Unit>>` wrappers now release failed-HRESULT partial primitive `Object`/`String` outputs with `IInspectable`/HSTRING cleanup; ABI ownership no longer treats `return Some(debugWrap(raw))`/`Ok(debugWrap(raw))` as ownership transfer; ignored-result audit now catches multiline `let _ =` `.ok()` discards and same-line `.ok()` before work when block comments contain braces; subset codegen now rejects impl preamble payload, duplicate impl section headers, and checked-in facade internal self-aliases. Verified `scripts/test_check_workspace_setup.py` (74 passed), `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, `scripts/check_windows_common_codegen.py --self-test`, `windows-bindgen` (185 passed), full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 600`, and quick quality gate. Elapsed logged work is about 2h41m from the continuation start; remaining target time is about 5h19m.
- 2026-05-19 14:16 +08:00 - Fixed Feynman/Kepler follow-ups: ignored-result audit now catches discarded multiline block expressions such as `let _ = match (...) { ... hr.ok() ... }`; ABI ownership now masks `try`/`catch`, lambdas, and nested helper functions so conditional or nested releases cannot satisfy QueryInterface ownership; subset codegen now rejects stale checked-in facade aliases for available requested symbols while still allowing skipped metadata aliases. Fixed generated descriptor caller wrappers to release and clear failed-HRESULT `CPointer<Unit>` interface outputs before `HRESULT(hr).check()`, and descriptor producer thunks now clear result slots before dispatch. Fixed re-entrant `InterfaceResource.close()` and `InterfaceHandle.close()` by clearing/marking closed before COM `Release`. Verified `scripts/test_check_workspace_setup.py` (80 passed), `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, `scripts/check_workspace_setup.py`, `windows-implement` (27 passed), `windows-interface` (17 passed), `windows-core` (52 passed), full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 600`, and quick quality gate. Elapsed logged work is about 3h06m from the continuation start; remaining target time is about 4h53m.
- 2026-05-19 14:40 +08:00 - Fixed Kuhn's cleanup findings: core/implement failed COM out-slot cleanup now clears the caller slot before `Release`, with re-entrant release regressions; property-value HSTRING/Inspectable array failure cleanup now clears the out slot before releasing elements and can release locally allocated partial arrays even when the implementation failed before publishing the size, using exact sizes tracked by public CoTaskMem helpers. Verified `windows-core` (55 passed), `windows-implement` (28 passed), and `windows-runtime` (194 passed). Elapsed logged work is about 3h30m from the continuation start; remaining target time is about 4h29m.
- 2026-05-19 15:05 +08:00 - Fixed Hilbert's resource findings and completed Avicenna's selected-symbol codegen sync: generic WinRT handle out-range cleanup now clears slots before `Release` to avoid re-entrant double release, and `CoTaskMemRealloc(ptr, 0)` clears allocation-size tracking for the freed block. Synchronized available selected `windows-common` output after rebuilding `windows-bindgen`, including `Primitive/String` projections as `HString`, while preserving checked-in WinUI sections unavailable locally. Verified `windows-core` (57 passed) and full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 600 --skip-build`. Elapsed logged work is about 3h55m from the continuation start; remaining target time is about 4h05m.
- 2026-05-19 15:14 +08:00 - Fixed Huygens' generated-wrapper/gate findings: `Primitive/Object` WinRT projections now surface as `windows_interface.IInspectable` instead of raw `CPointer<Unit>`, and the codegen gate now maps `src/Microsoft/...` files to `Microsoft.*` namespaces. Also updated ABI/workspace audits for generic `Owned...<T>(raw)` constructor guards and the public CoTaskMem wrapper helpers. Synchronized checked-in `windows-common` available output for the `IInspectable` projection change. Verified `scripts/test_check_workspace_setup.py` (83 passed), `scripts/check_workspace_setup.py`, `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, `windows-core` (57 passed), `windows-bindgen` (186 passed), and full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 600 --skip-build`. Elapsed logged work is about 4h04m from the continuation start; remaining target time is about 3h56m.
- 2026-05-19 16:37 +08:00 - Continued from the 15:14 log. Fixed generated facade aliases for generic WinRT types so `IIterable<T>`/`IReference<T>` style aliases forward type parameters instead of producing illegal bare generic aliases. Synchronized checked-in `windows-common` available output while preserving skipped WinUI sections. Integrated Zeno's confirmed `windows-implement` weak-reference fix: delegated owner `QueryInterface` now clears stale caller slots before dispatch and still releases failed partial outputs. Verified `windows-bindgen` (187 passed), `windows-implement` (30 passed), full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 600 --skip-build`, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. Elapsed logged work is about 5h28m from the continuation start; remaining target time is about 2h32m.
- 2026-05-19 16:45 +08:00 - Fixed two manual interface wrapper re-entrancy ownership bugs: `release()` now marks manual wrappers closed before calling COM `Release`, and `intoAbi()` marks borrowed/manual wrappers closed before the balancing `AddRef`, so re-entrant callbacks cannot release the same raw pointer or erase the returned ABI reference. Added direct `Release` and `AddRef` re-entrancy regressions. Integrated Socrates' gate fix so generated delegate/runtime ABI invariants scan all `windows-common/src/impl/symbols_*.cj` chunks, not only `symbols_0.cj`. Verified `windows-interface` (19 passed), `scripts/test_check_workspace_setup.py` (84 passed), `scripts/check_workspace_setup.py`, `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. Elapsed logged work is about 5h35m from the continuation start; remaining target time is about 2h25m.
- 2026-05-19 16:57 +08:00 - Cleaned generated WinRT delegate invoker interfaces: removed redundant `public` modifiers from interface members, rebuilt `windows-bindgen`, and synchronized checked-in `windows-common` available output using symbol-key replacement across impl chunks because regenerated subset chunk numbers differ from the checked-in superset. This eliminated the 45 `interface member implies public` warnings from `windows-common` builds. Integrated Volta's SAFEARRAY leak fix: `SafeArray.cloneSelf()` now destroys partially published `SAFEARRAY` outputs when `SafeArrayCopy` returns a failed HRESULT. Verified `windows-bindgen` (187 passed), `windows-safearray` (22 passed), full `scripts/check_windows_common_codegen.py --mode full --timeout-seconds 600 --skip-build`, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. Elapsed logged work is about 5h47m from the continuation start; remaining target time is about 2h13m.
- 2026-05-19 17:09 +08:00 - Fixed collection `IndexOf` producer-thunk null out-pointer handling: `IVectorView_Impl`, `IVector_Impl`, and `IObservableVector_Impl` now reject a null `index` out slot before dispatch instead of allowing implementations to return success without writing the required ABI output. Added three direct null-index regressions and a workspace setup invariant for generated/handwritten collection `IndexOf` thunks. Integrated Nash's `windows-registry` re-entrant close fix: `Transaction.close()` and `Key.close()` clear owned handle slots and mark closed before native close dispatch. Verified `scripts/test_check_workspace_setup.py` (86 passed), `windows-runtime` (197 passed), `windows-registry` (12 passed), `scripts/check_workspace_setup.py`, `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. The first runtime test attempt was blocked by a parallel `ld.lld` target-file permission error while Nash's registry test build was still active; after that process finished, the rerun passed. Elapsed logged work is about 5h59m from the continuation start; remaining target time is about 2h01m.
- 2026-05-19 17:26 +08:00 - Fixed Dirac's gate false-positive finding: the collection `IndexOf` workspace setup invariant now accepts equivalent combined null guards such as `if (result__.isNull() || arg1.isNull())` while still requiring an `arg1.isNull()`/`E_POINTER` guard before dispatch. Added the combined-guard regression. Also fixed re-entrant native cleanup for `Variant`, `PropVariant`, and `SafeArray`: `close()`/`reset()` now use a `closing` guard around `VariantClear`/`PropVariantClear`/`SafeArrayDestroy`, so COM `Release` callbacks that re-enter the same wrapper cannot recursively clear/destroy it, while failed native cleanup still leaves ownership intact for retry. Added fake `IUnknown.Release` re-entrancy regressions for all three resources. Verified `scripts/test_check_workspace_setup.py` (87 passed), `windows-variant` (13 passed), `windows-propvariant` (17 passed), `windows-safearray` (23 passed), `scripts/check_workspace_setup.py`, `scripts/check_ignored_results.py`, `scripts/check_abi_ownership.py`, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. Dirac's finalizer-release concern was intentionally not applied because this workspace's policy keeps native cleanup explicit and finalizers local-state-only. Elapsed logged work is about 6h16m from the continuation start; remaining target time is about 1h44m.
- 2026-05-19 17:41 +08:00 - Fixed the next verified bugs from the service/string/gate pass: `Service.run()` now unregisters its active service id in a `finally` path and forces fallback exceptions through StopPending/Stop/Stoppage before rethrowing; `decodeUtf8Lossy` now emits U+FFFD for an incomplete trailing UTF-8 sequence instead of dropping it; and the ignored-result audit now catches assigned inline control/lambda expressions such as `let f = { => if (...) { HRESULT(hr).ok() }; println(...) }` while still allowing tail-returning lambdas. Verified `windows-services` (8 passed) and `windows-strings` (11 passed) before the gate patch, then verified `python -m py_compile scripts\check_ignored_results.py scripts\test_check_workspace_setup.py`, `scripts/test_check_workspace_setup.py` (89 passed), `scripts/check_ignored_results.py`, `scripts/check_workspace_setup.py`, and `scripts/check_abi_ownership.py`. Elapsed logged work is about 6h31m from the continuation start; remaining target time is about 1h29m.
- 2026-05-19 18:05 +08:00 - Fixed Erdos' generated wrapper ownership findings. Raw COM/interface handle wrappers generated by `windows-bindgen` now implement `Resource`, distinguish owned vs borrowed handles, expose `close()`, `AddRef` for owned clones, and `Release` for owned ABI takes; raw delegate `close()` now clears ownership/closed state before calling COM `Release` so re-entrant release cannot double free. Rebuilt the generator, synchronized 131 available `windows-common` impl sections, mechanically upgraded 29 checked-in WinUI raw delegate sections that local metadata cannot regenerate, updated manifest hashes, and added a workspace invariant rejecting stale raw delegate wrappers without `Resource`. Also fixed the ABI ownership audit to follow qualified helper bindings such as `windows_core.queryInterfaceRaw(...)`. Verified Cangjie docs before `.cj` edits, then ran `windows-bindgen` (187 passed), `scripts/test_check_workspace_setup.py` (91 passed), `scripts/check_abi_ownership.py`, `scripts/check_ignored_results.py`, full `scripts/check_windows_common_codegen.py --mode full --skip-build --timeout-seconds 600`, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. Elapsed logged work is about 6h56m from the continuation start; remaining target time is about 1h04m.
- 2026-05-19 18:14 +08:00 - Extended the generated raw-handle fix to four checked-in WinUI raw class sections (`ItemCollection`, `UIElementCollection`, `TransitionCollection`, `TriggerCollection`) that also lacked owned `Resource` cleanup, and tightened the workspace invariant to reject any raw handle wrapper with `projectionKind()` and `private raw` unless it implements `Resource`. Fixed a separate `windows-libloading` ABI hazard: `resolveProcByOrdinal` now rejects ordinals outside `0..65535` before calling `GetProcAddress`, so negative or oversized values cannot be interpreted as invalid string pointers. Verified `scripts/test_check_workspace_setup.py` (91 passed), `scripts/check_workspace_setup.py`, quick `scripts/check_windows_common_codegen.py --mode quick --timeout-seconds 600`, and `windows-libloading` (12 passed through `cjv exec`). Elapsed logged work is about 7h05m from the continuation start; remaining target time is about 0h55m.
- 2026-05-19 18:25 +08:00 - Fixed two more quality-gate gaps from Pascal's review: QueryInterface `match (unsafe { ... })` bodies are now parsed from the match body instead of the inner unsafe block, and discarded owned wrapper constructors such as `let _ = IUnknown.fromAbiTake(raw)` no longer count as ownership consumption. Kept legitimate tail/returned wrappers accepted, including `Result<T>.Ok(value)` and final-expression transfers after explicit cleanup. Also finished the `windows-libloading` byte-array ABI fix by requiring public uncached module/proc-name byte inputs to be NUL-terminated before passing them to Win32. Verified `python -m py_compile scripts\check_abi_ownership.py scripts\test_check_workspace_setup.py`, `scripts/test_check_workspace_setup.py` (96 passed), `scripts/check_abi_ownership.py`, `scripts/check_workspace_setup.py`, `scripts/check_ignored_results.py`, and `windows-libloading` (13 passed through `cjv exec`). Elapsed logged work is about 7h16m from the continuation start; remaining target time is about 0h44m.
- 2026-05-19 18:37 +08:00 - Fixed Mill's WinUI helper leak: `IWindowFactory.CreateInstance`, `IFrameworkElement.FindName`, and `IXamlReaderStatics.Load` now release and clear failed-HRESULT partial COM out slots before throwing, including both `inner` and `result` for `CreateInstance`. Added WinUI XAML regressions covering single and double failed out-slot cleanup. Also promoted discarded owned wrapper construction to a standalone ABI audit, so any production `let _ = ...fromAbiTake(raw)`/`takeFromAbi(raw)` is rejected outside QueryInterface-specific cases too. Verified Cangjie docs for `CPointer`/`unsafe`/control-flow syntax before editing `.cj`, then ran `windows-winui3` (appsdk 2 passed, xaml 12 passed through `cjv exec`), `scripts/test_check_workspace_setup.py` (97 passed), `scripts/check_workspace_setup.py`, `scripts/check_abi_ownership.py`, `scripts/check_ignored_results.py`, full `scripts/check_windows_common_codegen.py --mode full --skip-build --timeout-seconds 600`, and quick `python scripts\run_windows_quality_gate.py --mode quick --codegen-timeout-seconds 600`. Elapsed logged work is about 7h28m from the continuation start; remaining target time is about 0h32m.
- 2026-05-19 18:51 +08:00 - Added a workspace setup invariant for the WinUI failed out-slot cleanup pattern so future manual `IWindowFactory.CreateInstance`, `IFrameworkElement.FindName`, or `IXamlReaderStatics.Load` changes cannot move `checkHRESULT` before cleanup, and require the cleanup helper to clear the slot before `releaseRaw`. Verified `python -m py_compile scripts\check_workspace_setup.py scripts\test_check_workspace_setup.py`, `scripts/test_check_workspace_setup.py` (99 passed), `scripts/check_workspace_setup.py`, and full `python scripts\run_windows_quality_gate.py --mode full --workspace-member windows-winui3 --workspace-member windows-libloading --workspace-member windows-services --workspace-member windows-strings --workspace-member windows-bindgen --workspace-timeout-seconds 360 --codegen-timeout-seconds 600` (full codegen, selected workspace tests, and macro fixtures all passed). Elapsed logged work is about 7h41m from the continuation start; remaining target time is about 0h19m.
- 2026-05-19 19:10 +08:00 - Final continuation verification completed after the 8-hour target. Ran a full workspace test pass with `$env:cjHeapSize='32GB'` and `cjv exec` for all test members (`windows-libloading`, `windows-result`, `windows-strings`, `windows-interface`, `windows-implement`, `windows-core`, `windows-polyfill`, `windows-runtime`, `windows-threading`, `windows-version`, `windows-numerics`, `windows-variant`, `windows-propvariant`, `windows-safearray`, `windows-targets`, `windows-registry`, `windows-services`, `windows-projection`, `windows-winui3`, `windows-bindgen`), all passed. Then ran full `python scripts\run_windows_quality_gate.py --mode full --workspace-timeout-seconds 480 --codegen-timeout-seconds 600`; full codegen, all workspace tests, and macro fixtures passed. Final elapsed work from 2026-05-19 11:09:36 +08:00 is 8h00m+; remaining target time is 0h00m.
- 2026-05-19 19:48 +08:00 - Delivery closeout after review: removed generated EOF blank-line drift, normalized `windows-common` impl chunks and manifest to LF bytes, and updated manifest hashes accordingly. Verified changed Python gate scripts with `py_compile`, `git diff --check`, `python scripts\check_workspace_setup.py`, and `$env:cjHeapSize='32GB'; python scripts\check_windows_common_codegen.py --mode quick --timeout-seconds 600`; all passed. This was a generated-file formatting/hash correction after the 19:10 full gate, not a logic change.
