// Auto-generated by windows-bindgen. Do not edit.
package Windows.Win32.System.Com.StructuredStorage

import windows_interface.*
import windows_result.*
import Windows.Win32.Foundation as Win32_Foundation
import Windows.Win32.System.Com as Win32_System_Com
import Windows.Win32.System.Variant as Win32_System_Variant
import Windows.Win32.Foundation.*


@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct BSTRBLOB {
    public var cbSize: UInt32 = 0
    public var pData: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CABOOL {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Win32_Foundation.VARIANT_BOOL> = CPointer<Win32_Foundation.VARIANT_BOOL>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CABSTR {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<CPointer<UInt16>> = CPointer<CPointer<UInt16>>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CABSTRBLOB {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<BSTRBLOB> = CPointer<BSTRBLOB>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAC {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CACLIPDATA {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<CLIPDATA> = CPointer<CLIPDATA>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CACLSID {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<GUID> = CPointer<GUID>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CACY {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Win32_System_Com.CY> = CPointer<Win32_System_Com.CY>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CADATE {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Float64> = CPointer<Float64>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CADBL {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Float64> = CPointer<Float64>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAFILETIME {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Win32_Foundation.FILETIME> = CPointer<Win32_Foundation.FILETIME>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAFLT {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Float32> = CPointer<Float32>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAH {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Int64> = CPointer<Int64>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAI {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Int16> = CPointer<Int16>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAL {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Int32> = CPointer<Int32>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CALPSTR {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<CPointer<UInt8>> = CPointer<CPointer<UInt8>>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CALPWSTR {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<CPointer<UInt16>> = CPointer<CPointer<UInt16>>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAPROPVARIANT {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<PROPVARIANT> = CPointer<PROPVARIANT>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CASCODE {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<Int32> = CPointer<Int32>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAUB {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAUH {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<UInt64> = CPointer<UInt64>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAUI {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CAUL {
    public var cElems: UInt32 = 0
    public var pElems: CPointer<UInt32> = CPointer<UInt32>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct CLIPDATA {
    public var cbSize: UInt32 = 0
    public var ulClipFmt: Int32 = 0
    public var pClipData: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct IEnumSTATSTGVtbl {
    public var base_: IUnknownVtbl = IUnknownVtbl()
    public var Next: CFunc<(CPointer<Unit>, UInt32, CPointer<Win32_System_Com.STATSTG>, CPointer<UInt32>) -> Int32> = { _, _, _, _ => -2147467263i32 }
    public var Skip: CFunc<(CPointer<Unit>, UInt32) -> Int32> = { _, _ => -2147467263i32 }
    public var Reset: CFunc<(CPointer<Unit>) -> Int32> = { _ => -2147467263i32 }
    public var Clone: CFunc<(CPointer<Unit>, CPointer<CPointer<Unit>>) -> Int32> = { _, _ => -2147467263i32 }
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
func takeIEnumSTATSTGFromAbi(raw: CPointer<Unit>): IEnumSTATSTG {
    IEnumSTATSTG.fromAbiTake(raw)
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
func viewIEnumSTATSTG(raw: CPointer<Unit>): IEnumSTATSTG {
    IEnumSTATSTG(raw)
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
public class IEnumSTATSTG <: ComInterface & Resource {
    private let raw_: CPointer<Unit>
    private let ownsHandle_: Bool
    private var closed_: Bool = false

    public init(raw: CPointer<Unit>) {
        this.raw_ = raw
        this.ownsHandle_ = false
    }

    private init(raw: CPointer<Unit>, takeOwnership!: Bool) {
        this.raw_ = raw
        this.ownsHandle_ = takeOwnership
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IEnumSTATSTG {
        IEnumSTATSTG(raw, takeOwnership: true)
    }

    public static func viewOf(raw: CPointer<Unit>): IEnumSTATSTG {
        IEnumSTATSTG(raw)
    }

    public static func iid(): GUID {
        return GUID(0x0000000D, 0x0000, 0x0000, 0xC0, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x46)
    }

    public static func descriptor(): InterfaceDescriptor<IEnumSTATSTG> {
        return InterfaceDescriptor<IEnumSTATSTG>("IEnumSTATSTG", IEnumSTATSTG.iid(), takeIEnumSTATSTGFromAbi, viewIEnumSTATSTG)
    }

    public func asRaw(): CPointer<Unit> {
        if (closed_) {
            return CPointer<Unit>()
        }
        raw_
    }

    public func isClosed(): Bool { closed_ }

    unsafe func vtbl(): IEnumSTATSTGVtbl {
        let liveRaw = asRaw()
        if (liveRaw.isNull()) {
            return IEnumSTATSTGVtbl()
        }
        let vtblPtrPtr = CPointer<CPointer<IEnumSTATSTGVtbl>>(liveRaw)
        let vtblPtr = vtblPtrPtr.read()
        return vtblPtr.read()
    }

    public func addRef(): UInt32 {
        let liveRaw = asRaw()
        if (liveRaw.isNull()) {
            return 0u32
        }
        unsafe {
            let baseVtblPtr = CPointer<CPointer<IUnknownVtbl>>(liveRaw).read()
            let baseVtbl = baseVtblPtr.read()
            return baseVtbl.AddRef(liveRaw)
        }
    }

    public func release(): UInt32 {
        let liveRaw = asRaw()
        if (liveRaw.isNull()) {
            return 0u32
        }
        unsafe {
            let baseVtblPtr = CPointer<CPointer<IUnknownVtbl>>(liveRaw).read()
            let baseVtbl = baseVtblPtr.read()
            let count = baseVtbl.Release(liveRaw)
            closed_ = true
            return count
        }
    }

    public unsafe func query<T>(descriptor: InterfaceDescriptor<T>): Option<T> {
        queryInterfaceAs(asRaw(), descriptor)
    }

    public func close(): Unit {
        let liveRaw = asRaw()
        if (!liveRaw.isNull() && ownsHandle_) {
            unsafe {
                let baseVtblPtr = CPointer<CPointer<IUnknownVtbl>>(liveRaw).read()
                let baseVtbl = baseVtblPtr.read()
                baseVtbl.Release(liveRaw)
            }
        }
        closed_ = true
    }

    public unsafe func Next(celt: UInt32, rgelt: CPointer<Win32_System_Com.STATSTG>, pceltFetched: CPointer<UInt32>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.Next(liveRaw, celt, rgelt, pceltFetched)
    }

    public unsafe func Skip(celt: UInt32): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.Skip(liveRaw, celt)
    }

    public unsafe func Reset(): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.Reset(liveRaw)
    }

    public unsafe func Clone(ppenum: CPointer<CPointer<Unit>>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.Clone(liveRaw, ppenum)
    }
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct IStorageVtbl {
    public var base_: IUnknownVtbl = IUnknownVtbl()
    public var CreateStream: CFunc<(CPointer<Unit>, CPointer<UInt16>, Win32_System_Com.STGM, UInt32, UInt32, CPointer<CPointer<Unit>>) -> Int32> = { _, _, _, _, _, _ => -2147467263i32 }
    public var OpenStream: CFunc<(CPointer<Unit>, CPointer<UInt16>, CPointer<Unit>, Win32_System_Com.STGM, UInt32, CPointer<CPointer<Unit>>) -> Int32> = { _, _, _, _, _, _ => -2147467263i32 }
    public var CreateStorage: CFunc<(CPointer<Unit>, CPointer<UInt16>, Win32_System_Com.STGM, UInt32, UInt32, CPointer<CPointer<Unit>>) -> Int32> = { _, _, _, _, _, _ => -2147467263i32 }
    public var OpenStorage: CFunc<(CPointer<Unit>, CPointer<UInt16>, CPointer<Unit>, Win32_System_Com.STGM, CPointer<CPointer<UInt16>>, UInt32, CPointer<CPointer<Unit>>) -> Int32> = { _, _, _, _, _, _, _ => -2147467263i32 }
    public var CopyTo: CFunc<(CPointer<Unit>, UInt32, CPointer<GUID>, CPointer<CPointer<UInt16>>, CPointer<Unit>) -> Int32> = { _, _, _, _, _ => -2147467263i32 }
    public var MoveElementTo: CFunc<(CPointer<Unit>, CPointer<UInt16>, CPointer<Unit>, CPointer<UInt16>, UInt32) -> Int32> = { _, _, _, _, _ => -2147467263i32 }
    public var Commit: CFunc<(CPointer<Unit>, UInt32) -> Int32> = { _, _ => -2147467263i32 }
    public var Revert: CFunc<(CPointer<Unit>) -> Int32> = { _ => -2147467263i32 }
    public var EnumElements: CFunc<(CPointer<Unit>, UInt32, CPointer<Unit>, UInt32, CPointer<CPointer<Unit>>) -> Int32> = { _, _, _, _, _ => -2147467263i32 }
    public var DestroyElement: CFunc<(CPointer<Unit>, CPointer<UInt16>) -> Int32> = { _, _ => -2147467263i32 }
    public var RenameElement: CFunc<(CPointer<Unit>, CPointer<UInt16>, CPointer<UInt16>) -> Int32> = { _, _, _ => -2147467263i32 }
    public var SetElementTimes: CFunc<(CPointer<Unit>, CPointer<UInt16>, CPointer<Win32_Foundation.FILETIME>, CPointer<Win32_Foundation.FILETIME>, CPointer<Win32_Foundation.FILETIME>) -> Int32> = { _, _, _, _, _ => -2147467263i32 }
    public var SetClass: CFunc<(CPointer<Unit>, CPointer<GUID>) -> Int32> = { _, _ => -2147467263i32 }
    public var SetStateBits: CFunc<(CPointer<Unit>, UInt32, UInt32) -> Int32> = { _, _, _ => -2147467263i32 }
    public var Stat: CFunc<(CPointer<Unit>, CPointer<Win32_System_Com.STATSTG>, UInt32) -> Int32> = { _, _, _ => -2147467263i32 }
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
func takeIStorageFromAbi(raw: CPointer<Unit>): IStorage {
    IStorage.fromAbiTake(raw)
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
func viewIStorage(raw: CPointer<Unit>): IStorage {
    IStorage(raw)
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
public class IStorage <: ComInterface & Resource {
    private let raw_: CPointer<Unit>
    private let ownsHandle_: Bool
    private var closed_: Bool = false

    public init(raw: CPointer<Unit>) {
        this.raw_ = raw
        this.ownsHandle_ = false
    }

    private init(raw: CPointer<Unit>, takeOwnership!: Bool) {
        this.raw_ = raw
        this.ownsHandle_ = takeOwnership
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IStorage {
        IStorage(raw, takeOwnership: true)
    }

    public static func viewOf(raw: CPointer<Unit>): IStorage {
        IStorage(raw)
    }

    public static func iid(): GUID {
        return GUID(0x0000000B, 0x0000, 0x0000, 0xC0, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x46)
    }

    public static func descriptor(): InterfaceDescriptor<IStorage> {
        return InterfaceDescriptor<IStorage>("IStorage", IStorage.iid(), takeIStorageFromAbi, viewIStorage)
    }

    public func asRaw(): CPointer<Unit> {
        if (closed_) {
            return CPointer<Unit>()
        }
        raw_
    }

    public func isClosed(): Bool { closed_ }

    unsafe func vtbl(): IStorageVtbl {
        let liveRaw = asRaw()
        if (liveRaw.isNull()) {
            return IStorageVtbl()
        }
        let vtblPtrPtr = CPointer<CPointer<IStorageVtbl>>(liveRaw)
        let vtblPtr = vtblPtrPtr.read()
        return vtblPtr.read()
    }

    public func addRef(): UInt32 {
        let liveRaw = asRaw()
        if (liveRaw.isNull()) {
            return 0u32
        }
        unsafe {
            let baseVtblPtr = CPointer<CPointer<IUnknownVtbl>>(liveRaw).read()
            let baseVtbl = baseVtblPtr.read()
            return baseVtbl.AddRef(liveRaw)
        }
    }

    public func release(): UInt32 {
        let liveRaw = asRaw()
        if (liveRaw.isNull()) {
            return 0u32
        }
        unsafe {
            let baseVtblPtr = CPointer<CPointer<IUnknownVtbl>>(liveRaw).read()
            let baseVtbl = baseVtblPtr.read()
            let count = baseVtbl.Release(liveRaw)
            closed_ = true
            return count
        }
    }

    public unsafe func query<T>(descriptor: InterfaceDescriptor<T>): Option<T> {
        queryInterfaceAs(asRaw(), descriptor)
    }

    public func close(): Unit {
        let liveRaw = asRaw()
        if (!liveRaw.isNull() && ownsHandle_) {
            unsafe {
                let baseVtblPtr = CPointer<CPointer<IUnknownVtbl>>(liveRaw).read()
                let baseVtbl = baseVtblPtr.read()
                baseVtbl.Release(liveRaw)
            }
        }
        closed_ = true
    }

    public unsafe func CreateStream(pwcsName: CPointer<UInt16>, grfMode: Win32_System_Com.STGM, reserved1: UInt32, reserved2: UInt32, ppstm: CPointer<CPointer<Unit>>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.CreateStream(liveRaw, pwcsName, grfMode, reserved1, reserved2, ppstm)
    }

    public unsafe func OpenStream(pwcsName: CPointer<UInt16>, reserved1: CPointer<Unit>, grfMode: Win32_System_Com.STGM, reserved2: UInt32, ppstm: CPointer<CPointer<Unit>>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.OpenStream(liveRaw, pwcsName, reserved1, grfMode, reserved2, ppstm)
    }

    public unsafe func CreateStorage(pwcsName: CPointer<UInt16>, grfMode: Win32_System_Com.STGM, reserved1: UInt32, reserved2: UInt32, ppstg: CPointer<CPointer<Unit>>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.CreateStorage(liveRaw, pwcsName, grfMode, reserved1, reserved2, ppstg)
    }

    public unsafe func OpenStorage(pwcsName: CPointer<UInt16>, pstgPriority: CPointer<Unit>, grfMode: Win32_System_Com.STGM, snbExclude: CPointer<CPointer<UInt16>>, reserved: UInt32, ppstg: CPointer<CPointer<Unit>>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.OpenStorage(liveRaw, pwcsName, pstgPriority, grfMode, snbExclude, reserved, ppstg)
    }

    public unsafe func CopyTo(ciidExclude: UInt32, rgiidExclude: CPointer<GUID>, snbExclude: CPointer<CPointer<UInt16>>, pstgDest: CPointer<Unit>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.CopyTo(liveRaw, ciidExclude, rgiidExclude, snbExclude, pstgDest)
    }

    public unsafe func MoveElementTo(pwcsName: CPointer<UInt16>, pstgDest: CPointer<Unit>, pwcsNewName: CPointer<UInt16>, grfFlags: UInt32): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.MoveElementTo(liveRaw, pwcsName, pstgDest, pwcsNewName, grfFlags)
    }

    public unsafe func Commit(grfCommitFlags: UInt32): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.Commit(liveRaw, grfCommitFlags)
    }

    public unsafe func Revert(): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.Revert(liveRaw)
    }

    public unsafe func EnumElements(reserved1: UInt32, reserved2: CPointer<Unit>, reserved3: UInt32, ppenum: CPointer<CPointer<Unit>>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.EnumElements(liveRaw, reserved1, reserved2, reserved3, ppenum)
    }

    public unsafe func DestroyElement(pwcsName: CPointer<UInt16>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.DestroyElement(liveRaw, pwcsName)
    }

    public unsafe func RenameElement(pwcsOldName: CPointer<UInt16>, pwcsNewName: CPointer<UInt16>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.RenameElement(liveRaw, pwcsOldName, pwcsNewName)
    }

    public unsafe func SetElementTimes(pwcsName: CPointer<UInt16>, pctime: CPointer<Win32_Foundation.FILETIME>, patime: CPointer<Win32_Foundation.FILETIME>, pmtime: CPointer<Win32_Foundation.FILETIME>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.SetElementTimes(liveRaw, pwcsName, pctime, patime, pmtime)
    }

    public unsafe func SetClass(clsid: CPointer<GUID>): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.SetClass(liveRaw, clsid)
    }

    public unsafe func SetStateBits(grfStateBits: UInt32, grfMask: UInt32): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.SetStateBits(liveRaw, grfStateBits, grfMask)
    }

    public unsafe func Stat(pstatstg: CPointer<Win32_System_Com.STATSTG>, grfStatFlag: UInt32): Int32 {
        let v = vtbl()
        let liveRaw = asRaw()
        return v.Stat(liveRaw, pstatstg, grfStatFlag)
    }
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct PROPVARIANT {
    public var Anonymous: PROPVARIANT__Anonymous_e__Union = PROPVARIANT__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPVARIANT__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $3> = VArray<UInt64, $3>(repeat: 0)

    public func anonymous(): PROPVARIANT__Anonymous_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<PROPVARIANT__Anonymous_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: PROPVARIANT__Anonymous_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<PROPVARIANT__Anonymous_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func decVal(): Win32_Foundation.DECIMAL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.DECIMAL>(p)
            return ptr.read()
        }
    }

    public mut func set_decVal(val: Win32_Foundation.DECIMAL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.DECIMAL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct PROPVARIANT__Anonymous_e__Union__Anonymous_e__Struct {
    public var vt: Win32_System_Variant.VARENUM = Win32_System_Variant.VARENUM()
    public var wReserved1: UInt16 = 0
    public var wReserved2: UInt16 = 0
    public var wReserved3: UInt16 = 0
    public var Anonymous: PROPVARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union = PROPVARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPVARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0)

    public func cVal(): Win32_Foundation.CHAR {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.CHAR>(p)
            return ptr.read()
        }
    }

    public mut func set_cVal(val: Win32_Foundation.CHAR): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.CHAR>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func bVal(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt8>(p)
            return ptr.read()
        }
    }

    public mut func set_bVal(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt8>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func iVal(): Int16 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int16>(p)
            return ptr.read()
        }
    }

    public mut func set_iVal(val: Int16): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int16>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func uiVal(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt16>(p)
            return ptr.read()
        }
    }

    public mut func set_uiVal(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt16>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func lVal(): Int32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            return ptr.read()
        }
    }

    public mut func set_lVal(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ulVal(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_ulVal(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func intVal(): Int32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            return ptr.read()
        }
    }

    public mut func set_intVal(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func uintVal(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_uintVal(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func hVal(): Int64 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int64>(p)
            return ptr.read()
        }
    }

    public mut func set_hVal(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int64>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func uhVal(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt64>(p)
            return ptr.read()
        }
    }

    public mut func set_uhVal(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt64>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func fltVal(): Float32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float32>(p)
            return ptr.read()
        }
    }

    public mut func set_fltVal(val: Float32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dblVal(): Float64 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float64>(p)
            return ptr.read()
        }
    }

    public mut func set_dblVal(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float64>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func boolVal(): Win32_Foundation.VARIANT_BOOL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.VARIANT_BOOL>(p)
            return ptr.read()
        }
    }

    public mut func set_boolVal(val: Win32_Foundation.VARIANT_BOOL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.VARIANT_BOOL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func __OBSOLETE__VARIANT_BOOL(): Win32_Foundation.VARIANT_BOOL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.VARIANT_BOOL>(p)
            return ptr.read()
        }
    }

    public mut func set___OBSOLETE__VARIANT_BOOL(val: Win32_Foundation.VARIANT_BOOL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.VARIANT_BOOL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func scode(): Int32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            return ptr.read()
        }
    }

    public mut func set_scode(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cyVal(): Win32_System_Com.CY {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_System_Com.CY>(p)
            return ptr.read()
        }
    }

    public mut func set_cyVal(val: Win32_System_Com.CY): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_System_Com.CY>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func date(): Float64 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float64>(p)
            return ptr.read()
        }
    }

    public mut func set_date(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float64>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func filetime(): Win32_Foundation.FILETIME {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.FILETIME>(p)
            return ptr.read()
        }
    }

    public mut func set_filetime(val: Win32_Foundation.FILETIME): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.FILETIME>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func puuid(): CPointer<GUID> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<GUID>>(p)
            return ptr.read()
        }
    }

    public mut func set_puuid(val: CPointer<GUID>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<GUID>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pclipdata(): CPointer<CLIPDATA> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CLIPDATA>>(p)
            return ptr.read()
        }
    }

    public mut func set_pclipdata(val: CPointer<CLIPDATA>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CLIPDATA>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func bstrVal(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_bstrVal(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func bstrblobVal(): BSTRBLOB {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<BSTRBLOB>(p)
            return ptr.read()
        }
    }

    public mut func set_bstrblobVal(val: BSTRBLOB): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<BSTRBLOB>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func blob(): Win32_System_Com.BLOB {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_System_Com.BLOB>(p)
            return ptr.read()
        }
    }

    public mut func set_blob(val: Win32_System_Com.BLOB): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_System_Com.BLOB>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszVal(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszVal(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pwszVal(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pwszVal(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func punkVal(): CPointer<Unit> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            return ptr.read()
        }
    }

    public mut func set_punkVal(val: CPointer<Unit>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pdispVal(): CPointer<Unit> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            return ptr.read()
        }
    }

    public mut func set_pdispVal(val: CPointer<Unit>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pStream(): CPointer<Unit> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            return ptr.read()
        }
    }

    public mut func set_pStream(val: CPointer<Unit>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pStorage(): CPointer<Unit> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            return ptr.read()
        }
    }

    public mut func set_pStorage(val: CPointer<Unit>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pVersionedStream(): CPointer<VERSIONEDSTREAM> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<VERSIONEDSTREAM>>(p)
            return ptr.read()
        }
    }

    public mut func set_pVersionedStream(val: CPointer<VERSIONEDSTREAM>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<VERSIONEDSTREAM>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func parray(): CPointer<Win32_System_Com.SAFEARRAY> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_System_Com.SAFEARRAY>>(p)
            return ptr.read()
        }
    }

    public mut func set_parray(val: CPointer<Win32_System_Com.SAFEARRAY>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_System_Com.SAFEARRAY>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cac(): CAC {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAC>(p)
            return ptr.read()
        }
    }

    public mut func set_cac(val: CAC): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAC>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func caub(): CAUB {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAUB>(p)
            return ptr.read()
        }
    }

    public mut func set_caub(val: CAUB): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAUB>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cai(): CAI {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAI>(p)
            return ptr.read()
        }
    }

    public mut func set_cai(val: CAI): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAI>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func caui(): CAUI {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAUI>(p)
            return ptr.read()
        }
    }

    public mut func set_caui(val: CAUI): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAUI>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cal(): CAL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAL>(p)
            return ptr.read()
        }
    }

    public mut func set_cal(val: CAL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func caul(): CAUL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAUL>(p)
            return ptr.read()
        }
    }

    public mut func set_caul(val: CAUL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAUL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cah(): CAH {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAH>(p)
            return ptr.read()
        }
    }

    public mut func set_cah(val: CAH): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAH>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cauh(): CAUH {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAUH>(p)
            return ptr.read()
        }
    }

    public mut func set_cauh(val: CAUH): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAUH>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func caflt(): CAFLT {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAFLT>(p)
            return ptr.read()
        }
    }

    public mut func set_caflt(val: CAFLT): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAFLT>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cadbl(): CADBL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CADBL>(p)
            return ptr.read()
        }
    }

    public mut func set_cadbl(val: CADBL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CADBL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cabool(): CABOOL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CABOOL>(p)
            return ptr.read()
        }
    }

    public mut func set_cabool(val: CABOOL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CABOOL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cascode(): CASCODE {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CASCODE>(p)
            return ptr.read()
        }
    }

    public mut func set_cascode(val: CASCODE): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CASCODE>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cacy(): CACY {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CACY>(p)
            return ptr.read()
        }
    }

    public mut func set_cacy(val: CACY): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CACY>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cadate(): CADATE {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CADATE>(p)
            return ptr.read()
        }
    }

    public mut func set_cadate(val: CADATE): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CADATE>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cafiletime(): CAFILETIME {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAFILETIME>(p)
            return ptr.read()
        }
    }

    public mut func set_cafiletime(val: CAFILETIME): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAFILETIME>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cauuid(): CACLSID {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CACLSID>(p)
            return ptr.read()
        }
    }

    public mut func set_cauuid(val: CACLSID): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CACLSID>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func caclipdata(): CACLIPDATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CACLIPDATA>(p)
            return ptr.read()
        }
    }

    public mut func set_caclipdata(val: CACLIPDATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CACLIPDATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cabstr(): CABSTR {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CABSTR>(p)
            return ptr.read()
        }
    }

    public mut func set_cabstr(val: CABSTR): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CABSTR>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cabstrblob(): CABSTRBLOB {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CABSTRBLOB>(p)
            return ptr.read()
        }
    }

    public mut func set_cabstrblob(val: CABSTRBLOB): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CABSTRBLOB>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func calpstr(): CALPSTR {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CALPSTR>(p)
            return ptr.read()
        }
    }

    public mut func set_calpstr(val: CALPSTR): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CALPSTR>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func calpwstr(): CALPWSTR {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CALPWSTR>(p)
            return ptr.read()
        }
    }

    public mut func set_calpwstr(val: CALPWSTR): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CALPWSTR>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func capropvar(): CAPROPVARIANT {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAPROPVARIANT>(p)
            return ptr.read()
        }
    }

    public mut func set_capropvar(val: CAPROPVARIANT): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CAPROPVARIANT>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pcVal(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pcVal(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pbVal(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pbVal(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func piVal(): CPointer<Int16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int16>>(p)
            return ptr.read()
        }
    }

    public mut func set_piVal(val: CPointer<Int16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func puiVal(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_puiVal(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func plVal(): CPointer<Int32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            return ptr.read()
        }
    }

    public mut func set_plVal(val: CPointer<Int32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pulVal(): CPointer<UInt32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt32>>(p)
            return ptr.read()
        }
    }

    public mut func set_pulVal(val: CPointer<UInt32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pintVal(): CPointer<Int32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            return ptr.read()
        }
    }

    public mut func set_pintVal(val: CPointer<Int32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func puintVal(): CPointer<UInt32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt32>>(p)
            return ptr.read()
        }
    }

    public mut func set_puintVal(val: CPointer<UInt32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pfltVal(): CPointer<Float32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float32>>(p)
            return ptr.read()
        }
    }

    public mut func set_pfltVal(val: CPointer<Float32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pdblVal(): CPointer<Float64> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float64>>(p)
            return ptr.read()
        }
    }

    public mut func set_pdblVal(val: CPointer<Float64>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float64>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pboolVal(): CPointer<Win32_Foundation.VARIANT_BOOL> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.VARIANT_BOOL>>(p)
            return ptr.read()
        }
    }

    public mut func set_pboolVal(val: CPointer<Win32_Foundation.VARIANT_BOOL>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.VARIANT_BOOL>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pdecVal(): CPointer<Win32_Foundation.DECIMAL> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.DECIMAL>>(p)
            return ptr.read()
        }
    }

    public mut func set_pdecVal(val: CPointer<Win32_Foundation.DECIMAL>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.DECIMAL>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pscode(): CPointer<Int32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            return ptr.read()
        }
    }

    public mut func set_pscode(val: CPointer<Int32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pcyVal(): CPointer<Win32_System_Com.CY> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_System_Com.CY>>(p)
            return ptr.read()
        }
    }

    public mut func set_pcyVal(val: CPointer<Win32_System_Com.CY>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_System_Com.CY>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pdate(): CPointer<Float64> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float64>>(p)
            return ptr.read()
        }
    }

    public mut func set_pdate(val: CPointer<Float64>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float64>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pbstrVal(): CPointer<CPointer<UInt16>> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<UInt16>>>(p)
            return ptr.read()
        }
    }

    public mut func set_pbstrVal(val: CPointer<CPointer<UInt16>>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<UInt16>>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ppunkVal(): CPointer<CPointer<Unit>> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
            return ptr.read()
        }
    }

    public mut func set_ppunkVal(val: CPointer<CPointer<Unit>>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ppdispVal(): CPointer<CPointer<Unit>> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
            return ptr.read()
        }
    }

    public mut func set_ppdispVal(val: CPointer<CPointer<Unit>>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pparray(): CPointer<CPointer<Win32_System_Com.SAFEARRAY>> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Win32_System_Com.SAFEARRAY>>>(p)
            return ptr.read()
        }
    }

    public mut func set_pparray(val: CPointer<CPointer<Win32_System_Com.SAFEARRAY>>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Win32_System_Com.SAFEARRAY>>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pvarVal(): CPointer<PROPVARIANT> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<PROPVARIANT>>(p)
            return ptr.read()
        }
    }

    public mut func set_pvarVal(val: CPointer<PROPVARIANT>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<PROPVARIANT>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Com_StructuredStorage == "on"]
@C
public struct VERSIONEDSTREAM {
    public var guidVersion: GUID = GUID()
    public var pStream: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}
