// Auto-generated by windows-bindgen. Do not edit.
package Windows.Win32.System.Variant

import windows_interface.*
import windows_result.*
import Windows.Win32.Foundation as Win32_Foundation
import Windows.Win32.System.Com as Win32_System_Com
import Windows.Win32.System.Ole as Win32_System_Ole
import Windows.Win32.Foundation.*


@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARENUM {
    public var value: UInt16 = 0

    public init() {}

    public init(value: UInt16) {
        this.value = value
    }

    public operator func ==(other: VARENUM): Bool {
        return this.value == other.value
    }
    public operator func !=(other: VARENUM): Bool {
        return this.value != other.value
    }

    public func contains(other: VARENUM): Bool {
        return (this.value & other.value) == other.value
    }

    public operator func |(other: VARENUM): VARENUM {
        return VARENUM(this.value | other.value)
    }
    public operator func &(other: VARENUM): VARENUM {
        return VARENUM(this.value & other.value)
    }
    public operator func !(): VARENUM {
        return VARENUM(!this.value)
    }
}

@When[Windows_Win32_System_Variant == "on"]
public let VT_EMPTY: VARENUM = VARENUM(0)

@When[Windows_Win32_System_Variant == "on"]
public let VT_NULL: VARENUM = VARENUM(1)

@When[Windows_Win32_System_Variant == "on"]
public let VT_I2: VARENUM = VARENUM(2)

@When[Windows_Win32_System_Variant == "on"]
public let VT_I4: VARENUM = VARENUM(3)

@When[Windows_Win32_System_Variant == "on"]
public let VT_R4: VARENUM = VARENUM(4)

@When[Windows_Win32_System_Variant == "on"]
public let VT_R8: VARENUM = VARENUM(5)

@When[Windows_Win32_System_Variant == "on"]
public let VT_CY: VARENUM = VARENUM(6)

@When[Windows_Win32_System_Variant == "on"]
public let VT_DATE: VARENUM = VARENUM(7)

@When[Windows_Win32_System_Variant == "on"]
public let VT_BSTR: VARENUM = VARENUM(8)

@When[Windows_Win32_System_Variant == "on"]
public let VT_DISPATCH: VARENUM = VARENUM(9)

@When[Windows_Win32_System_Variant == "on"]
public let VT_ERROR: VARENUM = VARENUM(10)

@When[Windows_Win32_System_Variant == "on"]
public let VT_BOOL: VARENUM = VARENUM(11)

@When[Windows_Win32_System_Variant == "on"]
public let VT_VARIANT: VARENUM = VARENUM(12)

@When[Windows_Win32_System_Variant == "on"]
public let VT_UNKNOWN: VARENUM = VARENUM(13)

@When[Windows_Win32_System_Variant == "on"]
public let VT_DECIMAL: VARENUM = VARENUM(14)

@When[Windows_Win32_System_Variant == "on"]
public let VT_I1: VARENUM = VARENUM(16)

@When[Windows_Win32_System_Variant == "on"]
public let VT_UI1: VARENUM = VARENUM(17)

@When[Windows_Win32_System_Variant == "on"]
public let VT_UI2: VARENUM = VARENUM(18)

@When[Windows_Win32_System_Variant == "on"]
public let VT_UI4: VARENUM = VARENUM(19)

@When[Windows_Win32_System_Variant == "on"]
public let VT_I8: VARENUM = VARENUM(20)

@When[Windows_Win32_System_Variant == "on"]
public let VT_UI8: VARENUM = VARENUM(21)

@When[Windows_Win32_System_Variant == "on"]
public let VT_INT: VARENUM = VARENUM(22)

@When[Windows_Win32_System_Variant == "on"]
public let VT_UINT: VARENUM = VARENUM(23)

@When[Windows_Win32_System_Variant == "on"]
public let VT_VOID: VARENUM = VARENUM(24)

@When[Windows_Win32_System_Variant == "on"]
public let VT_HRESULT: VARENUM = VARENUM(25)

@When[Windows_Win32_System_Variant == "on"]
public let VT_PTR: VARENUM = VARENUM(26)

@When[Windows_Win32_System_Variant == "on"]
public let VT_SAFEARRAY: VARENUM = VARENUM(27)

@When[Windows_Win32_System_Variant == "on"]
public let VT_CARRAY: VARENUM = VARENUM(28)

@When[Windows_Win32_System_Variant == "on"]
public let VT_USERDEFINED: VARENUM = VARENUM(29)

@When[Windows_Win32_System_Variant == "on"]
public let VT_LPSTR: VARENUM = VARENUM(30)

@When[Windows_Win32_System_Variant == "on"]
public let VT_LPWSTR: VARENUM = VARENUM(31)

@When[Windows_Win32_System_Variant == "on"]
public let VT_RECORD: VARENUM = VARENUM(36)

@When[Windows_Win32_System_Variant == "on"]
public let VT_INT_PTR: VARENUM = VARENUM(37)

@When[Windows_Win32_System_Variant == "on"]
public let VT_UINT_PTR: VARENUM = VARENUM(38)

@When[Windows_Win32_System_Variant == "on"]
public let VT_FILETIME: VARENUM = VARENUM(64)

@When[Windows_Win32_System_Variant == "on"]
public let VT_BLOB: VARENUM = VARENUM(65)

@When[Windows_Win32_System_Variant == "on"]
public let VT_STREAM: VARENUM = VARENUM(66)

@When[Windows_Win32_System_Variant == "on"]
public let VT_STORAGE: VARENUM = VARENUM(67)

@When[Windows_Win32_System_Variant == "on"]
public let VT_STREAMED_OBJECT: VARENUM = VARENUM(68)

@When[Windows_Win32_System_Variant == "on"]
public let VT_STORED_OBJECT: VARENUM = VARENUM(69)

@When[Windows_Win32_System_Variant == "on"]
public let VT_BLOB_OBJECT: VARENUM = VARENUM(70)

@When[Windows_Win32_System_Variant == "on"]
public let VT_CF: VARENUM = VARENUM(71)

@When[Windows_Win32_System_Variant == "on"]
public let VT_CLSID: VARENUM = VARENUM(72)

@When[Windows_Win32_System_Variant == "on"]
public let VT_VERSIONED_STREAM: VARENUM = VARENUM(73)

@When[Windows_Win32_System_Variant == "on"]
public let VT_BSTR_BLOB: VARENUM = VARENUM(4095)

@When[Windows_Win32_System_Variant == "on"]
public let VT_VECTOR: VARENUM = VARENUM(4096)

@When[Windows_Win32_System_Variant == "on"]
public let VT_ARRAY: VARENUM = VARENUM(8192)

@When[Windows_Win32_System_Variant == "on"]
public let VT_BYREF: VARENUM = VARENUM(16384)

@When[Windows_Win32_System_Variant == "on"]
public let VT_RESERVED: VARENUM = VARENUM(32768)

@When[Windows_Win32_System_Variant == "on"]
public let VT_ILLEGAL: VARENUM = VARENUM(65535)

@When[Windows_Win32_System_Variant == "on"]
public let VT_ILLEGALMASKED: VARENUM = VARENUM(4095)

@When[Windows_Win32_System_Variant == "on"]
public let VT_TYPEMASK: VARENUM = VARENUM(4095)

@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARIANT {
    public var Anonymous: VARIANT__Anonymous_e__Union = VARIANT__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Variant == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct VARIANT__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $3> = VArray<UInt64, $3>(repeat: 0)

    public func anonymous(): VARIANT__Anonymous_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: VARIANT__Anonymous_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func decVal(): Win32_Foundation.DECIMAL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.DECIMAL>(p)
            return ptr.read()
        }
    }

    public mut func set_decVal(val: Win32_Foundation.DECIMAL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.DECIMAL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct {
    public var vt: VARENUM = VARENUM()
    public var wReserved1: UInt16 = 0
    public var wReserved2: UInt16 = 0
    public var wReserved3: UInt16 = 0
    public var Anonymous: VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union = VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Variant == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0)

    public func llVal(): Int64 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int64>(p)
            return ptr.read()
        }
    }

    public mut func set_llVal(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int64>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func lVal(): Int32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            return ptr.read()
        }
    }

    public mut func set_lVal(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func bVal(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt8>(p)
            return ptr.read()
        }
    }

    public mut func set_bVal(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt8>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func iVal(): Int16 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int16>(p)
            return ptr.read()
        }
    }

    public mut func set_iVal(val: Int16): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int16>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func fltVal(): Float32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float32>(p)
            return ptr.read()
        }
    }

    public mut func set_fltVal(val: Float32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dblVal(): Float64 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float64>(p)
            return ptr.read()
        }
    }

    public mut func set_dblVal(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float64>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func boolVal(): Win32_Foundation.VARIANT_BOOL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.VARIANT_BOOL>(p)
            return ptr.read()
        }
    }

    public mut func set_boolVal(val: Win32_Foundation.VARIANT_BOOL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.VARIANT_BOOL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func __OBSOLETE__VARIANT_BOOL(): Win32_Foundation.VARIANT_BOOL {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.VARIANT_BOOL>(p)
            return ptr.read()
        }
    }

    public mut func set___OBSOLETE__VARIANT_BOOL(val: Win32_Foundation.VARIANT_BOOL): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.VARIANT_BOOL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func scode(): Int32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            return ptr.read()
        }
    }

    public mut func set_scode(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cyVal(): Win32_System_Com.CY {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_System_Com.CY>(p)
            return ptr.read()
        }
    }

    public mut func set_cyVal(val: Win32_System_Com.CY): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_System_Com.CY>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func date(): Float64 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float64>(p)
            return ptr.read()
        }
    }

    public mut func set_date(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Float64>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func bstrVal(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_bstrVal(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func punkVal(): CPointer<Unit> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            return ptr.read()
        }
    }

    public mut func set_punkVal(val: CPointer<Unit>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pdispVal(): CPointer<Unit> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            return ptr.read()
        }
    }

    public mut func set_pdispVal(val: CPointer<Unit>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func parray(): CPointer<Win32_System_Com.SAFEARRAY> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_System_Com.SAFEARRAY>>(p)
            return ptr.read()
        }
    }

    public mut func set_parray(val: CPointer<Win32_System_Com.SAFEARRAY>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_System_Com.SAFEARRAY>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pbVal(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pbVal(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func piVal(): CPointer<Int16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int16>>(p)
            return ptr.read()
        }
    }

    public mut func set_piVal(val: CPointer<Int16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func plVal(): CPointer<Int32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            return ptr.read()
        }
    }

    public mut func set_plVal(val: CPointer<Int32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pllVal(): CPointer<Int64> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int64>>(p)
            return ptr.read()
        }
    }

    public mut func set_pllVal(val: CPointer<Int64>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int64>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pfltVal(): CPointer<Float32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float32>>(p)
            return ptr.read()
        }
    }

    public mut func set_pfltVal(val: CPointer<Float32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pdblVal(): CPointer<Float64> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float64>>(p)
            return ptr.read()
        }
    }

    public mut func set_pdblVal(val: CPointer<Float64>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float64>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pboolVal(): CPointer<Win32_Foundation.VARIANT_BOOL> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.VARIANT_BOOL>>(p)
            return ptr.read()
        }
    }

    public mut func set_pboolVal(val: CPointer<Win32_Foundation.VARIANT_BOOL>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.VARIANT_BOOL>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func __OBSOLETE__VARIANT_PBOOL(): CPointer<Win32_Foundation.VARIANT_BOOL> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.VARIANT_BOOL>>(p)
            return ptr.read()
        }
    }

    public mut func set___OBSOLETE__VARIANT_PBOOL(val: CPointer<Win32_Foundation.VARIANT_BOOL>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.VARIANT_BOOL>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pscode(): CPointer<Int32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            return ptr.read()
        }
    }

    public mut func set_pscode(val: CPointer<Int32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pcyVal(): CPointer<Win32_System_Com.CY> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_System_Com.CY>>(p)
            return ptr.read()
        }
    }

    public mut func set_pcyVal(val: CPointer<Win32_System_Com.CY>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_System_Com.CY>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pdate(): CPointer<Float64> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float64>>(p)
            return ptr.read()
        }
    }

    public mut func set_pdate(val: CPointer<Float64>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Float64>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pbstrVal(): CPointer<CPointer<UInt16>> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<UInt16>>>(p)
            return ptr.read()
        }
    }

    public mut func set_pbstrVal(val: CPointer<CPointer<UInt16>>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<UInt16>>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ppunkVal(): CPointer<CPointer<Unit>> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
            return ptr.read()
        }
    }

    public mut func set_ppunkVal(val: CPointer<CPointer<Unit>>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ppdispVal(): CPointer<CPointer<Unit>> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
            return ptr.read()
        }
    }

    public mut func set_ppdispVal(val: CPointer<CPointer<Unit>>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pparray(): CPointer<CPointer<Win32_System_Com.SAFEARRAY>> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Win32_System_Com.SAFEARRAY>>>(p)
            return ptr.read()
        }
    }

    public mut func set_pparray(val: CPointer<CPointer<Win32_System_Com.SAFEARRAY>>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<CPointer<Win32_System_Com.SAFEARRAY>>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pvarVal(): CPointer<VARIANT> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<VARIANT>>(p)
            return ptr.read()
        }
    }

    public mut func set_pvarVal(val: CPointer<VARIANT>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<VARIANT>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func byref(): CPointer<Unit> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            return ptr.read()
        }
    }

    public mut func set_byref(val: CPointer<Unit>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cVal(): Win32_Foundation.CHAR {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.CHAR>(p)
            return ptr.read()
        }
    }

    public mut func set_cVal(val: Win32_Foundation.CHAR): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Foundation.CHAR>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func uiVal(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt16>(p)
            return ptr.read()
        }
    }

    public mut func set_uiVal(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt16>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ulVal(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_ulVal(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ullVal(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt64>(p)
            return ptr.read()
        }
    }

    public mut func set_ullVal(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt64>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func intVal(): Int32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            return ptr.read()
        }
    }

    public mut func set_intVal(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Int32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func uintVal(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_uintVal(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pdecVal(): CPointer<Win32_Foundation.DECIMAL> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.DECIMAL>>(p)
            return ptr.read()
        }
    }

    public mut func set_pdecVal(val: CPointer<Win32_Foundation.DECIMAL>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_Foundation.DECIMAL>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pcVal(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pcVal(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func puiVal(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_puiVal(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pulVal(): CPointer<UInt32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt32>>(p)
            return ptr.read()
        }
    }

    public mut func set_pulVal(val: CPointer<UInt32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pullVal(): CPointer<UInt64> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt64>>(p)
            return ptr.read()
        }
    }

    public mut func set_pullVal(val: CPointer<UInt64>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt64>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pintVal(): CPointer<Int32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            return ptr.read()
        }
    }

    public mut func set_pintVal(val: CPointer<Int32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Int32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func puintVal(): CPointer<UInt32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt32>>(p)
            return ptr.read()
        }
    }

    public mut func set_puintVal(val: CPointer<UInt32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func anonymous(): VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Variant == "on"]
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct {
    public var pvRecord: CPointer<Unit> = CPointer<Unit>()
    public var pRecInfo: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}
