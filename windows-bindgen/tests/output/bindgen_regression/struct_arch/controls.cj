// Auto-generated by windows-bindgen. Do not edit.
package Windows.Win32.UI.Controls

import windows_interface.*
import windows_result.*
import Windows.Win32.Foundation as Win32_Foundation
import Windows.Win32.Graphics.Gdi as Win32_Graphics_Gdi
import Windows.Win32.UI.WindowsAndMessaging as Win32_UI_WindowsAndMessaging
import Windows.Win32.Foundation.*


@When[Windows_Win32_UI_Controls == "on"]
public type HIMAGELIST = CPointer<Unit>

@When[Windows_Win32_UI_Controls == "on"]
public type HPROPSHEETPAGE = CPointer<Unit>

@When[Windows_Win32_UI_Controls == "on"]
public type LPFNPSPCALLBACKA = CFunc<(CPointer<Unit>, PSPCB_MESSAGE, CPointer<PROPSHEETPAGEA>) -> UInt32>

@When[Windows_Win32_UI_Controls == "on"]
public type LPFNPSPCALLBACKW = CFunc<(CPointer<Unit>, PSPCB_MESSAGE, CPointer<PROPSHEETPAGEW>) -> UInt32>

@When[Windows_Win32_UI_Controls == "on"]
public type PFNPROPSHEETCALLBACK = CFunc<(CPointer<Unit>, UInt32, IntNative) -> Int32>

@When[Windows_Win32_UI_Controls == "on"]
@C
public struct PROPSHEETHEADERA_V2 {
    public var dwSize: UInt32 = 0
    public var dwFlags: UInt32 = 0
    public var hwndParent: CPointer<Unit> = CPointer<Unit>()
    public var hInstance: CPointer<Unit> = CPointer<Unit>()
    public var Anonymous1: PROPSHEETHEADERA_V2__Anonymous1_e__Union = PROPSHEETHEADERA_V2__Anonymous1_e__Union()
    public var pszCaption: CPointer<UInt8> = CPointer<UInt8>()
    public var nPages: UInt32 = 0
    public var Anonymous2: PROPSHEETHEADERA_V2__Anonymous2_e__Union = PROPSHEETHEADERA_V2__Anonymous2_e__Union()
    public var Anonymous3: PROPSHEETHEADERA_V2__Anonymous3_e__Union = PROPSHEETHEADERA_V2__Anonymous3_e__Union()
    public var pfnCallback: PFNPROPSHEETCALLBACK = { _, _, _ => 0 }
    public var Anonymous4: PROPSHEETHEADERA_V2__Anonymous4_e__Union = PROPSHEETHEADERA_V2__Anonymous4_e__Union()
    public var hplWatermark: Win32_Graphics_Gdi.HPALETTE = CPointer<Unit>()
    public var Anonymous5: PROPSHEETHEADERA_V2__Anonymous5_e__Union = PROPSHEETHEADERA_V2__Anonymous5_e__Union()
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERA_V2__Anonymous1_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hIcon(): Win32_UI_WindowsAndMessaging.HICON {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_UI_WindowsAndMessaging.HICON>(p)
            return ptr.read()
        }
    }

    public mut func set_hIcon(val: Win32_UI_WindowsAndMessaging.HICON): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_UI_WindowsAndMessaging.HICON>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszIcon(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszIcon(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERA_V2__Anonymous2_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func nStartPage(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_nStartPage(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pStartPage(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pStartPage(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERA_V2__Anonymous3_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func ppsp(): CPointer<PROPSHEETPAGEA> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<PROPSHEETPAGEA>>(p)
            return ptr.read()
        }
    }

    public mut func set_ppsp(val: CPointer<PROPSHEETPAGEA>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<PROPSHEETPAGEA>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func phpage(): CPointer<HPROPSHEETPAGE> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<HPROPSHEETPAGE>>(p)
            return ptr.read()
        }
    }

    public mut func set_phpage(val: CPointer<HPROPSHEETPAGE>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<HPROPSHEETPAGE>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERA_V2__Anonymous4_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hbmWatermark(): Win32_Graphics_Gdi.HBITMAP {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            return ptr.read()
        }
    }

    public mut func set_hbmWatermark(val: Win32_Graphics_Gdi.HBITMAP): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszbmWatermark(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszbmWatermark(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERA_V2__Anonymous5_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hbmHeader(): Win32_Graphics_Gdi.HBITMAP {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            return ptr.read()
        }
    }

    public mut func set_hbmHeader(val: Win32_Graphics_Gdi.HBITMAP): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszbmHeader(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszbmHeader(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
@C
public struct PROPSHEETHEADERW_V2 {
    public var dwSize: UInt32 = 0
    public var dwFlags: UInt32 = 0
    public var hwndParent: CPointer<Unit> = CPointer<Unit>()
    public var hInstance: CPointer<Unit> = CPointer<Unit>()
    public var Anonymous1: PROPSHEETHEADERW_V2__Anonymous1_e__Union = PROPSHEETHEADERW_V2__Anonymous1_e__Union()
    public var pszCaption: CPointer<UInt16> = CPointer<UInt16>()
    public var nPages: UInt32 = 0
    public var Anonymous2: PROPSHEETHEADERW_V2__Anonymous2_e__Union = PROPSHEETHEADERW_V2__Anonymous2_e__Union()
    public var Anonymous3: PROPSHEETHEADERW_V2__Anonymous3_e__Union = PROPSHEETHEADERW_V2__Anonymous3_e__Union()
    public var pfnCallback: PFNPROPSHEETCALLBACK = { _, _, _ => 0 }
    public var Anonymous4: PROPSHEETHEADERW_V2__Anonymous4_e__Union = PROPSHEETHEADERW_V2__Anonymous4_e__Union()
    public var hplWatermark: Win32_Graphics_Gdi.HPALETTE = CPointer<Unit>()
    public var Anonymous5: PROPSHEETHEADERW_V2__Anonymous5_e__Union = PROPSHEETHEADERW_V2__Anonymous5_e__Union()
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERW_V2__Anonymous1_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hIcon(): Win32_UI_WindowsAndMessaging.HICON {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_UI_WindowsAndMessaging.HICON>(p)
            return ptr.read()
        }
    }

    public mut func set_hIcon(val: Win32_UI_WindowsAndMessaging.HICON): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_UI_WindowsAndMessaging.HICON>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszIcon(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszIcon(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERW_V2__Anonymous2_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func nStartPage(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_nStartPage(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pStartPage(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pStartPage(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERW_V2__Anonymous3_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func ppsp(): CPointer<PROPSHEETPAGEW> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<PROPSHEETPAGEW>>(p)
            return ptr.read()
        }
    }

    public mut func set_ppsp(val: CPointer<PROPSHEETPAGEW>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<PROPSHEETPAGEW>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func phpage(): CPointer<HPROPSHEETPAGE> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<HPROPSHEETPAGE>>(p)
            return ptr.read()
        }
    }

    public mut func set_phpage(val: CPointer<HPROPSHEETPAGE>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<HPROPSHEETPAGE>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERW_V2__Anonymous4_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hbmWatermark(): Win32_Graphics_Gdi.HBITMAP {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            return ptr.read()
        }
    }

    public mut func set_hbmWatermark(val: Win32_Graphics_Gdi.HBITMAP): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszbmWatermark(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszbmWatermark(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETHEADERW_V2__Anonymous5_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hbmHeader(): Win32_Graphics_Gdi.HBITMAP {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            return ptr.read()
        }
    }

    public mut func set_hbmHeader(val: Win32_Graphics_Gdi.HBITMAP): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszbmHeader(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszbmHeader(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
@C
public struct PROPSHEETPAGEA {
    public var dwSize: UInt32 = 0
    public var dwFlags: UInt32 = 0
    public var hInstance: CPointer<Unit> = CPointer<Unit>()
    public var Anonymous1: PROPSHEETPAGEA__Anonymous1_e__Union = PROPSHEETPAGEA__Anonymous1_e__Union()
    public var Anonymous2: PROPSHEETPAGEA__Anonymous2_e__Union = PROPSHEETPAGEA__Anonymous2_e__Union()
    public var pszTitle: CPointer<UInt8> = CPointer<UInt8>()
    public var pfnDlgProc: Win32_UI_WindowsAndMessaging.DLGPROC = { _, _, _, _ => 0 }
    public var lParam: IntNative = 0
    public var pfnCallback: LPFNPSPCALLBACKA = { _, _, _ => 0 }
    public var pcRefParent: CPointer<UInt32> = CPointer<UInt32>()
    public var pszHeaderTitle: CPointer<UInt8> = CPointer<UInt8>()
    public var pszHeaderSubTitle: CPointer<UInt8> = CPointer<UInt8>()
    public var hActCtx: CPointer<Unit> = CPointer<Unit>()
    public var Anonymous3: PROPSHEETPAGEA__Anonymous3_e__Union = PROPSHEETPAGEA__Anonymous3_e__Union()
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETPAGEA__Anonymous1_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func pszTemplate(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszTemplate(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pResource(): CPointer<Win32_UI_WindowsAndMessaging.DLGTEMPLATE> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_UI_WindowsAndMessaging.DLGTEMPLATE>>(p)
            return ptr.read()
        }
    }

    public mut func set_pResource(val: CPointer<Win32_UI_WindowsAndMessaging.DLGTEMPLATE>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_UI_WindowsAndMessaging.DLGTEMPLATE>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETPAGEA__Anonymous2_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hIcon(): Win32_UI_WindowsAndMessaging.HICON {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_UI_WindowsAndMessaging.HICON>(p)
            return ptr.read()
        }
    }

    public mut func set_hIcon(val: Win32_UI_WindowsAndMessaging.HICON): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_UI_WindowsAndMessaging.HICON>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszIcon(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszIcon(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETPAGEA__Anonymous3_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hbmHeader(): Win32_Graphics_Gdi.HBITMAP {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            return ptr.read()
        }
    }

    public mut func set_hbmHeader(val: Win32_Graphics_Gdi.HBITMAP): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszbmHeader(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszbmHeader(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
@C
public struct PROPSHEETPAGEW {
    public var dwSize: UInt32 = 0
    public var dwFlags: UInt32 = 0
    public var hInstance: CPointer<Unit> = CPointer<Unit>()
    public var Anonymous1: PROPSHEETPAGEW__Anonymous1_e__Union = PROPSHEETPAGEW__Anonymous1_e__Union()
    public var Anonymous2: PROPSHEETPAGEW__Anonymous2_e__Union = PROPSHEETPAGEW__Anonymous2_e__Union()
    public var pszTitle: CPointer<UInt16> = CPointer<UInt16>()
    public var pfnDlgProc: Win32_UI_WindowsAndMessaging.DLGPROC = { _, _, _, _ => 0 }
    public var lParam: IntNative = 0
    public var pfnCallback: LPFNPSPCALLBACKW = { _, _, _ => 0 }
    public var pcRefParent: CPointer<UInt32> = CPointer<UInt32>()
    public var pszHeaderTitle: CPointer<UInt16> = CPointer<UInt16>()
    public var pszHeaderSubTitle: CPointer<UInt16> = CPointer<UInt16>()
    public var hActCtx: CPointer<Unit> = CPointer<Unit>()
    public var Anonymous3: PROPSHEETPAGEW__Anonymous3_e__Union = PROPSHEETPAGEW__Anonymous3_e__Union()
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETPAGEW__Anonymous1_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func pszTemplate(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszTemplate(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pResource(): CPointer<Win32_UI_WindowsAndMessaging.DLGTEMPLATE> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_UI_WindowsAndMessaging.DLGTEMPLATE>>(p)
            return ptr.read()
        }
    }

    public mut func set_pResource(val: CPointer<Win32_UI_WindowsAndMessaging.DLGTEMPLATE>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Win32_UI_WindowsAndMessaging.DLGTEMPLATE>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETPAGEW__Anonymous2_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hIcon(): Win32_UI_WindowsAndMessaging.HICON {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_UI_WindowsAndMessaging.HICON>(p)
            return ptr.read()
        }
    }

    public mut func set_hIcon(val: Win32_UI_WindowsAndMessaging.HICON): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_UI_WindowsAndMessaging.HICON>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszIcon(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszIcon(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct PROPSHEETPAGEW__Anonymous3_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func hbmHeader(): Win32_Graphics_Gdi.HBITMAP {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            return ptr.read()
        }
    }

    public mut func set_hbmHeader(val: Win32_Graphics_Gdi.HBITMAP): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<Win32_Graphics_Gdi.HBITMAP>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszbmHeader(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszbmHeader(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_UI_Controls == "on"]
@C
public struct PSPCB_MESSAGE {
    public var value: UInt32 = 0

    public init() {}

    public init(value: UInt32) {
        this.value = value
    }

    public operator func ==(other: PSPCB_MESSAGE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: PSPCB_MESSAGE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_UI_Controls == "on"]
public let PSPCB_ADDREF: PSPCB_MESSAGE = PSPCB_MESSAGE(0)

@When[Windows_Win32_UI_Controls == "on"]
public let PSPCB_CREATE: PSPCB_MESSAGE = PSPCB_MESSAGE(2)

@When[Windows_Win32_UI_Controls == "on"]
public let PSPCB_RELEASE: PSPCB_MESSAGE = PSPCB_MESSAGE(1)

@When[Windows_Win32_UI_Controls == "on"]
public let PSPCB_SI_INITDIALOG: PSPCB_MESSAGE = PSPCB_MESSAGE(1025)
