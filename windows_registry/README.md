# windows_registry

Windows registry helpers for Cangjie bindings.

It contains Windows registry helper APIs built on generated common bindings.

This package is part of the windows-cj Cangjie Windows bindings workspace. Add it as a `cjpm` dependency when a generated binding or another windows-cj package requires it.
