# windows-cj optimization timelog

Start: 2026-05-20 02:45 +08:00
Minimum target duration: 16 hours

## Goal

Continue improving the active `windows-cj` workspace with concrete, verified changes. Prioritize production-readiness blockers that remain after the 2026-05-19 full gate: WinUI/WindowsAppSDK metadata parity, real interop smoke coverage for candidate COM value packages, projection/package boundary clarity, and quality-gate blind spots.

## Rules

- Before editing Cangjie code, query the Cangjie documentation tool for the syntax/API being used.
- Set `cjHeapSize=32GB` before any `cjpm` command.
- Run Cangjie-built binaries through `cjv exec`, preferably via the existing Python test runners.
- Keep `ref/` read-only and preserve unrelated user changes.
- Use short-lived sub agents for independent investigation or review, then close them after their findings are consumed.

## Log

- 2026-05-20 02:45 +08:00 - Started 16-hour optimization loop. Created 30-minute heartbeat automation `windows-cj-16h-optimization-loop` with 32 cycles.
- 2026-05-20 02:48 +08:00 - Confirmed `windows-cj` is a clean independent git worktree. Started three read-only sub agents for build/test entrypoints, code risk scan, and recent-progress synthesis.
- 2026-05-20 02:52 +08:00 - Read `NEXT_AGENT_TASKS.md` and the 2026-05-19 timelog. Baseline says full workspace tests and full quality gate passed after the previous 8-hour bugfix cycles; do not repeat blind ABI cleanup sweeps.
- 2026-05-20 02:57 +08:00 - Ran quick quality gate with `cjHeapSize=32GB`; passed py_compile, quick `windows-common` codegen/build, workspace setup audit, ignored-results audit, and ABI ownership audit.
- 2026-05-20 03:06 +08:00 - Fixed `SafeArrayOfInt32` / `SafeArrayOfFloat64` / `SafeArrayOfStrings` / `SafeArrayOfVariants` typed `withLength` overflow handling. Public `Int64` counts now preflight against the SAFEARRAY `UInt32` ABI limit before calling OleAut32. Added a debug create-attempt counter and regression proving `UInt32.Max + 1` is rejected before any native create attempt.
- 2026-05-20 03:10 +08:00 - Verified `windows-safearray` via `python scripts\run_windows_workspace_tests.py windows-safearray --timeout-seconds 300`: 24 passed, 0 failed.
- 2026-05-20 03:13 +08:00 - Review agent found one P2: the first test only counted successful creates, not create attempts. Added `debugObservedSafeArrayCreateAttemptCount()` and moved the test to that stricter signal.
- 2026-05-20 03:17 +08:00 - Re-verified `windows-safearray`: 24 passed. Ran quick quality gate again; passed. Incremental review reported no P0/P1/P2.
- 2026-05-20 03:25 +08:00 - Fixed generator `ConstantAttribute` handling for unsupported struct fields. The renderer now fails generation instead of emitting a comment and returning a default-initialized field, preventing silent wrong ABI constants.
- 2026-05-20 03:28 +08:00 - Added a `windows-bindgen` regression for an unsupported zero-sized fixed array field under `ConstantAttribute`. First compile exposed a Cangjie lambda capture rule; moved the captured record to an immutable value and reran.
- 2026-05-20 03:30 +08:00 - Verified `windows-bindgen` via `python scripts\run_windows_workspace_tests.py windows-bindgen --timeout-seconds 300`: 188 passed, 0 failed.
- 2026-05-20 03:32 +08:00 - Verified generator compatibility with checked-in `windows-common` available metadata subset using `python scripts\check_windows_common_codegen.py --mode full --skip-build --timeout-seconds 600`: passed; WinUI metadata remains blocked/skipped as expected.
- 2026-05-20 03:35 +08:00 - Ran quick quality gate after all changes; passed py_compile, quick `windows-common` codegen/build, workspace setup audit, ignored-results audit, and ABI ownership audit.
- 2026-05-20 03:38 +08:00 - Final review agent for current diff reported no P0/P1/P2.
- 2026-05-20 03:45 +08:00 - Heartbeat continuation: fixed ergonomic P/Invoke `BOOL + SetLastError` wrapper selection to match native helper semantics. `pinvokeReturnHint` now skips semantic BOOL predicates such as `Is*` / `Can*` without out parameters, avoiding false-as-error wrappers for legitimate predicate APIs.
- 2026-05-20 03:48 +08:00 - Extended the ergonomic P/Invoke test fixture with `IsTokenRestricted`, proving the raw method remains generated while `IsTokenRestrictedChecked` is suppressed. `windows-bindgen` passed: 188 tests, 0 failed.
- 2026-05-20 03:51 +08:00 - Verified generated output stability with `python scripts\check_windows_common_codegen.py --mode full --skip-build --timeout-seconds 600`: passed for available metadata subset; WinUI metadata remains blocked/skipped as expected.
- 2026-05-20 03:54 +08:00 - Ran quick quality gate again after the P/Invoke patch; passed all 5 steps.
- 2026-05-20 03:55 +08:00 - Closed read-only converter sub agent after consuming its TypeReference enum-resolution findings.
- 2026-05-20 03:56 +08:00 - Fixed `winmd-to-json` custom-attribute enum decoding for same-module `TypeReference` values. The converter now resolves same-module type references back to their `TypeDefinition` and carries the real enum underlying type instead of relying only on a hard-coded allowlist.
- 2026-05-20 03:57 +08:00 - Verified `winmd-to-json` with `dotnet build .\winmd-to-json\winmd-to-json.csproj` and `python .\scripts\convert_winmd_to_json.py --self-test`: both passed.
- 2026-05-20 03:58 +08:00 - Ran a real temporary conversion of `winmd\Windows.Win32.winmd`; succeeded with 325 JSON files in a temp output directory.
- 2026-05-20 04:00 +08:00 - Ran quick quality gate with `cjHeapSize=32GB` after the converter patch; passed all 5 steps.
- 2026-05-20 04:01 +08:00 - Review agent found two P2 issues in the first converter patch: nested same-module `TypeReference` values were not resolved, and resolution scanned every type definition on each decode. Reworked the resolver into a per-`MetadataReader` cached index with top-level and nested type maps.
- 2026-05-20 04:02 +08:00 - Re-verified the cached/nested converter path: `dotnet build` passed, `convert_winmd_to_json.py --self-test` passed, temporary `Windows.Win32.winmd` conversion produced 325 JSON files in 5.61s, and quick quality gate passed all 5 steps.
- 2026-05-20 04:17 +08:00 - Started the next heartbeat target: converter regression coverage for the same-module/nested enum `TypeReference` path. Spawned a read-only sub agent to compare fixture options while locally adding a converter self-test.
- 2026-05-20 04:21 +08:00 - Added hidden `winmd-to-json --self-test`. It builds an in-memory metadata fixture with a same-module top-level enum `TypeReference`, a nested enum `TypeReference`, an external same-name enum reference, and custom attributes whose fixed arguments exercise `JsCustomAttributeFixedArgument.Value`.
- 2026-05-20 04:23 +08:00 - Wired the converter metadata self-test into `scripts/run_windows_quality_gate.py` for both quick and full modes, and updated the quality-gate plan tests.
- 2026-05-20 04:26 +08:00 - Verified the new regression: `dotnet build` passed, `dotnet run --project .\winmd-to-json\winmd-to-json.csproj -- --self-test` passed, `python .\scripts\test_run_windows_quality_gate.py` passed 9 tests, temporary `Windows.Win32.winmd` conversion produced 325 JSON files, and quick quality gate passed all 6 steps.
- 2026-05-20 04:47 +08:00 - Started WinUI metadata parity loop. Converted local WindowsAppSDK 1.4 `lib\uap10.0` metadata into a temp JSON directory: 14 WinMD files produced 39 JSON files.
- 2026-05-20 04:49 +08:00 - Found a concrete WinUI gate blocker: with WinUI JSON present, `check_windows_common_codegen.py` expanded `plan.available` from 19 manifest requested features to the full selected-symbol closure, causing Windows `WinError 206` command-line-too-long before `cjv exec` could start.
- 2026-05-20 04:52 +08:00 - Fixed the gate plan split. `FeaturePlan.available` now stays as manifest-requested features; `FeaturePlan.subset_features` carries the expanded available closure only for blocked-subset comparisons. Added self-test coverage for both complete and blocked metadata plans.
- 2026-05-20 04:54 +08:00 - Re-ran WinUI full temp regeneration. Command line is no longer too long and generator runs; skipped list is reduced to 0 with WindowsAppSDK 1.4 `uap10.0`, but parity still fails because that metadata root lacks 7 checked-in Microsoft.UI.Xaml symbols and causes 9 missing selected symbols total.
- 2026-05-20 04:56 +08:00 - Improved full parity diagnostics. The gate now reports selected-symbol missing/extra samples, changed file hashes, and whether missing checked-in symbols are absent from the supplied metadata. Re-run confirmed absent symbols: `DispatcherShutdownMode`, `IApplication3`, `IDebugSettings3`, `IXamlRoot3`, `IXamlRoot4`, `LayoutCycleDebugBreakLevel`, `LayoutCycleTracingLevel`.
- 2026-05-20 04:58 +08:00 - Attempted to rebuild `windows-bindgen` with `cjHeapSize=32GB`; compile/link reached the final output but `ld.lld` could not write `target\release\bin\windows_bindgen.exe` due to Permission denied. No residual `windows_bindgen`/`cjv`/`cjpm` process was visible. Continued using the existing binary for script-level parity investigation.
- 2026-05-20 04:59 +08:00 - Verified script changes: `python .\scripts\check_windows_common_codegen.py --self-test` passed, `python .\scripts\test_run_windows_quality_gate.py` passed 9 tests, `git diff --check` passed, and quick quality gate passed all 6 steps.
- 2026-05-20 05:17 +08:00 - Continued WinUI parity investigation. Spawned a read-only sub agent for local NuGet symbol coverage while locally scanning all installed WindowsAppSDK/Microsoft.UI.Xaml candidate roots.
- 2026-05-20 05:19 +08:00 - Confirmed none of the 28 local WinUI/WindowsAppSDK metadata roots contain the 7 checked-in Microsoft.UI.Xaml symbols required for full parity. Temporarily downloaded `Microsoft.WindowsAppSDK 2.0.1` into a temp NuGet cache and confirmed that metadata also does not contain the symbols.
- 2026-05-20 05:21 +08:00 - Added WinUI selected-symbol preflight to `check_windows_common_codegen.py`. When optional WinUI metadata is present and no requested feature is blocked, the gate now fails fast if checked-in WinUI selected symbols are absent from the supplied metadata, avoiding a 60s doomed regeneration.
- 2026-05-20 05:23 +08:00 - Corrected the preflight to check only WinUI selected symbols; raw Win32 nested helper names can be generated symbols rather than literal top-level metadata type names.
- 2026-05-20 05:24 +08:00 - Added `convert_winmd_to_json.py --require-symbol <TYPE>` for `--list-candidates`, plus `microsoft.ui.winui` candidate discovery. Candidate probing now converts each listed root into a temp directory quietly and reports symbol hit/miss counts.
- 2026-05-20 05:26 +08:00 - Verified this loop: `convert_winmd_to_json.py --self-test`, `check_windows_common_codegen.py --self-test`, `py_compile`, sample `--list-candidates --require-symbol Microsoft.UI.Xaml.IApplication3`, `git diff --check`, and quick quality gate all passed.
- 2026-05-20 05:47 +08:00 - Continued WinUI metadata tooling loop. Spawned a read-only review sub agent for candidate probing performance/usability while locally improving `convert_winmd_to_json.py`.
- 2026-05-20 05:49 +08:00 - Optimized `--list-candidates --require-symbol`: the helper now publishes `winmd-to-json` once and reuses the tool across all candidate roots instead of publishing per candidate.
- 2026-05-20 05:51 +08:00 - Applied review feedback: symbol probing now respects `--tool-exe`, `--tool-dir`, `--project`, and `--configuration`; individual candidate probe failures are reported inline and no longer abort the whole candidate list.
- 2026-05-20 05:52 +08:00 - Documented the WinUI metadata probe/setup workflow in `windows-bindgen/README.md`, including `--require-symbol`, using matching raw roots for hash validation, avoiding unnecessary extra TFM roots, and expected fast-fail diagnostics for stale/mismatched metadata.
- 2026-05-20 05:53 +08:00 - Verified this loop: `convert_winmd_to_json.py --self-test`, `check_windows_common_codegen.py --self-test`, `dotnet build winmd-to-json`, `--list-candidates --require-symbol ... --tool-exe ...`, `git diff --check`, and quick quality gate all passed.
- 2026-05-20 06:17 +08:00 - Started native WinMD type-probe loop. Spawned a read-only sub agent for `winmd-to-json --contains-type` boundaries and implemented a converter fast path that scans metadata `TypeDefinition` names without building JSON or `JsTypeDefinition`.
- 2026-05-20 06:19 +08:00 - Added repeatable `winmd-to-json --contains-type <Full.Type>` outputting `CONTAINS_TYPE_FOUND/MISSING` lines plus a summary. The self-test fixture now verifies top-level and nested metadata type-name reporting.
- 2026-05-20 06:20 +08:00 - Rewired `convert_winmd_to_json.py --list-candidates --require-symbol` to call the native `--contains-type` probe instead of converting each candidate root to temporary JSON. Removed the old JSON type-name parsing helper and added parser self-test coverage.
- 2026-05-20 06:21 +08:00 - Verified native probing: known type probe reported `Windows.Win32.Foundation.HWND` found and `Microsoft.UI.Xaml.IApplication3` missing from `Windows.Win32.winmd`; five WinUI candidate roots probed in about 1.2s through `--tool-exe`.
- 2026-05-20 06:23 +08:00 - Verified this loop: `dotnet build winmd-to-json`, `dotnet run -- --self-test`, converter script self-test, `py_compile`, sample candidate probe, `git diff --check`, and quick quality gate all passed.
- 2026-05-20 06:47 +08:00 - Continued WinUI provenance loop. Closed the prior investigation sub agent, then spawned a short read-only JSON-schema sub agent while locally implementing `check_windows_common_codegen.py` selected-symbol provenance reporting.
- 2026-05-20 06:50 +08:00 - Added repeatable `--provenance-symbol <TYPE>` / `--report-selected-symbol-provenance` to the windows-common codegen gate. The report maps selected symbols to source JSON, raw `.winmd` filename/hash, matched raw root path, `SourceSet`, and `ContractVersionAttribute` contract/version values.
- 2026-05-20 06:51 +08:00 - Verified the diagnostic with a namespace-collision sample: `Microsoft.UI.Xaml.IApplication3` is missing from current supplied metadata, while `Windows.UI.Xaml.IApplication3` resolves to `.generated/winmd-json/Windows.UI.Xaml.json`, `Windows.winmd`, `winmd_main`, and `UniversalApiContract@0x00040000`.
- 2026-05-20 06:54 +08:00 - Verified this loop: `check_windows_common_codegen.py --self-test`, `py_compile`, sample provenance report, `git diff --check`, and quick quality gate all passed.
- 2026-05-20 07:17 +08:00 - Continued WinUI provenance loop. Added `check_windows_common_codegen.py --report-missing-winui-selected-symbols`, a compact bulk report for supplied-metadata gaps that includes same-short-name candidates plus provenance fields.
- 2026-05-20 07:20 +08:00 - Validated the new report against local WindowsAppSDK 1.4 `lib\uap10.0`: it reproduces exactly the 7 missing checked-in `Microsoft.UI.Xaml` symbols and shows `Windows.UI.Xaml.IApplication3` / `IDebugSettings3` as non-equivalent Windows SDK short-name collisions with their UniversalApiContract versions.
- 2026-05-20 07:47 +08:00 - Hardened the compact WinUI missing-symbol report with self-test coverage. The self-test now asserts the no-WinUI-root path emits only a blocked-feature summary, and the supplied-WinUI-root path emits same-short-name candidate provenance including source set, contract version, and raw source.
- 2026-05-20 07:50 +08:00 - Verified this loop: `check_windows_common_codegen.py --self-test`, `py_compile`, WindowsAppSDK 1.4 missing-symbol report, quality-gate plan tests, `git diff --check`, and quick quality gate all passed.
- 2026-05-20 08:17 +08:00 - Integrated the compact WinUI missing-symbol report into the full codegen gate failure path. When supplied WinUI metadata covers requested features but lacks checked-in selected symbols, the failure now prints the 7-symbol report inline instead of only telling the user to rerun another command.
- 2026-05-20 08:19 +08:00 - Verified the expected-failure path with local WindowsAppSDK 1.4 `uap10.0`: `check_windows_common_codegen.py --mode full --skip-build --winui-winmd-json-dir ... --winui-winmd-root ...` exits 1 and includes the compact `WINUI_MISSING_SELECTED_SYMBOL` report directly in the error.
- 2026-05-20 08:47 +08:00 - Added `windows-common codegen self-test` to the unified quick/full quality gate, so the Python-only WinUI provenance/report regressions run before the heavier codegen build step.
- 2026-05-20 08:50 +08:00 - Added a synthetic golden self-test for the known WindowsAppSDK 1.4 WinUI metadata blocker: when all 10 checked-in WinUI requested features are present but the 7 newer selected symbols are absent, the compact report must emit exactly those 7 missing symbols.
- 2026-05-20 08:53 +08:00 - Verified this loop: codegen self-test, quality-gate plan tests, `py_compile`, quick quality gate with the new 7-step plan all passed.
- 2026-05-20 09:17 +08:00 - Shifted to a general quality-gate blind spot. Added `python unit tests` to the unified quick/full quality gate using `python -m unittest discover -s scripts -p test_*.py`, so `test_check_workspace_setup.py` and `test_run_windows_quality_gate.py` actually execute in normal gate runs.
- 2026-05-20 09:18 +08:00 - Running the discovered tests exposed a stale assertion from the earlier `FeaturePlan.available` / `subset_features` split. Updated the test to assert manifest-requested available features separately from expanded subset features.
- 2026-05-20 09:20 +08:00 - Verified this loop: 108 discovered Python unit tests pass, quality-gate plan tests pass, quick quality gate now passes with 8 steps.
- 2026-05-20 09:47 +08:00 - Added `convert_winmd_to_json.py --self-test` to the unified quick/full quality gate as `winmd conversion helper self-test`, covering candidate/probe parser regressions before the heavier .NET metadata self-test.
- 2026-05-20 09:50 +08:00 - Investigated uncovered `generate_vector_input_abi.py --check-test`. It was drifting from checked-in vector ABI tests by rendering newer `CPointerHandle` / `acquireArrayRawData` scaffolding while checked-in tests use `windows_interface.allocateNativeValue` / `freeNativeValue`.
- 2026-05-20 09:52 +08:00 - Realigned the vector ABI test renderer with checked-in tests and made `generate_vector_input_abi.py --check-test` validate all five scalar specs when `--type` is omitted.
- 2026-05-20 09:54 +08:00 - Added `vector input ABI generator check` to the unified quick/full quality gate. Verified all five vector ABI test files match the generator and quick quality gate now passes with 10 steps.
- 2026-05-20 10:47 +08:00 - Recycled the WinUI candidate-probing review sub-agent and added `convert_winmd_to_json.py --require-known-winui-missing-symbols`, expanding to the seven checked-in WinUI selected-symbol gaps while preserving repeated `--require-symbol` order/de-dupe behavior.
- 2026-05-20 10:52 +08:00 - Updated `windows-bindgen` README and `check_windows_common_codegen.py` guidance to use the new one-flag WinUI candidate probe. Verified local candidate roots still report `symbols 0/7` for the known WindowsAppSDK 1.4/1.2 roots.
- 2026-05-20 10:59 +08:00 - Closed the review agent's test gap by adding a no-dotnet dispatch self-test for the known-only candidate-probe path. Verified `convert_winmd_to_json.py --self-test`, Python unit tests, `git diff --check`, and quick quality gate all pass.
- 2026-05-20 11:17 +08:00 - Started the runtime runner gate loop. Added `run_windows_runtime_tests.py --self-test`, factored runtime command construction so Cangjie test binaries are always launched through `cjv exec`, and made the runner reject unittest `--timeout-each` in favor of the external process-tree watchdog.
- 2026-05-20 11:21 +08:00 - Added `scripts/test_run_windows_runtime_tests.py` and strengthened `check_workspace_setup.py` so the runtime runner's `cjv exec`, `cjHeapSize=32GB`, summary parsing, and external-watchdog policy are audited.
- 2026-05-20 11:25 +08:00 - Added `windows-runtime runner self-test` to the unified quick/full quality gate. Verified runtime runner self-test, 112 discovered Python tests, setup audit, `git diff --check`, and quick quality gate all pass; quick gate now has 11 steps.
- 2026-05-20 11:47 +08:00 - Added `run_windows_workspace_tests.py --dry-run` and `--self-test` so full workspace direct-binary execution can be sampled safely without building or running Cangjie tests. The dry run prints exact `cjpm test --no-run` and `cjv exec` commands for selected members.
- 2026-05-20 11:50 +08:00 - Added `scripts/test_run_windows_workspace_tests.py`, covering member selection, exact unittest binary paths, centralized build/test command construction, stale binary cleanup, positive timeout validation, and summary parsing.
- 2026-05-20 11:54 +08:00 - Added `workspace runner self-test` to the unified quick/full quality gate and updated setup audit invariants for workspace runner dry-run/self-test and centralized command helpers. Verified 117 discovered Python tests, workspace self-test, dry-run planning, `git diff --check`, and quick quality gate all pass; quick gate now has 12 steps.
- 2026-05-20 12:17 +08:00 - Added `scripts/test_winui_demo_smoke.py`, covering WinUI demo smoke result diagnostics without launching UI: activation marker pass, missing-marker failure, timeout failure, and nonzero process exit propagation.
- 2026-05-20 12:23 +08:00 - Replaced the smoke no-direct-exe source-text check with a behavior test that mocks `subprocess.Popen`, calls `smoke_winui3.main()`, and asserts the actual command is `cjv exec +mem <demo-exe>` with `cwd=ROOT` and not direct `.exe` execution.
- 2026-05-20 12:25 +08:00 - Verified this loop: targeted smoke tests, 122 discovered Python tests, setup audit, `git diff --check`, quick-gate dry run, and full quick quality gate all pass. WinUI demo smoke remains opt-in and skipped by default.
- 2026-05-20 12:47 +08:00 - Started source-level parity hardening and queried Cangjie FFI docs before adding Cangjie fixture snippets. Added a WinUI XAML helper audit for manual callback COM invariants: QueryInterface success must AddRef, unknown IID must clear the out pointer before E_NOINTERFACE, and Release-to-zero must clear the callback slot then free native storage.
- 2026-05-20 12:53 +08:00 - Recycled the source-invariant explorer result and added a narrow ABI ownership audit for `windows-winui3` `queryRequired(...)` call sites, covering the transitive owned raw COM references returned by the helper around `queryInterfaceRaw`.
- 2026-05-20 13:01 +08:00 - Added synthetic tests for WinUI callback COM invariants and `queryRequired` ownership transfer/release. Verified 128 discovered Python tests, ABI ownership audit, setup audit, `git diff --check`, quick-gate dry run, and full quick quality gate all pass.
- 2026-05-20 13:17 +08:00 - Started generated Win32 checked-wrapper semantics loop. Spawned a read-only sub agent for native helper audit scope while locally inspecting generated `windows-common/src/Win32/**/native_helpers.cj` wrapper shapes.
- 2026-05-20 13:28 +08:00 - Added `check_windows_common_native_checked_wrappers()` to `check_workspace_setup.py`, auditing all checked-in native `*Checked` wrappers for same-file base helper, `if (Base(...))` success branch, `Ok(())` success result, and `errorFromThread()` failure result.
- 2026-05-20 13:28 +08:00 - Added focused unit tests for the checked-wrapper audit. Current generated output has 282 audited wrappers across Foundation, Security, Services, SystemInformation, and Threading native helper files. Verified 133 discovered Python tests, setup audit, `git diff --check`, and full quick quality gate all pass.
- 2026-05-20 13:34 +08:00 - Continued after user challenge that green gates do not mean no bugs. Fixed a new gate false-negative: native checked-wrapper static audit now masks Cangjie comments, single/multi-line strings, and raw strings before looking for required wrapper fragments.
- 2026-05-20 13:43 +08:00 - Recycled the native-helper explorer result into a concrete generator bug fix: `CancelWaitableTimer` was misclassified as a `Can*` semantic Bool predicate, so it lacked the SetLastError checked wrapper. `nativeMethodReturnsSemanticBool` now requires PascalCase prefix boundaries.
- 2026-05-20 13:51 +08:00 - Added generator regressions for common native helpers and P/Invoke ergonomic wrappers, synchronized `windows-common/src/Win32/System/Threading/native_helpers.cj` with `CancelWaitableTimerChecked`, updated the manifest hash, and added a setup audit guard for the known `Cancel*`/`Can*` class of bug. Verified windows-bindgen tests, codegen full subset, 137 Python tests, `git diff --check`, and quick quality gate all pass.
- 2026-05-20 14:02 +08:00 - Recycled the checked-wrapper audit sub-agent and replaced the `CancelWaitableTimer` one-off guard with metadata-driven coverage for all generated Win32 `BOOL + SetLastError` native helpers. The audit now requires `*Checked` wrappers for error-channel Bool helpers, skips only no-`Out` semantic predicates, and rejects `Out`-parameter Bool helpers without checked wrappers. Verified 140 Python tests, setup audit, `git diff --check`, and the 12-step quick quality gate all pass.
- 2026-05-20 14:33 +08:00 - Closed a quick-gate blind spot: `windows-bindgen/src/main_test.cj` Cangjie generator regressions now run in quick mode through `scripts/run_windows_workspace_tests.py windows-bindgen`, preserving `cjpm test --no-run` plus `cjv exec` execution. Added plan tests and a setup-audit invariant for the new gate step. Verified 188 windows-bindgen Cangjie tests, 141 Python tests, setup audit, `git diff --check`, and the 13-step quick quality gate all pass.
- 2026-05-20 14:49 +08:00 - Continued without waiting for the next heartbeat. Recycled the P/Invoke/runtime parity sub-agent result into a concrete setup-gate fix: SetLastError Bool ABI wrapper coverage now checks both `BOOL` and `BOOLEAN`, catching `ClosePrivateNamespace`-shaped regressions instead of only 32-bit Bool helpers.
- 2026-05-20 14:49 +08:00 - Added a `windows-bindgen` Cangjie regression for ergonomic P/Invoke query wrappers with leading parameters before `(riid, ppv)`, proving checked generic wrappers preserve leading argument order and interface descriptor constraints. Verified 189 `windows-bindgen` Cangjie tests, 118 setup-audit tests, 143 discovered Python tests, `git diff --check`, and the 13-step quick quality gate all pass.
- 2026-05-20 15:03 +08:00 - Recycled two P/Invoke explorer agents into generator fixes. Ergonomic `SetLastError` P/Invoke wrappers now treat `BOOLEAN` as a Bool ABI return and compare against `0u8`; query wrapper recognition now requires IID-like GUID parameter names so `rclsid, ppv` shapes are not rewritten into descriptor-based interface queries.
- 2026-05-20 15:03 +08:00 - Added Cangjie generator regressions for `Windows.Foundation.HResult` HRESULT parity, `BOOLEAN + SetLastError`, and class-id COM out pointer false positives. Verified 190 `windows-bindgen` Cangjie tests, `git diff --check`, and the 13-step quick quality gate all pass.
- 2026-05-20 15:19 +08:00 - Recycled two more sub-agents into concrete coverage and cleanup fixes. Quick quality gate now runs focused workspace Cangjie tests for `windows-bindgen`, `windows-core`, and `windows-interface`, with setup-audit and plan-test guards so core/interface regressions are no longer hidden until the full gate.
- 2026-05-20 15:19 +08:00 - ResultVoid P/Invoke wrappers now clear `[Out] ComOutPtrAttribute` slots before the raw call and release failed partial outputs before `HRESULT.check()`. Verified 143 Python tests, 118 setup-audit tests, 190 `windows-bindgen` Cangjie tests, 76 `windows-core`/`windows-interface` tests, `git diff --check`, setup audit, and the updated 13-step quick quality gate all pass.
- 2026-05-20 15:34 +08:00 - Recycled the automation audit into runner hardening. `run_windows_runtime_tests.py` now deletes stale `windows_runtime.exe`/sidecars before rebuilding, quick quality gate runs a real `windows-runtime` smoke test, `cjHeapSize=32GB` is forced after per-step env merging, workspace dry-run respects `--skip-build`, and `.cmd` automation files are rejected with `.sh`/`.ps1`/`.bat`.
- 2026-05-20 15:34 +08:00 - Recycled the P/Invoke parity audit into a generator fix. Ergonomic ResultValue wrappers now pre-clear leading `[Out] ComOutPtrAttribute` slots and release any failed partial leading COM outputs before `HRESULT.check()`, while keeping existing COM retval cleanup. Verified 146 Python tests, setup audit, runtime smoke, 191 `windows-bindgen` tests, 76 core/interface tests, `git diff --check`, and the updated 14-step quick quality gate all pass.
- 2026-05-20 15:49 +08:00 - Tightened the runtime smoke gate after discovering Cangjie unittest filters are exact, not prefix-based. The runtime runner now accepts repeated `--filter` values, builds once, and executes each selected smoke through `cjv exec`; quick gate covers missing activation, `PropertyValue` Int32 array round-trip, and `Uri`/`WwwFormUrlDecoder` HString collection projection.
- 2026-05-20 15:49 +08:00 - Completed the string-handle ResultValue cleanup fix. P/Invoke checked wrappers for `HRESULT` + retval `HSTRING*` / `BSTR*` now release failed partial handles, clear the raw local after cleanup, and return owning `windows_core.HString` / `windows_strings.BSTR` wrappers on success. Verified 147 Python tests, setup audit, three runtime smokes, 192 `windows-bindgen` tests, 76 core/interface tests, `git diff --check`, and the updated 14-step quick quality gate all pass.
- 2026-05-20 16:06 +08:00 - Recycled two more sub-agent findings into concrete fixes. Full windows-common regeneration now requires explicit `--allow-missing-winui-metadata` before falling back to an available-metadata subset, and generator builds delete stale `windows_bindgen.exe`/`.cjo` outputs before accepting the build result.
- 2026-05-20 16:06 +08:00 - Added setup-audit guards for false-green runtime/macro automation: quick runtime smoke functions must retain real WinRT call markers, and macro fixture executables must be launched through `cjv exec`, not directly. Fixed `BOOL + SetLastError + CanReturnMultipleSuccessValuesAttribute` wrapper classification so `GetMessageW`-shaped APIs do not throw on legal `0` returns. Verified 151 Python tests, setup audit, `windows-bindgen` 192 tests, `git diff --check`, and the 14-step quick quality gate all pass.
- 2026-05-20 16:20 +08:00 - Recycled the runtime automation sub-agent result into a source-audit fix: runtime smoke body marker checks now mask Cangjie comments and strings before validating real WinRT calls, so comment-only markers cannot keep the quick gate green. Verified 124 setup tests, setup audit, and the full quick quality gate.
- 2026-05-20 16:20 +08:00 - Recycled the P/Invoke cleanup sub-agent result into a generator fix: `[Out] [ComOutPtr]` pointer/by-ref parameters now get failed-HRESULT pre-clear/release cleanup even when the metadata type is a typed COM interface pointer rather than `void**`. Added an `MFCreateAttributes`-shaped regression and verified 153 Python tests, 192 `windows-bindgen` tests, `git diff --check`, setup audit, and the 14-step quick quality gate all pass.
- 2026-05-20 19:48 +08:00 - Continued without waiting for the heartbeat. Recycled the direct-value vector sub-agent finding into a runtime/generator parity fix: specialized `IVectorView`/`IVector` `IndexOf` vtable thunks now reject null index out slots and pre-clear both index/found outputs before dispatch. Added setup-audit coverage for 35 specialized branches, updated generated scalar vector ABI tests, verified `windows-runtime` 209 tests, 178 Python tests, setup audit, `git diff --check`, and the 15-step quick quality gate.

## Current Target

Completed loops:

1. SAFEARRAY typed length overflow guard.
2. `ConstantAttribute` unsupported-field generation failure instead of silent default ABI constants.
3. Ergonomic P/Invoke `BOOL + SetLastError` semantic predicate filtering aligned with native helper output.
4. `winmd-to-json` same-module and nested `TypeReference` enum underlying-type resolution for custom attributes, backed by a cached metadata index.
5. Converter regression self-test for same-module top-level enum, nested enum, and external same-name enum `TypeReference` custom-attribute fixed arguments; now part of quick/full quality gate.
6. WinUI metadata parity gate now avoids Windows command-line overflow when optional WinUI JSON is supplied, and reports whether remaining selected-symbol gaps are absent from the provided metadata root. Local WindowsAppSDK 1.4 `uap10.0` clears the skipped list but is too old/mismatched for full checked-in parity.
7. WinUI metadata root probing is now actionable: `convert_winmd_to_json.py --list-candidates --require-symbol <TYPE>` reports per-root symbol coverage, and the full parity gate fails fast for mismatched WinUI metadata roots.
8. WinUI metadata probe workflow is documented and faster/reusable: one converter publish per list operation, `--tool-exe` reuse supported, per-candidate failures isolated.
9. Native `winmd-to-json --contains-type` makes candidate probing lightweight by scanning WinMD metadata type definitions directly instead of producing temporary JSON per candidate.
10. `check_windows_common_codegen.py` now has a selected-symbol provenance report that distinguishes real metadata absence from namespace collisions and prints source JSON/root/hash/contract details.
11. `check_windows_common_codegen.py --report-missing-winui-selected-symbols` now produces a compact supplied-metadata gap report: no WinUI root yields blocked-feature summary only; WindowsAppSDK 1.4 yields the exact 7 checked-in WinUI symbol gaps plus same-short-name provenance.
12. The compact WinUI missing-symbol report is now covered by self-tests for both blocked metadata and supplied-root dependency-missing scenarios.
13. Full WinUI codegen gate failures now inline the compact missing-symbol report when supplied metadata cannot explain checked-in selected symbols.
14. Unified quick/full quality gate now runs `check_windows_common_codegen.py --self-test`; the known 7-symbol WinUI metadata blocker is protected by a synthetic golden report test.
15. Unified quick/full quality gate now runs all `scripts/test_*.py` Python unit tests; this immediately caught and fixed a stale `FeaturePlan.available` assertion.
16. Unified quick/full quality gate now runs both `convert_winmd_to_json.py --self-test` and `generate_vector_input_abi.py --check-test`; the vector ABI generator drift is closed for all five scalar test fixtures.
17. WinUI candidate probing now has a one-flag known-gap shortcut, `--require-known-winui-missing-symbols`, covered by helper and dispatch self-tests and documented in the WinUI metadata workflow.
18. The windows-runtime watchdog runner now has a quick-gate self-test and unit tests covering `cjv exec` command construction, summary parsing, positive watchdog timeout validation, and rejection of unittest `--timeout-each`.
19. The full workspace direct-binary runner now has safe dry-run sampling, a quick-gate self-test, and unit coverage for exact member/binary selection, `cjpm test --no-run`, `cjv exec`, stale output cleanup, summary parsing, and positive timeout validation.
20. WinUI demo smoke diagnostics now have non-UI unit coverage, including behavior-level proof that the smoke launcher uses `cjv exec +mem` instead of direct executable launch.
21. WinUI manual callback COM glue now has source-level invariants for QueryInterface/AddRef/out-pointer clearing and Release-to-zero slot cleanup/free ordering.
22. WinUI `queryRequired(...)` owned raw COM references are now audited in `check_abi_ownership.py`, so indirect QueryInterface results must be transferred to an owning wrapper or released.
23. Generated Win32 native `*Checked` wrappers are now audited in `check_workspace_setup.py`; 282 current wrappers must preserve the same-file Bool base call, `Ok(())` success path, and `errorFromThread()` failure translation.
24. The checked-wrapper audit now ignores Cangjie comments and string literals to avoid false positives from dead text.
25. `CancelWaitableTimer` now has a generated checked wrapper; `Cancel*` names no longer match the `Can*` semantic Bool predicate heuristic unless the prefix is followed by a PascalCase boundary. Current generated native checked-wrapper count is 283.
26. The setup audit now cross-checks generated Win32 helper methods against WinMD JSON metadata, so future `BOOL + SetLastError` native helpers with error-channel semantics cannot silently lose their `*Checked` wrappers. Semantic no-`Out` predicates remain exempt; `Out`-parameter Bool helpers remain required.
27. The unified quick quality gate now executes the Cangjie `windows-bindgen` generator tests, so generator-only regressions in `main_test.cj` no longer require a separate manual command to be covered.
28. P/Invoke query wrapper generation now has regression coverage for methods with leading parameters before `(riid, ppv)`, including preserved call argument order in the checked ergonomic wrapper.
29. The setup audit now treats both `BOOL` and `BOOLEAN` as Win32 Bool ABI SetLastError return types, so byte-sized Bool helpers cannot silently lose checked wrappers.
30. Ergonomic P/Invoke `SetLastError` wrappers now handle both `BOOL` and `BOOLEAN`, using the correct ABI zero literal for byte-sized `BOOLEAN` returns.
31. Ergonomic P/Invoke query wrapper detection now requires IID-like GUID parameter names, preventing class-id COM out pointer APIs from being rewritten into generic descriptor-based query wrappers.
32. The unified quick quality gate now executes focused Cangjie workspace tests for `windows-bindgen`, `windows-core`, and `windows-interface`, with setup-audit coverage for the expanded member list.
33. Ergonomic ResultVoid P/Invoke wrappers now pre-clear COM out slots and release failed partial COM outputs before surfacing the HRESULT failure.
34. Runtime test automation now clears stale unittest binaries before rebuilds, and quick quality gate runs a real `windows-runtime` smoke test through `cjv exec`.
35. Quality-gate automation now forces `cjHeapSize=32GB` after step env merging, workspace dry-run mirrors `--skip-build`, and `.cmd` scripts are banned with other shell automation.
36. Ergonomic ResultValue P/Invoke wrappers now pre-clear leading COM out slots and release failed partial leading COM outputs before surfacing HRESULT failures.
37. Runtime smoke automation now supports repeated exact filters in one build, so quick gate covers all three real WinRT smoke paths instead of a single unavailable-activation case.
38. Ergonomic ResultValue P/Invoke string-handle wrappers now return owning `HString`/`BSTR` resources on success and release failed partial `HSTRING*`/`BSTR*` retval outputs.
39. Full windows-common codegen regeneration now fails by default when requested WinUI/WindowsAppSDK metadata is missing; subset comparison requires the explicit `--allow-missing-winui-metadata` opt-in.
40. Codegen, runtime, and macro automation gates now guard more false-green paths: stale `windows_bindgen.exe`/`.cjo` outputs are deleted before generator builds, runtime smoke bodies must contain real WinRT call markers, and macro fixture binaries must run through `cjv exec`.
41. `BOOL + SetLastError + CanReturnMultipleSuccessValuesAttribute` P/Invokes no longer get checked wrappers that treat `0` as failure; this protects `GetMessageW`-shaped APIs where `0` is a normal success value such as `WM_QUIT`.
42. Runtime smoke source auditing now ignores Cangjie comments and string literals before checking required real-call markers, closing the comment-only false-green path.
43. P/Invoke failed-HRESULT cleanup now covers typed `[Out] [ComOutPtr]` COM interface pointer parameters, not just `void**` slots.
44. Runtime runner source auditing now parses `run_windows_runtime_tests.py` with Python AST checks, requiring real `cjpm test --no-run` and `cjv exec` command construction while rejecting direct `.exe` execution.
45. Generated descriptor thunks now transfer COM/interface out-param ownership with `intoAbi()` on successful `Result<T>` returns instead of publishing borrowed `asAbiPointer()` pointers.
46. Runtime/workspace unittest summary parsing now requires a real `Summary: TOTAL` footer block with `PASSED`, `FAILED`, and `ERROR`, so isolated count-like text cannot produce a false green.
47. COM descriptor codegen now rejects raw `OutSlot<CPointer<Unit>>` result bridges and direct `CPointer*` return methods, forcing typed ownership-transfer surfaces instead of borrowed raw pointer ABI outputs.
48. The vector input ABI generator now has `--check-all`, covering both generated ABI tests and exact `collections_runtime.cj` specialization fragments, and the quick quality gate now runs that stronger check.
49. Map/MapView specialized `Lookup` vtable thunks now clear HSTRING/interface out slots before dispatching to implementations, matching the generic thunk and preventing stale caller-owned outputs on failure.
50. The vector input ABI generator now has direct Python coverage for `--check-all`, collections-runtime fragment drift detection, and the continued `--type` requirement for write modes.
51. Collection interface-return thunks now retain cached WinRT wrappers before publishing generic COM out slots, rather than consuming wrapper ownership with `intoAbi()` and closing shared test objects.
52. Runtime/workspace unittest summary parsing now validates `Summary: TOTAL` against `PASSED + SKIPPED + FAILED + ERROR`, closing another count-block false-green path.
53. Delegate vtable thunks now map thrown `WindowsException` values back to their original HRESULT and plain exceptions to `E_FAIL`, with bindgen/runtime/common generated parity and focused regressions.
54. The real missing-activation runtime smoke now asserts `REGDB_E_CLASSNOTREG` instead of accepting any `Err(_)`, and setup audit guards that exact HRESULT assertion.
55. System activation factory loading now preserves `CoIncrementMTAUsage` failure HRESULTs instead of ignoring them and retrying activation as if MTA setup had succeeded.
56. Reg-free activation now distinguishes missing probe candidates from loaded candidate failures; once a candidate reports a real activation/query failure, the original HRESULT is returned instead of being collapsed to `REGDB_E_CLASSNOTREG`.
57. Default `IInspectableVtbl` methods now reject null out pointers with `E_POINTER` before writing `GetIids`, `GetRuntimeClassName`, or `GetTrustLevel` outputs; interface tests and setup audit guard the ABI default.
58. Quick quality gate now runs full `windows-runtime` workspace Cangjie tests alongside bindgen/core/interface, so runtime ABI cleanup and ownership regressions are no longer hidden behind three smoke filters.
59. Foundation interface/delegate return thunks now retain cached WinRT wrappers with `winrtStoreGenericOut(...)` instead of consuming wrapper ownership through `value.intoAbi()`, with a focused async completed-handler regression and setup-audit guard.
60. Raw ABI runtime-class wrappers generated by `windows-bindgen` now release WinRT array out slots on failed HRESULTs, covering both handle arrays (`winrtReleaseGenericArrayOut`) and copy arrays (`coTaskMemFree`) for out parameters and array returns.
61. `windows-implement` descriptor return thunks now publish typed WinRT outputs through `windows_core.winrtStoreGenericOut<T>(...)` instead of consuming returned wrappers with `value.intoAbi()`, with descriptor-codegen tests and setup-audit guards.
62. Quick quality gate now includes `windows-implement` in focused workspace Cangjie tests, closing the gate gap that allowed descriptor-codegen regressions to require manual test selection.
63. Quick quality gate now compiles and executes macro fixtures, including a descriptor-codegen compile fixture that renders `OutSlot<Fixture_IOutput>` output, verifies Cangjie-safe thunk identifiers, requires `Interface<T>` marker extensions, compiles the generated source, and executes it through `cjv exec`.
64. Collection generic input thunks now translate `winrtProjectGenericIn<T>` projection failures into HRESULTs instead of letting exceptions cross C ABI boundaries, and pre-clear Bool/index out slots before projection so failure paths cannot leave stale caller values.
65. Direct-value vector `IndexOf` specialized vtable thunks now match generic thunk behavior for null index out slots and stale out-slot cleanup; generator fragments and generated scalar ABI tests cover the behavior.
66. Direct-value vector specialized `IndexOf`/`SetAt`/`InsertAt`/`Append` vtable thunks now translate implementation-thrown `WindowsException`/plain `Exception` into HRESULTs instead of letting exceptions cross the C ABI boundary. The vector ABI generator emits the same inline `try/catch` fragments, setup audit guards 80+ specialized branches, and `windows-runtime` covers direct `IVector<Int32>` vtable exception paths.
67. Observable map/vector/property-set event thunks now clear `EventRegistrationTokenAbi` outputs before dispatch and translate add/remove event exceptions to HRESULTs. Added runtime coverage for stale token cleanup and exception conversion in `observable_map_forwarding_test.cj`, `observable_vector_forwarding_test.cj`, and `property_set_forwarding_test.cj`, plus setup-audit gates.
68. `IMemoryBufferReference.Closed`/`RemoveClosed` foundation event thunks now have the same token preclear and exception-to-HRESULT behavior as collection events. The setup audit now scans both `collections_runtime.cj` and `foundation_runtime.cj`, and `memory_buffer_close_forwarding_test.cj` covers stale token cleanup plus `WindowsException`/plain `Exception` conversion.
69. Generic WinRT raw ABI event add wrappers generated by `windows-bindgen` now pre-clear non-null `EventRegistrationToken` out pointers before `asRaw()` failure checks and vtable dispatch, preventing stale tokens on `E_POINTER` or failed calls. The checked-in `windows-common` generated surface and manifest hash were updated, with setup-audit and bindgen regression coverage.
70. Foundation manual `IAsyncInfo` and `IGetActivationFactory` thunks now pre-clear output slots and translate implementation-thrown `WindowsException`/plain `Exception` to HRESULTs instead of letting exceptions cross C ABI boundaries. Focused runtime tests and setup-audit coverage guard the behavior.
71. Runtime and generated vtable dispatcher `None` branches now clear stale out slots before returning `E_NOINTERFACE`: bindgen generic WinRT vtable rendering, foundation manual vtables, collection generic vtables, checked-in `windows-common/src/impl/symbols_1.cj`, and the windows-common manifest are synchronized with runtime/bindgen regressions.
72. Specialized collection `IMapView`/`IMap`/`IVectorView`/`IVector` direct ABI vtable branches now clear typed scalar, HSTRING/interface, Bool, UInt32, split, and IndexOf out slots on `E_NOINTERFACE`; setup audit now scans collection vtable `None` branches with out parameters instead of only checking representative fragments.
73. Residual manual lookup-miss paths now clear stale out slots: `IWwwFormUrlDecoderEntry` `Name`/`Value` foundation vtables clear HSTRING outputs, and aggregated `IMarshal` lookup misses clear `pcid`, `psize`, and `ppv` before returning `E_NOINTERFACE`. Runtime/core regressions and setup audit guard both paths.
74. The `@Interface` macro now clears likely raw ABI out slots in generated vtable `None` branches before returning `E_NOINTERFACE`, including pointer outs, scalar/struct outs, event-token outs, and out-array pairs while avoiding `CPointer<Unit>` input parameters. Macro fixture coverage exercises stale `innerInterface`, `result`, and event-token outputs.

- 2026-05-20 20:23 +08:00 - Continued active execution after the heartbeat window. Fixed two more runtime ABI bugs: specialized direct-value vector thunks now map thrown exceptions to HRESULTs, and observable event add/remove thunks clear token out slots plus translate exceptions. Verified Cangjie docs before writing, closed/recycled Aquinas/Locke/Hegel sub-agents, ran `windows-runtime` 213/213, 186 Python tests, setup audit, generator `--check-all`, `git diff --check`, and the 15-step quick quality gate successfully.
- 2026-05-20 20:36 +08:00 - Recycled Jason into the foundation event fix. `IMemoryBufferReference` event thunks now pre-clear `EventRegistrationTokenAbi` outputs and catch implementation exceptions. Verified `windows-runtime` 214/214, 186 Python tests, setup audit, `git diff --check`, generator `--check-all`, and the 15-step quick quality gate successfully.
- 2026-05-20 21:05 +08:00 - Recycled Confucius/Socrates into a generator-layer raw event fix. `renderGenericWinrtInterfaceMethods` now emits `result.write(0i64)` guards for generated `EventRegistrationToken` out pointers before early `E_POINTER` paths, checked-in `windows-common` mirrors the generator output, and the manifest hash is updated. Verified `windows-bindgen` 194/194, `windows-common` quick codegen gate/build, 189 Python tests, setup audit, vector generator check, and `git diff --check` with only existing CRLF warnings.
- 2026-05-20 21:06 +08:00 - Closed the foundation manual thunk exception gap. `IAsyncInfo` output thunks now clear stale ABI slots before dispatch and all manual `IAsyncInfo`/`IGetActivationFactory` C ABI thunks catch `WindowsException`/plain `Exception` as HRESULTs. Verified `windows-runtime` 216/216, 192 Python tests, setup audit, and `git diff --check` with only existing CRLF warnings.
- 2026-05-20 22:25 +08:00 - Continued without waiting for the next heartbeat after the user correction. Recycled Goodall's collection-vtable audit into a concrete runtime fix: 85 specialized `IMapView`/`IMap`/`IVectorView`/`IVector` `None` branches now clear stale out slots through shared helpers, including typed scalar/DateTime/HSTRING/interface/Bool/UInt32/split/IndexOf outputs. Added four direct-vtable regressions and strengthened setup audit to reject future collection out-slot `None` branches that return bare `E_NOINTERFACE`. Verified `windows-runtime` 225/225, focused setup tests, and full setup audit.
- 2026-05-20 23:03 +08:00 - Continued active execution and recycled Sartre/Epicurus into the remaining macro/manual stale-output findings. Fixed `IWwwFormUrlDecoderEntry` and aggregated `IMarshal` lookup-miss out-slot cleanup, then patched the `@Interface` macro so generated vtable `None` branches clear likely ABI out slots before `E_NOINTERFACE`. Verified `windows-runtime` 226/226, `windows-core` 60/60, macro fixtures through `cjv exec`, `windows-interface` 20/20, `windows-common` build, setup audit, 200 Python tests, vector generator check, windows-common quick codegen, and `git diff --check` with only CRLF warnings.

Next active target should continue from: scan remaining direct `E_NOINTERFACE`/failed-HRESULT paths for null-out-pointer, stale-output, and exception-crossing behavior not yet covered by the strengthened setup audits, then turn the next real ABI gap into a focused regression.
