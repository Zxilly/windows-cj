[package]
  name = "windows_sample_shell"
  version = "0.1.0"
  description = "Shell automation: drive the desktop IShellDispatch2 to ShellExecute a URL"
  authors = ["Zxilly"]
  repository = "https://github.com/Zxilly/windows-cj"
  license = ["MIT"]
  output-type = "executable"
  cjc-version = "1.1.0"
  compile-option = "-Woff unused"

[dependencies]
  windows_sample_shell_bindings = { path = "./bindings" }
  windows_strings = { path = "../../../windows_strings" }
  windows_core = { path = "../../../windows_core" }
  windows_variant = { path = "../../../windows_variant" }
  windows_interface = { path = "../../../windows_interface" }
