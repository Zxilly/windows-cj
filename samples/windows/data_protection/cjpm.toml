[package]
  name = "windows_sample_data_protection"
  version = "0.1.0"
  description = "data_protection sample port"
  output-type = "executable"
  cjc-version = "1.1.0"
  compile-option = "--experimental --incremental-compile -Woff unused"

[dependencies]
  windows_sample_data_protection_bindings = { path = "./bindings" }
  windows_core = { path = "../../../windows_core" }
  windows_strings = { path = "../../../windows_strings" }
