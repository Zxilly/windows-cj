[package]
  name = "windows_sample_privileges"
  version = "0.1.0"
  description = "Print the current process token privileges via GetTokenInformation and LookupPrivilegeNameW"
  authors = ["Zxilly"]
  repository = "https://github.com/Zxilly/windows-cj"
  license = ["MIT"]
  output-type = "executable"
  cjc-version = "1.1.0"
  compile-option = "-Woff unused"

[dependencies]
  windows_sample_privileges_bindings = { path = "./bindings" }
  windows_strings = { path = "../../../windows_strings" }
