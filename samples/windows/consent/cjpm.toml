[package]
  name = "windows_sample_consent"
  version = "0.1.0"
  description = "Request a Windows user-consent (Windows Hello) verification for a window via IUserConsentVerifierInterop and await the IAsyncOperation"
  authors = ["Zxilly"]
  repository = "https://github.com/Zxilly/windows-cj"
  license = ["MIT"]
  output-type = "executable"
  cjc-version = "1.1.0"
  compile-option = "--experimental --incremental-compile -Woff unused"

[dependencies]
  windows_sample_consent_bindings = { path = "./bindings" }
  windows_core = { path = "../../../windows_core" }
  windows_strings = { path = "../../../windows_strings" }
  windows_interface = { path = "../../../windows_interface" }
