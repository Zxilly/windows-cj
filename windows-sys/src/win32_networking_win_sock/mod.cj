// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.win32_networking_win_sock

import windows_interface.*
import windows_result.*
import windows_sys.win32_foundation
import windows_sys.win32_foundation.*


@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct ADDRESS_FAMILY {
    public var value: UInt16 = 0

    public init() {}

    public init(value: UInt16) {
        this.value = value
    }

    public operator func ==(other: ADDRESS_FAMILY): Bool {
        return this.value == other.value
    }
    public operator func !=(other: ADDRESS_FAMILY): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_networking_win_sock == "on"]
public let AF_INET: ADDRESS_FAMILY = ADDRESS_FAMILY(2)

@When[feat_windows_win32_networking_win_sock == "on"]
public let AF_INET6: ADDRESS_FAMILY = ADDRESS_FAMILY(23)

@When[feat_windows_win32_networking_win_sock == "on"]
public let AF_UNSPEC: ADDRESS_FAMILY = ADDRESS_FAMILY(0)

@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct IN6_ADDR {
    public var u: IN6_ADDR__u_e__Union = IN6_ADDR__u_e__Union()
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct IN6_ADDR__u_e__Union {
    public var _raw: VArray<UInt16, $8> = VArray<UInt16, $8>(repeat: 0)

    public func byte(): VArray<UInt8, $16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt8, $16>>(p)
            return ptr.read()
        }
    }

    public mut func set_byte(val: VArray<UInt8, $16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt8, $16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func word(): VArray<UInt16, $8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt16, $8>>(p)
            return ptr.read()
        }
    }

    public mut func set_word(val: VArray<UInt16, $8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt16, $8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct SOCKADDR {
    public var sa_family: ADDRESS_FAMILY = ADDRESS_FAMILY()
    public var sa_data: VArray<win32_foundation.CHAR, $14> = VArray<win32_foundation.CHAR, $14>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct SCOPE_ID {
    public var Anonymous: SCOPE_ID__Anonymous_e__Union = SCOPE_ID__Anonymous_e__Union()
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct SCOPE_ID__Anonymous_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public func anonymous(): SCOPE_ID__Anonymous_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<SCOPE_ID__Anonymous_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: SCOPE_ID__Anonymous_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<SCOPE_ID__Anonymous_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func value(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_value(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct SCOPE_ID__Anonymous_e__Union__Anonymous_e__Struct {
    public var _bitfield: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct SOCKADDR_IN6 {
    public var sin6_family: ADDRESS_FAMILY = ADDRESS_FAMILY()
    public var sin6_port: UInt16 = 0
    public var sin6_flowinfo: UInt32 = 0
    public var sin6_addr: IN6_ADDR = IN6_ADDR()
    public var Anonymous: SOCKADDR_IN6__Anonymous_e__Union = SOCKADDR_IN6__Anonymous_e__Union()
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct SOCKADDR_IN6__Anonymous_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public func sin6_scope_id(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_sin6_scope_id(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func sin6_scope_struct(): SCOPE_ID {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<SCOPE_ID>(p)
            return ptr.read()
        }
    }

    public mut func set_sin6_scope_struct(val: SCOPE_ID): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<SCOPE_ID>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct SOCKADDR_STORAGE {
    public var ss_family: ADDRESS_FAMILY = ADDRESS_FAMILY()
    public var __ss_pad1: VArray<win32_foundation.CHAR, $6> = VArray<win32_foundation.CHAR, $6>(repeat: 0)
    public var __ss_align: Int64 = 0
    public var __ss_pad2: VArray<win32_foundation.CHAR, $112> = VArray<win32_foundation.CHAR, $112>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct SOCKET_ADDRESS {
    public var lpSockaddr: CPointer<SOCKADDR> = CPointer<SOCKADDR>()
    public var iSockaddrLength: Int32 = 0
    public init() {}
}

@When[feat_windows_win32_networking_win_sock == "on"]
@C
public struct SOCKET_ADDRESS_LIST {
    public var iAddressCount: Int32 = 0
    public var Address: VArray<SOCKET_ADDRESS, $1> = VArray<SOCKET_ADDRESS, $1>(repeat: SOCKET_ADDRESS())
    public init() {}
}


