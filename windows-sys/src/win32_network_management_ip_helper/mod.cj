// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.win32_network_management_ip_helper

import windows_interface.*
import windows_result.*
import windows_sys.win32_foundation
import windows_sys.win32_networking_win_sock
import windows_sys.win32_foundation.*


@When[feat_windows_win32_network_management_ip_helper == "on"]
@C
public struct NET_ADDRESS_FORMAT {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: NET_ADDRESS_FORMAT): Bool {
        return this.value == other.value
    }
    public operator func !=(other: NET_ADDRESS_FORMAT): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_ip_helper == "on"]
public let NET_ADDRESS_FORMAT_UNSPECIFIED: NET_ADDRESS_FORMAT = NET_ADDRESS_FORMAT(0)

@When[feat_windows_win32_network_management_ip_helper == "on"]
public let NET_ADDRESS_DNS_NAME: NET_ADDRESS_FORMAT = NET_ADDRESS_FORMAT(1)

@When[feat_windows_win32_network_management_ip_helper == "on"]
public let NET_ADDRESS_IPV4: NET_ADDRESS_FORMAT = NET_ADDRESS_FORMAT(2)

@When[feat_windows_win32_network_management_ip_helper == "on"]
public let NET_ADDRESS_IPV6: NET_ADDRESS_FORMAT = NET_ADDRESS_FORMAT(3)

@When[feat_windows_win32_network_management_ip_helper == "on"]
@C
public struct NET_ADDRESS_INFO {
    public var Format: NET_ADDRESS_FORMAT = NET_ADDRESS_FORMAT()
    public var Anonymous: NET_ADDRESS_INFO__Anonymous_e__Union = NET_ADDRESS_INFO__Anonymous_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_ip_helper == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct NET_ADDRESS_INFO__Anonymous_e__Union {
    public var _raw: VArray<UInt32, $131> = VArray<UInt32, $131>(repeat: 0)

    public func namedAddress(): NET_ADDRESS_INFO__Anonymous_e__Union__NamedAddress_e__Struct {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<NET_ADDRESS_INFO__Anonymous_e__Union__NamedAddress_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_namedAddress(val: NET_ADDRESS_INFO__Anonymous_e__Union__NamedAddress_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<NET_ADDRESS_INFO__Anonymous_e__Union__NamedAddress_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ipv4Address(): win32_networking_win_sock.SOCKADDR_IN {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<win32_networking_win_sock.SOCKADDR_IN>(p)
            return ptr.read()
        }
    }

    public mut func set_ipv4Address(val: win32_networking_win_sock.SOCKADDR_IN): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<win32_networking_win_sock.SOCKADDR_IN>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ipv6Address(): win32_networking_win_sock.SOCKADDR_IN6 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<win32_networking_win_sock.SOCKADDR_IN6>(p)
            return ptr.read()
        }
    }

    public mut func set_ipv6Address(val: win32_networking_win_sock.SOCKADDR_IN6): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<win32_networking_win_sock.SOCKADDR_IN6>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ipAddress(): win32_networking_win_sock.SOCKADDR {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<win32_networking_win_sock.SOCKADDR>(p)
            return ptr.read()
        }
    }

    public mut func set_ipAddress(val: win32_networking_win_sock.SOCKADDR): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<win32_networking_win_sock.SOCKADDR>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_ip_helper == "on"]
@C
public struct NET_ADDRESS_INFO__Anonymous_e__Union__NamedAddress_e__Struct {
    public var Address: VArray<UInt16, $256> = VArray<UInt16, $256>(repeat: 0)
    public var Port: VArray<UInt16, $6> = VArray<UInt16, $6>(repeat: 0)
    public init() {}
}
