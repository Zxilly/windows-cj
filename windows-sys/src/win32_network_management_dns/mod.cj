// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.win32_network_management_dns

import windows_interface.*
import windows_result.*
import windows_sys.win32_foundation
import windows_sys.win32_foundation.*


@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_AAAA_DATA {
    public var Ip6Address: IP6_ADDRESS = IP6_ADDRESS()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_ADDR {
    public var MaxSa: VArray<win32_foundation.CHAR, $32> = VArray<win32_foundation.CHAR, $32>(repeat: 0)
    public var Data: DNS_ADDR__Data_e__Union = DNS_ADDR__Data_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// packed(1)
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_ADDR__Data_e__Union {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0)

    public func dnsAddrUserDword(): VArray<UInt32, $8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt8>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt32, $8>>(p)
            return ptr.read()
        }
    }

    public mut func set_dnsAddrUserDword(val: VArray<UInt32, $8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt8>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt32, $8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// packed(1)
@C
public struct DNS_ADDR_ARRAY {
    public var MaxCount: UInt32 = 0
    public var AddrCount: UInt32 = 0
    public var Tag: UInt32 = 0
    public var Family: UInt16 = 0
    public var WordReserved: UInt16 = 0
    public var Flags: UInt32 = 0
    public var MatchFlag: UInt32 = 0
    public var Reserved1: UInt32 = 0
    public var Reserved2: UInt32 = 0
    public var AddrArray: VArray<DNS_ADDR, $1> = VArray<DNS_ADDR, $1>(repeat: DNS_ADDR())
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_APPLICATION_SETTINGS {
    public var Version: UInt32 = 0
    public var Flags: UInt64 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_ATMA_DATA {
    public var AddressType: UInt8 = 0
    public var Address: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_A_DATA {
    public var IpAddress: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CHARSET {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_CHARSET): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_CHARSET): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsCharSetUnknown: DNS_CHARSET = DNS_CHARSET(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsCharSetUnicode: DNS_CHARSET = DNS_CHARSET(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsCharSetUtf8: DNS_CHARSET = DNS_CHARSET(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsCharSetAnsi: DNS_CHARSET = DNS_CHARSET(3)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONFIG_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_CONFIG_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_CONFIG_TYPE): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigPrimaryDomainName_W: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigPrimaryDomainName_A: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigPrimaryDomainName_UTF8: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigAdapterDomainName_W: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(3)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigAdapterDomainName_A: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(4)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigAdapterDomainName_UTF8: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(5)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigDnsServerList: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(6)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigSearchList: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(7)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigAdapterInfo: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(8)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigPrimaryHostNameRegistrationEnabled: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(9)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigAdapterHostNameRegistrationEnabled: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(10)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigAddressRegistrationMaxCount: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(11)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigHostName_W: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(12)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigHostName_A: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(13)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigHostName_UTF8: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(14)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigFullHostName_W: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(15)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigFullHostName_A: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(16)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigFullHostName_UTF8: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(17)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsConfigNameServer: DNS_CONFIG_TYPE = DNS_CONFIG_TYPE(18)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_IFINDEX_ENTRY {
    public var pwszConnectionName: CPointer<UInt16> = CPointer<UInt16>()
    public var dwIfIndex: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_IFINDEX_LIST {
    public var pConnectionIfIndexEntries: CPointer<DNS_CONNECTION_IFINDEX_ENTRY> = CPointer<DNS_CONNECTION_IFINDEX_ENTRY>()
    public var nEntries: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_NAME {
    public var wszName: VArray<UInt16, $65> = VArray<UInt16, $65>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_NAME_LIST {
    public var cNames: UInt32 = 0
    public var pNames: CPointer<DNS_CONNECTION_NAME> = CPointer<DNS_CONNECTION_NAME>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_POLICY_ENTRY {
    public var pwszHost: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszAppId: CPointer<UInt16> = CPointer<UInt16>()
    public var cbAppSid: UInt32 = 0
    public var pbAppSid: CPointer<UInt8> = CPointer<UInt8>()
    public var nConnections: UInt32 = 0
    public var ppwszConnections: CPointer<CPointer<UInt16>> = CPointer<CPointer<UInt16>>()
    public var dwPolicyEntryFlags: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_POLICY_ENTRY_LIST {
    public var pPolicyEntries: CPointer<DNS_CONNECTION_POLICY_ENTRY> = CPointer<DNS_CONNECTION_POLICY_ENTRY>()
    public var nEntries: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_POLICY_TAG {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_CONNECTION_POLICY_TAG): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_CONNECTION_POLICY_TAG): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let TAG_DNS_CONNECTION_POLICY_TAG_DEFAULT: DNS_CONNECTION_POLICY_TAG = DNS_CONNECTION_POLICY_TAG(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let TAG_DNS_CONNECTION_POLICY_TAG_CONNECTION_MANAGER: DNS_CONNECTION_POLICY_TAG = DNS_CONNECTION_POLICY_TAG(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let TAG_DNS_CONNECTION_POLICY_TAG_WWWPT: DNS_CONNECTION_POLICY_TAG = DNS_CONNECTION_POLICY_TAG(2)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_ELEMENT {
    public var Type: DNS_CONNECTION_PROXY_TYPE = DNS_CONNECTION_PROXY_TYPE()
    public var Info: DNS_CONNECTION_PROXY_INFO = DNS_CONNECTION_PROXY_INFO()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO {
    public var Version: UInt32 = 0
    public var pwszFriendlyName: CPointer<UInt16> = CPointer<UInt16>()
    public var Flags: UInt32 = 0
    public var Switch: DNS_CONNECTION_PROXY_INFO_SWITCH = DNS_CONNECTION_PROXY_INFO_SWITCH()
    public var Anonymous: DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union = DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $6> = VArray<UInt64, $6>(repeat: 0)

    public func config(): DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG>(p)
            return ptr.read()
        }
    }

    public mut func set_config(val: DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func script(): DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT>(p)
            return ptr.read()
        }
    }

    public mut func set_script(val: DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_CONFIG {
    public var pwszServer: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszUsername: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszPassword: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszException: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszExtraInfo: CPointer<UInt16> = CPointer<UInt16>()
    public var Port: UInt16 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO__Anonymous_e__Union__DNS_CONNECTION_PROXY_INFO_SCRIPT {
    public var pwszScript: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszUsername: CPointer<UInt16> = CPointer<UInt16>()
    public var pwszPassword: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO_EX {
    public var ProxyInfo: DNS_CONNECTION_PROXY_INFO = DNS_CONNECTION_PROXY_INFO()
    public var dwInterfaceIndex: UInt32 = 0
    public var pwszConnectionName: CPointer<UInt16> = CPointer<UInt16>()
    public var fDirectConfiguration: Int32 = 0
    public var hConnection: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_INFO_SWITCH {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_CONNECTION_PROXY_INFO_SWITCH): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_CONNECTION_PROXY_INFO_SWITCH): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_SWITCH_CONFIG: DNS_CONNECTION_PROXY_INFO_SWITCH = DNS_CONNECTION_PROXY_INFO_SWITCH(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_SWITCH_SCRIPT: DNS_CONNECTION_PROXY_INFO_SWITCH = DNS_CONNECTION_PROXY_INFO_SWITCH(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_SWITCH_WPAD: DNS_CONNECTION_PROXY_INFO_SWITCH = DNS_CONNECTION_PROXY_INFO_SWITCH(2)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_LIST {
    public var cProxies: UInt32 = 0
    public var pProxies: CPointer<DNS_CONNECTION_PROXY_ELEMENT> = CPointer<DNS_CONNECTION_PROXY_ELEMENT>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CONNECTION_PROXY_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_CONNECTION_PROXY_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_CONNECTION_PROXY_TYPE): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_TYPE_NULL: DNS_CONNECTION_PROXY_TYPE = DNS_CONNECTION_PROXY_TYPE(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_TYPE_HTTP: DNS_CONNECTION_PROXY_TYPE = DNS_CONNECTION_PROXY_TYPE(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_TYPE_WAP: DNS_CONNECTION_PROXY_TYPE = DNS_CONNECTION_PROXY_TYPE(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_TYPE_SOCKS4: DNS_CONNECTION_PROXY_TYPE = DNS_CONNECTION_PROXY_TYPE(4)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_TYPE_SOCKS5: DNS_CONNECTION_PROXY_TYPE = DNS_CONNECTION_PROXY_TYPE(5)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_CUSTOM_SERVER {
    public var dwServerType: UInt32 = 0
    public var ullFlags: UInt64 = 0
    public var Anonymous1: DNS_CUSTOM_SERVER__Anonymous1_e__Union = DNS_CUSTOM_SERVER__Anonymous1_e__Union()
    public var Anonymous2: DNS_CUSTOM_SERVER__Anonymous2_e__Union = DNS_CUSTOM_SERVER__Anonymous2_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_CUSTOM_SERVER__Anonymous1_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func pwszTemplate(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pwszTemplate(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pwszHostname(): CPointer<UInt16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            return ptr.read()
        }
    }

    public mut func set_pwszHostname(val: CPointer<UInt16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_CUSTOM_SERVER__Anonymous2_e__Union {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0)

    public func maxSa(): VArray<win32_foundation.CHAR, $32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt8>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<win32_foundation.CHAR, $32>>(p)
            return ptr.read()
        }
    }

    public mut func set_maxSa(val: VArray<win32_foundation.CHAR, $32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt8>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<win32_foundation.CHAR, $32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_DHCID_DATA {
    public var dwByteCount: UInt32 = 0
    public var DHCID: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_DS_DATA {
    public var wKeyTag: UInt16 = 0
    public var chAlgorithm: UInt8 = 0
    public var chDigestType: UInt8 = 0
    public var wDigestLength: UInt16 = 0
    public var wPad: UInt16 = 0
    public var Digest: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_FREE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_FREE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_FREE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsFreeFlat: DNS_FREE_TYPE = DNS_FREE_TYPE(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsFreeRecordList: DNS_FREE_TYPE = DNS_FREE_TYPE(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsFreeParsedMessageFields: DNS_FREE_TYPE = DNS_FREE_TYPE(2)

@When[feat_windows_win32_network_management_dns == "on"]
// packed(1)
@C
public struct DNS_HEADER {
    public var Xid: UInt16 = 0
    public var _bitfield1: UInt8 = 0
    public var _bitfield2: UInt8 = 0
    public var QuestionCount: UInt16 = 0
    public var AnswerCount: UInt16 = 0
    public var NameServerCount: UInt16 = 0
    public var AdditionalCount: UInt16 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_HEADER_EXT {
    public var _bitfield: UInt16 = 0
    public var chRcode: UInt8 = 0
    public var chVersion: UInt8 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_KEY_DATA {
    public var wFlags: UInt16 = 0
    public var chProtocol: UInt8 = 0
    public var chAlgorithm: UInt8 = 0
    public var wKeyLength: UInt16 = 0
    public var wPad: UInt16 = 0
    public var Key: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_LOC_DATA {
    public var wVersion: UInt16 = 0
    public var wSize: UInt16 = 0
    public var wHorPrec: UInt16 = 0
    public var wVerPrec: UInt16 = 0
    public var dwLatitude: UInt32 = 0
    public var dwLongitude: UInt32 = 0
    public var dwAltitude: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_MESSAGE_BUFFER {
    public var MessageHead: DNS_HEADER = DNS_HEADER()
    public var MessageBody: VArray<win32_foundation.CHAR, $1> = VArray<win32_foundation.CHAR, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_MINFO_DATAA {
    public var pNameMailbox: CPointer<UInt8> = CPointer<UInt8>()
    public var pNameErrorsMailbox: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_MINFO_DATAW {
    public var pNameMailbox: CPointer<UInt16> = CPointer<UInt16>()
    public var pNameErrorsMailbox: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_MX_DATAA {
    public var pNameExchange: CPointer<UInt8> = CPointer<UInt8>()
    public var wPreference: UInt16 = 0
    public var Pad: UInt16 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_MX_DATAW {
    public var pNameExchange: CPointer<UInt16> = CPointer<UInt16>()
    public var wPreference: UInt16 = 0
    public var Pad: UInt16 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NAME_FORMAT {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_NAME_FORMAT): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_NAME_FORMAT): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsNameDomain: DNS_NAME_FORMAT = DNS_NAME_FORMAT(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsNameDomainLabel: DNS_NAME_FORMAT = DNS_NAME_FORMAT(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsNameHostnameFull: DNS_NAME_FORMAT = DNS_NAME_FORMAT(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsNameHostnameLabel: DNS_NAME_FORMAT = DNS_NAME_FORMAT(3)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsNameWildcard: DNS_NAME_FORMAT = DNS_NAME_FORMAT(4)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsNameSrvRecord: DNS_NAME_FORMAT = DNS_NAME_FORMAT(5)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsNameValidateTld: DNS_NAME_FORMAT = DNS_NAME_FORMAT(6)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NAPTR_DATAA {
    public var wOrder: UInt16 = 0
    public var wPreference: UInt16 = 0
    public var pFlags: CPointer<UInt8> = CPointer<UInt8>()
    public var pService: CPointer<UInt8> = CPointer<UInt8>()
    public var pRegularExpression: CPointer<UInt8> = CPointer<UInt8>()
    public var pReplacement: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NAPTR_DATAW {
    public var wOrder: UInt16 = 0
    public var wPreference: UInt16 = 0
    public var pFlags: CPointer<UInt16> = CPointer<UInt16>()
    public var pService: CPointer<UInt16> = CPointer<UInt16>()
    public var pRegularExpression: CPointer<UInt16> = CPointer<UInt16>()
    public var pReplacement: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NSEC3PARAM_DATA {
    public var chAlgorithm: UInt8 = 0
    public var bFlags: UInt8 = 0
    public var wIterations: UInt16 = 0
    public var bSaltLength: UInt8 = 0
    public var bPad: VArray<UInt8, $3> = VArray<UInt8, $3>(repeat: 0)
    public var pbSalt: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NSEC3_DATA {
    public var chAlgorithm: UInt8 = 0
    public var bFlags: UInt8 = 0
    public var wIterations: UInt16 = 0
    public var bSaltLength: UInt8 = 0
    public var bHashLength: UInt8 = 0
    public var wTypeBitMapsLength: UInt16 = 0
    public var chData: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NSEC_DATAA {
    public var pNextDomainName: CPointer<UInt8> = CPointer<UInt8>()
    public var wTypeBitMapsLength: UInt16 = 0
    public var wPad: UInt16 = 0
    public var TypeBitMaps: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NSEC_DATAW {
    public var pNextDomainName: CPointer<UInt16> = CPointer<UInt16>()
    public var wTypeBitMapsLength: UInt16 = 0
    public var wPad: UInt16 = 0
    public var TypeBitMaps: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NULL_DATA {
    public var dwByteCount: UInt32 = 0
    public var Data: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NXT_DATAA {
    public var pNameNext: CPointer<UInt8> = CPointer<UInt8>()
    public var wNumTypes: UInt16 = 0
    public var wTypes: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_NXT_DATAW {
    public var pNameNext: CPointer<UInt16> = CPointer<UInt16>()
    public var wNumTypes: UInt16 = 0
    public var wTypes: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_OPT_DATA {
    public var wDataLength: UInt16 = 0
    public var wPad: UInt16 = 0
    public var Data: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
public type DNS_PROXY_COMPLETION_ROUTINE = CFunc<(CPointer<Unit>, Int32) -> Unit>

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_PROXY_INFORMATION {
    public var version: UInt32 = 0
    public var proxyInformationType: DNS_PROXY_INFORMATION_TYPE = DNS_PROXY_INFORMATION_TYPE()
    public var proxyName: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_PROXY_INFORMATION_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_PROXY_INFORMATION_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_PROXY_INFORMATION_TYPE): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROXY_INFORMATION_DIRECT: DNS_PROXY_INFORMATION_TYPE = DNS_PROXY_INFORMATION_TYPE(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROXY_INFORMATION_DEFAULT_SETTINGS: DNS_PROXY_INFORMATION_TYPE = DNS_PROXY_INFORMATION_TYPE(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROXY_INFORMATION_PROXY_NAME: DNS_PROXY_INFORMATION_TYPE = DNS_PROXY_INFORMATION_TYPE(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROXY_INFORMATION_DOES_NOT_EXIST: DNS_PROXY_INFORMATION_TYPE = DNS_PROXY_INFORMATION_TYPE(3)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_PTR_DATAA {
    public var pNameHost: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_PTR_DATAW {
    public var pNameHost: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_QUERY_CANCEL {
    public var Reserved: VArray<win32_foundation.CHAR, $32> = VArray<win32_foundation.CHAR, $32>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_QUERY_OPTIONS {
    public var value: UInt32 = 0

    public init() {}

    public init(value: UInt32) {
        this.value = value
    }

    public operator func ==(other: DNS_QUERY_OPTIONS): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_QUERY_OPTIONS): Bool {
        return this.value != other.value
    }

    public func contains(other: DNS_QUERY_OPTIONS): Bool {
        return (this.value & other.value) == other.value
    }

    public operator func |(other: DNS_QUERY_OPTIONS): DNS_QUERY_OPTIONS {
        return DNS_QUERY_OPTIONS(this.value | other.value)
    }
    public operator func &(other: DNS_QUERY_OPTIONS): DNS_QUERY_OPTIONS {
        return DNS_QUERY_OPTIONS(this.value & other.value)
    }
    public operator func !(): DNS_QUERY_OPTIONS {
        return DNS_QUERY_OPTIONS(!this.value)
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_PARSE_ALL_RECORDS: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_STANDARD: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_ACCEPT_TRUNCATED_RESPONSE: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_USE_TCP_ONLY: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_NO_RECURSION: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(4)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_BYPASS_CACHE: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(8)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_NO_WIRE_QUERY: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(16)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_NO_LOCAL_NAME: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(32)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_NO_HOSTS_FILE: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(64)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_NO_NETBT: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(128)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_WIRE_ONLY: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(256)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_RETURN_MESSAGE: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(512)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_MULTICAST_ONLY: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(1024)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_NO_MULTICAST: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(2048)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_TREAT_AS_FQDN: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(4096)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_ADDRCONFIG: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(8192)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_DUAL_ADDR: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(16384)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_DONT_RESET_TTL_VALUES: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(1048576)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_DISABLE_IDN_ENCODING: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(2097152)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_APPEND_MULTILABEL: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(8388608)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_DNSSEC_OK: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(16777216)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_DNSSEC_CHECKING_DISABLED: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(33554432)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_DNSSEC_REQUIRED: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(67108864)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_RESERVED: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(4026531840)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_CACHE_ONLY: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(16)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_REQUEST_VERSION1: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_REQUEST_VERSION2: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_RESULTS_VERSION1: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_REQUEST_VERSION3: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(3)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_RAW_RESULTS_VERSION1: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_RAW_REQUEST_VERSION1: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_QUERY_RAW_OPTION_BEST_EFFORT_PARSE: DNS_QUERY_OPTIONS = DNS_QUERY_OPTIONS(1)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_QUERY_RAW_CANCEL {
    public var reserved: VArray<win32_foundation.CHAR, $32> = VArray<win32_foundation.CHAR, $32>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
public type DNS_QUERY_RAW_COMPLETION_ROUTINE = CFunc<(CPointer<Unit>, CPointer<DNS_QUERY_RAW_RESULT>) -> Unit>

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_QUERY_RAW_REQUEST {
    public var version: UInt32 = 0
    public var resultsVersion: UInt32 = 0
    public var dnsQueryRawSize: UInt32 = 0
    public var dnsQueryRaw: CPointer<UInt8> = CPointer<UInt8>()
    public var dnsQueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var dnsQueryType: UInt16 = 0
    public var queryOptions: UInt64 = 0
    public var interfaceIndex: UInt32 = 0
    public var queryCompletionCallback: DNS_QUERY_RAW_COMPLETION_ROUTINE = { _, _ => () }
    public var queryContext: CPointer<Unit> = CPointer<Unit>()
    public var queryRawOptions: UInt64 = 0
    public var customServersSize: UInt32 = 0
    public var customServers: CPointer<DNS_CUSTOM_SERVER> = CPointer<DNS_CUSTOM_SERVER>()
    public var protocol: UInt32 = 0
    public var Anonymous: DNS_QUERY_RAW_REQUEST__Anonymous_e__Union = DNS_QUERY_RAW_REQUEST__Anonymous_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_QUERY_RAW_REQUEST__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0)

    public func maxSa(): VArray<win32_foundation.CHAR, $32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt8>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<win32_foundation.CHAR, $32>>(p)
            return ptr.read()
        }
    }

    public mut func set_maxSa(val: VArray<win32_foundation.CHAR, $32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt8>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<win32_foundation.CHAR, $32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_QUERY_RAW_RESULT {
    public var version: UInt32 = 0
    public var queryStatus: Int32 = 0
    public var queryOptions: UInt64 = 0
    public var queryRawOptions: UInt64 = 0
    public var responseFlags: UInt64 = 0
    public var queryRawResponseSize: UInt32 = 0
    public var queryRawResponse: CPointer<UInt8> = CPointer<UInt8>()
    public var queryRecords: CPointer<DNS_RECORDA> = CPointer<DNS_RECORDA>()
    public var protocol: UInt32 = 0
    public var Anonymous: DNS_QUERY_RAW_RESULT__Anonymous_e__Union = DNS_QUERY_RAW_RESULT__Anonymous_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_QUERY_RAW_RESULT__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0)

    public func maxSa(): VArray<win32_foundation.CHAR, $32> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt8>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<win32_foundation.CHAR, $32>>(p)
            return ptr.read()
        }
    }

    public mut func set_maxSa(val: VArray<win32_foundation.CHAR, $32>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt8>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<win32_foundation.CHAR, $32>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_QUERY_REQUEST {
    public var Version: UInt32 = 0
    public var QueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var QueryType: UInt16 = 0
    public var QueryOptions: UInt64 = 0
    public var pDnsServerList: CPointer<DNS_ADDR_ARRAY> = CPointer<DNS_ADDR_ARRAY>()
    public var InterfaceIndex: UInt32 = 0
    public var pQueryCompletionCallback: PDNS_QUERY_COMPLETION_ROUTINE = { _, _ => () }
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_QUERY_REQUEST3 {
    public var Version: UInt32 = 0
    public var QueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var QueryType: UInt16 = 0
    public var QueryOptions: UInt64 = 0
    public var pDnsServerList: CPointer<DNS_ADDR_ARRAY> = CPointer<DNS_ADDR_ARRAY>()
    public var InterfaceIndex: UInt32 = 0
    public var pQueryCompletionCallback: PDNS_QUERY_COMPLETION_ROUTINE = { _, _ => () }
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public var IsNetworkQueryRequired: Int32 = 0
    public var RequiredNetworkIndex: UInt32 = 0
    public var cCustomServers: UInt32 = 0
    public var pCustomServers: CPointer<DNS_CUSTOM_SERVER> = CPointer<DNS_CUSTOM_SERVER>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_QUERY_RESULT {
    public var Version: UInt32 = 0
    public var QueryStatus: Int32 = 0
    public var QueryOptions: UInt64 = 0
    public var pQueryRecords: CPointer<DNS_RECORDA> = CPointer<DNS_RECORDA>()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_RECORDA {
    public var pNext: CPointer<DNS_RECORDA> = CPointer<DNS_RECORDA>()
    public var pName: CPointer<UInt8> = CPointer<UInt8>()
    public var wType: UInt16 = 0
    public var wDataLength: UInt16 = 0
    public var Flags: DNS_RECORDA__Flags_e__Union = DNS_RECORDA__Flags_e__Union()
    public var dwTtl: UInt32 = 0
    public var dwReserved: UInt32 = 0
    public var Data: DNS_RECORDA__Data_e__Union = DNS_RECORDA__Data_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_RECORDA__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public func dw(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_dw(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func s(): DNS_RECORD_FLAGS {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_RECORD_FLAGS>(p)
            return ptr.read()
        }
    }

    public mut func set_s(val: DNS_RECORD_FLAGS): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_RECORD_FLAGS>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_RECORDA__Data_e__Union {
    public var _raw: VArray<UInt64, $7> = VArray<UInt64, $7>(repeat: 0)

    public func a(): DNS_A_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_A_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_a(val: DNS_A_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_A_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func soa(): DNS_SOA_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SOA_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_soa(val: DNS_SOA_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SOA_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func soa2(): DNS_SOA_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SOA_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_soa2(val: DNS_SOA_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SOA_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ptr(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_ptr(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ptr2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_ptr2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ns(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_ns(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ns2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_ns2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cname(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_cname(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cname2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_cname2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dname(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_dname(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dname2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_dname2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mb(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mb(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mb2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mb2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func md(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_md(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func md2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_md2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mf(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mf(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mf2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mf2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mg(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mg(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mg2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mg2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mr(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mr(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mr2(): DNS_PTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mr2(val: DNS_PTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func minfo(): DNS_MINFO_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_minfo(val: DNS_MINFO_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func minfo2(): DNS_MINFO_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_minfo2(val: DNS_MINFO_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rp(): DNS_MINFO_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_rp(val: DNS_MINFO_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rp2(): DNS_MINFO_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_rp2(val: DNS_MINFO_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mx(): DNS_MX_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mx(val: DNS_MX_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mx2(): DNS_MX_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_mx2(val: DNS_MX_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func afsdb(): DNS_MX_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_afsdb(val: DNS_MX_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func afsdb2(): DNS_MX_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_afsdb2(val: DNS_MX_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rt(): DNS_MX_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_rt(val: DNS_MX_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rt2(): DNS_MX_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_rt2(val: DNS_MX_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func hinfo(): DNS_TXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_hinfo(val: DNS_TXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func hinfo2(): DNS_TXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_hinfo2(val: DNS_TXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func isdn(): DNS_TXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_isdn(val: DNS_TXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func isdn2(): DNS_TXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_isdn2(val: DNS_TXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func txt(): DNS_TXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_txt(val: DNS_TXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func txt2(): DNS_TXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_txt2(val: DNS_TXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func x25(): DNS_TXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_x25(val: DNS_TXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func null(): DNS_NULL_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NULL_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_null(val: DNS_NULL_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NULL_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wks(): DNS_WKS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WKS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_wks(val: DNS_WKS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WKS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wks2(): DNS_WKS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WKS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_wks2(val: DNS_WKS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WKS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func aaaa(): DNS_AAAA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_AAAA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_aaaa(val: DNS_AAAA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_AAAA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func key(): DNS_KEY_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_key(val: DNS_KEY_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func key2(): DNS_KEY_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_key2(val: DNS_KEY_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func sig(): DNS_SIG_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_sig(val: DNS_SIG_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func sig2(): DNS_SIG_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_sig2(val: DNS_SIG_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func atma(): DNS_ATMA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_ATMA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_atma(val: DNS_ATMA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_ATMA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func atma2(): DNS_ATMA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_ATMA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_atma2(val: DNS_ATMA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_ATMA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nxt(): DNS_NXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_nxt(val: DNS_NXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nxt2(): DNS_NXT_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NXT_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_nxt2(val: DNS_NXT_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NXT_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func srv(): DNS_SRV_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SRV_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_srv(val: DNS_SRV_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SRV_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func srv2(): DNS_SRV_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SRV_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_srv2(val: DNS_SRV_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SRV_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func naptr(): DNS_NAPTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NAPTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_naptr(val: DNS_NAPTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NAPTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func naptr2(): DNS_NAPTR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NAPTR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_naptr2(val: DNS_NAPTR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NAPTR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func opt(): DNS_OPT_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_opt(val: DNS_OPT_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func opt2(): DNS_OPT_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_opt2(val: DNS_OPT_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ds(): DNS_DS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_ds(val: DNS_DS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ds2(): DNS_DS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_ds2(val: DNS_DS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rrsig(): DNS_SIG_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_rrsig(val: DNS_SIG_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rrsig2(): DNS_SIG_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_rrsig2(val: DNS_SIG_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec(): DNS_NSEC_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec(val: DNS_NSEC_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec2(): DNS_NSEC_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec2(val: DNS_NSEC_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dnskey(): DNS_KEY_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_dnskey(val: DNS_KEY_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dnskey2(): DNS_KEY_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_dnskey2(val: DNS_KEY_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tkey(): DNS_TKEY_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TKEY_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_tkey(val: DNS_TKEY_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TKEY_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tkey2(): DNS_TKEY_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TKEY_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_tkey2(val: DNS_TKEY_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TKEY_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tsig(): DNS_TSIG_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TSIG_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_tsig(val: DNS_TSIG_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TSIG_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tsig2(): DNS_TSIG_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TSIG_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_tsig2(val: DNS_TSIG_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TSIG_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wins(): DNS_WINS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_wins(val: DNS_WINS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wins2(): DNS_WINS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_wins2(val: DNS_WINS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func winsr(): DNS_WINSR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_winsr(val: DNS_WINSR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func winsR(): DNS_WINSR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_winsR(val: DNS_WINSR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nbstat(): DNS_WINSR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_nbstat(val: DNS_WINSR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nbstat2(): DNS_WINSR_DATAA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAA>(p)
            return ptr.read()
        }
    }

    public mut func set_nbstat2(val: DNS_WINSR_DATAA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dhcid(): DNS_DHCID_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DHCID_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_dhcid(val: DNS_DHCID_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DHCID_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec3(): DNS_NSEC3_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec3(val: DNS_NSEC3_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec32(): DNS_NSEC3_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec32(val: DNS_NSEC3_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec3PARAM(): DNS_NSEC3PARAM_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec3PARAM(val: DNS_NSEC3PARAM_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec3Param(): DNS_NSEC3PARAM_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec3Param(val: DNS_NSEC3PARAM_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tlsa(): DNS_TLSA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TLSA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_tlsa(val: DNS_TLSA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TLSA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tlsa2(): DNS_TLSA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TLSA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_tlsa2(val: DNS_TLSA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TLSA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func svcb(): DNS_SVCB_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SVCB_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_svcb(val: DNS_SVCB_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SVCB_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func svcb2(): DNS_SVCB_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SVCB_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_svcb2(val: DNS_SVCB_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SVCB_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func unknown(): DNS_UNKNOWN_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_unknown(val: DNS_UNKNOWN_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func unknown2(): DNS_UNKNOWN_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_unknown2(val: DNS_UNKNOWN_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pDataPtr(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pDataPtr(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_RECORDW {
    public var pNext: CPointer<DNS_RECORDW> = CPointer<DNS_RECORDW>()
    public var pName: CPointer<UInt16> = CPointer<UInt16>()
    public var wType: UInt16 = 0
    public var wDataLength: UInt16 = 0
    public var Flags: DNS_RECORDW__Flags_e__Union = DNS_RECORDW__Flags_e__Union()
    public var dwTtl: UInt32 = 0
    public var dwReserved: UInt32 = 0
    public var Data: DNS_RECORDW__Data_e__Union = DNS_RECORDW__Data_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_RECORDW__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public func dw(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_dw(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func s(): DNS_RECORD_FLAGS {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_RECORD_FLAGS>(p)
            return ptr.read()
        }
    }

    public mut func set_s(val: DNS_RECORD_FLAGS): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_RECORD_FLAGS>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_RECORDW__Data_e__Union {
    public var _raw: VArray<UInt64, $7> = VArray<UInt64, $7>(repeat: 0)

    public func a(): DNS_A_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_A_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_a(val: DNS_A_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_A_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func soa(): DNS_SOA_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SOA_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_soa(val: DNS_SOA_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SOA_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func soa2(): DNS_SOA_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SOA_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_soa2(val: DNS_SOA_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SOA_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ptr(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_ptr(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ptr2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_ptr2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ns(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_ns(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ns2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_ns2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cname(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_cname(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func cname2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_cname2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dname(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_dname(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dname2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_dname2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mb(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mb(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mb2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mb2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func md(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_md(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func md2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_md2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mf(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mf(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mf2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mf2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mg(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mg(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mg2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mg2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mr(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mr(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mr2(): DNS_PTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mr2(val: DNS_PTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_PTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func minfo(): DNS_MINFO_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_minfo(val: DNS_MINFO_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func minfo2(): DNS_MINFO_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_minfo2(val: DNS_MINFO_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rp(): DNS_MINFO_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_rp(val: DNS_MINFO_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rp2(): DNS_MINFO_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_rp2(val: DNS_MINFO_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MINFO_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mx(): DNS_MX_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mx(val: DNS_MX_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func mx2(): DNS_MX_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_mx2(val: DNS_MX_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func afsdb(): DNS_MX_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_afsdb(val: DNS_MX_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func afsdb2(): DNS_MX_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_afsdb2(val: DNS_MX_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rt(): DNS_MX_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_rt(val: DNS_MX_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rt2(): DNS_MX_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_rt2(val: DNS_MX_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_MX_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func hinfo(): DNS_TXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_hinfo(val: DNS_TXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func hinfo2(): DNS_TXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_hinfo2(val: DNS_TXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func isdn(): DNS_TXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_isdn(val: DNS_TXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func isdn2(): DNS_TXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_isdn2(val: DNS_TXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func txt(): DNS_TXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_txt(val: DNS_TXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func txt2(): DNS_TXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_txt2(val: DNS_TXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func x25(): DNS_TXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_x25(val: DNS_TXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func null(): DNS_NULL_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NULL_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_null(val: DNS_NULL_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NULL_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wks(): DNS_WKS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WKS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_wks(val: DNS_WKS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WKS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wks2(): DNS_WKS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WKS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_wks2(val: DNS_WKS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WKS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func aaaa(): DNS_AAAA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_AAAA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_aaaa(val: DNS_AAAA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_AAAA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func key(): DNS_KEY_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_key(val: DNS_KEY_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func key2(): DNS_KEY_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_key2(val: DNS_KEY_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func sig(): DNS_SIG_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_sig(val: DNS_SIG_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func sig2(): DNS_SIG_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_sig2(val: DNS_SIG_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func atma(): DNS_ATMA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_ATMA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_atma(val: DNS_ATMA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_ATMA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func atma2(): DNS_ATMA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_ATMA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_atma2(val: DNS_ATMA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_ATMA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nxt(): DNS_NXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_nxt(val: DNS_NXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nxt2(): DNS_NXT_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NXT_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_nxt2(val: DNS_NXT_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NXT_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func srv(): DNS_SRV_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SRV_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_srv(val: DNS_SRV_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SRV_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func srv2(): DNS_SRV_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SRV_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_srv2(val: DNS_SRV_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SRV_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func naptr(): DNS_NAPTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NAPTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_naptr(val: DNS_NAPTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NAPTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func naptr2(): DNS_NAPTR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NAPTR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_naptr2(val: DNS_NAPTR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NAPTR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func opt(): DNS_OPT_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_opt(val: DNS_OPT_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func opt2(): DNS_OPT_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_opt2(val: DNS_OPT_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ds(): DNS_DS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_ds(val: DNS_DS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ds2(): DNS_DS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_ds2(val: DNS_DS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rrsig(): DNS_SIG_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_rrsig(val: DNS_SIG_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func rrsig2(): DNS_SIG_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_rrsig2(val: DNS_SIG_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SIG_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec(): DNS_NSEC_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec(val: DNS_NSEC_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec2(): DNS_NSEC_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec2(val: DNS_NSEC_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dnskey(): DNS_KEY_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_dnskey(val: DNS_KEY_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dnskey2(): DNS_KEY_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_dnskey2(val: DNS_KEY_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_KEY_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tkey(): DNS_TKEY_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TKEY_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_tkey(val: DNS_TKEY_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TKEY_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tkey2(): DNS_TKEY_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TKEY_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_tkey2(val: DNS_TKEY_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TKEY_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tsig(): DNS_TSIG_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TSIG_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_tsig(val: DNS_TSIG_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TSIG_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tsig2(): DNS_TSIG_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TSIG_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_tsig2(val: DNS_TSIG_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TSIG_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wins(): DNS_WINS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_wins(val: DNS_WINS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wins2(): DNS_WINS_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINS_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_wins2(val: DNS_WINS_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINS_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func winsr(): DNS_WINSR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_winsr(val: DNS_WINSR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func winsR(): DNS_WINSR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_winsR(val: DNS_WINSR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nbstat(): DNS_WINSR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_nbstat(val: DNS_WINSR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nbstat2(): DNS_WINSR_DATAW {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAW>(p)
            return ptr.read()
        }
    }

    public mut func set_nbstat2(val: DNS_WINSR_DATAW): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_WINSR_DATAW>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func dhcid(): DNS_DHCID_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DHCID_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_dhcid(val: DNS_DHCID_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_DHCID_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec3(): DNS_NSEC3_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec3(val: DNS_NSEC3_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec32(): DNS_NSEC3_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec32(val: DNS_NSEC3_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec3PARAM(): DNS_NSEC3PARAM_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec3PARAM(val: DNS_NSEC3PARAM_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func nsec3Param(): DNS_NSEC3PARAM_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_nsec3Param(val: DNS_NSEC3PARAM_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_NSEC3PARAM_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tlsa(): DNS_TLSA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TLSA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_tlsa(val: DNS_TLSA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TLSA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func tlsa2(): DNS_TLSA_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TLSA_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_tlsa2(val: DNS_TLSA_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_TLSA_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func svcb(): DNS_SVCB_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SVCB_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_svcb(val: DNS_SVCB_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SVCB_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func svcb2(): DNS_SVCB_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SVCB_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_svcb2(val: DNS_SVCB_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_SVCB_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func unknown(): DNS_UNKNOWN_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_unknown(val: DNS_UNKNOWN_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func unknown2(): DNS_UNKNOWN_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_unknown2(val: DNS_UNKNOWN_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_UNKNOWN_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pDataPtr(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pDataPtr(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_RECORD_FLAGS {
    public var _bitfield: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_RECORD_OPTW {
    public var pNext: CPointer<DNS_RECORDW> = CPointer<DNS_RECORDW>()
    public var pName: CPointer<UInt16> = CPointer<UInt16>()
    public var wType: UInt16 = 0
    public var wDataLength: UInt16 = 0
    public var Flags: DNS_RECORD_OPTW__Flags_e__Union = DNS_RECORD_OPTW__Flags_e__Union()
    public var ExtHeader: DNS_HEADER_EXT = DNS_HEADER_EXT()
    public var wPayloadSize: UInt16 = 0
    public var wReserved: UInt16 = 0
    public var Data: DNS_RECORD_OPTW__Data_e__Union = DNS_RECORD_OPTW__Data_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_RECORD_OPTW__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public func dw(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_dw(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func s(): DNS_RECORD_FLAGS {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_RECORD_FLAGS>(p)
            return ptr.read()
        }
    }

    public mut func set_s(val: DNS_RECORD_FLAGS): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_RECORD_FLAGS>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_RECORD_OPTW__Data_e__Union {
    public var _raw: VArray<UInt16, $3> = VArray<UInt16, $3>(repeat: 0)

    public func opt(): DNS_OPT_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_opt(val: DNS_OPT_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func opt2(): DNS_OPT_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_opt2(val: DNS_OPT_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_RRSET {
    public var pFirstRR: CPointer<DNS_RECORDA> = CPointer<DNS_RECORDA>()
    public var pLastRR: CPointer<DNS_RECORDA> = CPointer<DNS_RECORDA>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SECTION {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_SECTION): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_SECTION): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSectionQuestion: DNS_SECTION = DNS_SECTION(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSectionAnswer: DNS_SECTION = DNS_SECTION(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSectionAuthority: DNS_SECTION = DNS_SECTION(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSectionAddtional: DNS_SECTION = DNS_SECTION(3)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SERVICE_BROWSE_REQUEST {
    public var Version: UInt32 = 0
    public var InterfaceIndex: UInt32 = 0
    public var QueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var Anonymous: DNS_SERVICE_BROWSE_REQUEST__Anonymous_e__Union = DNS_SERVICE_BROWSE_REQUEST__Anonymous_e__Union()
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_SERVICE_BROWSE_REQUEST__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SERVICE_CANCEL {
    public var reserved: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SERVICE_INSTANCE {
    public var pszInstanceName: CPointer<UInt16> = CPointer<UInt16>()
    public var pszHostName: CPointer<UInt16> = CPointer<UInt16>()
    public var ip4Address: CPointer<UInt32> = CPointer<UInt32>()
    public var ip6Address: CPointer<IP6_ADDRESS> = CPointer<IP6_ADDRESS>()
    public var wPort: UInt16 = 0
    public var wPriority: UInt16 = 0
    public var wWeight: UInt16 = 0
    public var dwPropertyCount: UInt32 = 0
    public var keys: CPointer<CPointer<UInt16>> = CPointer<CPointer<UInt16>>()
    public var values: CPointer<CPointer<UInt16>> = CPointer<CPointer<UInt16>>()
    public var dwInterfaceIndex: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SERVICE_REGISTER_REQUEST {
    public var Version: UInt32 = 0
    public var InterfaceIndex: UInt32 = 0
    public var pServiceInstance: CPointer<DNS_SERVICE_INSTANCE> = CPointer<DNS_SERVICE_INSTANCE>()
    public var pRegisterCompletionCallback: PDNS_SERVICE_REGISTER_COMPLETE = { _, _, _ => () }
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public var hCredentials: CPointer<Unit> = CPointer<Unit>()
    public var unicastEnabled: Int32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SERVICE_RESOLVE_REQUEST {
    public var Version: UInt32 = 0
    public var InterfaceIndex: UInt32 = 0
    public var QueryName: CPointer<UInt16> = CPointer<UInt16>()
    public var pResolveCompletionCallback: PDNS_SERVICE_RESOLVE_COMPLETE = { _, _, _ => () }
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SIG_DATAA {
    public var wTypeCovered: UInt16 = 0
    public var chAlgorithm: UInt8 = 0
    public var chLabelCount: UInt8 = 0
    public var dwOriginalTtl: UInt32 = 0
    public var dwExpiration: UInt32 = 0
    public var dwTimeSigned: UInt32 = 0
    public var wKeyTag: UInt16 = 0
    public var wSignatureLength: UInt16 = 0
    public var pNameSigner: CPointer<UInt8> = CPointer<UInt8>()
    public var Signature: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SIG_DATAW {
    public var wTypeCovered: UInt16 = 0
    public var chAlgorithm: UInt8 = 0
    public var chLabelCount: UInt8 = 0
    public var dwOriginalTtl: UInt32 = 0
    public var dwExpiration: UInt32 = 0
    public var dwTimeSigned: UInt32 = 0
    public var wKeyTag: UInt16 = 0
    public var wSignatureLength: UInt16 = 0
    public var pNameSigner: CPointer<UInt16> = CPointer<UInt16>()
    public var Signature: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SOA_DATAA {
    public var pNamePrimaryServer: CPointer<UInt8> = CPointer<UInt8>()
    public var pNameAdministrator: CPointer<UInt8> = CPointer<UInt8>()
    public var dwSerialNo: UInt32 = 0
    public var dwRefresh: UInt32 = 0
    public var dwRetry: UInt32 = 0
    public var dwExpire: UInt32 = 0
    public var dwDefaultTtl: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SOA_DATAW {
    public var pNamePrimaryServer: CPointer<UInt16> = CPointer<UInt16>()
    public var pNameAdministrator: CPointer<UInt16> = CPointer<UInt16>()
    public var dwSerialNo: UInt32 = 0
    public var dwRefresh: UInt32 = 0
    public var dwRetry: UInt32 = 0
    public var dwExpire: UInt32 = 0
    public var dwDefaultTtl: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SRV_DATAA {
    public var pNameTarget: CPointer<UInt8> = CPointer<UInt8>()
    public var wPriority: UInt16 = 0
    public var wWeight: UInt16 = 0
    public var wPort: UInt16 = 0
    public var Pad: UInt16 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SRV_DATAW {
    public var pNameTarget: CPointer<UInt16> = CPointer<UInt16>()
    public var wPriority: UInt16 = 0
    public var wWeight: UInt16 = 0
    public var wPort: UInt16 = 0
    public var Pad: UInt16 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_DATA {
    public var wSvcPriority: UInt16 = 0
    public var pszTargetName: CPointer<UInt8> = CPointer<UInt8>()
    public var cSvcParams: UInt16 = 0
    public var pSvcParams: CPointer<DNS_SVCB_PARAM> = CPointer<DNS_SVCB_PARAM>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_PARAM {
    public var wSvcParamKey: UInt16 = 0
    public var Anonymous: DNS_SVCB_PARAM__Anonymous_e__Union = DNS_SVCB_PARAM__Anonymous_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct DNS_SVCB_PARAM__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public func pIpv4Hints(): CPointer<DNS_SVCB_PARAM_IPV4> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_IPV4>>(p)
            return ptr.read()
        }
    }

    public mut func set_pIpv4Hints(val: CPointer<DNS_SVCB_PARAM_IPV4>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_IPV4>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pIpv6Hints(): CPointer<DNS_SVCB_PARAM_IPV6> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_IPV6>>(p)
            return ptr.read()
        }
    }

    public mut func set_pIpv6Hints(val: CPointer<DNS_SVCB_PARAM_IPV6>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_IPV6>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pMandatory(): CPointer<DNS_SVCB_PARAM_MANDATORY> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_MANDATORY>>(p)
            return ptr.read()
        }
    }

    public mut func set_pMandatory(val: CPointer<DNS_SVCB_PARAM_MANDATORY>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_MANDATORY>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pAlpn(): CPointer<DNS_SVCB_PARAM_ALPN> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_ALPN>>(p)
            return ptr.read()
        }
    }

    public mut func set_pAlpn(val: CPointer<DNS_SVCB_PARAM_ALPN>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_ALPN>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func wPort(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt16>(p)
            return ptr.read()
        }
    }

    public mut func set_wPort(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt16>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pUnknown(): CPointer<DNS_SVCB_PARAM_UNKNOWN> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_UNKNOWN>>(p)
            return ptr.read()
        }
    }

    public mut func set_pUnknown(val: CPointer<DNS_SVCB_PARAM_UNKNOWN>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<DNS_SVCB_PARAM_UNKNOWN>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pszDohPath(): CPointer<UInt8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            return ptr.read()
        }
    }

    public mut func set_pszDohPath(val: CPointer<UInt8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<UInt8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func pReserved(): CPointer<Unit> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            return ptr.read()
        }
    }

    public mut func set_pReserved(val: CPointer<Unit>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<CPointer<Unit>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_PARAM_ALPN {
    public var cIds: UInt16 = 0
    public var rgIds: VArray<DNS_SVCB_PARAM_ALPN_ID, $1> = VArray<DNS_SVCB_PARAM_ALPN_ID, $1>(repeat: DNS_SVCB_PARAM_ALPN_ID())
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_PARAM_ALPN_ID {
    public var cBytes: UInt8 = 0
    public var pbId: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_PARAM_IPV4 {
    public var cIps: UInt16 = 0
    public var rgIps: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_PARAM_IPV6 {
    public var cIps: UInt16 = 0
    public var rgIps: VArray<IP6_ADDRESS, $1> = VArray<IP6_ADDRESS, $1>(repeat: IP6_ADDRESS())
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_PARAM_MANDATORY {
    public var cMandatoryKeys: UInt16 = 0
    public var rgwMandatoryKeys: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_PARAM_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DNS_SVCB_PARAM_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DNS_SVCB_PARAM_TYPE): Bool {
        return this.value != other.value
    }
}

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamMandatory: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(0)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamAlpn: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(1)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamNoDefaultAlpn: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(2)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamPort: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(3)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamIpv4Hint: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(4)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamEch: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(5)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamIpv6Hint: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(6)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamDohPath: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(7)

@When[feat_windows_win32_network_management_dns == "on"]
public let DnsSvcbParamDohPathOpenDns: DNS_SVCB_PARAM_TYPE = DNS_SVCB_PARAM_TYPE(65432)

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_SVCB_PARAM_UNKNOWN {
    public var cBytes: UInt16 = 0
    public var pbSvcParamValue: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_TKEY_DATAA {
    public var pNameAlgorithm: CPointer<UInt8> = CPointer<UInt8>()
    public var pAlgorithmPacket: CPointer<UInt8> = CPointer<UInt8>()
    public var pKey: CPointer<UInt8> = CPointer<UInt8>()
    public var pOtherData: CPointer<UInt8> = CPointer<UInt8>()
    public var dwCreateTime: UInt32 = 0
    public var dwExpireTime: UInt32 = 0
    public var wMode: UInt16 = 0
    public var wError: UInt16 = 0
    public var wKeyLength: UInt16 = 0
    public var wOtherLength: UInt16 = 0
    public var cAlgNameLength: UInt8 = 0
    public var bPacketPointers: Int32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_TKEY_DATAW {
    public var pNameAlgorithm: CPointer<UInt16> = CPointer<UInt16>()
    public var pAlgorithmPacket: CPointer<UInt8> = CPointer<UInt8>()
    public var pKey: CPointer<UInt8> = CPointer<UInt8>()
    public var pOtherData: CPointer<UInt8> = CPointer<UInt8>()
    public var dwCreateTime: UInt32 = 0
    public var dwExpireTime: UInt32 = 0
    public var wMode: UInt16 = 0
    public var wError: UInt16 = 0
    public var wKeyLength: UInt16 = 0
    public var wOtherLength: UInt16 = 0
    public var cAlgNameLength: UInt8 = 0
    public var bPacketPointers: Int32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_TLSA_DATA {
    public var bCertUsage: UInt8 = 0
    public var bSelector: UInt8 = 0
    public var bMatchingType: UInt8 = 0
    public var bCertificateAssociationDataLength: UInt16 = 0
    public var bPad: VArray<UInt8, $3> = VArray<UInt8, $3>(repeat: 0)
    public var bCertificateAssociationData: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_TSIG_DATAA {
    public var pNameAlgorithm: CPointer<UInt8> = CPointer<UInt8>()
    public var pAlgorithmPacket: CPointer<UInt8> = CPointer<UInt8>()
    public var pSignature: CPointer<UInt8> = CPointer<UInt8>()
    public var pOtherData: CPointer<UInt8> = CPointer<UInt8>()
    public var i64CreateTime: Int64 = 0
    public var wFudgeTime: UInt16 = 0
    public var wOriginalXid: UInt16 = 0
    public var wError: UInt16 = 0
    public var wSigLength: UInt16 = 0
    public var wOtherLength: UInt16 = 0
    public var cAlgNameLength: UInt8 = 0
    public var bPacketPointers: Int32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_TSIG_DATAW {
    public var pNameAlgorithm: CPointer<UInt16> = CPointer<UInt16>()
    public var pAlgorithmPacket: CPointer<UInt8> = CPointer<UInt8>()
    public var pSignature: CPointer<UInt8> = CPointer<UInt8>()
    public var pOtherData: CPointer<UInt8> = CPointer<UInt8>()
    public var i64CreateTime: Int64 = 0
    public var wFudgeTime: UInt16 = 0
    public var wOriginalXid: UInt16 = 0
    public var wError: UInt16 = 0
    public var wSigLength: UInt16 = 0
    public var wOtherLength: UInt16 = 0
    public var cAlgNameLength: UInt8 = 0
    public var bPacketPointers: Int32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_TXT_DATAA {
    public var dwStringCount: UInt32 = 0
    public var pStringArray: VArray<CPointer<UInt8>, $1> = VArray<CPointer<UInt8>, $1>(repeat: CPointer<UInt8>())
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_TXT_DATAW {
    public var dwStringCount: UInt32 = 0
    public var pStringArray: VArray<CPointer<UInt16>, $1> = VArray<CPointer<UInt16>, $1>(repeat: CPointer<UInt16>())
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_UNKNOWN_DATA {
    public var dwByteCount: UInt32 = 0
    public var bData: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_WINSR_DATAA {
    public var dwMappingFlag: UInt32 = 0
    public var dwLookupTimeout: UInt32 = 0
    public var dwCacheTimeout: UInt32 = 0
    public var pNameResultDomain: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_WINSR_DATAW {
    public var dwMappingFlag: UInt32 = 0
    public var dwLookupTimeout: UInt32 = 0
    public var dwCacheTimeout: UInt32 = 0
    public var pNameResultDomain: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_WINS_DATA {
    public var dwMappingFlag: UInt32 = 0
    public var dwLookupTimeout: UInt32 = 0
    public var dwCacheTimeout: UInt32 = 0
    public var cWinsServerCount: UInt32 = 0
    public var WinsServers: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// packed(1)
@C
public struct DNS_WIRE_QUESTION {
    public var QuestionType: UInt16 = 0
    public var QuestionClass: UInt16 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// packed(1)
@C
public struct DNS_WIRE_RECORD {
    public var RecordType: UInt16 = 0
    public var RecordClass: UInt16 = 0
    public var TimeToLive: UInt32 = 0
    public var DataLength: UInt16 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct DNS_WKS_DATA {
    public var IpAddress: UInt32 = 0
    public var chProtocol: UInt8 = 0
    public var BitMask: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct IP4_ARRAY {
    public var AddrCount: UInt32 = 0
    public var AddrArray: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on" && (arch == "x86_64" || arch == "aarch64")]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct IP6_ADDRESS {
    public var _raw: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0)

    public func ip6Qword(): VArray<UInt64, $2> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt64, $2>>(p)
            return ptr.read()
        }
    }

    public mut func set_ip6Qword(val: VArray<UInt64, $2>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt64, $2>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ip6Dword(): VArray<UInt32, $4> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt32, $4>>(p)
            return ptr.read()
        }
    }

    public mut func set_ip6Dword(val: VArray<UInt32, $4>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt32, $4>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ip6Word(): VArray<UInt16, $8> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt16, $8>>(p)
            return ptr.read()
        }
    }

    public mut func set_ip6Word(val: VArray<UInt16, $8>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt16, $8>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func ip6Byte(): VArray<UInt8, $16> {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt8, $16>>(p)
            return ptr.read()
        }
    }

    public mut func set_ip6Byte(val: VArray<UInt8, $16>): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt64>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<VArray<UInt8, $16>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct MDNS_QUERY_HANDLE {
    public var nameBuf: VArray<UInt16, $256> = VArray<UInt16, $256>(repeat: 0)
    public var wType: UInt16 = 0
    public var pSubscription: CPointer<Unit> = CPointer<Unit>()
    public var pWnfCallbackParams: CPointer<Unit> = CPointer<Unit>()
    public var stateNameData: VArray<UInt32, $2> = VArray<UInt32, $2>(repeat: 0)
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct MDNS_QUERY_REQUEST {
    public var Version: UInt32 = 0
    public var ulRefCount: UInt32 = 0
    public var Query: CPointer<UInt16> = CPointer<UInt16>()
    public var QueryType: UInt16 = 0
    public var QueryOptions: UInt64 = 0
    public var InterfaceIndex: UInt32 = 0
    public var pQueryCallback: PMDNS_QUERY_CALLBACK = { _, _, _ => () }
    public var pQueryContext: CPointer<Unit> = CPointer<Unit>()
    public var fAnswerReceived: Int32 = 0
    public var ulResendCount: UInt32 = 0
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
public type PDNS_QUERY_COMPLETION_ROUTINE = CFunc<(CPointer<Unit>, CPointer<DNS_QUERY_RESULT>) -> Unit>

@When[feat_windows_win32_network_management_dns == "on"]
public type PDNS_SERVICE_BROWSE_CALLBACK = CFunc<(UInt32, CPointer<Unit>, CPointer<DNS_RECORDW>) -> Unit>

@When[feat_windows_win32_network_management_dns == "on"]
public type PDNS_SERVICE_REGISTER_COMPLETE = CFunc<(UInt32, CPointer<Unit>, CPointer<DNS_SERVICE_INSTANCE>) -> Unit>

@When[feat_windows_win32_network_management_dns == "on"]
public type PDNS_SERVICE_RESOLVE_COMPLETE = CFunc<(UInt32, CPointer<Unit>, CPointer<DNS_SERVICE_INSTANCE>) -> Unit>

@When[feat_windows_win32_network_management_dns == "on"]
public type PMDNS_QUERY_CALLBACK = CFunc<(CPointer<Unit>, CPointer<MDNS_QUERY_HANDLE>, CPointer<DNS_QUERY_RESULT>) -> Unit>

@When[feat_windows_win32_network_management_dns == "on"]
@C
public struct _DnsRecordOptA {
    public var pNext: CPointer<DNS_RECORDA> = CPointer<DNS_RECORDA>()
    public var pName: CPointer<UInt8> = CPointer<UInt8>()
    public var wType: UInt16 = 0
    public var wDataLength: UInt16 = 0
    public var Flags: _DnsRecordOptA__Flags_e__Union = _DnsRecordOptA__Flags_e__Union()
    public var ExtHeader: DNS_HEADER_EXT = DNS_HEADER_EXT()
    public var wPayloadSize: UInt16 = 0
    public var wReserved: UInt16 = 0
    public var Data: _DnsRecordOptA__Data_e__Union = _DnsRecordOptA__Data_e__Union()
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct _DnsRecordOptA__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public func dw(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            return ptr.read()
        }
    }

    public mut func set_dw(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<UInt32>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func s(): DNS_RECORD_FLAGS {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_RECORD_FLAGS>(p)
            return ptr.read()
        }
    }

    public mut func set_s(val: DNS_RECORD_FLAGS): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt32>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_RECORD_FLAGS>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_windows_win32_network_management_dns == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct _DnsRecordOptA__Data_e__Union {
    public var _raw: VArray<UInt16, $3> = VArray<UInt16, $3>(repeat: 0)

    public func opt(): DNS_OPT_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_opt(val: DNS_OPT_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func opt2(): DNS_OPT_DATA {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            return ptr.read()
        }
    }

    public mut func set_opt2(val: DNS_OPT_DATA): Unit {
        unsafe {
            var rawStorage = _raw
            let storage = CPointer<UInt16>(inout rawStorage)
            let raw = CPointer<UInt8>(storage)
            let p = raw
            let ptr = CPointer<DNS_OPT_DATA>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[feat_dnsapi == "on"]
foreign {
    func DnsAcquireContextHandle_A(CredentialFlags: UInt32, Credentials: CPointer<Unit>, pContext: CPointer<CPointer<Unit>>): Int32
    func DnsAcquireContextHandle_W(CredentialFlags: UInt32, Credentials: CPointer<Unit>, pContext: CPointer<CPointer<Unit>>): Int32
    func DnsCancelQuery(pCancelHandle: CPointer<DNS_QUERY_CANCEL>): Int32
    func DnsCancelQueryRaw(cancelHandle: CPointer<DNS_QUERY_RAW_CANCEL>): Int32
    func DnsConnectionDeletePolicyEntries(PolicyEntryTag: DNS_CONNECTION_POLICY_TAG): UInt32
    func DnsConnectionDeleteProxyInfo(pwszConnectionName: CPointer<UInt16>, Type: DNS_CONNECTION_PROXY_TYPE): UInt32
    func DnsConnectionFreeNameList(pNameList: CPointer<DNS_CONNECTION_NAME_LIST>): Unit
    func DnsConnectionFreeProxyInfo(pProxyInfo: CPointer<DNS_CONNECTION_PROXY_INFO>): Unit
    func DnsConnectionFreeProxyInfoEx(pProxyInfoEx: CPointer<DNS_CONNECTION_PROXY_INFO_EX>): Unit
    func DnsConnectionFreeProxyList(pProxyList: CPointer<DNS_CONNECTION_PROXY_LIST>): Unit
    func DnsConnectionGetNameList(pNameList: CPointer<DNS_CONNECTION_NAME_LIST>): UInt32
    func DnsConnectionGetProxyInfo(pwszConnectionName: CPointer<UInt16>, Type: DNS_CONNECTION_PROXY_TYPE, pProxyInfo: CPointer<DNS_CONNECTION_PROXY_INFO>): UInt32
    func DnsConnectionGetProxyInfoForHostUrl(pwszHostUrl: CPointer<UInt16>, pSelectionContext: CPointer<UInt8>, dwSelectionContextLength: UInt32, dwExplicitInterfaceIndex: UInt32, pProxyInfoEx: CPointer<DNS_CONNECTION_PROXY_INFO_EX>): UInt32
    func DnsConnectionGetProxyInfoForHostUrlEx(pwszHostUrl: CPointer<UInt16>, pSelectionContext: CPointer<UInt8>, dwSelectionContextLength: UInt32, dwExplicitInterfaceIndex: UInt32, pwszConnectionName: CPointer<UInt16>, pProxyInfoEx: CPointer<DNS_CONNECTION_PROXY_INFO_EX>): UInt32
    func DnsConnectionGetProxyList(pwszConnectionName: CPointer<UInt16>, pProxyList: CPointer<DNS_CONNECTION_PROXY_LIST>): UInt32
    func DnsConnectionSetPolicyEntries(PolicyEntryTag: DNS_CONNECTION_POLICY_TAG, pPolicyEntryList: CPointer<DNS_CONNECTION_POLICY_ENTRY_LIST>): UInt32
    func DnsConnectionSetProxyInfo(pwszConnectionName: CPointer<UInt16>, Type: DNS_CONNECTION_PROXY_TYPE, pProxyInfo: CPointer<DNS_CONNECTION_PROXY_INFO>): UInt32
    func DnsConnectionUpdateIfIndexTable(pConnectionIfIndexEntries: CPointer<DNS_CONNECTION_IFINDEX_LIST>): UInt32
    func DnsExtractRecordsFromMessage_UTF8(pDnsBuffer: CPointer<DNS_MESSAGE_BUFFER>, wMessageLength: UInt16, ppRecord: CPointer<CPointer<DNS_RECORDA>>): Int32
    func DnsExtractRecordsFromMessage_W(pDnsBuffer: CPointer<DNS_MESSAGE_BUFFER>, wMessageLength: UInt16, ppRecord: CPointer<CPointer<DNS_RECORDA>>): Int32
    func DnsFree(pData: CPointer<Unit>, FreeType: DNS_FREE_TYPE): Unit
    func DnsFreeCustomServers(pcServers: CPointer<UInt32>, ppServers: CPointer<CPointer<DNS_CUSTOM_SERVER>>): Unit
    func DnsFreeProxyName(proxyName: CPointer<UInt16>): Unit
    func DnsGetApplicationSettings(pcServers: CPointer<UInt32>, ppDefaultServers: CPointer<CPointer<DNS_CUSTOM_SERVER>>, pSettings: CPointer<DNS_APPLICATION_SETTINGS>): UInt32
    func DnsGetProxyInformation(hostName: CPointer<UInt16>, proxyInformation: CPointer<DNS_PROXY_INFORMATION>, defaultProxyInformation: CPointer<DNS_PROXY_INFORMATION>, completionRoutine: DNS_PROXY_COMPLETION_ROUTINE, completionContext: CPointer<Unit>): UInt32
    func DnsIsFlatRecord(pRecord: CPointer<DNS_RECORDA>, ullFlags: UInt64, pfFlat: CPointer<Int32>): Int32
    func DnsModifyRecordsInSet_A(pAddRecords: CPointer<DNS_RECORDA>, pDeleteRecords: CPointer<DNS_RECORDA>, Options: UInt32, hCredentials: CPointer<Unit>, pExtraList: CPointer<Unit>, pReserved: CPointer<Unit>): Int32
    func DnsModifyRecordsInSet_UTF8(pAddRecords: CPointer<DNS_RECORDA>, pDeleteRecords: CPointer<DNS_RECORDA>, Options: UInt32, hCredentials: CPointer<Unit>, pExtraList: CPointer<Unit>, pReserved: CPointer<Unit>): Int32
    func DnsModifyRecordsInSet_W(pAddRecords: CPointer<DNS_RECORDA>, pDeleteRecords: CPointer<DNS_RECORDA>, Options: UInt32, hCredentials: CPointer<Unit>, pExtraList: CPointer<Unit>, pReserved: CPointer<Unit>): Int32
    func DnsNameCompare_A(pName1: CPointer<UInt8>, pName2: CPointer<UInt8>): Int32
    func DnsNameCompare_W(pName1: CPointer<UInt16>, pName2: CPointer<UInt16>): Int32
    func DnsQueryConfig(Config: DNS_CONFIG_TYPE, Flag: UInt32, pwsAdapterName: CPointer<UInt16>, pReserved: CPointer<Unit>, pBuffer: CPointer<Unit>, pBufLen: CPointer<UInt32>): Int32
    func DnsQueryEx(pQueryRequest: CPointer<DNS_QUERY_REQUEST>, pQueryResults: CPointer<DNS_QUERY_RESULT>, pCancelHandle: CPointer<DNS_QUERY_CANCEL>): Int32
    func DnsQueryRaw(queryRequest: CPointer<DNS_QUERY_RAW_REQUEST>, cancelHandle: CPointer<DNS_QUERY_RAW_CANCEL>): Int32
    func DnsQueryRawResultFree(queryResults: CPointer<DNS_QUERY_RAW_RESULT>): Unit
    func DnsQuery_A(pszName: CPointer<UInt8>, wType: UInt16, Options: DNS_QUERY_OPTIONS, pExtra: CPointer<Unit>, ppQueryResults: CPointer<CPointer<DNS_RECORDA>>, pReserved: CPointer<CPointer<Unit>>): win32_foundation.WIN32_ERROR
    func DnsQuery_UTF8(pszName: CPointer<UInt8>, wType: UInt16, Options: DNS_QUERY_OPTIONS, pExtra: CPointer<Unit>, ppQueryResults: CPointer<CPointer<DNS_RECORDA>>, pReserved: CPointer<CPointer<Unit>>): win32_foundation.WIN32_ERROR
    func DnsQuery_W(pszName: CPointer<UInt16>, wType: UInt16, Options: DNS_QUERY_OPTIONS, pExtra: CPointer<Unit>, ppQueryResults: CPointer<CPointer<DNS_RECORDA>>, pReserved: CPointer<CPointer<Unit>>): win32_foundation.WIN32_ERROR
    func DnsRecordCompare(pRecord1: CPointer<DNS_RECORDA>, pRecord2: CPointer<DNS_RECORDA>): Int32
    func DnsRecordCopyEx(pRecord: CPointer<DNS_RECORDA>, CharSetIn: DNS_CHARSET, CharSetOut: DNS_CHARSET): CPointer<DNS_RECORDA>
    func DnsRecordSetCompare(pRR1: CPointer<DNS_RECORDA>, pRR2: CPointer<DNS_RECORDA>, ppDiff1: CPointer<CPointer<DNS_RECORDA>>, ppDiff2: CPointer<CPointer<DNS_RECORDA>>): Int32
    func DnsRecordSetCopyEx(pRecordSet: CPointer<DNS_RECORDA>, CharSetIn: DNS_CHARSET, CharSetOut: DNS_CHARSET): CPointer<DNS_RECORDA>
    func DnsRecordSetDetach(pRecordList: CPointer<DNS_RECORDA>): CPointer<DNS_RECORDA>
    func DnsReleaseContextHandle(hContext: CPointer<Unit>): Unit
    func DnsReplaceRecordSetA(pReplaceSet: CPointer<DNS_RECORDA>, Options: UInt32, hContext: CPointer<Unit>, pExtraInfo: CPointer<Unit>, pReserved: CPointer<Unit>): Int32
    func DnsReplaceRecordSetUTF8(pReplaceSet: CPointer<DNS_RECORDA>, Options: UInt32, hContext: CPointer<Unit>, pExtraInfo: CPointer<Unit>, pReserved: CPointer<Unit>): Int32
    func DnsReplaceRecordSetW(pReplaceSet: CPointer<DNS_RECORDA>, Options: UInt32, hContext: CPointer<Unit>, pExtraInfo: CPointer<Unit>, pReserved: CPointer<Unit>): Int32
    func DnsServiceBrowse(pRequest: CPointer<DNS_SERVICE_BROWSE_REQUEST>, pCancel: CPointer<DNS_SERVICE_CANCEL>): Int32
    func DnsServiceBrowseCancel(pCancelHandle: CPointer<DNS_SERVICE_CANCEL>): Int32
    func DnsServiceConstructInstance(pServiceName: CPointer<UInt16>, pHostName: CPointer<UInt16>, pIp4: CPointer<UInt32>, pIp6: CPointer<IP6_ADDRESS>, wPort: UInt16, wPriority: UInt16, wWeight: UInt16, dwPropertiesCount: UInt32, keys: CPointer<CPointer<UInt16>>, values: CPointer<CPointer<UInt16>>): CPointer<DNS_SERVICE_INSTANCE>
    func DnsServiceCopyInstance(pOrig: CPointer<DNS_SERVICE_INSTANCE>): CPointer<DNS_SERVICE_INSTANCE>
    func DnsServiceDeRegister(pRequest: CPointer<DNS_SERVICE_REGISTER_REQUEST>, pCancel: CPointer<DNS_SERVICE_CANCEL>): UInt32
    func DnsServiceFreeInstance(pInstance: CPointer<DNS_SERVICE_INSTANCE>): Unit
    func DnsServiceRegister(pRequest: CPointer<DNS_SERVICE_REGISTER_REQUEST>, pCancel: CPointer<DNS_SERVICE_CANCEL>): UInt32
    func DnsServiceRegisterCancel(pCancelHandle: CPointer<DNS_SERVICE_CANCEL>): UInt32
    func DnsServiceResolve(pRequest: CPointer<DNS_SERVICE_RESOLVE_REQUEST>, pCancel: CPointer<DNS_SERVICE_CANCEL>): Int32
    func DnsServiceResolveCancel(pCancelHandle: CPointer<DNS_SERVICE_CANCEL>): Int32
    func DnsSetApplicationSettings(cServers: UInt32, pServers: CPointer<DNS_CUSTOM_SERVER>, pSettings: CPointer<DNS_APPLICATION_SETTINGS>): UInt32
    func DnsStartMulticastQuery(pQueryRequest: CPointer<MDNS_QUERY_REQUEST>, pHandle: CPointer<MDNS_QUERY_HANDLE>): Int32
    func DnsStopMulticastQuery(pHandle: CPointer<MDNS_QUERY_HANDLE>): Int32
    func DnsValidateName_A(pszName: CPointer<UInt8>, Format: DNS_NAME_FORMAT): Int32
    func DnsValidateName_UTF8(pszName: CPointer<UInt8>, Format: DNS_NAME_FORMAT): Int32
    func DnsValidateName_W(pszName: CPointer<UInt16>, Format: DNS_NAME_FORMAT): Int32
    func DnsWriteQuestionToBuffer_UTF8(pDnsBuffer: CPointer<DNS_MESSAGE_BUFFER>, pdwBufferSize: CPointer<UInt32>, pszName: CPointer<UInt8>, wType: UInt16, Xid: UInt16, fRecursionDesired: Int32): Int32
    func DnsWriteQuestionToBuffer_W(pDnsBuffer: CPointer<DNS_MESSAGE_BUFFER>, pdwBufferSize: CPointer<UInt32>, pszName: CPointer<UInt16>, wType: UInt16, Xid: UInt16, fRecursionDesired: Int32): Int32
}

@When[feat_windows_win32_network_management_dns == "on"]
public let SIZEOF_IP4_ADDRESS: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let IP4_ADDRESS_STRING_LENGTH: UInt32 = 16u32
@When[feat_windows_win32_network_management_dns == "on"]
public let IP4_ADDRESS_STRING_BUFFER_LENGTH: UInt32 = 16u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_ADDR_MAX_SOCKADDR_LENGTH: UInt32 = 32u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_ZERO: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_A: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NS: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MD: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MF: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_CNAME: UInt32 = 5u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_SOA: UInt32 = 6u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MB: UInt32 = 7u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MG: UInt32 = 8u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MR: UInt32 = 9u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NULL: UInt32 = 10u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_WKS: UInt32 = 11u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_PTR: UInt32 = 12u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_HINFO: UInt32 = 13u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MINFO: UInt32 = 14u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MX: UInt32 = 15u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_TEXT: UInt32 = 16u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_RP: UInt32 = 17u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_AFSDB: UInt32 = 18u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_X25: UInt32 = 19u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_ISDN: UInt32 = 20u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_RT: UInt32 = 21u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NSAP: UInt32 = 22u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NSAPPTR: UInt32 = 23u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_SIG: UInt32 = 24u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_KEY: UInt32 = 25u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_PX: UInt32 = 26u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_GPOS: UInt32 = 27u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_AAAA: UInt32 = 28u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_LOC: UInt32 = 29u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NXT: UInt32 = 30u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_EID: UInt32 = 31u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NIMLOC: UInt32 = 32u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_SRV: UInt32 = 33u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_ATMA: UInt32 = 34u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NAPTR: UInt32 = 35u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_KX: UInt32 = 36u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_CERT: UInt32 = 37u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_A6: UInt32 = 38u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_DNAME: UInt32 = 39u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_SINK: UInt32 = 40u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_OPT: UInt32 = 41u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_DS: UInt32 = 43u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_RRSIG: UInt32 = 46u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NSEC: UInt32 = 47u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_DNSKEY: UInt32 = 48u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_DHCID: UInt32 = 49u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NSEC3: UInt32 = 50u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NSEC3PARAM: UInt32 = 51u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_TLSA: UInt32 = 52u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_SVCB: UInt32 = 64u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_HTTPS: UInt32 = 65u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_UINFO: UInt32 = 100u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_UID: UInt32 = 101u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_GID: UInt32 = 102u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_UNSPEC: UInt32 = 103u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_ADDRS: UInt32 = 248u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_TKEY: UInt32 = 249u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_TSIG: UInt32 = 250u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_IXFR: UInt32 = 251u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_AXFR: UInt32 = 252u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MAILB: UInt32 = 253u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_MAILA: UInt32 = 254u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_ALL: UInt32 = 255u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_ANY: UInt32 = 255u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_WINS: UInt32 = 65281u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_WINSR: UInt32 = 65282u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TYPE_NBSTAT: UInt32 = 65282u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_A: UInt32 = 256u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NS: UInt32 = 512u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MD: UInt32 = 768u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MF: UInt32 = 1024u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_CNAME: UInt32 = 1280u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_SOA: UInt32 = 1536u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MB: UInt32 = 1792u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MG: UInt32 = 2048u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MR: UInt32 = 2304u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NULL: UInt32 = 2560u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_WKS: UInt32 = 2816u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_PTR: UInt32 = 3072u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_HINFO: UInt32 = 3328u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MINFO: UInt32 = 3584u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MX: UInt32 = 3840u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_TEXT: UInt32 = 4096u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_RP: UInt32 = 4352u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_AFSDB: UInt32 = 4608u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_X25: UInt32 = 4864u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_ISDN: UInt32 = 5120u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_RT: UInt32 = 5376u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NSAP: UInt32 = 5632u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NSAPPTR: UInt32 = 5888u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_SIG: UInt32 = 6144u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_KEY: UInt32 = 6400u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_PX: UInt32 = 6656u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_GPOS: UInt32 = 6912u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_AAAA: UInt32 = 7168u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_LOC: UInt32 = 7424u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NXT: UInt32 = 7680u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_EID: UInt32 = 7936u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NIMLOC: UInt32 = 8192u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_SRV: UInt32 = 8448u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_ATMA: UInt32 = 8704u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NAPTR: UInt32 = 8960u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_KX: UInt32 = 9216u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_CERT: UInt32 = 9472u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_A6: UInt32 = 9728u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_DNAME: UInt32 = 9984u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_SINK: UInt32 = 10240u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_OPT: UInt32 = 10496u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_DS: UInt32 = 11008u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_RRSIG: UInt32 = 11776u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NSEC: UInt32 = 12032u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_DNSKEY: UInt32 = 12288u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_DHCID: UInt32 = 12544u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NSEC3: UInt32 = 12800u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_NSEC3PARAM: UInt32 = 13056u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_TLSA: UInt32 = 13312u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_UINFO: UInt32 = 25600u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_UID: UInt32 = 25856u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_GID: UInt32 = 26112u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_UNSPEC: UInt32 = 26368u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_TKEY: UInt32 = 63744u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_TSIG: UInt32 = 64000u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_IXFR: UInt32 = 64256u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_AXFR: UInt32 = 64512u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MAILB: UInt32 = 64768u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_MAILA: UInt32 = 65024u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_ALL: UInt32 = 65280u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_ANY: UInt32 = 65280u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_WINS: UInt32 = 511u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RTYPE_WINSR: UInt32 = 767u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_ATMA_FORMAT_E164: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_ATMA_FORMAT_AESA: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_ATMA_MAX_ADDR_LENGTH: UInt32 = 20u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_ATMA_AESA_ADDR_LENGTH: UInt32 = 20u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_ATMA_MAX_RECORD_LENGTH: UInt32 = 21u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_RSAMD5: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_RSASHA1: UInt32 = 5u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_RSASHA1_NSEC3: UInt32 = 7u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_RSASHA256: UInt32 = 8u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_RSASHA512: UInt32 = 10u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_ECDSAP256_SHA256: UInt32 = 13u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_ECDSAP384_SHA384: UInt32 = 14u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_NULL: UInt32 = 253u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_ALGORITHM_PRIVATE: UInt32 = 254u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_DIGEST_ALGORITHM_SHA1: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_DIGEST_ALGORITHM_SHA256: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_DIGEST_ALGORITHM_SHA384: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_PROTOCOL_NONE: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_PROTOCOL_TLS: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_PROTOCOL_EMAIL: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_PROTOCOL_DNSSEC: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_PROTOCOL_IPSEC: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_NOAUTH: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_NOCONF: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_FLAG2: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_EXTEND: UInt32 = 8u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_FLAG4: UInt32 = 16u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_FLAG5: UInt32 = 32u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_USER: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_ZONE: UInt32 = 64u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_HOST: UInt32 = 128u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_NTPE3: UInt32 = 192u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_FLAG8: UInt32 = 256u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_FLAG9: UInt32 = 512u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_FLAG10: UInt32 = 1024u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_FLAG11: UInt32 = 2048u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG0: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG1: UInt32 = 4096u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG2: UInt32 = 8192u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG3: UInt32 = 12288u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG4: UInt32 = 16384u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG5: UInt32 = 20480u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG6: UInt32 = 24576u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG7: UInt32 = 28672u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG8: UInt32 = 32768u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG9: UInt32 = 36864u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG10: UInt32 = 40960u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG11: UInt32 = 45056u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG12: UInt32 = 49152u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG13: UInt32 = 53248u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG14: UInt32 = 57344u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSSEC_KEY_FLAG_SIG15: UInt32 = 61440u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TKEY_MODE_SERVER_ASSIGN: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TKEY_MODE_DIFFIE_HELLMAN: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TKEY_MODE_GSS: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_TKEY_MODE_RESOLVER_ASSIGN: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DDR_MAX_IP_HINTS: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_SECTION: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_QUESTION: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_ANSWER: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_AUTHORITY: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_ADDITIONAL: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_ZONE: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_PREREQ: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_UPDATE: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_DELETE: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNSREC_NOEXIST: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RFC_MAX_UDP_PACKET_LENGTH: UInt32 = 512u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_NAME_LENGTH: UInt32 = 255u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_LABEL_LENGTH: UInt32 = 63u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_NAME_BUFFER_LENGTH: UInt32 = 256u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_LABEL_BUFFER_LENGTH: UInt32 = 64u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CUSTOM_SERVER_TYPE_UDP: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CUSTOM_SERVER_TYPE_DOH: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CUSTOM_SERVER_TYPE_DOT: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CUSTOM_SERVER_UDP_FALLBACK: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CUSTOM_SERVER_UPGRADE_FROM_WELL_KNOWN_SERVERS: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let IP6_ADDRESS_STRING_LENGTH: UInt32 = 65u32
@When[feat_windows_win32_network_management_dns == "on"]
public let IP6_ADDRESS_STRING_BUFFER_LENGTH: UInt32 = 65u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_ADDRESS_STRING_LENGTH: UInt32 = 65u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PORT_HOST_ORDER: UInt32 = 53u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PORT_NET_ORDER: UInt32 = 13568u32
@When[feat_windows_win32_network_management_dns == "on"]
public let INTERNET_DEFAULT_DNS_PORT: UInt32 = 53u32
@When[feat_windows_win32_network_management_dns == "on"]
public let INTERNET_DEFAULT_DOT_PORT: UInt32 = 853u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_IP4_REVERSE_NAME_LENGTH: UInt32 = 31u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_IP6_REVERSE_NAME_LENGTH: UInt32 = 75u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_REVERSE_NAME_LENGTH: UInt32 = 75u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_IP4_REVERSE_NAME_BUFFER_LENGTH: UInt32 = 31u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_IP6_REVERSE_NAME_BUFFER_LENGTH: UInt32 = 75u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_REVERSE_NAME_BUFFER_LENGTH: UInt32 = 75u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_MAX_TEXT_STRING_LENGTH: UInt32 = 255u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_COMPRESSED_QUESTION_NAME: UInt32 = 49164u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_OPCODE_QUERY: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_OPCODE_IQUERY: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_OPCODE_SERVER_STATUS: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_OPCODE_UNKNOWN: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_OPCODE_NOTIFY: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_OPCODE_UPDATE: UInt32 = 5u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NOERROR: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_FORMERR: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_SERVFAIL: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NXDOMAIN: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NOTIMPL: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_REFUSED: UInt32 = 5u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_YXDOMAIN: UInt32 = 6u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_YXRRSET: UInt32 = 7u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NXRRSET: UInt32 = 8u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NOTAUTH: UInt32 = 9u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NOTZONE: UInt32 = 10u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_MAX: UInt32 = 15u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_BADVERS: UInt32 = 16u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_BADSIG: UInt32 = 16u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_BADKEY: UInt32 = 17u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_BADTIME: UInt32 = 18u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NO_ERROR: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_FORMAT_ERROR: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_SERVER_FAILURE: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NAME_ERROR: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCODE_NOT_IMPLEMENTED: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CLASS_INTERNET: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CLASS_CSNET: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CLASS_CHAOS: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CLASS_HESIOD: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CLASS_NONE: UInt32 = 254u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CLASS_ALL: UInt32 = 255u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CLASS_ANY: UInt32 = 255u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CLASS_UNICAST_RESPONSE: UInt32 = 32768u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_INTERNET: UInt32 = 256u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_CSNET: UInt32 = 512u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_CHAOS: UInt32 = 768u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_HESIOD: UInt32 = 1024u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_NONE: UInt32 = 65024u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_ALL: UInt32 = 65280u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_ANY: UInt32 = 65280u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_UNICAST_RESPONSE: UInt32 = 128u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_RCLASS_MDNS_CACHE_FLUSH: UInt32 = 128u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_WINS_FLAG_SCOPE: UInt32 = 2147483648u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_WINS_FLAG_LOCAL: UInt32 = 65536u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONFIG_FLAG_ALLOC: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_APP_SETTINGS_VERSION1: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_APP_SETTINGS_EXCLUSIVE_SERVERS: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROTOCOL_UNSPECIFIED: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROTOCOL_UDP: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROTOCOL_TCP: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROTOCOL_DOH: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROTOCOL_DOT: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_PROTOCOL_NO_WIRE: UInt32 = 5u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_SECURITY_USE_DEFAULT: UInt32 = 0u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_SECURITY_OFF: UInt32 = 16u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_SECURITY_ON: UInt32 = 32u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_SECURITY_ONLY: UInt32 = 256u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_CACHE_SECURITY_CONTEXT: UInt32 = 512u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_TEST_USE_LOCAL_SYS_ACCT: UInt32 = 1024u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_FORCE_SECURITY_NEGO: UInt32 = 2048u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_TRY_ALL_MASTER_SERVERS: UInt32 = 4096u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_SKIP_NO_UPDATE_ADAPTERS: UInt32 = 8192u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_REMOTE_SERVER: UInt32 = 16384u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_UPDATE_RESERVED: UInt32 = 4294901760u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_VALSVR_ERROR_INVALID_ADDR: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_VALSVR_ERROR_INVALID_NAME: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_VALSVR_ERROR_UNREACHABLE: UInt32 = 3u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_VALSVR_ERROR_NO_RESPONSE: UInt32 = 4u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_VALSVR_ERROR_NO_AUTH: UInt32 = 5u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_VALSVR_ERROR_REFUSED: UInt32 = 6u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_VALSVR_ERROR_NO_TCP: UInt32 = 16u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_VALSVR_ERROR_UNKNOWN: UInt32 = 255u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_NAME_MAX_LENGTH: UInt32 = 64u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_CURRENT_VERSION: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_SERVER_MAX_LENGTH: UInt32 = 256u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_FRIENDLY_NAME_MAX_LENGTH: UInt32 = 64u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_USERNAME_MAX_LENGTH: UInt32 = 128u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_PASSWORD_MAX_LENGTH: UInt32 = 128u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_EXCEPTION_MAX_LENGTH: UInt32 = 1024u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_EXTRA_INFO_MAX_LENGTH: UInt32 = 1024u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_FLAG_DISABLED: UInt32 = 1u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_PROXY_INFO_FLAG_BYPASSLOCAL: UInt32 = 2u32
@When[feat_windows_win32_network_management_dns == "on"]
public let DNS_CONNECTION_POLICY_ENTRY_ONDEMAND: UInt32 = 1u32
