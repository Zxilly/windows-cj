// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.Win32.Graphics.Dwm

import windows_interface.{GUID}
import windows_interface as windows_interface
import windows_polyfill.{packedReadFloat64, packedReadInt32, packedReadUInt32, packedReadUInt64, packedReadUInt8, packedReadValue, packedWriteFloat64, packedWriteInt32, packedWriteUInt32, packedWriteUInt64, packedWriteUInt8, packedWriteValue}
import windows_polyfill as windows_polyfill
@When[Windows_Win32_Foundation == "on"]
import windows_sys.Win32.Foundation as Win32_Foundation
@When[Windows_Win32_Graphics_Gdi == "on"]
import windows_sys.Win32.Graphics.Gdi as Win32_Graphics_Gdi
import windows_sys.Win32._impl_l0 as Win32__impl_l0


@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWMFLIP3DWINDOWPOLICY {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWMFLIP3DWINDOWPOLICY): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWMFLIP3DWINDOWPOLICY): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMFLIP3DWINDOWPOLICY_DWMFLIP3D_DEFAULT: DWMFLIP3DWINDOWPOLICY = DWMFLIP3DWINDOWPOLICY(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMFLIP3DWINDOWPOLICY_DWMFLIP3D_EXCLUDEBELOW: DWMFLIP3DWINDOWPOLICY = DWMFLIP3DWINDOWPOLICY(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMFLIP3DWINDOWPOLICY_DWMFLIP3D_EXCLUDEABOVE: DWMFLIP3DWINDOWPOLICY = DWMFLIP3DWINDOWPOLICY(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMFLIP3DWINDOWPOLICY_DWMFLIP3D_LAST: DWMFLIP3DWINDOWPOLICY = DWMFLIP3DWINDOWPOLICY(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWMNCRENDERINGPOLICY {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWMNCRENDERINGPOLICY): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWMNCRENDERINGPOLICY): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMNCRENDERINGPOLICY_DWMNCRP_USEWINDOWSTYLE: DWMNCRENDERINGPOLICY = DWMNCRENDERINGPOLICY(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMNCRENDERINGPOLICY_DWMNCRP_DISABLED: DWMNCRENDERINGPOLICY = DWMNCRENDERINGPOLICY(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMNCRENDERINGPOLICY_DWMNCRP_ENABLED: DWMNCRENDERINGPOLICY = DWMNCRENDERINGPOLICY(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMNCRENDERINGPOLICY_DWMNCRP_LAST: DWMNCRENDERINGPOLICY = DWMNCRENDERINGPOLICY(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWMTRANSITION_OWNEDWINDOW_TARGET {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWMTRANSITION_OWNEDWINDOW_TARGET): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWMTRANSITION_OWNEDWINDOW_TARGET): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTRANSITION_OWNEDWINDOW_TARGET_DWMTRANSITION_OWNEDWINDOW_NULL: DWMTRANSITION_OWNEDWINDOW_TARGET = DWMTRANSITION_OWNEDWINDOW_TARGET(-1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTRANSITION_OWNEDWINDOW_TARGET_DWMTRANSITION_OWNEDWINDOW_REPOSITION: DWMTRANSITION_OWNEDWINDOW_TARGET = DWMTRANSITION_OWNEDWINDOW_TARGET(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWMWINDOWATTRIBUTE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWMWINDOWATTRIBUTE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWMWINDOWATTRIBUTE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_NCRENDERING_ENABLED: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_NCRENDERING_POLICY: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_TRANSITIONS_FORCEDISABLED: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_ALLOW_NCPAINT: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_CAPTION_BUTTON_BOUNDS: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(5)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_NONCLIENT_RTL_LAYOUT: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(6)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_FORCE_ICONIC_REPRESENTATION: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(7)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_FLIP3D_POLICY: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(8)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_EXTENDED_FRAME_BOUNDS: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(9)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_HAS_ICONIC_BITMAP: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(10)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_DISALLOW_PEEK: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(11)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_EXCLUDED_FROM_PEEK: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(12)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_CLOAK: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(13)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_CLOAKED: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(14)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_FREEZE_REPRESENTATION: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(15)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_PASSIVE_UPDATE_MODE: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(16)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_USE_HOSTBACKDROPBRUSH: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(17)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_USE_IMMERSIVE_DARK_MODE: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(20)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_WINDOW_CORNER_PREFERENCE: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(33)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_BORDER_COLOR: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(34)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_CAPTION_COLOR: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(35)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_TEXT_COLOR: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(36)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_VISIBLE_FRAME_BORDER_THICKNESS: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(37)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_SYSTEMBACKDROP_TYPE: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(38)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_REDIRECTIONBITMAP_ALPHA: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(39)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_BORDER_MARGINS: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(40)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWINDOWATTRIBUTE_DWMWA_LAST: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(41)

@When[Windows_Win32_Graphics_Dwm == "on" && Windows_Win32_Graphics_Gdi == "on"]
// packed(1)
@C
public struct DWM_BLURBEHIND {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    public mut prop dwFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fEnable: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRgnBlur: Win32_Graphics_Gdi.HRGN {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32_Graphics_Gdi.HRGN = CPointer<Unit>()
                return packedReadValue<Win32_Graphics_Gdi.HRGN>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fTransitionOnMaximized: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWM_SHOWCONTACT {
    public var value: UInt32 = 0

    public init() {}

    public init(value: UInt32) {
        this.value = value
    }

    public operator func ==(other: DWM_SHOWCONTACT): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_SHOWCONTACT): Bool {
        return this.value != other.value
    }

    public func contains(other: DWM_SHOWCONTACT): Bool {
        return (this.value & other.value) == other.value
    }

    public operator func |(other: DWM_SHOWCONTACT): DWM_SHOWCONTACT {
        return DWM_SHOWCONTACT(this.value | other.value)
    }
    public operator func &(other: DWM_SHOWCONTACT): DWM_SHOWCONTACT {
        return DWM_SHOWCONTACT(this.value & other.value)
    }
    public operator func !(): DWM_SHOWCONTACT {
        return DWM_SHOWCONTACT(!this.value)
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SHOWCONTACT_DWMSC_DOWN: DWM_SHOWCONTACT = DWM_SHOWCONTACT(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SHOWCONTACT_DWMSC_UP: DWM_SHOWCONTACT = DWM_SHOWCONTACT(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SHOWCONTACT_DWMSC_DRAG: DWM_SHOWCONTACT = DWM_SHOWCONTACT(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SHOWCONTACT_DWMSC_HOLD: DWM_SHOWCONTACT = DWM_SHOWCONTACT(8)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SHOWCONTACT_DWMSC_PENBARREL: DWM_SHOWCONTACT = DWM_SHOWCONTACT(16)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SHOWCONTACT_DWMSC_NONE: DWM_SHOWCONTACT = DWM_SHOWCONTACT(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SHOWCONTACT_DWMSC_ALL: DWM_SHOWCONTACT = DWM_SHOWCONTACT(4294967295)

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWM_SOURCE_FRAME_SAMPLING {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWM_SOURCE_FRAME_SAMPLING): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_SOURCE_FRAME_SAMPLING): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SOURCE_FRAME_SAMPLING_DWM_SOURCE_FRAME_SAMPLING_POINT: DWM_SOURCE_FRAME_SAMPLING = DWM_SOURCE_FRAME_SAMPLING(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SOURCE_FRAME_SAMPLING_DWM_SOURCE_FRAME_SAMPLING_COVERAGE: DWM_SOURCE_FRAME_SAMPLING = DWM_SOURCE_FRAME_SAMPLING(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SOURCE_FRAME_SAMPLING_DWM_SOURCE_FRAME_SAMPLING_LAST: DWM_SOURCE_FRAME_SAMPLING = DWM_SOURCE_FRAME_SAMPLING(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWM_SYSTEMBACKDROP_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWM_SYSTEMBACKDROP_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_SYSTEMBACKDROP_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SYSTEMBACKDROP_TYPE_DWMSBT_AUTO: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SYSTEMBACKDROP_TYPE_DWMSBT_NONE: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SYSTEMBACKDROP_TYPE_DWMSBT_MAINWINDOW: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SYSTEMBACKDROP_TYPE_DWMSBT_TRANSIENTWINDOW: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SYSTEMBACKDROP_TYPE_DWMSBT_TABBEDWINDOW: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWM_TAB_WINDOW_REQUIREMENTS {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWM_TAB_WINDOW_REQUIREMENTS): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_TAB_WINDOW_REQUIREMENTS): Bool {
        return this.value != other.value
    }

    public func contains(other: DWM_TAB_WINDOW_REQUIREMENTS): Bool {
        return (this.value & other.value) == other.value
    }

    public operator func |(other: DWM_TAB_WINDOW_REQUIREMENTS): DWM_TAB_WINDOW_REQUIREMENTS {
        return DWM_TAB_WINDOW_REQUIREMENTS(this.value | other.value)
    }
    public operator func &(other: DWM_TAB_WINDOW_REQUIREMENTS): DWM_TAB_WINDOW_REQUIREMENTS {
        return DWM_TAB_WINDOW_REQUIREMENTS(this.value & other.value)
    }
    public operator func !(): DWM_TAB_WINDOW_REQUIREMENTS {
        return DWM_TAB_WINDOW_REQUIREMENTS(!this.value)
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_NONE: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_IMPLEMENTED_BY_SYSTEM: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_WINDOW_RELATIONSHIP: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_WINDOW_STYLES: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_WINDOW_REGION: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(8)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_WINDOW_DWM_ATTRIBUTES: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(16)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_WINDOW_MARGINS: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(32)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_TABBING_ENABLED: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(64)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_USER_POLICY: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(128)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_GROUP_POLICY: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(256)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TAB_WINDOW_REQUIREMENTS_DWMTWR_APP_COMPAT: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(512)

@When[Windows_Win32_Graphics_Dwm == "on"]
// packed(1)
@C
public struct DWM_THUMBNAIL_PROPERTIES {
    public var _raw: VArray<UInt8, $45> = VArray<UInt8, $45>(repeat: 0u8)

    public mut prop dwFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rcDestination: Win32_Foundation.RECT {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32_Foundation.RECT = Win32_Foundation.RECT()
                return packedReadValue<Win32_Foundation.RECT>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rcSource: Win32_Foundation.RECT {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32_Foundation.RECT = Win32_Foundation.RECT()
                return packedReadValue<Win32_Foundation.RECT>(base, 20, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop opacity: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 36)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 36, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fVisible: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt32(base, 37)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 37, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fSourceClientAreaOnly: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt32(base, 41)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 41, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct DWM_WINDOW_CORNER_PREFERENCE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWM_WINDOW_CORNER_PREFERENCE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_WINDOW_CORNER_PREFERENCE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_WINDOW_CORNER_PREFERENCE_DWMWCP_DEFAULT: DWM_WINDOW_CORNER_PREFERENCE = DWM_WINDOW_CORNER_PREFERENCE(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_WINDOW_CORNER_PREFERENCE_DWMWCP_DONOTROUND: DWM_WINDOW_CORNER_PREFERENCE = DWM_WINDOW_CORNER_PREFERENCE(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_WINDOW_CORNER_PREFERENCE_DWMWCP_ROUND: DWM_WINDOW_CORNER_PREFERENCE = DWM_WINDOW_CORNER_PREFERENCE(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_WINDOW_CORNER_PREFERENCE_DWMWCP_ROUNDSMALL: DWM_WINDOW_CORNER_PREFERENCE = DWM_WINDOW_CORNER_PREFERENCE(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public struct GESTURE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: GESTURE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: GESTURE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_PEN_TAP: GESTURE_TYPE = GESTURE_TYPE(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_PEN_DOUBLETAP: GESTURE_TYPE = GESTURE_TYPE(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_PEN_RIGHTTAP: GESTURE_TYPE = GESTURE_TYPE(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_PEN_PRESSANDHOLD: GESTURE_TYPE = GESTURE_TYPE(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_PEN_PRESSANDHOLDABORT: GESTURE_TYPE = GESTURE_TYPE(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_TOUCH_TAP: GESTURE_TYPE = GESTURE_TYPE(5)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_TOUCH_DOUBLETAP: GESTURE_TYPE = GESTURE_TYPE(6)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_TOUCH_RIGHTTAP: GESTURE_TYPE = GESTURE_TYPE(7)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_TOUCH_PRESSANDHOLD: GESTURE_TYPE = GESTURE_TYPE(8)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_TOUCH_PRESSANDHOLDABORT: GESTURE_TYPE = GESTURE_TYPE(9)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GESTURE_TYPE_GT_TOUCH_PRESSANDTAP: GESTURE_TYPE = GESTURE_TYPE(10)

@When[Windows_Win32_Graphics_Dwm == "on"]
// packed(1)
@C
public struct MilMatrix3x2D {
    public var _raw: VArray<UInt8, $48> = VArray<UInt8, $48>(repeat: 0u8)

    public mut prop s_11: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadFloat64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteFloat64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop s_12: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadFloat64(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteFloat64(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop s_21: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadFloat64(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteFloat64(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop s_22: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadFloat64(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteFloat64(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dx: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadFloat64(base, 32)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteFloat64(base, 32, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dy: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadFloat64(base, 40)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteFloat64(base, 40, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
// packed(1)
@C
public struct UNSIGNED_RATIO {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    public mut prop uiNumerator: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uiDenominator: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
// packed(1)
@C
public struct DWM_PRESENT_PARAMETERS {
    public var _raw: VArray<UInt8, $40> = VArray<UInt8, $40>(repeat: 0u8)

    public mut prop cbSize: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fQueue: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshStart: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cBuffer: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fUseSourceRate: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rateSource: UNSIGNED_RATIO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: UNSIGNED_RATIO = UNSIGNED_RATIO()
                return packedReadValue<UNSIGNED_RATIO>(base, 24, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshesPerFrame: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 32)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 32, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop eSampling: DWM_SOURCE_FRAME_SAMPLING {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return DWM_SOURCE_FRAME_SAMPLING(packedReadInt32(base, 36))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 36, val.value)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
// packed(1)
@C
public struct DWM_TIMING_INFO {
    public var _raw: VArray<UInt8, $292> = VArray<UInt8, $292>(repeat: 0u8)

    public mut prop cbSize: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rateRefresh: UNSIGNED_RATIO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: UNSIGNED_RATIO = UNSIGNED_RATIO()
                return packedReadValue<UNSIGNED_RATIO>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop qpcRefreshPeriod: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rateCompose: UNSIGNED_RATIO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: UNSIGNED_RATIO = UNSIGNED_RATIO()
                return packedReadValue<UNSIGNED_RATIO>(base, 20, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop qpcVBlank: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 28)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefresh: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 36)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 36, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cdxRefresh: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 44)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 44, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop qpcCompose: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 48)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 48, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFrame: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 56)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 56, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cdxPresent: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 64)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 64, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshFrame: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 68)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 68, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFrameSubmitted: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 76)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 76, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cdxPresentSubmitted: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 84)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 84, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFrameConfirmed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 88)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 88, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cdxPresentConfirmed: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 96)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 96, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshConfirmed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 100)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 100, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cdxRefreshConfirmed: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 108)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 108, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramesLate: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 112)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 112, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramesOutstanding: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 120)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 120, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFrameDisplayed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 124)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 124, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop qpcFrameDisplayed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 132)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 132, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshFrameDisplayed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 140)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 140, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFrameComplete: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 148)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 148, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop qpcFrameComplete: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 156)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 156, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramePending: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 164)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 164, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop qpcFramePending: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 172)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 172, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramesDisplayed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 180)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 180, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramesComplete: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 188)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 188, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramesPending: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 196)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 196, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramesAvailable: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 204)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 204, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramesDropped: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 212)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 212, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cFramesMissed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 220)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 220, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshNextDisplayed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 228)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 228, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshNextPresented: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 236)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 236, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshesDisplayed: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 244)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 244, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshesPresented: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 252)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 252, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cRefreshStarted: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 260)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 260, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cPixelsReceived: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 268)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 268, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cPixelsDrawn: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 276)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 276, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cBuffersEmpty: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 284)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 284, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[DWMAPI == "on"]
foreign {
    func DwmAttachMilContent(hwnd: CPointer<Unit>): Int32
    func DwmDefWindowProc(hWnd: CPointer<Unit>, msg: UInt32, wParam: UIntNative, lParam: IntNative, plResult: CPointer<IntNative>): Int32
    func DwmDetachMilContent(hwnd: CPointer<Unit>): Int32
    func DwmEnableComposition(uCompositionAction: UInt32): Int32
    func DwmEnableMMCSS(fEnableMMCSS: Int32): Int32
    func DwmFlush(): Int32
    func DwmGetColorizationColor(pcrColorization: CPointer<UInt32>, pfOpaqueBlend: CPointer<Int32>): Int32
    func DwmGetCompositionTimingInfo(hwnd: CPointer<Unit>, pTimingInfo: CPointer<DWM_TIMING_INFO>): Int32
    func DwmGetGraphicsStreamClient(uIndex: UInt32, pClientUuid: CPointer<GUID>): Int32
    func DwmGetGraphicsStreamTransformHint(uIndex: UInt32, pTransform: CPointer<MilMatrix3x2D>): Int32
    func DwmGetTransportAttributes(pfIsRemoting: CPointer<Int32>, pfIsConnected: CPointer<Int32>, pDwGeneration: CPointer<UInt32>): Int32
    // param-hint: appWindow optional
    func DwmGetUnmetTabRequirements(appWindow: CPointer<Unit>, value: CPointer<Int32>): Int32
    // param-hint: pvAttribute bytes=param[3]
    func DwmGetWindowAttribute(hwnd: CPointer<Unit>, dwAttribute: UInt32, pvAttribute: CPointer<Unit>, cbAttribute: UInt32): Int32
    func DwmInvalidateIconicBitmaps(hwnd: CPointer<Unit>): Int32
    func DwmIsCompositionEnabled(pfEnabled: CPointer<Int32>): Int32
    func DwmModifyPreviousDxFrameDuration(hwnd: CPointer<Unit>, cRefreshes: Int32, fRelative: Int32): Int32
    func DwmQueryThumbnailSourceSize(hThumbnail: IntNative, pSize: CPointer<Win32_Foundation.SIZE>): Int32
    func DwmRegisterThumbnail(hwndDestination: CPointer<Unit>, hwndSource: CPointer<Unit>, phThumbnailId: CPointer<IntNative>): Int32
    // param-hint: pdwPointerID len=param[1]
    // param-hint: pPoints len=param[1]
    func DwmRenderGesture(gt: Int32, cContacts: UInt32, pdwPointerID: CPointer<UInt32>, pPoints: CPointer<Win32_Foundation.POINT>): Int32
    func DwmSetDxFrameDuration(hwnd: CPointer<Unit>, cRefreshes: Int32): Int32
    func DwmSetPresentParameters(hwnd: CPointer<Unit>, pPresentParams: CPointer<DWM_PRESENT_PARAMETERS>): Int32
    // param-hint: pvAttribute bytes=param[3]
    func DwmSetWindowAttribute(hwnd: CPointer<Unit>, dwAttribute: UInt32, pvAttribute: CPointer<Unit>, cbAttribute: UInt32): Int32
    func DwmShowContact(dwPointerID: UInt32, eShowContact: UInt32): Int32
    func DwmTetherContact(dwPointerID: UInt32, fEnable: Int32, ptTether: Win32_Foundation.POINT): Int32
    func DwmTransitionOwnedWindow(hwnd: CPointer<Unit>, target: Int32): Int32
    func DwmUnregisterThumbnail(hThumbnailId: IntNative): Int32
    func DwmUpdateThumbnailProperties(hThumbnailId: IntNative, ptnProperties: CPointer<DWM_THUMBNAIL_PROPERTIES>): Int32
}

@When[DWMAPI == "on" && Windows_Win32_Graphics_Gdi == "on"]
foreign {
    func DwmEnableBlurBehindWindow(hWnd: CPointer<Unit>, pBlurBehind: CPointer<DWM_BLURBEHIND>): Int32
    // param-hint: pptClient optional
    func DwmSetIconicLivePreviewBitmap(hwnd: CPointer<Unit>, hbmp: Win32_Graphics_Gdi.HBITMAP, pptClient: CPointer<Win32_Foundation.POINT>, dwSITFlags: UInt32): Int32
    func DwmSetIconicThumbnail(hwnd: CPointer<Unit>, hbmp: Win32_Graphics_Gdi.HBITMAP, dwSITFlags: UInt32): Int32
}

@When[DWMAPI == "on" && Windows_Win32_UI_Controls == "on"]
foreign {
    func DwmExtendFrameIntoClientArea(hWnd: CPointer<Unit>, pMarInset: CPointer<Win32__impl_l0.MARGINS>): Int32
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_BB_ENABLE: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_BB_BLURREGION: UInt32 = 2u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_BB_TRANSITIONONMAXIMIZED: UInt32 = 4u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_COLOR_DEFAULT: UInt32 = 4294967295u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_COLOR_NONE: UInt32 = 4294967294u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_CLOAKED_APP: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_CLOAKED_SHELL: UInt32 = 2u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_CLOAKED_INHERITED: UInt32 = 4u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_RECTDESTINATION: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_RECTSOURCE: UInt32 = 2u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_OPACITY: UInt32 = 4u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_VISIBLE: UInt32 = 8u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_SOURCECLIENTAREAONLY: UInt32 = 16u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_FRAME_DURATION_DEFAULT: Int32 = -1i32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_EC_DISABLECOMPOSITION: UInt32 = 0u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_EC_ENABLECOMPOSITION: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SIT_DISPLAYFRAME: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let c_DwmMaxQueuedBuffers: UInt32 = 8u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let c_DwmMaxMonitors: UInt32 = 16u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let c_DwmMaxAdapters: UInt32 = 16u32
