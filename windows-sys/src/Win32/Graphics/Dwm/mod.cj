// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.Win32.Graphics.Dwm

import windows_interface.*
import windows_result.*
import windows_polyfill.*
import windows_sys.Win32.Foundation as Win32_Foundation
import windows_sys.Win32.Graphics.Gdi as Win32_Graphics_Gdi
import windows_sys.Win32.impl as Win32_impl
import windows_sys.Win32.Foundation.*


@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWMFLIP3DWINDOWPOLICY {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWMFLIP3DWINDOWPOLICY): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWMFLIP3DWINDOWPOLICY): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMFLIP3D_DEFAULT: DWMFLIP3DWINDOWPOLICY = DWMFLIP3DWINDOWPOLICY(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMFLIP3D_EXCLUDEBELOW: DWMFLIP3DWINDOWPOLICY = DWMFLIP3DWINDOWPOLICY(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMFLIP3D_EXCLUDEABOVE: DWMFLIP3DWINDOWPOLICY = DWMFLIP3DWINDOWPOLICY(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMFLIP3D_LAST: DWMFLIP3DWINDOWPOLICY = DWMFLIP3DWINDOWPOLICY(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWMNCRENDERINGPOLICY {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWMNCRENDERINGPOLICY): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWMNCRENDERINGPOLICY): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMNCRP_USEWINDOWSTYLE: DWMNCRENDERINGPOLICY = DWMNCRENDERINGPOLICY(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMNCRP_DISABLED: DWMNCRENDERINGPOLICY = DWMNCRENDERINGPOLICY(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMNCRP_ENABLED: DWMNCRENDERINGPOLICY = DWMNCRENDERINGPOLICY(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMNCRP_LAST: DWMNCRENDERINGPOLICY = DWMNCRENDERINGPOLICY(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWMTRANSITION_OWNEDWINDOW_TARGET {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWMTRANSITION_OWNEDWINDOW_TARGET): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWMTRANSITION_OWNEDWINDOW_TARGET): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTRANSITION_OWNEDWINDOW_NULL: DWMTRANSITION_OWNEDWINDOW_TARGET = DWMTRANSITION_OWNEDWINDOW_TARGET(-1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTRANSITION_OWNEDWINDOW_REPOSITION: DWMTRANSITION_OWNEDWINDOW_TARGET = DWMTRANSITION_OWNEDWINDOW_TARGET(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWMWINDOWATTRIBUTE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWMWINDOWATTRIBUTE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWMWINDOWATTRIBUTE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_NCRENDERING_ENABLED: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_NCRENDERING_POLICY: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_TRANSITIONS_FORCEDISABLED: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_ALLOW_NCPAINT: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_CAPTION_BUTTON_BOUNDS: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(5)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_NONCLIENT_RTL_LAYOUT: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(6)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_FORCE_ICONIC_REPRESENTATION: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(7)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_FLIP3D_POLICY: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(8)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_EXTENDED_FRAME_BOUNDS: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(9)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_HAS_ICONIC_BITMAP: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(10)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_DISALLOW_PEEK: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(11)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_EXCLUDED_FROM_PEEK: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(12)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_CLOAK: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(13)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_CLOAKED: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(14)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_FREEZE_REPRESENTATION: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(15)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_PASSIVE_UPDATE_MODE: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(16)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_USE_HOSTBACKDROPBRUSH: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(17)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_USE_IMMERSIVE_DARK_MODE: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(20)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_WINDOW_CORNER_PREFERENCE: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(33)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_BORDER_COLOR: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(34)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_CAPTION_COLOR: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(35)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_TEXT_COLOR: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(36)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_VISIBLE_FRAME_BORDER_THICKNESS: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(37)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_SYSTEMBACKDROP_TYPE: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(38)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_REDIRECTIONBITMAP_ALPHA: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(39)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_BORDER_MARGINS: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(40)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_LAST: DWMWINDOWATTRIBUTE = DWMWINDOWATTRIBUTE(41)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_BLURBEHIND {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    public func dwFlags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_dwFlags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func fEnable(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 4)
        }
    }

    public mut func set_fEnable(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `hRgnBlur`: type `Win32_Graphics_Gdi.HRGN` not supported by polyfill; skipped

    public func fTransitionOnMaximized(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 16)
        }
    }

    public mut func set_fTransitionOnMaximized(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 16, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_PRESENT_PARAMETERS {
    public var _raw: VArray<UInt8, $40> = VArray<UInt8, $40>(repeat: 0u8)

    public func cbSize(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_cbSize(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func fQueue(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 4)
        }
    }

    public mut func set_fQueue(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func cRefreshStart(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 8)
        }
    }

    public mut func set_cRefreshStart(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 8, val)
            _raw = rawStorage
        }
    }

    public func cBuffer(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_cBuffer(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }

    public func fUseSourceRate(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 20)
        }
    }

    public mut func set_fUseSourceRate(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 20, val)
            _raw = rawStorage
        }
    }

    // packed field `rateSource`: type `UNSIGNED_RATIO` not supported by polyfill; skipped

    public func cRefreshesPerFrame(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 32)
        }
    }

    public mut func set_cRefreshesPerFrame(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 32, val)
            _raw = rawStorage
        }
    }

    // packed field `eSampling`: type `DWM_SOURCE_FRAME_SAMPLING` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_SHOWCONTACT {
    public var value: UInt32 = 0

    public init() {}

    public init(value: UInt32) {
        this.value = value
    }

    public operator func ==(other: DWM_SHOWCONTACT): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_SHOWCONTACT): Bool {
        return this.value != other.value
    }

    public func contains(other: DWM_SHOWCONTACT): Bool {
        return (this.value & other.value) == other.value
    }

    public operator func |(other: DWM_SHOWCONTACT): DWM_SHOWCONTACT {
        return DWM_SHOWCONTACT(this.value | other.value)
    }
    public operator func &(other: DWM_SHOWCONTACT): DWM_SHOWCONTACT {
        return DWM_SHOWCONTACT(this.value & other.value)
    }
    public operator func !(): DWM_SHOWCONTACT {
        return DWM_SHOWCONTACT(!this.value)
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSC_DOWN: DWM_SHOWCONTACT = DWM_SHOWCONTACT(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSC_UP: DWM_SHOWCONTACT = DWM_SHOWCONTACT(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSC_DRAG: DWM_SHOWCONTACT = DWM_SHOWCONTACT(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSC_HOLD: DWM_SHOWCONTACT = DWM_SHOWCONTACT(8)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSC_PENBARREL: DWM_SHOWCONTACT = DWM_SHOWCONTACT(16)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSC_NONE: DWM_SHOWCONTACT = DWM_SHOWCONTACT(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSC_ALL: DWM_SHOWCONTACT = DWM_SHOWCONTACT(4294967295)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_SOURCE_FRAME_SAMPLING {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWM_SOURCE_FRAME_SAMPLING): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_SOURCE_FRAME_SAMPLING): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SOURCE_FRAME_SAMPLING_POINT: DWM_SOURCE_FRAME_SAMPLING = DWM_SOURCE_FRAME_SAMPLING(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SOURCE_FRAME_SAMPLING_COVERAGE: DWM_SOURCE_FRAME_SAMPLING = DWM_SOURCE_FRAME_SAMPLING(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SOURCE_FRAME_SAMPLING_LAST: DWM_SOURCE_FRAME_SAMPLING = DWM_SOURCE_FRAME_SAMPLING(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_SYSTEMBACKDROP_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWM_SYSTEMBACKDROP_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_SYSTEMBACKDROP_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSBT_AUTO: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSBT_NONE: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSBT_MAINWINDOW: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSBT_TRANSIENTWINDOW: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMSBT_TABBEDWINDOW: DWM_SYSTEMBACKDROP_TYPE = DWM_SYSTEMBACKDROP_TYPE(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_TAB_WINDOW_REQUIREMENTS {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWM_TAB_WINDOW_REQUIREMENTS): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_TAB_WINDOW_REQUIREMENTS): Bool {
        return this.value != other.value
    }

    public func contains(other: DWM_TAB_WINDOW_REQUIREMENTS): Bool {
        return (this.value & other.value) == other.value
    }

    public operator func |(other: DWM_TAB_WINDOW_REQUIREMENTS): DWM_TAB_WINDOW_REQUIREMENTS {
        return DWM_TAB_WINDOW_REQUIREMENTS(this.value | other.value)
    }
    public operator func &(other: DWM_TAB_WINDOW_REQUIREMENTS): DWM_TAB_WINDOW_REQUIREMENTS {
        return DWM_TAB_WINDOW_REQUIREMENTS(this.value & other.value)
    }
    public operator func !(): DWM_TAB_WINDOW_REQUIREMENTS {
        return DWM_TAB_WINDOW_REQUIREMENTS(!this.value)
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_NONE: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_IMPLEMENTED_BY_SYSTEM: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_WINDOW_RELATIONSHIP: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_WINDOW_STYLES: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_WINDOW_REGION: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(8)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_WINDOW_DWM_ATTRIBUTES: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(16)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_WINDOW_MARGINS: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(32)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_TABBING_ENABLED: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(64)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_USER_POLICY: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(128)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_GROUP_POLICY: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(256)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMTWR_APP_COMPAT: DWM_TAB_WINDOW_REQUIREMENTS = DWM_TAB_WINDOW_REQUIREMENTS(512)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_THUMBNAIL_PROPERTIES {
    public var _raw: VArray<UInt8, $45> = VArray<UInt8, $45>(repeat: 0u8)

    public func dwFlags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_dwFlags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `rcDestination`: type `Win32_Foundation.RECT` not supported by polyfill; skipped

    // packed field `rcSource`: type `Win32_Foundation.RECT` not supported by polyfill; skipped

    public func opacity(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 36)
        }
    }

    public mut func set_opacity(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 36, val)
            _raw = rawStorage
        }
    }

    public func fVisible(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 37)
        }
    }

    public mut func set_fVisible(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 37, val)
            _raw = rawStorage
        }
    }

    public func fSourceClientAreaOnly(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 41)
        }
    }

    public mut func set_fSourceClientAreaOnly(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 41, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_TIMING_INFO {
    public var _raw: VArray<UInt8, $292> = VArray<UInt8, $292>(repeat: 0u8)

    public func cbSize(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_cbSize(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `rateRefresh`: type `UNSIGNED_RATIO` not supported by polyfill; skipped

    public func qpcRefreshPeriod(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 12)
        }
    }

    public mut func set_qpcRefreshPeriod(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 12, val)
            _raw = rawStorage
        }
    }

    // packed field `rateCompose`: type `UNSIGNED_RATIO` not supported by polyfill; skipped

    public func qpcVBlank(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 28)
        }
    }

    public mut func set_qpcVBlank(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 28, val)
            _raw = rawStorage
        }
    }

    public func cRefresh(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 36)
        }
    }

    public mut func set_cRefresh(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 36, val)
            _raw = rawStorage
        }
    }

    public func cdxRefresh(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 44)
        }
    }

    public mut func set_cdxRefresh(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 44, val)
            _raw = rawStorage
        }
    }

    public func qpcCompose(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 48)
        }
    }

    public mut func set_qpcCompose(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 48, val)
            _raw = rawStorage
        }
    }

    public func cFrame(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 56)
        }
    }

    public mut func set_cFrame(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 56, val)
            _raw = rawStorage
        }
    }

    public func cdxPresent(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 64)
        }
    }

    public mut func set_cdxPresent(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 64, val)
            _raw = rawStorage
        }
    }

    public func cRefreshFrame(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 68)
        }
    }

    public mut func set_cRefreshFrame(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 68, val)
            _raw = rawStorage
        }
    }

    public func cFrameSubmitted(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 76)
        }
    }

    public mut func set_cFrameSubmitted(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 76, val)
            _raw = rawStorage
        }
    }

    public func cdxPresentSubmitted(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 84)
        }
    }

    public mut func set_cdxPresentSubmitted(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 84, val)
            _raw = rawStorage
        }
    }

    public func cFrameConfirmed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 88)
        }
    }

    public mut func set_cFrameConfirmed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 88, val)
            _raw = rawStorage
        }
    }

    public func cdxPresentConfirmed(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 96)
        }
    }

    public mut func set_cdxPresentConfirmed(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 96, val)
            _raw = rawStorage
        }
    }

    public func cRefreshConfirmed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 100)
        }
    }

    public mut func set_cRefreshConfirmed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 100, val)
            _raw = rawStorage
        }
    }

    public func cdxRefreshConfirmed(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 108)
        }
    }

    public mut func set_cdxRefreshConfirmed(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 108, val)
            _raw = rawStorage
        }
    }

    public func cFramesLate(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 112)
        }
    }

    public mut func set_cFramesLate(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 112, val)
            _raw = rawStorage
        }
    }

    public func cFramesOutstanding(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 120)
        }
    }

    public mut func set_cFramesOutstanding(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 120, val)
            _raw = rawStorage
        }
    }

    public func cFrameDisplayed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 124)
        }
    }

    public mut func set_cFrameDisplayed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 124, val)
            _raw = rawStorage
        }
    }

    public func qpcFrameDisplayed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 132)
        }
    }

    public mut func set_qpcFrameDisplayed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 132, val)
            _raw = rawStorage
        }
    }

    public func cRefreshFrameDisplayed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 140)
        }
    }

    public mut func set_cRefreshFrameDisplayed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 140, val)
            _raw = rawStorage
        }
    }

    public func cFrameComplete(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 148)
        }
    }

    public mut func set_cFrameComplete(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 148, val)
            _raw = rawStorage
        }
    }

    public func qpcFrameComplete(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 156)
        }
    }

    public mut func set_qpcFrameComplete(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 156, val)
            _raw = rawStorage
        }
    }

    public func cFramePending(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 164)
        }
    }

    public mut func set_cFramePending(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 164, val)
            _raw = rawStorage
        }
    }

    public func qpcFramePending(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 172)
        }
    }

    public mut func set_qpcFramePending(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 172, val)
            _raw = rawStorage
        }
    }

    public func cFramesDisplayed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 180)
        }
    }

    public mut func set_cFramesDisplayed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 180, val)
            _raw = rawStorage
        }
    }

    public func cFramesComplete(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 188)
        }
    }

    public mut func set_cFramesComplete(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 188, val)
            _raw = rawStorage
        }
    }

    public func cFramesPending(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 196)
        }
    }

    public mut func set_cFramesPending(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 196, val)
            _raw = rawStorage
        }
    }

    public func cFramesAvailable(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 204)
        }
    }

    public mut func set_cFramesAvailable(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 204, val)
            _raw = rawStorage
        }
    }

    public func cFramesDropped(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 212)
        }
    }

    public mut func set_cFramesDropped(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 212, val)
            _raw = rawStorage
        }
    }

    public func cFramesMissed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 220)
        }
    }

    public mut func set_cFramesMissed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 220, val)
            _raw = rawStorage
        }
    }

    public func cRefreshNextDisplayed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 228)
        }
    }

    public mut func set_cRefreshNextDisplayed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 228, val)
            _raw = rawStorage
        }
    }

    public func cRefreshNextPresented(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 236)
        }
    }

    public mut func set_cRefreshNextPresented(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 236, val)
            _raw = rawStorage
        }
    }

    public func cRefreshesDisplayed(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 244)
        }
    }

    public mut func set_cRefreshesDisplayed(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 244, val)
            _raw = rawStorage
        }
    }

    public func cRefreshesPresented(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 252)
        }
    }

    public mut func set_cRefreshesPresented(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 252, val)
            _raw = rawStorage
        }
    }

    public func cRefreshStarted(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 260)
        }
    }

    public mut func set_cRefreshStarted(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 260, val)
            _raw = rawStorage
        }
    }

    public func cPixelsReceived(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 268)
        }
    }

    public mut func set_cPixelsReceived(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 268, val)
            _raw = rawStorage
        }
    }

    public func cPixelsDrawn(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 276)
        }
    }

    public mut func set_cPixelsDrawn(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 276, val)
            _raw = rawStorage
        }
    }

    public func cBuffersEmpty(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 284)
        }
    }

    public mut func set_cBuffersEmpty(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 284, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct DWM_WINDOW_CORNER_PREFERENCE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: DWM_WINDOW_CORNER_PREFERENCE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: DWM_WINDOW_CORNER_PREFERENCE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWCP_DEFAULT: DWM_WINDOW_CORNER_PREFERENCE = DWM_WINDOW_CORNER_PREFERENCE(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWCP_DONOTROUND: DWM_WINDOW_CORNER_PREFERENCE = DWM_WINDOW_CORNER_PREFERENCE(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWCP_ROUND: DWM_WINDOW_CORNER_PREFERENCE = DWM_WINDOW_CORNER_PREFERENCE(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWCP_ROUNDSMALL: DWM_WINDOW_CORNER_PREFERENCE = DWM_WINDOW_CORNER_PREFERENCE(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct GESTURE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: GESTURE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: GESTURE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_PEN_TAP: GESTURE_TYPE = GESTURE_TYPE(0)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_PEN_DOUBLETAP: GESTURE_TYPE = GESTURE_TYPE(1)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_PEN_RIGHTTAP: GESTURE_TYPE = GESTURE_TYPE(2)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_PEN_PRESSANDHOLD: GESTURE_TYPE = GESTURE_TYPE(3)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_PEN_PRESSANDHOLDABORT: GESTURE_TYPE = GESTURE_TYPE(4)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_TOUCH_TAP: GESTURE_TYPE = GESTURE_TYPE(5)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_TOUCH_DOUBLETAP: GESTURE_TYPE = GESTURE_TYPE(6)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_TOUCH_RIGHTTAP: GESTURE_TYPE = GESTURE_TYPE(7)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_TOUCH_PRESSANDHOLD: GESTURE_TYPE = GESTURE_TYPE(8)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_TOUCH_PRESSANDHOLDABORT: GESTURE_TYPE = GESTURE_TYPE(9)

@When[Windows_Win32_Graphics_Dwm == "on"]
public let GT_TOUCH_PRESSANDTAP: GESTURE_TYPE = GESTURE_TYPE(10)

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct MilMatrix3x2D {
    public var _raw: VArray<UInt8, $48> = VArray<UInt8, $48>(repeat: 0u8)

    public func s_11(): Float64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadFloat64(base, 0)
        }
    }

    public mut func set_s_11(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteFloat64(base, 0, val)
            _raw = rawStorage
        }
    }

    public func s_12(): Float64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadFloat64(base, 8)
        }
    }

    public mut func set_s_12(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteFloat64(base, 8, val)
            _raw = rawStorage
        }
    }

    public func s_21(): Float64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadFloat64(base, 16)
        }
    }

    public mut func set_s_21(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteFloat64(base, 16, val)
            _raw = rawStorage
        }
    }

    public func s_22(): Float64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadFloat64(base, 24)
        }
    }

    public mut func set_s_22(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteFloat64(base, 24, val)
            _raw = rawStorage
        }
    }

    public func dx(): Float64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadFloat64(base, 32)
        }
    }

    public mut func set_dx(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteFloat64(base, 32, val)
            _raw = rawStorage
        }
    }

    public func dy(): Float64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadFloat64(base, 40)
        }
    }

    public mut func set_dy(val: Float64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteFloat64(base, 40, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Graphics_Dwm == "on"]
@C
public struct UNSIGNED_RATIO {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    public func uiNumerator(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_uiNumerator(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func uiDenominator(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_uiDenominator(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[DWMAPI == "on"]
foreign {
    func DwmAttachMilContent(hwnd: CPointer<Unit>): Int32
    func DwmDefWindowProc(hWnd: CPointer<Unit>, msg: UInt32, wParam: UIntNative, lParam: IntNative, plResult: CPointer<IntNative>): Int32
    func DwmDetachMilContent(hwnd: CPointer<Unit>): Int32
    func DwmEnableBlurBehindWindow(hWnd: CPointer<Unit>, pBlurBehind: CPointer<DWM_BLURBEHIND>): Int32
    func DwmEnableComposition(uCompositionAction: UInt32): Int32
    func DwmEnableMMCSS(fEnableMMCSS: Int32): Int32
    func DwmExtendFrameIntoClientArea(hWnd: CPointer<Unit>, pMarInset: CPointer<Win32_impl.MARGINS>): Int32
    func DwmFlush(): Int32
    func DwmGetColorizationColor(pcrColorization: CPointer<UInt32>, pfOpaqueBlend: CPointer<Int32>): Int32
    func DwmGetCompositionTimingInfo(hwnd: CPointer<Unit>, pTimingInfo: CPointer<DWM_TIMING_INFO>): Int32
    func DwmGetGraphicsStreamClient(uIndex: UInt32, pClientUuid: CPointer<GUID>): Int32
    func DwmGetGraphicsStreamTransformHint(uIndex: UInt32, pTransform: CPointer<MilMatrix3x2D>): Int32
    func DwmGetTransportAttributes(pfIsRemoting: CPointer<Int32>, pfIsConnected: CPointer<Int32>, pDwGeneration: CPointer<UInt32>): Int32
    func DwmGetUnmetTabRequirements(appWindow: CPointer<Unit>, value: CPointer<DWM_TAB_WINDOW_REQUIREMENTS>): Int32
    func DwmGetWindowAttribute(hwnd: CPointer<Unit>, dwAttribute: UInt32, pvAttribute: CPointer<Unit>, cbAttribute: UInt32): Int32
    func DwmInvalidateIconicBitmaps(hwnd: CPointer<Unit>): Int32
    func DwmIsCompositionEnabled(pfEnabled: CPointer<Int32>): Int32
    func DwmModifyPreviousDxFrameDuration(hwnd: CPointer<Unit>, cRefreshes: Int32, fRelative: Int32): Int32
    func DwmQueryThumbnailSourceSize(hThumbnail: IntNative, pSize: CPointer<Win32_Foundation.SIZE>): Int32
    func DwmRegisterThumbnail(hwndDestination: CPointer<Unit>, hwndSource: CPointer<Unit>, phThumbnailId: CPointer<IntNative>): Int32
    func DwmRenderGesture(gt: GESTURE_TYPE, cContacts: UInt32, pdwPointerID: CPointer<UInt32>, pPoints: CPointer<Win32_Foundation.POINT>): Int32
    func DwmSetDxFrameDuration(hwnd: CPointer<Unit>, cRefreshes: Int32): Int32
    func DwmSetIconicLivePreviewBitmap(hwnd: CPointer<Unit>, hbmp: Win32_Graphics_Gdi.HBITMAP, pptClient: CPointer<Win32_Foundation.POINT>, dwSITFlags: UInt32): Int32
    func DwmSetIconicThumbnail(hwnd: CPointer<Unit>, hbmp: Win32_Graphics_Gdi.HBITMAP, dwSITFlags: UInt32): Int32
    func DwmSetPresentParameters(hwnd: CPointer<Unit>, pPresentParams: CPointer<DWM_PRESENT_PARAMETERS>): Int32
    func DwmSetWindowAttribute(hwnd: CPointer<Unit>, dwAttribute: UInt32, pvAttribute: CPointer<Unit>, cbAttribute: UInt32): Int32
    func DwmShowContact(dwPointerID: UInt32, eShowContact: DWM_SHOWCONTACT): Int32
    func DwmTetherContact(dwPointerID: UInt32, fEnable: Int32, ptTether: Win32_Foundation.POINT): Int32
    func DwmTransitionOwnedWindow(hwnd: CPointer<Unit>, target: DWMTRANSITION_OWNEDWINDOW_TARGET): Int32
    func DwmUnregisterThumbnail(hThumbnailId: IntNative): Int32
    func DwmUpdateThumbnailProperties(hThumbnailId: IntNative, ptnProperties: CPointer<DWM_THUMBNAIL_PROPERTIES>): Int32
}

@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_BB_ENABLE: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_BB_BLURREGION: UInt32 = 2u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_BB_TRANSITIONONMAXIMIZED: UInt32 = 4u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_COLOR_DEFAULT: UInt32 = 4294967295u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWMWA_COLOR_NONE: UInt32 = 4294967294u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_CLOAKED_APP: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_CLOAKED_SHELL: UInt32 = 2u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_CLOAKED_INHERITED: UInt32 = 4u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_RECTDESTINATION: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_RECTSOURCE: UInt32 = 2u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_OPACITY: UInt32 = 4u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_VISIBLE: UInt32 = 8u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_TNP_SOURCECLIENTAREAONLY: UInt32 = 16u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_FRAME_DURATION_DEFAULT: Int32 = -1i32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_EC_DISABLECOMPOSITION: UInt32 = 0u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_EC_ENABLECOMPOSITION: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let DWM_SIT_DISPLAYFRAME: UInt32 = 1u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let c_DwmMaxQueuedBuffers: UInt32 = 8u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let c_DwmMaxMonitors: UInt32 = 16u32
@When[Windows_Win32_Graphics_Dwm == "on"]
public let c_DwmMaxAdapters: UInt32 = 16u32
