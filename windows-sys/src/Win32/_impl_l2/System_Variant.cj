// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.Win32._impl_l2

import windows_interface.{GUID}
import windows_interface as windows_interface
import windows_polyfill.{unionReadFloat32, unionReadFloat64, unionReadInt16, unionReadInt32, unionReadInt64, unionReadInt8, unionReadUInt16, unionReadUInt32, unionReadUInt64, unionReadUInt8, unionWriteFloat32, unionWriteFloat64, unionWriteInt16, unionWriteInt32, unionWriteInt64, unionWriteInt8, unionWriteUInt16, unionWriteUInt32, unionWriteUInt64, unionWriteUInt8}
import windows_polyfill as windows_polyfill
@When[Windows_Win32_Foundation == "on"]
import windows_sys.Win32.Foundation as Win32_Foundation
import windows_sys.Win32._impl_l0 as Win32__impl_l0
import windows_sys.Win32._impl_l1 as Win32__impl_l1


@When[OLEAUT32 == "on"]
foreign {
    func DosDateTimeToVariantTime(wDosDate: UInt16, wDosTime: UInt16, pvtime: CPointer<Float64>): Int32
    func SystemTimeToVariantTime(lpSystemTime: CPointer<Win32_Foundation.SYSTEMTIME>, pvtime: CPointer<Float64>): Int32
    func VariantTimeToDosDateTime(vtime: Float64, pwDosDate: CPointer<UInt16>, pwDosTime: CPointer<UInt16>): Int32
    func VariantTimeToSystemTime(vtime: Float64, lpSystemTime: CPointer<Win32_Foundation.SYSTEMTIME>): Int32
}

@When[OLEAUT32 == "on" && (Windows_Win32_System_Com == "on" && Windows_Win32_System_Ole == "on")]
foreign {
    func VARIANT_UserFree(param0: CPointer<UInt32>, param1: CPointer<VARIANT>): Unit
    func VARIANT_UserFree64(param0: CPointer<UInt32>, param1: CPointer<VARIANT>): Unit
    func VARIANT_UserMarshal(param0: CPointer<UInt32>, param1: CPointer<UInt8>, param2: CPointer<VARIANT>): CPointer<UInt8>
    func VARIANT_UserMarshal64(param0: CPointer<UInt32>, param1: CPointer<UInt8>, param2: CPointer<VARIANT>): CPointer<UInt8>
    func VARIANT_UserSize(param0: CPointer<UInt32>, param1: UInt32, param2: CPointer<VARIANT>): UInt32
    func VARIANT_UserSize64(param0: CPointer<UInt32>, param1: UInt32, param2: CPointer<VARIANT>): UInt32
    func VARIANT_UserUnmarshal(param0: CPointer<UInt32>, param1: CPointer<UInt8>, param2: CPointer<VARIANT>): CPointer<UInt8>
    func VARIANT_UserUnmarshal64(param0: CPointer<UInt32>, param1: CPointer<UInt8>, param2: CPointer<VARIANT>): CPointer<UInt8>
    func VariantChangeType(pvargDest: CPointer<VARIANT>, pvarSrc: CPointer<VARIANT>, wFlags: UInt16, vt: UInt16): Int32
    func VariantChangeTypeEx(pvargDest: CPointer<VARIANT>, pvarSrc: CPointer<VARIANT>, lcid: UInt32, wFlags: UInt16, vt: UInt16): Int32
    func VariantClear(pvarg: CPointer<VARIANT>): Int32
    func VariantCopy(pvargDest: CPointer<VARIANT>, pvargSrc: CPointer<VARIANT>): Int32
    func VariantCopyInd(pvarDest: CPointer<VARIANT>, pvargSrc: CPointer<VARIANT>): Int32
    func VariantInit(pvarg: CPointer<VARIANT>): Unit
}

@When[PROPSYS == "on" && (Windows_Win32_System_Com == "on" && Windows_Win32_System_Ole == "on")]
foreign {
    // param-hint: pvars len=param[1]
    func ClearVariantArray(pvars: CPointer<VARIANT>, cvars: UInt32): Unit
    // param-hint: prgf len=param[1]
    func InitVariantFromBooleanArray(prgf: CPointer<Int32>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    // param-hint: pv bytes=param[1]
    func InitVariantFromBuffer(pv: CPointer<Unit>, cb: UInt32, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgn len=param[1]
    func InitVariantFromDoubleArray(prgn: CPointer<Float64>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    func InitVariantFromFileTime(pft: CPointer<Win32_Foundation.FILETIME>, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgft len=param[1] optional
    func InitVariantFromFileTimeArray(prgft: CPointer<Win32_Foundation.FILETIME>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    func InitVariantFromGUIDAsString(guid: CPointer<GUID>, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgn len=param[1]
    func InitVariantFromInt16Array(prgn: CPointer<Int16>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgn len=param[1]
    func InitVariantFromInt32Array(prgn: CPointer<Int32>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgn len=param[1]
    func InitVariantFromInt64Array(prgn: CPointer<Int64>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    func InitVariantFromResource(hinst: CPointer<Unit>, id: UInt32, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgsz len=param[1]
    func InitVariantFromStringArray(prgsz: CPointer<CPointer<UInt16>>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgn len=param[1]
    func InitVariantFromUInt16Array(prgn: CPointer<UInt16>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgn len=param[1]
    func InitVariantFromUInt32Array(prgn: CPointer<UInt32>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    // param-hint: prgn len=param[1]
    func InitVariantFromUInt64Array(prgn: CPointer<UInt64>, cElems: UInt32, pvar: CPointer<VARIANT>): Int32
    func InitVariantFromVariantArrayElem(varIn: CPointer<VARIANT>, iElem: UInt32, pvar: CPointer<VARIANT>): Int32
    func VariantCompare(var1: CPointer<VARIANT>, var2: CPointer<VARIANT>): Int32
    func VariantGetBooleanElem(var_: CPointer<VARIANT>, iElem: UInt32, pfVal: CPointer<Int32>): Int32
    func VariantGetDoubleElem(var_: CPointer<VARIANT>, iElem: UInt32, pnVal: CPointer<Float64>): Int32
    func VariantGetElementCount(varIn: CPointer<VARIANT>): UInt32
    func VariantGetInt16Elem(var_: CPointer<VARIANT>, iElem: UInt32, pnVal: CPointer<Int16>): Int32
    func VariantGetInt32Elem(var_: CPointer<VARIANT>, iElem: UInt32, pnVal: CPointer<Int32>): Int32
    func VariantGetInt64Elem(var_: CPointer<VARIANT>, iElem: UInt32, pnVal: CPointer<Int64>): Int32
    func VariantGetStringElem(var_: CPointer<VARIANT>, iElem: UInt32, ppszVal: CPointer<CPointer<UInt16>>): Int32
    func VariantGetUInt16Elem(var_: CPointer<VARIANT>, iElem: UInt32, pnVal: CPointer<UInt16>): Int32
    func VariantGetUInt32Elem(var_: CPointer<VARIANT>, iElem: UInt32, pnVal: CPointer<UInt32>): Int32
    func VariantGetUInt64Elem(var_: CPointer<VARIANT>, iElem: UInt32, pnVal: CPointer<UInt64>): Int32
    func VariantToBoolean(varIn: CPointer<VARIANT>, pfRet: CPointer<Int32>): Int32
    // param-hint: prgf len=param[2]
    func VariantToBooleanArray(var_: CPointer<VARIANT>, prgf: CPointer<Int32>, crgn: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToBooleanArrayAlloc(var_: CPointer<VARIANT>, pprgf: CPointer<CPointer<Int32>>, pcElem: CPointer<UInt32>): Int32
    func VariantToBooleanWithDefault(varIn: CPointer<VARIANT>, fDefault: Int32): Int32
    // param-hint: pv bytes=param[2]
    func VariantToBuffer(varIn: CPointer<VARIANT>, pv: CPointer<Unit>, cb: UInt32): Int32
    func VariantToDosDateTime(varIn: CPointer<VARIANT>, pwDate: CPointer<UInt16>, pwTime: CPointer<UInt16>): Int32
    func VariantToDouble(varIn: CPointer<VARIANT>, pdblRet: CPointer<Float64>): Int32
    // param-hint: prgn len=param[2]
    func VariantToDoubleArray(var_: CPointer<VARIANT>, prgn: CPointer<Float64>, crgn: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToDoubleArrayAlloc(var_: CPointer<VARIANT>, pprgn: CPointer<CPointer<Float64>>, pcElem: CPointer<UInt32>): Int32
    func VariantToDoubleWithDefault(varIn: CPointer<VARIANT>, dblDefault: Float64): Float64
    func VariantToFileTime(varIn: CPointer<VARIANT>, stfOut: Int32, pftOut: CPointer<Win32_Foundation.FILETIME>): Int32
    func VariantToGUID(varIn: CPointer<VARIANT>, pguid: CPointer<GUID>): Int32
    func VariantToInt16(varIn: CPointer<VARIANT>, piRet: CPointer<Int16>): Int32
    // param-hint: prgn len=param[2]
    func VariantToInt16Array(var_: CPointer<VARIANT>, prgn: CPointer<Int16>, crgn: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToInt16ArrayAlloc(var_: CPointer<VARIANT>, pprgn: CPointer<CPointer<Int16>>, pcElem: CPointer<UInt32>): Int32
    func VariantToInt16WithDefault(varIn: CPointer<VARIANT>, iDefault: Int16): Int16
    func VariantToInt32(varIn: CPointer<VARIANT>, plRet: CPointer<Int32>): Int32
    // param-hint: prgn len=param[2]
    func VariantToInt32Array(var_: CPointer<VARIANT>, prgn: CPointer<Int32>, crgn: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToInt32ArrayAlloc(var_: CPointer<VARIANT>, pprgn: CPointer<CPointer<Int32>>, pcElem: CPointer<UInt32>): Int32
    func VariantToInt32WithDefault(varIn: CPointer<VARIANT>, lDefault: Int32): Int32
    func VariantToInt64(varIn: CPointer<VARIANT>, pllRet: CPointer<Int64>): Int32
    // param-hint: prgn len=param[2]
    func VariantToInt64Array(var_: CPointer<VARIANT>, prgn: CPointer<Int64>, crgn: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToInt64ArrayAlloc(var_: CPointer<VARIANT>, pprgn: CPointer<CPointer<Int64>>, pcElem: CPointer<UInt32>): Int32
    func VariantToInt64WithDefault(varIn: CPointer<VARIANT>, llDefault: Int64): Int64
    // param-hint: pszBuf len=param[2]
    func VariantToString(varIn: CPointer<VARIANT>, pszBuf: CPointer<UInt16>, cchBuf: UInt32): Int32
    func VariantToStringAlloc(varIn: CPointer<VARIANT>, ppszBuf: CPointer<CPointer<UInt16>>): Int32
    // param-hint: prgsz len=param[2]
    func VariantToStringArray(var_: CPointer<VARIANT>, prgsz: CPointer<CPointer<UInt16>>, crgsz: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToStringArrayAlloc(var_: CPointer<VARIANT>, pprgsz: CPointer<CPointer<CPointer<UInt16>>>, pcElem: CPointer<UInt32>): Int32
    // param-hint: pszDefault optional
    func VariantToStringWithDefault(varIn: CPointer<VARIANT>, pszDefault: CPointer<UInt16>): CPointer<UInt16>
    func VariantToUInt16(varIn: CPointer<VARIANT>, puiRet: CPointer<UInt16>): Int32
    // param-hint: prgn len=param[2]
    func VariantToUInt16Array(var_: CPointer<VARIANT>, prgn: CPointer<UInt16>, crgn: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToUInt16ArrayAlloc(var_: CPointer<VARIANT>, pprgn: CPointer<CPointer<UInt16>>, pcElem: CPointer<UInt32>): Int32
    func VariantToUInt16WithDefault(varIn: CPointer<VARIANT>, uiDefault: UInt16): UInt16
    func VariantToUInt32(varIn: CPointer<VARIANT>, pulRet: CPointer<UInt32>): Int32
    // param-hint: prgn len=param[2]
    func VariantToUInt32Array(var_: CPointer<VARIANT>, prgn: CPointer<UInt32>, crgn: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToUInt32ArrayAlloc(var_: CPointer<VARIANT>, pprgn: CPointer<CPointer<UInt32>>, pcElem: CPointer<UInt32>): Int32
    func VariantToUInt32WithDefault(varIn: CPointer<VARIANT>, ulDefault: UInt32): UInt32
    func VariantToUInt64(varIn: CPointer<VARIANT>, pullRet: CPointer<UInt64>): Int32
    // param-hint: prgn len=param[2]
    func VariantToUInt64Array(var_: CPointer<VARIANT>, prgn: CPointer<UInt64>, crgn: UInt32, pcElem: CPointer<UInt32>): Int32
    func VariantToUInt64ArrayAlloc(var_: CPointer<VARIANT>, pprgn: CPointer<CPointer<UInt64>>, pcElem: CPointer<UInt32>): Int32
    func VariantToUInt64WithDefault(varIn: CPointer<VARIANT>, ullDefault: UInt64): UInt64
}

@When[Windows_Win32_System_Variant == "on" && (Windows_Win32_System_Com == "on" && Windows_Win32_System_Com == "on" && Windows_Win32_System_Ole == "on" && Windows_Win32_System_Ole == "on")]
@C
public struct VARIANT {
    public var Anonymous: VARIANT__Anonymous_e__Union = VARIANT__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Variant == "on" && (Windows_Win32_System_Com == "on" && Windows_Win32_System_Com == "on" && Windows_Win32_System_Ole == "on" && Windows_Win32_System_Ole == "on")]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct VARIANT__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $3> = VArray<UInt64, $3>(repeat: 0)

    public mut prop anonymous: VARIANT__Anonymous_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop decVal: Win32_Foundation.DECIMAL {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32_Foundation.DECIMAL>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32_Foundation.DECIMAL>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Variant == "on" && (Windows_Win32_System_Com == "on" && Windows_Win32_System_Com == "on" && Windows_Win32_System_Ole == "on" && Windows_Win32_System_Ole == "on")]
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct {
    public var vt: UInt16 = 0
    public var wReserved1: UInt16 = 0
    public var wReserved2: UInt16 = 0
    public var wReserved3: UInt16 = 0
    public var Anonymous: VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union = VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Variant == "on" && (Windows_Win32_System_Com == "on" && Windows_Win32_System_Com == "on" && Windows_Win32_System_Ole == "on" && Windows_Win32_System_Ole == "on")]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0)

    public mut prop llVal: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop lVal: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop bVal: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt8(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt8(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop iVal: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fltVal: Float32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadFloat32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteFloat32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dblVal: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadFloat64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteFloat64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop boolVal: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop __OBSOLETE__VARIANT_BOOL: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop scode: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cyVal: Win32__impl_l0.CY {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.CY>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.CY>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop date: Float64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadFloat64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteFloat64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop bstrVal: CPointer<UInt16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop punkVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pdispVal: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop parray: CPointer<Win32__impl_l1.SAFEARRAY> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l1.SAFEARRAY>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l1.SAFEARRAY>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pbVal: CPointer<UInt8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop piVal: CPointer<Int16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop plVal: CPointer<Int32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pllVal: CPointer<Int64> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int64>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int64>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pfltVal: CPointer<Float32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Float32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Float32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pdblVal: CPointer<Float64> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Float64>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Float64>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pboolVal: CPointer<Int16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop __OBSOLETE__VARIANT_PBOOL: CPointer<Int16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pscode: CPointer<Int32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pcyVal: CPointer<Win32__impl_l0.CY> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.CY>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.CY>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pdate: CPointer<Float64> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Float64>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Float64>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pbstrVal: CPointer<CPointer<UInt16>> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<UInt16>>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<UInt16>>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ppunkVal: CPointer<CPointer<Unit>> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ppdispVal: CPointer<CPointer<Unit>> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<Unit>>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pparray: CPointer<CPointer<Win32__impl_l1.SAFEARRAY>> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<Win32__impl_l1.SAFEARRAY>>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<Win32__impl_l1.SAFEARRAY>>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pvarVal: CPointer<VARIANT> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<VARIANT>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<VARIANT>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop byref: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cVal: Int8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt8(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt8(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uiVal: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ulVal: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ullVal: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop intVal: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uintVal: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pdecVal: CPointer<Win32_Foundation.DECIMAL> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32_Foundation.DECIMAL>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32_Foundation.DECIMAL>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pcVal: CPointer<UInt8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop puiVal: CPointer<UInt16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pulVal: CPointer<UInt32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pullVal: CPointer<UInt64> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt64>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt64>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pintVal: CPointer<Int32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop puintVal: CPointer<UInt32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt32>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt32>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Variant == "on" && (Windows_Win32_System_Com == "on" && Windows_Win32_System_Com == "on" && Windows_Win32_System_Ole == "on" && Windows_Win32_System_Ole == "on")]
@C
public struct VARIANT__Anonymous_e__Union__Anonymous_e__Struct__Anonymous_e__Union__Anonymous_e__Struct {
    public var pvRecord: CPointer<Unit> = CPointer<Unit>()
    public var pRecInfo: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}
