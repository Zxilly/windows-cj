// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.Win32.Devices.Usb

import windows_interface.*
import windows_result.*
import windows_polyfill.*
import windows_sys.Win32.Foundation as Win32_Foundation
import windows_sys.Win32.System.IO as Win32_System_IO
import windows_sys.Win32.Foundation.*


@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct ALTERNATE_INTERFACE {
    public var InterfaceNumber: UInt16 = 0
    public var AlternateInterfaceNumber: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct BM_REQUEST_TYPE {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)

    public func s(): BM_REQUEST_TYPE__BM {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<BM_REQUEST_TYPE__BM>(p)
            return ptr.read()
        }
    }

    public mut func set_s(val: BM_REQUEST_TYPE__BM): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<BM_REQUEST_TYPE__BM>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func b(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt8(base, 0)
        }
    }

    public mut func set_b(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct BM_REQUEST_TYPE__BM {
    public var _bitfield: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct CHANNEL_INFO {
    public var EventChannelSize: UInt32 = 0
    public var uReadDataAlignment: UInt32 = 0
    public var uWriteDataAlignment: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct DEVICE_DESCRIPTOR {
    public var usVendorId: UInt16 = 0
    public var usProductId: UInt16 = 0
    public var usBcdDevice: UInt16 = 0
    public var usLanguageId: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct DRV_VERSION {
    public var major: UInt32 = 0
    public var minor: UInt32 = 0
    public var internal: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct EUSB2_ISOCH_ENDPOINT_COMPANION_DESCRIPTOR {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func wMaxPacketSize(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_wMaxPacketSize(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func dwBytesPerInterval(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_dwBytesPerInterval(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct HCD_ISO_STAT_COUNTERS {
    public var _raw: VArray<UInt8, $72> = VArray<UInt8, $72>(repeat: 0u8)

    public func lateUrbs(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set_lateUrbs(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func doubleBufferedPackets(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_doubleBufferedPackets(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func transfersCF_5ms(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_transfersCF_5ms(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func transfersCF_2ms(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 6)
        }
    }

    public mut func set_transfersCF_2ms(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 6, val)
            _raw = rawStorage
        }
    }

    public func transfersCF_1ms(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 8)
        }
    }

    public mut func set_transfersCF_1ms(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 8, val)
            _raw = rawStorage
        }
    }

    public func maxInterruptLatency(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 10)
        }
    }

    public mut func set_maxInterruptLatency(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 10, val)
            _raw = rawStorage
        }
    }

    public func badStartFrame(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 12)
        }
    }

    public mut func set_badStartFrame(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 12, val)
            _raw = rawStorage
        }
    }

    public func staleUrbs(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 14)
        }
    }

    public mut func set_staleUrbs(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 14, val)
            _raw = rawStorage
        }
    }

    public func isoPacketNotAccesed(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 16)
        }
    }

    public mut func set_isoPacketNotAccesed(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 16, val)
            _raw = rawStorage
        }
    }

    public func isoPacketHWError(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 18)
        }
    }

    public mut func set_isoPacketHWError(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 18, val)
            _raw = rawStorage
        }
    }

    public func smallestUrbPacketCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 20)
        }
    }

    public mut func set_smallestUrbPacketCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 20, val)
            _raw = rawStorage
        }
    }

    public func largestUrbPacketCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 22)
        }
    }

    public mut func set_largestUrbPacketCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 22, val)
            _raw = rawStorage
        }
    }

    public func isoCRC_Error(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 24)
        }
    }

    public mut func set_isoCRC_Error(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 24, val)
            _raw = rawStorage
        }
    }

    public func isoOVERRUN_Error(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 26)
        }
    }

    public mut func set_isoOVERRUN_Error(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 26, val)
            _raw = rawStorage
        }
    }

    public func isoINTERNAL_Error(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 28)
        }
    }

    public mut func set_isoINTERNAL_Error(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 28, val)
            _raw = rawStorage
        }
    }

    public func isoUNKNOWN_Error(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 30)
        }
    }

    public mut func set_isoUNKNOWN_Error(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 30, val)
            _raw = rawStorage
        }
    }

    public func isoBytesTransferred(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 32)
        }
    }

    public mut func set_isoBytesTransferred(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 32, val)
            _raw = rawStorage
        }
    }

    public func lateMissedCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 36)
        }
    }

    public mut func set_lateMissedCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 36, val)
            _raw = rawStorage
        }
    }

    public func hwIsoMissedCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 38)
        }
    }

    public mut func set_hwIsoMissedCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 38, val)
            _raw = rawStorage
        }
    }

    // packed field `Reserved7`: type `VArray<UInt32, $8>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct HCD_STAT_COUNTERS {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public func bytesTransferred(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_bytesTransferred(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func isoMissedCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_isoMissedCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func dataOverrunErrorCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 6)
        }
    }

    public mut func set_dataOverrunErrorCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 6, val)
            _raw = rawStorage
        }
    }

    public func crcErrorCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 8)
        }
    }

    public mut func set_crcErrorCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 8, val)
            _raw = rawStorage
        }
    }

    public func scheduleOverrunCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 10)
        }
    }

    public mut func set_scheduleOverrunCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 10, val)
            _raw = rawStorage
        }
    }

    public func timeoutErrorCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 12)
        }
    }

    public mut func set_timeoutErrorCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 12, val)
            _raw = rawStorage
        }
    }

    public func internalHcErrorCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 14)
        }
    }

    public mut func set_internalHcErrorCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 14, val)
            _raw = rawStorage
        }
    }

    public func bufferOverrunErrorCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 16)
        }
    }

    public mut func set_bufferOverrunErrorCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 16, val)
            _raw = rawStorage
        }
    }

    public func swErrorCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 18)
        }
    }

    public mut func set_swErrorCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 18, val)
            _raw = rawStorage
        }
    }

    public func stallPidCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 20)
        }
    }

    public mut func set_stallPidCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 20, val)
            _raw = rawStorage
        }
    }

    public func portDisableCount(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 22)
        }
    }

    public mut func set_portDisableCount(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 22, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct HCD_STAT_INFORMATION_1 {
    public var _raw: VArray<UInt8, $44> = VArray<UInt8, $44>(repeat: 0u8)

    public func reserved1(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_reserved1(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func reserved2(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_reserved2(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func resetCounters(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_resetCounters(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func timeRead(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 12)
        }
    }

    public mut func set_timeRead(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 12, val)
            _raw = rawStorage
        }
    }

    // packed field `Counters`: type `HCD_STAT_COUNTERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct HCD_STAT_INFORMATION_2 {
    public var _raw: VArray<UInt8, $120> = VArray<UInt8, $120>(repeat: 0u8)

    public func reserved1(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_reserved1(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func reserved2(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_reserved2(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func resetCounters(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_resetCounters(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func timeRead(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 12)
        }
    }

    public mut func set_timeRead(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 12, val)
            _raw = rawStorage
        }
    }

    public func lockedMemoryUsed(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 20)
        }
    }

    public mut func set_lockedMemoryUsed(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 20, val)
            _raw = rawStorage
        }
    }

    // packed field `Counters`: type `HCD_STAT_COUNTERS` not supported by polyfill; skipped

    // packed field `IsoCounters`: type `HCD_ISO_STAT_COUNTERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct HUB_DEVICE_CONFIG_INFO {
    public var _raw: VArray<UInt8, $172> = VArray<UInt8, $172>(repeat: 0u8)

    public func version(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_version(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func length(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_length(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `HubFlags`: type `USB_HUB_CAP_FLAGS` not supported by polyfill; skipped

    // packed field `HardwareIds`: type `USB_ID_STRING` not supported by polyfill; skipped

    // packed field `CompatibleIds`: type `USB_ID_STRING` not supported by polyfill; skipped

    // packed field `DeviceDescription`: type `USB_ID_STRING` not supported by polyfill; skipped

    // packed field `Reserved`: type `VArray<UInt32, $19>` not supported by polyfill; skipped

    // packed field `UxdSettings`: type `USB_HUB_DEVICE_UXD_SETTINGS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct IO_BLOCK {
    public var uOffset: UInt32 = 0
    public var uLength: UInt32 = 0
    public var pbyData: CPointer<UInt8> = CPointer<UInt8>()
    public var uIndex: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct IO_BLOCK_EX {
    public var uOffset: UInt32 = 0
    public var uLength: UInt32 = 0
    public var pbyData: CPointer<UInt8> = CPointer<UInt8>()
    public var uIndex: UInt32 = 0
    public var bRequest: UInt8 = 0
    public var bmRequestType: UInt8 = 0
    public var fTransferDirectionIn: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct OS_STRING {
    public var bLength: UInt8 = 0
    public var bDescriptorType: UInt8 = 0
    public var MicrosoftString: VArray<UInt16, $7> = VArray<UInt16, $7>(repeat: 0)
    public var bVendorCode: UInt8 = 0
    public var Anonymous: OS_STRING__Anonymous_e__Union = OS_STRING__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct OS_STRING__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)

    public func bPad(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt8(base, 0)
        }
    }

    public mut func set_bPad(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bFlags(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt8(base, 0)
        }
    }

    public mut func set_bFlags(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct PACKET_PARAMETERS {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    public func deviceAddress(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_deviceAddress(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func endpointAddress(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_endpointAddress(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func maximumPacketSize(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_maximumPacketSize(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func timeout(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_timeout(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func flags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_flags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func dataLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_dataLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func hubDeviceAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 16)
        }
    }

    public mut func set_hubDeviceAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 16, val)
            _raw = rawStorage
        }
    }

    public func portTTNumber(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 18)
        }
    }

    public mut func set_portTTNumber(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 18, val)
            _raw = rawStorage
        }
    }

    public func errorCount(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 20)
        }
    }

    public mut func set_errorCount(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 20, val)
            _raw = rawStorage
        }
    }

    // packed field `Pad`: type `VArray<UInt8, $3>` not supported by polyfill; skipped

    public func usbdStatusCode(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 24)
        }
    }

    public mut func set_usbdStatusCode(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 24, val)
            _raw = rawStorage
        }
    }

    // packed field `Data`: type `VArray<UInt8, $4>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct PIPE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: PIPE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: PIPE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let EVENT_PIPE: PIPE_TYPE = PIPE_TYPE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let READ_DATA_PIPE: PIPE_TYPE = PIPE_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let WRITE_DATA_PIPE: PIPE_TYPE = PIPE_TYPE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let ALL_PIPE: PIPE_TYPE = PIPE_TYPE(3)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct RAW_PIPE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: RAW_PIPE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: RAW_PIPE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let USBSCAN_PIPE_CONTROL: RAW_PIPE_TYPE = RAW_PIPE_TYPE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let USBSCAN_PIPE_ISOCHRONOUS: RAW_PIPE_TYPE = RAW_PIPE_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let USBSCAN_PIPE_BULK: RAW_PIPE_TYPE = RAW_PIPE_TYPE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let USBSCAN_PIPE_INTERRUPT: RAW_PIPE_TYPE = RAW_PIPE_TYPE(3)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct RAW_RESET_PORT_PARAMETERS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func portNumber(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set_portNumber(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func portStatus(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_portStatus(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct RAW_ROOTPORT_FEATURE {
    public var _raw: VArray<UInt8, $6> = VArray<UInt8, $6>(repeat: 0u8)

    public func portNumber(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set_portNumber(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func portFeature(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_portFeature(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func portStatus(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_portStatus(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct RAW_ROOTPORT_PARAMETERS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func portNumber(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set_portNumber(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func portStatus(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_portStatus(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct URB {
    public var Anonymous: URB__Anonymous_e__Union = URB__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct URB__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $152> = VArray<UInt8, $152>(repeat: 0)

    public func urbHeader(): _URB_HEADER {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_HEADER>(p)
            return ptr.read()
        }
    }

    public mut func set_urbHeader(val: _URB_HEADER): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_HEADER>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbSelectInterface(): _URB_SELECT_INTERFACE {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_SELECT_INTERFACE>(p)
            return ptr.read()
        }
    }

    public mut func set_urbSelectInterface(val: _URB_SELECT_INTERFACE): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_SELECT_INTERFACE>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbSelectConfiguration(): _URB_SELECT_CONFIGURATION {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_SELECT_CONFIGURATION>(p)
            return ptr.read()
        }
    }

    public mut func set_urbSelectConfiguration(val: _URB_SELECT_CONFIGURATION): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_SELECT_CONFIGURATION>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbPipeRequest(): _URB_PIPE_REQUEST {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_PIPE_REQUEST>(p)
            return ptr.read()
        }
    }

    public mut func set_urbPipeRequest(val: _URB_PIPE_REQUEST): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_PIPE_REQUEST>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbFrameLengthControl(): _URB_FRAME_LENGTH_CONTROL {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_FRAME_LENGTH_CONTROL>(p)
            return ptr.read()
        }
    }

    public mut func set_urbFrameLengthControl(val: _URB_FRAME_LENGTH_CONTROL): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_FRAME_LENGTH_CONTROL>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbGetFrameLength(): _URB_GET_FRAME_LENGTH {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_GET_FRAME_LENGTH>(p)
            return ptr.read()
        }
    }

    public mut func set_urbGetFrameLength(val: _URB_GET_FRAME_LENGTH): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_GET_FRAME_LENGTH>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbSetFrameLength(): _URB_SET_FRAME_LENGTH {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_SET_FRAME_LENGTH>(p)
            return ptr.read()
        }
    }

    public mut func set_urbSetFrameLength(val: _URB_SET_FRAME_LENGTH): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_SET_FRAME_LENGTH>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbGetCurrentFrameNumber(): _URB_GET_CURRENT_FRAME_NUMBER {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_GET_CURRENT_FRAME_NUMBER>(p)
            return ptr.read()
        }
    }

    public mut func set_urbGetCurrentFrameNumber(val: _URB_GET_CURRENT_FRAME_NUMBER): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_GET_CURRENT_FRAME_NUMBER>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbControlTransfer(): _URB_CONTROL_TRANSFER {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_TRANSFER>(p)
            return ptr.read()
        }
    }

    public mut func set_urbControlTransfer(val: _URB_CONTROL_TRANSFER): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_TRANSFER>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbControlTransferEx(): _URB_CONTROL_TRANSFER_EX {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_TRANSFER_EX>(p)
            return ptr.read()
        }
    }

    public mut func set_urbControlTransferEx(val: _URB_CONTROL_TRANSFER_EX): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_TRANSFER_EX>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbBulkOrInterruptTransfer(): _URB_BULK_OR_INTERRUPT_TRANSFER {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_BULK_OR_INTERRUPT_TRANSFER>(p)
            return ptr.read()
        }
    }

    public mut func set_urbBulkOrInterruptTransfer(val: _URB_BULK_OR_INTERRUPT_TRANSFER): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_BULK_OR_INTERRUPT_TRANSFER>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbIsochronousTransfer(): _URB_ISOCH_TRANSFER {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_ISOCH_TRANSFER>(p)
            return ptr.read()
        }
    }

    public mut func set_urbIsochronousTransfer(val: _URB_ISOCH_TRANSFER): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_ISOCH_TRANSFER>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbControlDescriptorRequest(): _URB_CONTROL_DESCRIPTOR_REQUEST {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_DESCRIPTOR_REQUEST>(p)
            return ptr.read()
        }
    }

    public mut func set_urbControlDescriptorRequest(val: _URB_CONTROL_DESCRIPTOR_REQUEST): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_DESCRIPTOR_REQUEST>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbControlGetStatusRequest(): _URB_CONTROL_GET_STATUS_REQUEST {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_GET_STATUS_REQUEST>(p)
            return ptr.read()
        }
    }

    public mut func set_urbControlGetStatusRequest(val: _URB_CONTROL_GET_STATUS_REQUEST): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_GET_STATUS_REQUEST>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbControlFeatureRequest(): _URB_CONTROL_FEATURE_REQUEST {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_FEATURE_REQUEST>(p)
            return ptr.read()
        }
    }

    public mut func set_urbControlFeatureRequest(val: _URB_CONTROL_FEATURE_REQUEST): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_FEATURE_REQUEST>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbControlVendorClassRequest(): _URB_CONTROL_VENDOR_OR_CLASS_REQUEST {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_VENDOR_OR_CLASS_REQUEST>(p)
            return ptr.read()
        }
    }

    public mut func set_urbControlVendorClassRequest(val: _URB_CONTROL_VENDOR_OR_CLASS_REQUEST): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_VENDOR_OR_CLASS_REQUEST>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbControlGetInterfaceRequest(): _URB_CONTROL_GET_INTERFACE_REQUEST {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_GET_INTERFACE_REQUEST>(p)
            return ptr.read()
        }
    }

    public mut func set_urbControlGetInterfaceRequest(val: _URB_CONTROL_GET_INTERFACE_REQUEST): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_GET_INTERFACE_REQUEST>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbControlGetConfigurationRequest(): _URB_CONTROL_GET_CONFIGURATION_REQUEST {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_GET_CONFIGURATION_REQUEST>(p)
            return ptr.read()
        }
    }

    public mut func set_urbControlGetConfigurationRequest(val: _URB_CONTROL_GET_CONFIGURATION_REQUEST): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_CONTROL_GET_CONFIGURATION_REQUEST>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbOSFeatureDescriptorRequest(): _URB_OS_FEATURE_DESCRIPTOR_REQUEST {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_OS_FEATURE_DESCRIPTOR_REQUEST>(p)
            return ptr.read()
        }
    }

    public mut func set_urbOSFeatureDescriptorRequest(val: _URB_OS_FEATURE_DESCRIPTOR_REQUEST): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_OS_FEATURE_DESCRIPTOR_REQUEST>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbOpenStaticStreams(): _URB_OPEN_STATIC_STREAMS {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_OPEN_STATIC_STREAMS>(p)
            return ptr.read()
        }
    }

    public mut func set_urbOpenStaticStreams(val: _URB_OPEN_STATIC_STREAMS): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_OPEN_STATIC_STREAMS>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func urbGetIsochPipeTransferPathDelays(): _URB_GET_ISOCH_PIPE_TRANSFER_PATH_DELAYS {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_GET_ISOCH_PIPE_TRANSFER_PATH_DELAYS>(p)
            return ptr.read()
        }
    }

    public mut func set_urbGetIsochPipeTransferPathDelays(val: _URB_GET_ISOCH_PIPE_TRANSFER_PATH_DELAYS): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<_URB_GET_ISOCH_PIPE_TRANSFER_PATH_DELAYS>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB4_CONFIG_SPACE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB4_CONFIG_SPACE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB4_CONFIG_SPACE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let USB4PathConfigurationSpace: USB4_CONFIG_SPACE_TYPE = USB4_CONFIG_SPACE_TYPE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let USB4AdapterConfigurationSpace: USB4_CONFIG_SPACE_TYPE = USB4_CONFIG_SPACE_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let USB4RouterConfigurationSpace: USB4_CONFIG_SPACE_TYPE = USB4_CONFIG_SPACE_TYPE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let USB4CounterConfigurationSpace: USB4_CONFIG_SPACE_TYPE = USB4_CONFIG_SPACE_TYPE(3)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB4_HRD_DEBUG_READ_CONFIGURATION_SPACE_INPUT {
    public var _raw: VArray<UInt8, $21> = VArray<UInt8, $21>(repeat: 0u8)

    // packed field `Route`: type `USB4_HRD_DEBUG_ROUTE_STRING` not supported by polyfill; skipped

    public func adapterNumber(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 8)
        }
    }

    public mut func set_adapterNumber(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 8, val)
            _raw = rawStorage
        }
    }

    // packed field `ConfigurationSpaceType`: type `USB4_CONFIG_SPACE_TYPE` not supported by polyfill; skipped

    public func dwOffset(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 13)
        }
    }

    public mut func set_dwOffset(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 13, val)
            _raw = rawStorage
        }
    }

    public func dwLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 17)
        }
    }

    public mut func set_dwLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 17, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB4_HRD_DEBUG_READ_CONFIGURATION_SPACE_OUTPUT {
    public var _raw: VArray<UInt8, $244> = VArray<UInt8, $244>(repeat: 0u8)

    // packed field `Usb4Status`: type `USB4_STATUS` not supported by polyfill; skipped

    // packed field `Data`: type `VArray<UInt32, $60>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB4_HRD_DEBUG_ROUTE_STRING {
    public var Depth: UInt8 = 0
    public var Route: VArray<UInt8, $7> = VArray<UInt8, $7>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB4_STATUS {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB4_STATUS): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB4_STATUS): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrConn: USB4_STATUS = USB4_STATUS(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrLink: USB4_STATUS = USB4_STATUS(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrAddr: USB4_STATUS = USB4_STATUS(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrAdp: USB4_STATUS = USB4_STATUS(4)

@When[Windows_Win32_Devices_Usb == "on"]
public let HpAck: USB4_STATUS = USB4_STATUS(7)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrEnum: USB4_STATUS = USB4_STATUS(8)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrNua: USB4_STATUS = USB4_STATUS(9)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrLen: USB4_STATUS = USB4_STATUS(11)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrHec: USB4_STATUS = USB4_STATUS(12)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrFc: USB4_STATUS = USB4_STATUS(13)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrPlug: USB4_STATUS = USB4_STATUS(14)

@When[Windows_Win32_Devices_Usb == "on"]
public let ErrLock: USB4_STATUS = USB4_STATUS(15)

@When[Windows_Win32_Devices_Usb == "on"]
public let DpBw: USB4_STATUS = USB4_STATUS(32)

@When[Windows_Win32_Devices_Usb == "on"]
public let RopCmplt: USB4_STATUS = USB4_STATUS(33)

@When[Windows_Win32_Devices_Usb == "on"]
public let PopCmplt: USB4_STATUS = USB4_STATUS(34)

@When[Windows_Win32_Devices_Usb == "on"]
public let PcieWake: USB4_STATUS = USB4_STATUS(35)

@When[Windows_Win32_Devices_Usb == "on"]
public let DpConChange: USB4_STATUS = USB4_STATUS(36)

@When[Windows_Win32_Devices_Usb == "on"]
public let DpTxDiscovery: USB4_STATUS = USB4_STATUS(37)

@When[Windows_Win32_Devices_Usb == "on"]
public let LinkRecovery: USB4_STATUS = USB4_STATUS(38)

@When[Windows_Win32_Devices_Usb == "on"]
public let AsymLink: USB4_STATUS = USB4_STATUS(39)

@When[Windows_Win32_Devices_Usb == "on"]
public let PollingSkipped: USB4_STATUS = USB4_STATUS(252)

@When[Windows_Win32_Devices_Usb == "on"]
public let PollingTimeout: USB4_STATUS = USB4_STATUS(253)

@When[Windows_Win32_Devices_Usb == "on"]
public let StatusSuccess: USB4_STATUS = USB4_STATUS(254)

@When[Windows_Win32_Devices_Usb == "on"]
public let StatusUnknown: USB4_STATUS = USB4_STATUS(255)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_DEVICE_INFORMATION {
    public var OffsetNext: UInt32 = 0
    public var UsbdDeviceHandle: CPointer<Unit> = CPointer<Unit>()
    public var DeviceDescriptor: USB_DEVICE_DESCRIPTOR = USB_DEVICE_DESCRIPTOR()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_ENDPOINT_OFFLOAD_INFORMATION {
    public var _raw: VArray<UInt8, $130> = VArray<UInt8, $130>(repeat: 0u8)

    public func size(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_size(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func endpointAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_endpointAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func resourceId(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 6)
        }
    }

    public mut func set_resourceId(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 6, val)
            _raw = rawStorage
        }
    }

    // packed field `Mode`: type `USBD_ENDPOINT_OFFLOAD_MODE` not supported by polyfill; skipped

    public func _bitfield1(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 14)
        }
    }

    public mut func set__bitfield1(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 14, val)
            _raw = rawStorage
        }
    }

    public func _bitfield2(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 18)
        }
    }

    public mut func set__bitfield2(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 18, val)
            _raw = rawStorage
        }
    }

    public func transferSegmentLA(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 22)
        }
    }

    public mut func set_transferSegmentLA(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 22, val)
            _raw = rawStorage
        }
    }

    // packed field `TransferSegmentVA`: type `CPointer<Unit>` not supported by polyfill; skipped

    // packed field `TransferRingSize`: type `UIntNative` not supported by polyfill; skipped

    public func transferRingInitialCycleBit(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 46)
        }
    }

    public mut func set_transferRingInitialCycleBit(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 46, val)
            _raw = rawStorage
        }
    }

    public func messageNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 50)
        }
    }

    public mut func set_messageNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 50, val)
            _raw = rawStorage
        }
    }

    public func eventRingSegmentLA(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 54)
        }
    }

    public mut func set_eventRingSegmentLA(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 54, val)
            _raw = rawStorage
        }
    }

    // packed field `EventRingSegmentVA`: type `CPointer<Unit>` not supported by polyfill; skipped

    // packed field `EventRingSize`: type `UIntNative` not supported by polyfill; skipped

    public func eventRingInitialCycleBit(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 78)
        }
    }

    public mut func set_eventRingInitialCycleBit(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 78, val)
            _raw = rawStorage
        }
    }

    public func clientTransferRingSegmentPAIn(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 82)
        }
    }

    public mut func set_clientTransferRingSegmentPAIn(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 82, val)
            _raw = rawStorage
        }
    }

    // packed field `ClientTransferRingSizeIn`: type `UIntNative` not supported by polyfill; skipped

    public func clientDataBufferPAIn(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 98)
        }
    }

    public mut func set_clientDataBufferPAIn(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 98, val)
            _raw = rawStorage
        }
    }

    // packed field `ClientDataBufferSizeIn`: type `UIntNative` not supported by polyfill; skipped

    public func clientDataBufferLAOut(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 114)
        }
    }

    public mut func set_clientDataBufferLAOut(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 114, val)
            _raw = rawStorage
        }
    }

    // packed field `ClientDataBufferVAOut`: type `CPointer<Unit>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_ENDPOINT_OFFLOAD_INFORMATION_V1 {
    public var _raw: VArray<UInt8, $82> = VArray<UInt8, $82>(repeat: 0u8)

    public func size(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_size(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func endpointAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_endpointAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func resourceId(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 6)
        }
    }

    public mut func set_resourceId(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 6, val)
            _raw = rawStorage
        }
    }

    // packed field `Mode`: type `USBD_ENDPOINT_OFFLOAD_MODE` not supported by polyfill; skipped

    public func _bitfield1(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 14)
        }
    }

    public mut func set__bitfield1(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 14, val)
            _raw = rawStorage
        }
    }

    public func _bitfield2(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 18)
        }
    }

    public mut func set__bitfield2(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 18, val)
            _raw = rawStorage
        }
    }

    public func transferSegmentLA(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 22)
        }
    }

    public mut func set_transferSegmentLA(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 22, val)
            _raw = rawStorage
        }
    }

    // packed field `TransferSegmentVA`: type `CPointer<Unit>` not supported by polyfill; skipped

    // packed field `TransferRingSize`: type `UIntNative` not supported by polyfill; skipped

    public func transferRingInitialCycleBit(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 46)
        }
    }

    public mut func set_transferRingInitialCycleBit(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 46, val)
            _raw = rawStorage
        }
    }

    public func messageNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 50)
        }
    }

    public mut func set_messageNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 50, val)
            _raw = rawStorage
        }
    }

    public func eventRingSegmentLA(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 54)
        }
    }

    public mut func set_eventRingSegmentLA(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 54, val)
            _raw = rawStorage
        }
    }

    // packed field `EventRingSegmentVA`: type `CPointer<Unit>` not supported by polyfill; skipped

    // packed field `EventRingSize`: type `UIntNative` not supported by polyfill; skipped

    public func eventRingInitialCycleBit(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 78)
        }
    }

    public mut func set_eventRingInitialCycleBit(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 78, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_ENDPOINT_OFFLOAD_MODE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USBD_ENDPOINT_OFFLOAD_MODE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USBD_ENDPOINT_OFFLOAD_MODE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbdEndpointOffloadModeNotSupported: USBD_ENDPOINT_OFFLOAD_MODE = USBD_ENDPOINT_OFFLOAD_MODE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbdEndpointOffloadSoftwareAssisted: USBD_ENDPOINT_OFFLOAD_MODE = USBD_ENDPOINT_OFFLOAD_MODE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbdEndpointOffloadHardwareAssisted: USBD_ENDPOINT_OFFLOAD_MODE = USBD_ENDPOINT_OFFLOAD_MODE(2)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_INTERFACE_INFORMATION {
    public var Length: UInt16 = 0
    public var InterfaceNumber: UInt8 = 0
    public var AlternateSetting: UInt8 = 0
    public var Class: UInt8 = 0
    public var SubClass: UInt8 = 0
    public var Protocol: UInt8 = 0
    public var Reserved: UInt8 = 0
    public var InterfaceHandle: CPointer<Unit> = CPointer<Unit>()
    public var NumberOfPipes: UInt32 = 0
    public var Pipes: VArray<USBD_PIPE_INFORMATION, $1> = VArray<USBD_PIPE_INFORMATION, $1>(repeat: USBD_PIPE_INFORMATION())
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_ISO_PACKET_DESCRIPTOR {
    public var Offset: UInt32 = 0
    public var Length: UInt32 = 0
    public var Status: Int32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_PIPE_INFORMATION {
    public var MaximumPacketSize: UInt16 = 0
    public var EndpointAddress: UInt8 = 0
    public var Interval: UInt8 = 0
    public var PipeType: USBD_PIPE_TYPE = USBD_PIPE_TYPE()
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var MaximumTransferSize: UInt32 = 0
    public var PipeFlags: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_PIPE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USBD_PIPE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USBD_PIPE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbdPipeTypeControl: USBD_PIPE_TYPE = USBD_PIPE_TYPE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbdPipeTypeIsochronous: USBD_PIPE_TYPE = USBD_PIPE_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbdPipeTypeBulk: USBD_PIPE_TYPE = USBD_PIPE_TYPE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbdPipeTypeInterrupt: USBD_PIPE_TYPE = USBD_PIPE_TYPE(3)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_STREAM_INFORMATION {
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var StreamID: UInt32 = 0
    public var MaximumTransferSize: UInt32 = 0
    public var PipeFlags: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBD_VERSION_INFORMATION {
    public var USBDI_Version: UInt32 = 0
    public var Supported_USB_Version: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_BUS_CONFIGURATION_INFO {
    public var ConfigurationName: VArray<UInt16, $40> = VArray<UInt16, $40>(repeat: 0)
    public var IsCurrent: Win32_Foundation.BOOLEAN = 0
    public var IsActive: Win32_Foundation.BOOLEAN = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_BUS_SPEED {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USBFN_BUS_SPEED): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USBFN_BUS_SPEED): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnBusSpeedLow: USBFN_BUS_SPEED = USBFN_BUS_SPEED(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnBusSpeedFull: USBFN_BUS_SPEED = USBFN_BUS_SPEED(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnBusSpeedHigh: USBFN_BUS_SPEED = USBFN_BUS_SPEED(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnBusSpeedSuper: USBFN_BUS_SPEED = USBFN_BUS_SPEED(3)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnBusSpeedMaximum: USBFN_BUS_SPEED = USBFN_BUS_SPEED(4)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_CLASS_INFORMATION_PACKET {
    public var FullSpeedClassInterface: USBFN_CLASS_INTERFACE = USBFN_CLASS_INTERFACE()
    public var HighSpeedClassInterface: USBFN_CLASS_INTERFACE = USBFN_CLASS_INTERFACE()
    public var InterfaceName: VArray<UInt16, $40> = VArray<UInt16, $40>(repeat: 0)
    public var InterfaceGuid: VArray<UInt16, $39> = VArray<UInt16, $39>(repeat: 0)
    public var HasInterfaceGuid: Win32_Foundation.BOOLEAN = 0
    public var SuperSpeedClassInterface: USBFN_CLASS_INTERFACE = USBFN_CLASS_INTERFACE()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_CLASS_INFORMATION_PACKET_EX {
    public var FullSpeedClassInterfaceEx: USBFN_CLASS_INTERFACE_EX = USBFN_CLASS_INTERFACE_EX()
    public var HighSpeedClassInterfaceEx: USBFN_CLASS_INTERFACE_EX = USBFN_CLASS_INTERFACE_EX()
    public var SuperSpeedClassInterfaceEx: USBFN_CLASS_INTERFACE_EX = USBFN_CLASS_INTERFACE_EX()
    public var InterfaceName: VArray<UInt16, $40> = VArray<UInt16, $40>(repeat: 0)
    public var InterfaceGuid: VArray<UInt16, $39> = VArray<UInt16, $39>(repeat: 0)
    public var HasInterfaceGuid: Win32_Foundation.BOOLEAN = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_CLASS_INTERFACE {
    public var InterfaceNumber: UInt8 = 0
    public var PipeCount: UInt8 = 0
    public var PipeArr: VArray<USBFN_PIPE_INFORMATION, $16> = VArray<USBFN_PIPE_INFORMATION, $16>(repeat: USBFN_PIPE_INFORMATION())
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_CLASS_INTERFACE_EX {
    public var BaseInterfaceNumber: UInt8 = 0
    public var InterfaceCount: UInt8 = 0
    public var PipeCount: UInt8 = 0
    public var PipeArr: VArray<USBFN_PIPE_INFORMATION, $16> = VArray<USBFN_PIPE_INFORMATION, $16>(repeat: USBFN_PIPE_INFORMATION())
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_DEVICE_STATE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USBFN_DEVICE_STATE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USBFN_DEVICE_STATE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDeviceStateMinimum: USBFN_DEVICE_STATE = USBFN_DEVICE_STATE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDeviceStateAttached: USBFN_DEVICE_STATE = USBFN_DEVICE_STATE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDeviceStateDefault: USBFN_DEVICE_STATE = USBFN_DEVICE_STATE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDeviceStateDetached: USBFN_DEVICE_STATE = USBFN_DEVICE_STATE(3)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDeviceStateAddressed: USBFN_DEVICE_STATE = USBFN_DEVICE_STATE(4)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDeviceStateConfigured: USBFN_DEVICE_STATE = USBFN_DEVICE_STATE(5)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDeviceStateSuspended: USBFN_DEVICE_STATE = USBFN_DEVICE_STATE(6)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDeviceStateStateMaximum: USBFN_DEVICE_STATE = USBFN_DEVICE_STATE(7)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_DIRECTION {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USBFN_DIRECTION): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USBFN_DIRECTION): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDirectionMinimum: USBFN_DIRECTION = USBFN_DIRECTION(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDirectionIn: USBFN_DIRECTION = USBFN_DIRECTION(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDirectionOut: USBFN_DIRECTION = USBFN_DIRECTION(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDirectionTx: USBFN_DIRECTION = USBFN_DIRECTION(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDirectionRx: USBFN_DIRECTION = USBFN_DIRECTION(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDirectionMaximum: USBFN_DIRECTION = USBFN_DIRECTION(3)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_EVENT {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USBFN_EVENT): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USBFN_EVENT): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventMinimum: USBFN_EVENT = USBFN_EVENT(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventAttach: USBFN_EVENT = USBFN_EVENT(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventReset: USBFN_EVENT = USBFN_EVENT(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventDetach: USBFN_EVENT = USBFN_EVENT(3)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventSuspend: USBFN_EVENT = USBFN_EVENT(4)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventResume: USBFN_EVENT = USBFN_EVENT(5)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventSetupPacket: USBFN_EVENT = USBFN_EVENT(6)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventConfigured: USBFN_EVENT = USBFN_EVENT(7)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventUnConfigured: USBFN_EVENT = USBFN_EVENT(8)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventPortType: USBFN_EVENT = USBFN_EVENT(9)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventBusTearDown: USBFN_EVENT = USBFN_EVENT(10)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventSetInterface: USBFN_EVENT = USBFN_EVENT(11)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnEventMaximum: USBFN_EVENT = USBFN_EVENT(12)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_INTERFACE_INFO {
    public var InterfaceNumber: UInt8 = 0
    public var Speed: USBFN_BUS_SPEED = USBFN_BUS_SPEED()
    public var Size: UInt16 = 0
    public var InterfaceDescriptorSet: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_NOTIFICATION {
    public var Event: USBFN_EVENT = USBFN_EVENT()
    public var u: USBFN_NOTIFICATION__u_e__Union = USBFN_NOTIFICATION__u_e__Union()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USBFN_NOTIFICATION__u_e__Union {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0)

    public func busSpeed(): USBFN_BUS_SPEED {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USBFN_BUS_SPEED>(p)
            return ptr.read()
        }
    }

    public mut func set_busSpeed(val: USBFN_BUS_SPEED): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USBFN_BUS_SPEED>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func setupPacket(): USB_DEFAULT_PIPE_SETUP_PACKET {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEFAULT_PIPE_SETUP_PACKET>(p)
            return ptr.read()
        }
    }

    public mut func set_setupPacket(val: USB_DEFAULT_PIPE_SETUP_PACKET): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEFAULT_PIPE_SETUP_PACKET>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func configurationValue(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_configurationValue(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func portType(): USBFN_PORT_TYPE {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USBFN_PORT_TYPE>(p)
            return ptr.read()
        }
    }

    public mut func set_portType(val: USBFN_PORT_TYPE): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USBFN_PORT_TYPE>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func alternateInterface(): ALTERNATE_INTERFACE {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<ALTERNATE_INTERFACE>(p)
            return ptr.read()
        }
    }

    public mut func set_alternateInterface(val: ALTERNATE_INTERFACE): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<ALTERNATE_INTERFACE>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_PIPE_INFORMATION {
    public var EpDesc: USB_ENDPOINT_DESCRIPTOR = USB_ENDPOINT_DESCRIPTOR()
    public var PipeId: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_PORT_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USBFN_PORT_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USBFN_PORT_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnUnknownPort: USBFN_PORT_TYPE = USBFN_PORT_TYPE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnStandardDownstreamPort: USBFN_PORT_TYPE = USBFN_PORT_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnChargingDownstreamPort: USBFN_PORT_TYPE = USBFN_PORT_TYPE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnDedicatedChargingPort: USBFN_PORT_TYPE = USBFN_PORT_TYPE(3)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnInvalidDedicatedChargingPort: USBFN_PORT_TYPE = USBFN_PORT_TYPE(4)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnProprietaryDedicatedChargingPort: USBFN_PORT_TYPE = USBFN_PORT_TYPE(5)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbfnPortTypeMaximum: USBFN_PORT_TYPE = USBFN_PORT_TYPE(6)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBFN_USB_STRING {
    public var StringIndex: UInt8 = 0
    public var UsbString: VArray<UInt16, $255> = VArray<UInt16, $255>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBSCAN_GET_DESCRIPTOR {
    public var DescriptorType: UInt8 = 0
    public var Index: UInt8 = 0
    public var LanguageId: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBSCAN_PIPE_CONFIGURATION {
    public var NumberOfPipes: UInt32 = 0
    public var PipeInfo: VArray<USBSCAN_PIPE_INFORMATION, $8> = VArray<USBSCAN_PIPE_INFORMATION, $8>(repeat: USBSCAN_PIPE_INFORMATION())
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBSCAN_PIPE_INFORMATION {
    public var MaximumPacketSize: UInt16 = 0
    public var EndpointAddress: UInt8 = 0
    public var Interval: UInt8 = 0
    public var PipeType: RAW_PIPE_TYPE = RAW_PIPE_TYPE()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBSCAN_TIMEOUT {
    public var TimeoutRead: UInt32 = 0
    public var TimeoutWrite: UInt32 = 0
    public var TimeoutEvent: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_BANDWIDTH_INFO_REQUEST {
    public var _raw: VArray<UInt8, $60> = VArray<UInt8, $60>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `BandwidthInformation`: type `USB_BANDWIDTH_INFO` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_BUS_STATISTICS_0_REQUEST {
    public var _raw: VArray<UInt8, $72> = VArray<UInt8, $72>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `BusStatistics0`: type `USB_BUS_STATISTICS_0` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_CLOSE_RAW_DEVICE {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Parameters`: type `USB_CLOSE_RAW_DEVICE_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_CONTROLLER_INFO_0 {
    public var _raw: VArray<UInt8, $40> = VArray<UInt8, $40>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Info0`: type `USB_CONTROLLER_INFO_0` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_CONTROLLER_UNICODE_NAME {
    public var _raw: VArray<UInt8, $22> = VArray<UInt8, $22>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `UnicodeName`: type `USB_UNICODE_NAME` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_GET_DRIVER_VERSION {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Parameters`: type `USB_DRIVER_VERSION_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_GET_USB2HW_VERSION {
    public var _raw: VArray<UInt8, $17> = VArray<UInt8, $17>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Parameters`: type `USB_USB2HW_VERSION_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_OPEN_RAW_DEVICE {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Parameters`: type `USB_OPEN_RAW_DEVICE_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_PASS_THRU_REQUEST {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `PassThru`: type `USB_PASS_THRU_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_POWER_INFO_REQUEST {
    public var _raw: VArray<UInt8, $50> = VArray<UInt8, $50>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `PowerInformation`: type `USB_POWER_INFO` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_RAW_RESET_ROOT_PORT {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Parameters`: type `RAW_RESET_PORT_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_REFRESH_HCT_REG {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    public func flags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_flags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_REQUEST_HEADER {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    public func usbUserRequest(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_usbUserRequest(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `UsbUserStatusCode`: type `USB_USER_ERROR_CODE` not supported by polyfill; skipped

    public func requestBufferLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_requestBufferLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func actualBufferLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_actualBufferLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_ROOTPORT_FEATURE_REQUEST {
    public var _raw: VArray<UInt8, $22> = VArray<UInt8, $22>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Parameters`: type `RAW_ROOTPORT_FEATURE` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_ROOTPORT_PARAMETERS {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Parameters`: type `RAW_ROOTPORT_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_SEND_ONE_PACKET {
    public var _raw: VArray<UInt8, $48> = VArray<UInt8, $48>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `PacketParameters`: type `PACKET_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USBUSER_SEND_RAW_COMMAND {
    public var _raw: VArray<UInt8, $44> = VArray<UInt8, $44>(repeat: 0u8)

    // packed field `Header`: type `USBUSER_REQUEST_HEADER` not supported by polyfill; skipped

    // packed field `Parameters`: type `USB_SEND_RAW_COMMAND_PARAMETERS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_20_PORT_CHANGE {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_20_PORT_CHANGE__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_20_PORT_CHANGE__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_20_PORT_CHANGE__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_20_PORT_CHANGE__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_20_PORT_CHANGE__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_20_PORT_STATUS {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_20_PORT_STATUS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_20_PORT_STATUS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_20_PORT_STATUS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_20_PORT_STATUS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_20_PORT_STATUS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_30_HUB_DESCRIPTOR {
    public var _raw: VArray<UInt8, $12> = VArray<UInt8, $12>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bNumberOfPorts(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bNumberOfPorts(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func wHubCharacteristics(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 3)
        }
    }

    public mut func set_wHubCharacteristics(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 3, val)
            _raw = rawStorage
        }
    }

    public func bPowerOnToPowerGood(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 5)
        }
    }

    public mut func set_bPowerOnToPowerGood(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 5, val)
            _raw = rawStorage
        }
    }

    public func bHubControlCurrent(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_bHubControlCurrent(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }

    public func bHubHdrDecLat(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 7)
        }
    }

    public mut func set_bHubHdrDecLat(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 7, val)
            _raw = rawStorage
        }
    }

    public func wHubDelay(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 8)
        }
    }

    public mut func set_wHubDelay(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 8, val)
            _raw = rawStorage
        }
    }

    public func deviceRemovable(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 10)
        }
    }

    public mut func set_deviceRemovable(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 10, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_30_PORT_CHANGE {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_30_PORT_CHANGE__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_PORT_CHANGE__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_30_PORT_CHANGE__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_PORT_CHANGE__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_30_PORT_CHANGE__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_30_PORT_STATUS {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_30_PORT_STATUS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_PORT_STATUS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_30_PORT_STATUS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_PORT_STATUS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_30_PORT_STATUS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_ACQUIRE_INFO {
    public var _raw: VArray<UInt8, $10> = VArray<UInt8, $10>(repeat: 0u8)

    // packed field `NotificationType`: type `USB_NOTIFICATION_TYPE` not supported by polyfill; skipped

    public func totalSize(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_totalSize(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `Buffer`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_BANDWIDTH_INFO {
    public var _raw: VArray<UInt8, $44> = VArray<UInt8, $44>(repeat: 0u8)

    public func deviceCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_deviceCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func totalBusBandwidth(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_totalBusBandwidth(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func total32secBandwidth(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_total32secBandwidth(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func allocedBulkAndControl(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_allocedBulkAndControl(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func allocedIso(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_allocedIso(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }

    public func allocedInterrupt_1ms(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 20)
        }
    }

    public mut func set_allocedInterrupt_1ms(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 20, val)
            _raw = rawStorage
        }
    }

    public func allocedInterrupt_2ms(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 24)
        }
    }

    public mut func set_allocedInterrupt_2ms(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 24, val)
            _raw = rawStorage
        }
    }

    public func allocedInterrupt_4ms(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 28)
        }
    }

    public mut func set_allocedInterrupt_4ms(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 28, val)
            _raw = rawStorage
        }
    }

    public func allocedInterrupt_8ms(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 32)
        }
    }

    public mut func set_allocedInterrupt_8ms(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 32, val)
            _raw = rawStorage
        }
    }

    public func allocedInterrupt_16ms(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 36)
        }
    }

    public mut func set_allocedInterrupt_16ms(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 36, val)
            _raw = rawStorage
        }
    }

    public func allocedInterrupt_32ms(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 40)
        }
    }

    public mut func set_allocedInterrupt_32ms(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 40, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_BOS_DESCRIPTOR {
    public var _raw: VArray<UInt8, $5> = VArray<UInt8, $5>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func wTotalLength(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_wTotalLength(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bNumDeviceCaps(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 4)
        }
    }

    public mut func set_bNumDeviceCaps(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 4, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_BUS_NOTIFICATION {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    // packed field `NotificationType`: type `USB_NOTIFICATION_TYPE` not supported by polyfill; skipped

    public func totalBandwidth(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_totalBandwidth(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func consumedBandwidth(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_consumedBandwidth(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func controllerNameLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_controllerNameLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_BUS_STATISTICS_0 {
    public var _raw: VArray<UInt8, $56> = VArray<UInt8, $56>(repeat: 0u8)

    public func deviceCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_deviceCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func currentSystemTime(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 4)
        }
    }

    public mut func set_currentSystemTime(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 4, val)
            _raw = rawStorage
        }
    }

    public func currentUsbFrame(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_currentUsbFrame(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func bulkBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_bulkBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }

    public func isoBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 20)
        }
    }

    public mut func set_isoBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 20, val)
            _raw = rawStorage
        }
    }

    public func interruptBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 24)
        }
    }

    public mut func set_interruptBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 24, val)
            _raw = rawStorage
        }
    }

    public func controlDataBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 28)
        }
    }

    public mut func set_controlDataBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 28, val)
            _raw = rawStorage
        }
    }

    public func pciInterruptCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 32)
        }
    }

    public mut func set_pciInterruptCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 32, val)
            _raw = rawStorage
        }
    }

    public func hardResetCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 36)
        }
    }

    public mut func set_hardResetCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 36, val)
            _raw = rawStorage
        }
    }

    public func workerSignalCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 40)
        }
    }

    public mut func set_workerSignalCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 40, val)
            _raw = rawStorage
        }
    }

    public func commonBufferBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 44)
        }
    }

    public mut func set_commonBufferBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 44, val)
            _raw = rawStorage
        }
    }

    public func workerIdleTimeMs(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 48)
        }
    }

    public mut func set_workerIdleTimeMs(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 48, val)
            _raw = rawStorage
        }
    }

    // packed field `RootHubEnabled`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    public func rootHubDevicePowerState(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 53)
        }
    }

    public mut func set_rootHubDevicePowerState(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 53, val)
            _raw = rawStorage
        }
    }

    public func unused(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 54)
        }
    }

    public mut func set_unused(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 54, val)
            _raw = rawStorage
        }
    }

    public func nameIndex(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 55)
        }
    }

    public mut func set_nameIndex(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 55, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
public type USB_CHANGE_REGISTRATION_HANDLE = CPointer<Unit>

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CLOSE_RAW_DEVICE_PARAMETERS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func xxx(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_xxx(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_COMMON_DESCRIPTOR {
    public var bLength: UInt8 = 0
    public var bDescriptorType: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_COMPOSITE_DEVICE_INFO {
    public var DeviceDescriptor: USB_DEVICE_DESCRIPTOR = USB_DEVICE_DESCRIPTOR()
    public var CurrentConfigDescriptor: USB_CONFIGURATION_DESCRIPTOR = USB_CONFIGURATION_DESCRIPTOR()
    public var CurrentConfigurationValue: UInt8 = 0
    public var NumberOfFunctions: UInt8 = 0
    public var FunctionInfo: VArray<USB_COMPOSITE_FUNCTION_INFO, $1> = VArray<USB_COMPOSITE_FUNCTION_INFO, $1>(repeat: USB_COMPOSITE_FUNCTION_INFO())
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_COMPOSITE_FUNCTION_INFO {
    public var FunctionNumber: UInt8 = 0
    public var BaseInterfaceNumber: UInt8 = 0
    public var NumberOfInterfaces: UInt8 = 0
    public var FunctionIsIdle: Win32_Foundation.BOOLEAN = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CONFIGURATION_DESCRIPTOR {
    public var _raw: VArray<UInt8, $9> = VArray<UInt8, $9>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func wTotalLength(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_wTotalLength(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bNumInterfaces(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 4)
        }
    }

    public mut func set_bNumInterfaces(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 4, val)
            _raw = rawStorage
        }
    }

    public func bConfigurationValue(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 5)
        }
    }

    public mut func set_bConfigurationValue(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 5, val)
            _raw = rawStorage
        }
    }

    public func iConfiguration(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_iConfiguration(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }

    public func bmAttributes(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 7)
        }
    }

    public mut func set_bmAttributes(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 7, val)
            _raw = rawStorage
        }
    }

    public func maxPower(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 8)
        }
    }

    public mut func set_maxPower(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 8, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CONFIGURATION_POWER_DESCRIPTOR {
    public var _raw: VArray<UInt8, $18> = VArray<UInt8, $18>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    // packed field `SelfPowerConsumedD0`: type `VArray<UInt8, $3>` not supported by polyfill; skipped

    public func bPowerSummaryId(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 5)
        }
    }

    public mut func set_bPowerSummaryId(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 5, val)
            _raw = rawStorage
        }
    }

    public func bBusPowerSavingD1(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_bBusPowerSavingD1(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }

    public func bSelfPowerSavingD1(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 7)
        }
    }

    public mut func set_bSelfPowerSavingD1(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 7, val)
            _raw = rawStorage
        }
    }

    public func bBusPowerSavingD2(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 8)
        }
    }

    public mut func set_bBusPowerSavingD2(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 8, val)
            _raw = rawStorage
        }
    }

    public func bSelfPowerSavingD2(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 9)
        }
    }

    public mut func set_bSelfPowerSavingD2(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 9, val)
            _raw = rawStorage
        }
    }

    public func bBusPowerSavingD3(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 10)
        }
    }

    public mut func set_bBusPowerSavingD3(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 10, val)
            _raw = rawStorage
        }
    }

    public func bSelfPowerSavingD3(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 11)
        }
    }

    public mut func set_bSelfPowerSavingD3(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 11, val)
            _raw = rawStorage
        }
    }

    public func transitionTimeFromD1(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 12)
        }
    }

    public mut func set_transitionTimeFromD1(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 12, val)
            _raw = rawStorage
        }
    }

    public func transitionTimeFromD2(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 14)
        }
    }

    public mut func set_transitionTimeFromD2(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 14, val)
            _raw = rawStorage
        }
    }

    public func transitionTimeFromD3(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 16)
        }
    }

    public mut func set_transitionTimeFromD3(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 16, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CONNECTION_NOTIFICATION {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    // packed field `NotificationType`: type `USB_NOTIFICATION_TYPE` not supported by polyfill; skipped

    public func connectionNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_connectionNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func requestedBandwidth(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_requestedBandwidth(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func enumerationFailReason(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_enumerationFailReason(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func powerRequested(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_powerRequested(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }

    public func hubNameLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 20)
        }
    }

    public mut func set_hubNameLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 20, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CONNECTION_STATUS {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_CONNECTION_STATUS): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_CONNECTION_STATUS): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let NoDeviceConnected: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceConnected: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceFailedEnumeration: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceGeneralFailure: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(3)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceCausedOvercurrent: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(4)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceNotEnoughPower: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(5)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceNotEnoughBandwidth: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(6)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceHubNestedTooDeeply: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(7)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceInLegacyHub: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(8)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceEnumerating: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(9)

@When[Windows_Win32_Devices_Usb == "on"]
public let DeviceReset: USB_CONNECTION_STATUS = USB_CONNECTION_STATUS(10)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CONTROLLER_DEVICE_INFO {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    public func pciVendorId(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_pciVendorId(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func pciDeviceId(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_pciDeviceId(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func pciRevision(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_pciRevision(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func numberOfRootPorts(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_numberOfRootPorts(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func hcFeatureFlags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_hcFeatureFlags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CONTROLLER_FLAVOR {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_CONTROLLER_FLAVOR): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_CONTROLLER_FLAVOR): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let USB_HcGeneric: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let OHCI_Generic: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(100)

@When[Windows_Win32_Devices_Usb == "on"]
public let OHCI_Hydra: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(101)

@When[Windows_Win32_Devices_Usb == "on"]
public let OHCI_NEC: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(102)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Generic: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(200)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Piix4: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(201)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Piix3: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(202)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Ich2: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(203)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Reserved204: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(204)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Ich1: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(205)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Ich3m: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(206)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Ich4: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(207)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Ich5: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(208)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Ich6: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(209)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_Intel: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(249)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_VIA: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(250)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_VIA_x01: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(251)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_VIA_x02: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(252)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_VIA_x03: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(253)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_VIA_x04: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(254)

@When[Windows_Win32_Devices_Usb == "on"]
public let UHCI_VIA_x0E_FIFO: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(264)

@When[Windows_Win32_Devices_Usb == "on"]
public let EHCI_Generic: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(1000)

@When[Windows_Win32_Devices_Usb == "on"]
public let EHCI_NEC: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(2000)

@When[Windows_Win32_Devices_Usb == "on"]
public let EHCI_Lucent: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(3000)

@When[Windows_Win32_Devices_Usb == "on"]
public let EHCI_NVIDIA_Tegra2: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(4000)

@When[Windows_Win32_Devices_Usb == "on"]
public let EHCI_NVIDIA_Tegra3: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(4001)

@When[Windows_Win32_Devices_Usb == "on"]
public let EHCI_Intel_Medfield: USB_CONTROLLER_FLAVOR = USB_CONTROLLER_FLAVOR(5001)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CONTROLLER_INFO_0 {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public func pciVendorId(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_pciVendorId(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func pciDeviceId(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_pciDeviceId(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func pciRevision(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_pciRevision(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func numberOfRootPorts(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_numberOfRootPorts(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    // packed field `ControllerFlavor`: type `USB_CONTROLLER_FLAVOR` not supported by polyfill; skipped

    public func hcFeatureFlags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 20)
        }
    }

    public mut func set_hcFeatureFlags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 20, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_CYCLE_PORT_PARAMS {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func statusReturned(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_statusReturned(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEFAULT_PIPE_SETUP_PACKET {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    // packed field `bmRequestType`: type `BM_REQUEST_TYPE` not supported by polyfill; skipped

    public func bRequest(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bRequest(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    // packed field `wValue`: type `USB_DEFAULT_PIPE_SETUP_PACKET__wValue` not supported by polyfill; skipped

    // packed field `wIndex`: type `USB_DEFAULT_PIPE_SETUP_PACKET__wIndex` not supported by polyfill; skipped

    public func wLength(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 6)
        }
    }

    public mut func set_wLength(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 6, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEFAULT_PIPE_SETUP_PACKET__wValue {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func anonymous(): USB_DEFAULT_PIPE_SETUP_PACKET__wValue__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEFAULT_PIPE_SETUP_PACKET__wValue__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEFAULT_PIPE_SETUP_PACKET__wValue__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEFAULT_PIPE_SETUP_PACKET__wValue__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func w(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_w(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEFAULT_PIPE_SETUP_PACKET__wValue__Anonymous_e__Struct {
    public var LowByte: UInt8 = 0
    public var HiByte: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEFAULT_PIPE_SETUP_PACKET__wIndex {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func anonymous(): USB_DEFAULT_PIPE_SETUP_PACKET__wIndex__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEFAULT_PIPE_SETUP_PACKET__wIndex__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEFAULT_PIPE_SETUP_PACKET__wIndex__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEFAULT_PIPE_SETUP_PACKET__wIndex__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func w(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_w(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEFAULT_PIPE_SETUP_PACKET__wIndex__Anonymous_e__Struct {
    public var LowByte: UInt8 = 0
    public var HiByte: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DESCRIPTOR_REQUEST {
    public var _raw: VArray<UInt8, $13> = VArray<UInt8, $13>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `SetupPacket`: type `USB_DESCRIPTOR_REQUEST__SetupPacket_e__Struct` not supported by polyfill; skipped

    // packed field `Data`: type `VArray<UInt8, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DESCRIPTOR_REQUEST__SetupPacket_e__Struct {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    public func bmRequest(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bmRequest(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bRequest(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bRequest(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func wValue(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_wValue(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func wIndex(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_wIndex(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func wLength(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 6)
        }
    }

    public mut func set_wLength(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 6, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR {
    public var _raw: VArray<UInt8, $48> = VArray<UInt8, $48>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bDevCapabilityType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bDevCapabilityType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func iAddtionalInfoURL(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_iAddtionalInfoURL(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }

    public func bNumberOfAlternateModes(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 4)
        }
    }

    public mut func set_bNumberOfAlternateModes(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 4, val)
            _raw = rawStorage
        }
    }

    public func bPreferredAlternateMode(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 5)
        }
    }

    public mut func set_bPreferredAlternateMode(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 5, val)
            _raw = rawStorage
        }
    }

    // packed field `VconnPower`: type `USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__VconnPower_e__Union` not supported by polyfill; skipped

    // packed field `bmConfigured`: type `VArray<UInt8, $32>` not supported by polyfill; skipped

    public func bReserved(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 40)
        }
    }

    public mut func set_bReserved(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 40, val)
            _raw = rawStorage
        }
    }

    // packed field `AlternateMode`: type `VArray<USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__Anonymous_e__Struct, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__VconnPower_e__Union {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__VconnPower_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__VconnPower_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__VconnPower_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__VconnPower_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__VconnPower_e__Union__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_BILLBOARD_DESCRIPTOR__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func wsvid(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set_wsvid(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bAlternateMode(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bAlternateMode(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func iAlternateModeSetting(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_iAlternateModeSetting(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_CONTAINER_ID_DESCRIPTOR {
    public var bLength: UInt8 = 0
    public var bDescriptorType: UInt8 = 0
    public var bDevCapabilityType: UInt8 = 0
    public var bReserved: UInt8 = 0
    public var ContainerID: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_DESCRIPTOR {
    public var bLength: UInt8 = 0
    public var bDescriptorType: UInt8 = 0
    public var bDevCapabilityType: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR {
    public var bLength: UInt8 = 0
    public var bDescriptorType: UInt8 = 0
    public var bDevCapabilityType: UInt8 = 0
    public var bcdDescriptorVersion: UInt8 = 0
    public var bmAttributes: USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR__bmAttributes_e__Union = USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR__bmAttributes_e__Union()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR__bmAttributes_e__Union {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func asUlong(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_asUlong(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_FIRMWARE_STATUS_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bDevCapabilityType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bDevCapabilityType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bReserved(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_bReserved(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }

    // packed field `bmCapabilities`: type `USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR__bmCapabilities_e__Union` not supported by polyfill; skipped

    public func wMinVoltage(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 6)
        }
    }

    public mut func set_wMinVoltage(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 6, val)
            _raw = rawStorage
        }
    }

    public func wMaxVoltage(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 8)
        }
    }

    public mut func set_wMaxVoltage(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 8, val)
            _raw = rawStorage
        }
    }

    public func wReserved(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 10)
        }
    }

    public mut func set_wReserved(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 10, val)
            _raw = rawStorage
        }
    }

    public func dwMaxOperatingPower(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_dwMaxOperatingPower(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func dwMaxPeakPower(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_dwMaxPeakPower(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }

    public func dwMaxPeakPowerTime(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 20)
        }
    }

    public mut func set_dwMaxPeakPowerTime(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 20, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR__bmCapabilities_e__Union {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR__bmCapabilities_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR__bmCapabilities_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR__bmCapabilities_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR__bmCapabilities_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT_DESCRIPTOR__bmCapabilities_e__Union__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_PLATFORM_DESCRIPTOR {
    public var _raw: VArray<UInt8, $5> = VArray<UInt8, $5>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bDevCapabilityType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bDevCapabilityType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bReserved(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_bReserved(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }

    // packed field `PlatformCapabilityUuid`: layout unknown; skipped

    // packed field `CapabililityData`: type `VArray<UInt8, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR {
    public var _raw: VArray<UInt8, $18> = VArray<UInt8, $18>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bDevCapabilityType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bDevCapabilityType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bReserved(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_bReserved(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }

    // packed field `bmAttributes`: type `USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR__bmAttributes_e__Union` not supported by polyfill; skipped

    public func bmProviderPorts(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 8)
        }
    }

    public mut func set_bmProviderPorts(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 8, val)
            _raw = rawStorage
        }
    }

    public func bmConsumerPorts(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 10)
        }
    }

    public mut func set_bmConsumerPorts(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 10, val)
            _raw = rawStorage
        }
    }

    public func bcdBCVersion(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 12)
        }
    }

    public mut func set_bcdBCVersion(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 12, val)
            _raw = rawStorage
        }
    }

    public func bcdPDVersion(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 14)
        }
    }

    public mut func set_bcdPDVersion(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 14, val)
            _raw = rawStorage
        }
    }

    public func bcdUSBTypeCVersion(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 16)
        }
    }

    public mut func set_bcdUSBTypeCVersion(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 16, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR__bmAttributes_e__Union {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func asUlong(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_asUlong(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_POWER_DELIVERY_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func asUlong32(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_asUlong32(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bDevCapabilityType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bDevCapabilityType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bReserved(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_bReserved(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }

    // packed field `bmAttributes`: type `USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__bmAttributes_e__Union` not supported by polyfill; skipped

    // packed field `wFunctionalitySupport`: type `USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__wFunctionalitySupport_e__Union` not supported by polyfill; skipped

    public func wReserved(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 10)
        }
    }

    public mut func set_wReserved(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 10, val)
            _raw = rawStorage
        }
    }

    // packed field `bmSublinkSpeedAttr`: type `VArray<USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__bmAttributes_e__Union {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func asUlong(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_asUlong(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__wFunctionalitySupport_e__Union {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__wFunctionalitySupport_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__wFunctionalitySupport_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__wFunctionalitySupport_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__wFunctionalitySupport_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB_DESCRIPTOR__wFunctionalitySupport_e__Union__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_SUPERSPEED_USB_DESCRIPTOR {
    public var _raw: VArray<UInt8, $10> = VArray<UInt8, $10>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bDevCapabilityType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bDevCapabilityType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bmAttributes(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_bmAttributes(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }

    public func wSpeedsSupported(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_wSpeedsSupported(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func bFunctionalitySupport(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_bFunctionalitySupport(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }

    public func bu1DevExitLat(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 7)
        }
    }

    public mut func set_bu1DevExitLat(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 7, val)
            _raw = rawStorage
        }
    }

    public func wu2DevExitLat(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 8)
        }
    }

    public mut func set_wu2DevExitLat(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 8, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR {
    public var bLength: UInt8 = 0
    public var bDescriptorType: UInt8 = 0
    public var bDevCapabilityType: UInt8 = 0
    public var bmAttributes: USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR__bmAttributes_e__Union = USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR__bmAttributes_e__Union()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR__bmAttributes_e__Union {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func asUlong(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_asUlong(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CAPABILITY_USB20_EXTENSION_DESCRIPTOR__bmAttributes_e__Union__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_CHARACTERISTICS {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public func version(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_version(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `Reserved`: type `VArray<UInt32, $2>` not supported by polyfill; skipped

    public func usbDeviceCharacteristicsFlags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_usbDeviceCharacteristicsFlags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func maximumSendPathDelayInMilliSeconds(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_maximumSendPathDelayInMilliSeconds(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }

    public func maximumCompletionPathDelayInMilliSeconds(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 20)
        }
    }

    public mut func set_maximumCompletionPathDelayInMilliSeconds(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 20, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_DESCRIPTOR {
    public var _raw: VArray<UInt8, $18> = VArray<UInt8, $18>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bcdUSB(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_bcdUSB(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bDeviceClass(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 4)
        }
    }

    public mut func set_bDeviceClass(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 4, val)
            _raw = rawStorage
        }
    }

    public func bDeviceSubClass(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 5)
        }
    }

    public mut func set_bDeviceSubClass(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 5, val)
            _raw = rawStorage
        }
    }

    public func bDeviceProtocol(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_bDeviceProtocol(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }

    public func bMaxPacketSize0(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 7)
        }
    }

    public mut func set_bMaxPacketSize0(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 7, val)
            _raw = rawStorage
        }
    }

    public func idVendor(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 8)
        }
    }

    public mut func set_idVendor(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 8, val)
            _raw = rawStorage
        }
    }

    public func idProduct(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 10)
        }
    }

    public mut func set_idProduct(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 10, val)
            _raw = rawStorage
        }
    }

    public func bcdDevice(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 12)
        }
    }

    public mut func set_bcdDevice(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 12, val)
            _raw = rawStorage
        }
    }

    public func iManufacturer(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 14)
        }
    }

    public mut func set_iManufacturer(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 14, val)
            _raw = rawStorage
        }
    }

    public func iProduct(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 15)
        }
    }

    public mut func set_iProduct(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 15, val)
            _raw = rawStorage
        }
    }

    public func iSerialNumber(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 16)
        }
    }

    public mut func set_iSerialNumber(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 16, val)
            _raw = rawStorage
        }
    }

    public func bNumConfigurations(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 17)
        }
    }

    public mut func set_bNumConfigurations(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 17, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_INFO {
    public var _raw: VArray<UInt8, $1078> = VArray<UInt8, $1078>(repeat: 0u8)

    // packed field `DeviceState`: type `USB_DEVICE_STATE` not supported by polyfill; skipped

    public func portNumber(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_portNumber(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `DeviceDescriptor`: type `USB_DEVICE_DESCRIPTOR` not supported by polyfill; skipped

    public func currentConfigurationValue(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 24)
        }
    }

    public mut func set_currentConfigurationValue(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 24, val)
            _raw = rawStorage
        }
    }

    // packed field `Speed`: type `USB_DEVICE_SPEED` not supported by polyfill; skipped

    public func deviceAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 29)
        }
    }

    public mut func set_deviceAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 29, val)
            _raw = rawStorage
        }
    }

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 31)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 31, val)
            _raw = rawStorage
        }
    }

    // packed field `ConnectionStatus`: type `USB_CONNECTION_STATUS` not supported by polyfill; skipped

    // packed field `PnpHardwareId`: type `VArray<UInt16, $128>` not supported by polyfill; skipped

    // packed field `PnpCompatibleId`: type `VArray<UInt16, $128>` not supported by polyfill; skipped

    // packed field `SerialNumberId`: type `VArray<UInt16, $128>` not supported by polyfill; skipped

    // packed field `PnpDeviceDescription`: type `VArray<UInt16, $128>` not supported by polyfill; skipped

    public func numberOfOpenPipes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 1063)
        }
    }

    public mut func set_numberOfOpenPipes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 1063, val)
            _raw = rawStorage
        }
    }

    // packed field `PipeList`: type `VArray<USB_PIPE_INFO, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_NODE_INFO {
    public var _raw: VArray<UInt8, $1202> = VArray<UInt8, $1202>(repeat: 0u8)

    public func sig(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_sig(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func lengthInBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_lengthInBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `DeviceDescription`: type `VArray<UInt16, $40>` not supported by polyfill; skipped

    // packed field `NodeType`: type `USB_WMI_DEVICE_NODE_TYPE` not supported by polyfill; skipped

    // packed field `BusAddress`: type `USB_TOPOLOGY_ADDRESS` not supported by polyfill; skipped

    // packed field `Anonymous`: type `USB_DEVICE_NODE_INFO__Anonymous_e__Union` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_NODE_INFO__Anonymous_e__Union {
    public var _raw: VArray<UInt8, $1078> = VArray<UInt8, $1078>(repeat: 0)

    public func usbDeviceInfo(): USB_DEVICE_INFO {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_INFO>(p)
            return ptr.read()
        }
    }

    public mut func set_usbDeviceInfo(val: USB_DEVICE_INFO): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_INFO>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func hubDeviceInfo(): USB_HUB_DEVICE_INFO {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_DEVICE_INFO>(p)
            return ptr.read()
        }
    }

    public mut func set_hubDeviceInfo(val: USB_HUB_DEVICE_INFO): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_DEVICE_INFO>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func compositeDeviceInfo(): USB_COMPOSITE_DEVICE_INFO {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_COMPOSITE_DEVICE_INFO>(p)
            return ptr.read()
        }
    }

    public mut func set_compositeDeviceInfo(val: USB_COMPOSITE_DEVICE_INFO): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_COMPOSITE_DEVICE_INFO>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func controllerDeviceInfo(): USB_CONTROLLER_DEVICE_INFO {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_CONTROLLER_DEVICE_INFO>(p)
            return ptr.read()
        }
    }

    public mut func set_controllerDeviceInfo(val: USB_CONTROLLER_DEVICE_INFO): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_CONTROLLER_DEVICE_INFO>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func deviceInformation(): VArray<UInt8, $4> {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<VArray<UInt8, $4>>(p)
            return ptr.read()
        }
    }

    public mut func set_deviceInformation(val: VArray<UInt8, $4>): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<VArray<UInt8, $4>>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_PERFORMANCE_INFO {
    public var _raw: VArray<UInt8, $228> = VArray<UInt8, $228>(repeat: 0u8)

    public func bulkBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_bulkBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func controlDataBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_controlDataBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func isoBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_isoBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func interruptBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_interruptBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func bulkUrbCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_bulkUrbCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }

    public func controlUrbCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 20)
        }
    }

    public mut func set_controlUrbCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 20, val)
            _raw = rawStorage
        }
    }

    public func isoUrbCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 24)
        }
    }

    public mut func set_isoUrbCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 24, val)
            _raw = rawStorage
        }
    }

    public func interruptUrbCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 28)
        }
    }

    public mut func set_interruptUrbCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 28, val)
            _raw = rawStorage
        }
    }

    // packed field `AllocedInterrupt`: type `VArray<UInt32, $6>` not supported by polyfill; skipped

    public func allocedIso(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 56)
        }
    }

    public mut func set_allocedIso(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 56, val)
            _raw = rawStorage
        }
    }

    public func total32secBandwidth(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 60)
        }
    }

    public mut func set_total32secBandwidth(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 60, val)
            _raw = rawStorage
        }
    }

    public func totalTtBandwidth(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 64)
        }
    }

    public mut func set_totalTtBandwidth(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 64, val)
            _raw = rawStorage
        }
    }

    // packed field `DeviceDescription`: type `VArray<UInt16, $60>` not supported by polyfill; skipped

    // packed field `DeviceSpeed`: type `USB_DEVICE_SPEED` not supported by polyfill; skipped

    public func totalIsoLatency(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 192)
        }
    }

    public mut func set_totalIsoLatency(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 192, val)
            _raw = rawStorage
        }
    }

    public func droppedIsoPackets(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 196)
        }
    }

    public mut func set_droppedIsoPackets(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 196, val)
            _raw = rawStorage
        }
    }

    public func transferErrors(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 200)
        }
    }

    public mut func set_transferErrors(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 200, val)
            _raw = rawStorage
        }
    }

    public func pciInterruptCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 204)
        }
    }

    public mut func set_pciInterruptCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 204, val)
            _raw = rawStorage
        }
    }

    public func hcIdleState(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 208)
        }
    }

    public mut func set_hcIdleState(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 208, val)
            _raw = rawStorage
        }
    }

    public func hcAsyncIdleState(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 212)
        }
    }

    public mut func set_hcAsyncIdleState(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 212, val)
            _raw = rawStorage
        }
    }

    public func hcAsyncCacheFlushCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 216)
        }
    }

    public mut func set_hcAsyncCacheFlushCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 216, val)
            _raw = rawStorage
        }
    }

    public func hcPeriodicIdleState(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 220)
        }
    }

    public mut func set_hcPeriodicIdleState(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 220, val)
            _raw = rawStorage
        }
    }

    public func hcPeriodicCacheFlushCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 224)
        }
    }

    public mut func set_hcPeriodicCacheFlushCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 224, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_QUALIFIER_DESCRIPTOR {
    public var _raw: VArray<UInt8, $10> = VArray<UInt8, $10>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bcdUSB(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_bcdUSB(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bDeviceClass(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 4)
        }
    }

    public mut func set_bDeviceClass(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 4, val)
            _raw = rawStorage
        }
    }

    public func bDeviceSubClass(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 5)
        }
    }

    public mut func set_bDeviceSubClass(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 5, val)
            _raw = rawStorage
        }
    }

    public func bDeviceProtocol(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_bDeviceProtocol(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }

    public func bMaxPacketSize0(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 7)
        }
    }

    public mut func set_bMaxPacketSize0(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 7, val)
            _raw = rawStorage
        }
    }

    public func bNumConfigurations(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 8)
        }
    }

    public mut func set_bNumConfigurations(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 8, val)
            _raw = rawStorage
        }
    }

    public func bReserved(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 9)
        }
    }

    public mut func set_bReserved(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 9, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_SPEED {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_DEVICE_SPEED): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_DEVICE_SPEED): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbLowSpeed: USB_DEVICE_SPEED = USB_DEVICE_SPEED(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbFullSpeed: USB_DEVICE_SPEED = USB_DEVICE_SPEED(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbHighSpeed: USB_DEVICE_SPEED = USB_DEVICE_SPEED(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbSuperSpeed: USB_DEVICE_SPEED = USB_DEVICE_SPEED(3)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_STATE {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_DEVICE_STATUS {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_DEVICE_STATUS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_STATUS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_DEVICE_STATUS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_DEVICE_STATUS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_STATUS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DEVICE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_DEVICE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_DEVICE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let Usb11Device: USB_DEVICE_TYPE = USB_DEVICE_TYPE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let Usb20Device: USB_DEVICE_TYPE = USB_DEVICE_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_DRIVER_VERSION_PARAMETERS {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    public func driverTrackingCode(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_driverTrackingCode(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func usbdi_Version(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_usbdi_Version(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func usbuser_Version(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_usbuser_Version(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    // packed field `CheckedPortDriver`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    // packed field `CheckedMiniportDriver`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    public func usb_Version(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 14)
        }
    }

    public mut func set_usb_Version(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 14, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_ENDPOINT_DESCRIPTOR {
    public var _raw: VArray<UInt8, $7> = VArray<UInt8, $7>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bEndpointAddress(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bEndpointAddress(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bmAttributes(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_bmAttributes(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }

    public func wMaxPacketSize(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_wMaxPacketSize(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func bInterval(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_bInterval(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_ENDPOINT_STATUS {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_ENDPOINT_STATUS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_ENDPOINT_STATUS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_ENDPOINT_STATUS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_ENDPOINT_STATUS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_ENDPOINT_STATUS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_FRAME_NUMBER_AND_QPC_FOR_TIME_SYNC_INFORMATION {
    public var _raw: VArray<UInt8, $60> = VArray<UInt8, $60>(repeat: 0u8)

    // packed field `TimeTrackingHandle`: type `CPointer<Unit>` not supported by polyfill; skipped

    public func inputFrameNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_inputFrameNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func inputMicroFrameNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_inputMicroFrameNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func queryPerformanceCounterAtInputFrameOrMicroFrame(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 16)
        }
    }

    public mut func set_queryPerformanceCounterAtInputFrameOrMicroFrame(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 16, val)
            _raw = rawStorage
        }
    }

    public func queryPerformanceCounterFrequency(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 24)
        }
    }

    public mut func set_queryPerformanceCounterFrequency(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 24, val)
            _raw = rawStorage
        }
    }

    public func predictedAccuracyInMicroSeconds(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 32)
        }
    }

    public mut func set_predictedAccuracyInMicroSeconds(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 32, val)
            _raw = rawStorage
        }
    }

    public func currentGenerationID(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 36)
        }
    }

    public mut func set_currentGenerationID(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 36, val)
            _raw = rawStorage
        }
    }

    public func currentQueryPerformanceCounter(): Int64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt64(base, 40)
        }
    }

    public mut func set_currentQueryPerformanceCounter(val: Int64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt64(base, 40, val)
            _raw = rawStorage
        }
    }

    public func currentHardwareFrameNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 48)
        }
    }

    public mut func set_currentHardwareFrameNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 48, val)
            _raw = rawStorage
        }
    }

    public func currentHardwareMicroFrameNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 52)
        }
    }

    public mut func set_currentHardwareMicroFrameNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 52, val)
            _raw = rawStorage
        }
    }

    public func currentUSBFrameNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 56)
        }
    }

    public mut func set_currentUSBFrameNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 56, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_FUNCTION_SUSPEND_OPTIONS {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)

    public func asUchar(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt8(base, 0)
        }
    }

    public mut func set_asUchar(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_FUNCTION_SUSPEND_OPTIONS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_FUNCTION_SUSPEND_OPTIONS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_FUNCTION_SUSPEND_OPTIONS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_FUNCTION_SUSPEND_OPTIONS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_FUNCTION_SUSPEND_OPTIONS__Anonymous_e__Struct {
    public var _bitfield: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HCD_DRIVERKEY_NAME {
    public var _raw: VArray<UInt8, $6> = VArray<UInt8, $6>(repeat: 0u8)

    public func actualLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_actualLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `DriverKeyName`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_HIGH_SPEED_MAXPACKET {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func us(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_us(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HIGH_SPEED_MAXPACKET__MP {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_HUB_30_PORT_REMOTE_WAKE_MASK {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)

    public func asUchar8(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt8(base, 0)
        }
    }

    public mut func set_asUchar8(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_HUB_30_PORT_REMOTE_WAKE_MASK__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_30_PORT_REMOTE_WAKE_MASK__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_HUB_30_PORT_REMOTE_WAKE_MASK__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_30_PORT_REMOTE_WAKE_MASK__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_30_PORT_REMOTE_WAKE_MASK__Anonymous_e__Struct {
    public var _bitfield: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_CAPABILITIES {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_CAPABILITIES_EX {
    public var CapabilityFlags: USB_HUB_CAP_FLAGS = USB_HUB_CAP_FLAGS()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_HUB_CAP_FLAGS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func ul(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_ul(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_HUB_CAP_FLAGS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_CAP_FLAGS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_HUB_CAP_FLAGS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_CAP_FLAGS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_CAP_FLAGS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_HUB_CHANGE {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_HUB_CHANGE__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_CHANGE__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_HUB_CHANGE__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_CHANGE__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_CHANGE__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_DESCRIPTOR {
    public var _raw: VArray<UInt8, $71> = VArray<UInt8, $71>(repeat: 0u8)

    public func bDescriptorLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bDescriptorLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bNumberOfPorts(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bNumberOfPorts(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func wHubCharacteristics(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 3)
        }
    }

    public mut func set_wHubCharacteristics(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 3, val)
            _raw = rawStorage
        }
    }

    public func bPowerOnToPowerGood(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 5)
        }
    }

    public mut func set_bPowerOnToPowerGood(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 5, val)
            _raw = rawStorage
        }
    }

    public func bHubControlCurrent(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_bHubControlCurrent(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }

    // packed field `bRemoveAndPowerMask`: type `VArray<UInt8, $64>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_DEVICE_INFO {
    public var _raw: VArray<UInt8, $103> = VArray<UInt8, $103>(repeat: 0u8)

    // packed field `HubDescriptor`: type `USB_HUB_DESCRIPTOR` not supported by polyfill; skipped

    public func hubNumber(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 71)
        }
    }

    public mut func set_hubNumber(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 71, val)
            _raw = rawStorage
        }
    }

    public func deviceAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 75)
        }
    }

    public mut func set_deviceAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 75, val)
            _raw = rawStorage
        }
    }

    // packed field `HubIsSelfPowered`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    // packed field `HubIsRootHub`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    // packed field `HubCapabilities`: type `USB_HUB_CAPABILITIES` not supported by polyfill; skipped

    public func numberOfHubPorts(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 83)
        }
    }

    public mut func set_numberOfHubPorts(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 83, val)
            _raw = rawStorage
        }
    }

    // packed field `PortInfo`: type `VArray<USB_HUB_PORT_INFORMATION, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_DEVICE_UXD_SETTINGS {
    public var _raw: VArray<UInt8, $36> = VArray<UInt8, $36>(repeat: 0u8)

    public func version(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_version(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `PnpGuid`: layout unknown; skipped

    // packed field `OwnerGuid`: layout unknown; skipped

    public func deleteOnShutdown(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_deleteOnShutdown(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func deleteOnReload(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_deleteOnReload(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    public func deleteOnDisconnect(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_deleteOnDisconnect(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    // packed field `Reserved`: type `VArray<UInt32, $5>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_INFORMATION {
    public var HubDescriptor: USB_HUB_DESCRIPTOR = USB_HUB_DESCRIPTOR()
    public var HubIsBusPowered: Win32_Foundation.BOOLEAN = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_INFORMATION_EX {
    public var _raw: VArray<UInt8, $77> = VArray<UInt8, $77>(repeat: 0u8)

    // packed field `HubType`: type `USB_HUB_TYPE` not supported by polyfill; skipped

    public func highestPortNumber(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_highestPortNumber(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `u`: type `USB_HUB_INFORMATION_EX__u_e__Union` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_HUB_INFORMATION_EX__u_e__Union {
    public var _raw: VArray<UInt8, $71> = VArray<UInt8, $71>(repeat: 0)

    public func usbHubDescriptor(): USB_HUB_DESCRIPTOR {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_DESCRIPTOR>(p)
            return ptr.read()
        }
    }

    public mut func set_usbHubDescriptor(val: USB_HUB_DESCRIPTOR): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_DESCRIPTOR>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func usb30HubDescriptor(): USB_30_HUB_DESCRIPTOR {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_HUB_DESCRIPTOR>(p)
            return ptr.read()
        }
    }

    public mut func set_usb30HubDescriptor(val: USB_30_HUB_DESCRIPTOR): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_HUB_DESCRIPTOR>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_NAME {
    public var _raw: VArray<UInt8, $6> = VArray<UInt8, $6>(repeat: 0u8)

    public func actualLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_actualLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `HubName`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_NODE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_HUB_NODE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_HUB_NODE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbHub: USB_HUB_NODE = USB_HUB_NODE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbMIParent: USB_HUB_NODE = USB_HUB_NODE(1)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_PORT_INFORMATION {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    // packed field `DeviceState`: type `USB_DEVICE_STATE` not supported by polyfill; skipped

    public func portNumber(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_portNumber(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func deviceAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 6)
        }
    }

    public mut func set_deviceAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 6, val)
            _raw = rawStorage
        }
    }

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }

    // packed field `ConnectionStatus`: type `USB_CONNECTION_STATUS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_HUB_STATUS {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_HUB_STATUS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_STATUS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_HUB_STATUS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_STATUS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_STATUS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_HUB_STATUS_AND_CHANGE {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func asUlong32(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_asUlong32(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_HUB_STATUS_AND_CHANGE__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_STATUS_AND_CHANGE__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_HUB_STATUS_AND_CHANGE__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_STATUS_AND_CHANGE__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_STATUS_AND_CHANGE__Anonymous_e__Struct {
    public var HubStatus: USB_HUB_STATUS = USB_HUB_STATUS()
    public var HubChange: USB_HUB_CHANGE = USB_HUB_CHANGE()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_HUB_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_HUB_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_HUB_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbRootHub: USB_HUB_TYPE = USB_HUB_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let Usb20Hub: USB_HUB_TYPE = USB_HUB_TYPE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let Usb30Hub: USB_HUB_TYPE = USB_HUB_TYPE(3)

@When[Windows_Win32_Devices_Usb == "on"]
public type USB_IDLE_CALLBACK = CFunc<(CPointer<Unit>) -> Unit>

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_IDLE_CALLBACK_INFO {
    public var IdleCallback: USB_IDLE_CALLBACK = { _ => () }
    public var IdleContext: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_ID_STRING {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    public func languageId(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set_languageId(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func pad(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_pad(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func lengthInBytes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_lengthInBytes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `Buffer`: type `CPointer<UInt16>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_INTERFACE_ASSOCIATION_DESCRIPTOR {
    public var bLength: UInt8 = 0
    public var bDescriptorType: UInt8 = 0
    public var bFirstInterface: UInt8 = 0
    public var bInterfaceCount: UInt8 = 0
    public var bFunctionClass: UInt8 = 0
    public var bFunctionSubClass: UInt8 = 0
    public var bFunctionProtocol: UInt8 = 0
    public var iFunction: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_INTERFACE_DESCRIPTOR {
    public var bLength: UInt8 = 0
    public var bDescriptorType: UInt8 = 0
    public var bInterfaceNumber: UInt8 = 0
    public var bAlternateSetting: UInt8 = 0
    public var bNumEndpoints: UInt8 = 0
    public var bInterfaceClass: UInt8 = 0
    public var bInterfaceSubClass: UInt8 = 0
    public var bInterfaceProtocol: UInt8 = 0
    public var iInterface: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_INTERFACE_POWER_DESCRIPTOR {
    public var _raw: VArray<UInt8, $15> = VArray<UInt8, $15>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bmCapabilitiesFlags(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bmCapabilitiesFlags(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    public func bBusPowerSavingD1(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 3)
        }
    }

    public mut func set_bBusPowerSavingD1(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 3, val)
            _raw = rawStorage
        }
    }

    public func bSelfPowerSavingD1(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 4)
        }
    }

    public mut func set_bSelfPowerSavingD1(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 4, val)
            _raw = rawStorage
        }
    }

    public func bBusPowerSavingD2(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 5)
        }
    }

    public mut func set_bBusPowerSavingD2(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 5, val)
            _raw = rawStorage
        }
    }

    public func bSelfPowerSavingD2(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 6)
        }
    }

    public mut func set_bSelfPowerSavingD2(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 6, val)
            _raw = rawStorage
        }
    }

    public func bBusPowerSavingD3(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 7)
        }
    }

    public mut func set_bBusPowerSavingD3(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 7, val)
            _raw = rawStorage
        }
    }

    public func bSelfPowerSavingD3(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 8)
        }
    }

    public mut func set_bSelfPowerSavingD3(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 8, val)
            _raw = rawStorage
        }
    }

    public func transitionTimeFromD1(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 9)
        }
    }

    public mut func set_transitionTimeFromD1(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 9, val)
            _raw = rawStorage
        }
    }

    public func transitionTimeFromD2(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 11)
        }
    }

    public mut func set_transitionTimeFromD2(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 11, val)
            _raw = rawStorage
        }
    }

    public func transitionTimeFromD3(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 13)
        }
    }

    public mut func set_transitionTimeFromD3(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 13, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_INTERFACE_STATUS {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_INTERFACE_STATUS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_INTERFACE_STATUS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_INTERFACE_STATUS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_INTERFACE_STATUS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_INTERFACE_STATUS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0u8)

    public func _bitfield(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_MI_PARENT_INFORMATION {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func numberOfInterfaces(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_numberOfInterfaces(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_CONNECTION_ATTRIBUTES {
    public var _raw: VArray<UInt8, $12> = VArray<UInt8, $12>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `ConnectionStatus`: type `USB_CONNECTION_STATUS` not supported by polyfill; skipped

    public func portAttributes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 8)
        }
    }

    public mut func set_portAttributes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 8, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_CONNECTION_DRIVERKEY_NAME {
    public var _raw: VArray<UInt8, $10> = VArray<UInt8, $10>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func actualLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_actualLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `DriverKeyName`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_CONNECTION_INFORMATION {
    public var _raw: VArray<UInt8, $46> = VArray<UInt8, $46>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `DeviceDescriptor`: type `USB_DEVICE_DESCRIPTOR` not supported by polyfill; skipped

    public func currentConfigurationValue(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 22)
        }
    }

    public mut func set_currentConfigurationValue(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 22, val)
            _raw = rawStorage
        }
    }

    // packed field `LowSpeed`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    // packed field `DeviceIsHub`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    public func deviceAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 25)
        }
    }

    public mut func set_deviceAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 25, val)
            _raw = rawStorage
        }
    }

    public func numberOfOpenPipes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 27)
        }
    }

    public mut func set_numberOfOpenPipes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 27, val)
            _raw = rawStorage
        }
    }

    // packed field `ConnectionStatus`: type `USB_CONNECTION_STATUS` not supported by polyfill; skipped

    // packed field `PipeList`: type `VArray<USB_PIPE_INFO, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_CONNECTION_INFORMATION_EX {
    public var _raw: VArray<UInt8, $46> = VArray<UInt8, $46>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `DeviceDescriptor`: type `USB_DEVICE_DESCRIPTOR` not supported by polyfill; skipped

    public func currentConfigurationValue(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 22)
        }
    }

    public mut func set_currentConfigurationValue(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 22, val)
            _raw = rawStorage
        }
    }

    public func speed(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 23)
        }
    }

    public mut func set_speed(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 23, val)
            _raw = rawStorage
        }
    }

    // packed field `DeviceIsHub`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    public func deviceAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 25)
        }
    }

    public mut func set_deviceAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 25, val)
            _raw = rawStorage
        }
    }

    public func numberOfOpenPipes(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 27)
        }
    }

    public mut func set_numberOfOpenPipes(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 27, val)
            _raw = rawStorage
        }
    }

    // packed field `ConnectionStatus`: type `USB_CONNECTION_STATUS` not supported by polyfill; skipped

    // packed field `PipeList`: type `VArray<USB_PIPE_INFO, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_CONNECTION_INFORMATION_EX_V2 {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func length(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_length(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `SupportedUsbProtocols`: type `USB_PROTOCOLS` not supported by polyfill; skipped

    // packed field `Flags`: type `USB_NODE_CONNECTION_INFORMATION_EX_V2_FLAGS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_NODE_CONNECTION_INFORMATION_EX_V2_FLAGS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func ul(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_ul(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_NODE_CONNECTION_INFORMATION_EX_V2_FLAGS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_NODE_CONNECTION_INFORMATION_EX_V2_FLAGS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_NODE_CONNECTION_INFORMATION_EX_V2_FLAGS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_NODE_CONNECTION_INFORMATION_EX_V2_FLAGS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_CONNECTION_INFORMATION_EX_V2_FLAGS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_CONNECTION_NAME {
    public var _raw: VArray<UInt8, $10> = VArray<UInt8, $10>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func actualLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_actualLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `NodeName`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_CONNECTION_SUPERSPEEDPLUS_INFORMATION {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func length(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_length(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `RxSuperSpeedPlus`: type `USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED` not supported by polyfill; skipped

    public func rxLaneCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_rxLaneCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    // packed field `TxSuperSpeedPlus`: type `USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED` not supported by polyfill; skipped

    public func txLaneCount(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 20)
        }
    }

    public mut func set_txLaneCount(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 20, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NODE_INFORMATION {
    public var _raw: VArray<UInt8, $76> = VArray<UInt8, $76>(repeat: 0u8)

    // packed field `NodeType`: type `USB_HUB_NODE` not supported by polyfill; skipped

    // packed field `u`: type `USB_NODE_INFORMATION__u_e__Union` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_NODE_INFORMATION__u_e__Union {
    public var _raw: VArray<UInt8, $72> = VArray<UInt8, $72>(repeat: 0)

    public func hubInformation(): USB_HUB_INFORMATION {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_INFORMATION>(p)
            return ptr.read()
        }
    }

    public mut func set_hubInformation(val: USB_HUB_INFORMATION): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_HUB_INFORMATION>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func miParentInformation(): USB_MI_PARENT_INFORMATION {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_MI_PARENT_INFORMATION>(p)
            return ptr.read()
        }
    }

    public mut func set_miParentInformation(val: USB_MI_PARENT_INFORMATION): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_MI_PARENT_INFORMATION>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NOTIFICATION {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    // packed field `NotificationType`: type `USB_NOTIFICATION_TYPE` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_NOTIFICATION_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_NOTIFICATION_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_NOTIFICATION_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let EnumerationFailure: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let InsufficentBandwidth: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let InsufficentPower: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let OverCurrent: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(3)

@When[Windows_Win32_Devices_Usb == "on"]
public let ResetOvercurrent: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(4)

@When[Windows_Win32_Devices_Usb == "on"]
public let AcquireBusInfo: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(5)

@When[Windows_Win32_Devices_Usb == "on"]
public let AcquireHubName: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(6)

@When[Windows_Win32_Devices_Usb == "on"]
public let AcquireControllerName: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(7)

@When[Windows_Win32_Devices_Usb == "on"]
public let HubOvercurrent: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(8)

@When[Windows_Win32_Devices_Usb == "on"]
public let HubPowerChange: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(9)

@When[Windows_Win32_Devices_Usb == "on"]
public let HubNestedTooDeeply: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(10)

@When[Windows_Win32_Devices_Usb == "on"]
public let ModernDeviceInLegacyHub: USB_NOTIFICATION_TYPE = USB_NOTIFICATION_TYPE(11)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_OPEN_RAW_DEVICE_PARAMETERS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func portStatus(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 0)
        }
    }

    public mut func set_portStatus(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func maxPacketEp0(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_maxPacketEp0(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_PASS_THRU_PARAMETERS {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    // packed field `FunctionGUID`: layout unknown; skipped

    public func parameterLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_parameterLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `Parameters`: type `VArray<UInt8, $4>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_PIPE_INFO {
    public var _raw: VArray<UInt8, $11> = VArray<UInt8, $11>(repeat: 0u8)

    // packed field `EndpointDescriptor`: type `USB_ENDPOINT_DESCRIPTOR` not supported by polyfill; skipped

    public func scheduleOffset(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 7)
        }
    }

    public mut func set_scheduleOffset(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 7, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_PORT_CHANGE {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func usb20PortChange(): USB_20_PORT_CHANGE {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_20_PORT_CHANGE>(p)
            return ptr.read()
        }
    }

    public mut func set_usb20PortChange(val: USB_20_PORT_CHANGE): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_20_PORT_CHANGE>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func usb30PortChange(): USB_30_PORT_CHANGE {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_PORT_CHANGE>(p)
            return ptr.read()
        }
    }

    public mut func set_usb30PortChange(val: USB_30_PORT_CHANGE): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_PORT_CHANGE>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_PORT_CONNECTOR_PROPERTIES {
    public var _raw: VArray<UInt8, $18> = VArray<UInt8, $18>(repeat: 0u8)

    public func connectionIndex(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_connectionIndex(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func actualLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_actualLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    // packed field `UsbPortProperties`: type `USB_PORT_PROPERTIES` not supported by polyfill; skipped

    public func companionIndex(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 12)
        }
    }

    public mut func set_companionIndex(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 12, val)
            _raw = rawStorage
        }
    }

    public func companionPortNumber(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 14)
        }
    }

    public mut func set_companionPortNumber(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 14, val)
            _raw = rawStorage
        }
    }

    // packed field `CompanionHubSymbolicLinkName`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_PORT_EXT_STATUS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func asUlong32(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_asUlong32(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_PORT_EXT_STATUS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PORT_EXT_STATUS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_PORT_EXT_STATUS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PORT_EXT_STATUS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_PORT_EXT_STATUS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_PORT_EXT_STATUS_AND_CHANGE {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0)

    public func asUlong64(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt64(base, 0)
        }
    }

    public mut func set_asUlong64(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt64(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_PORT_EXT_STATUS_AND_CHANGE__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PORT_EXT_STATUS_AND_CHANGE__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_PORT_EXT_STATUS_AND_CHANGE__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PORT_EXT_STATUS_AND_CHANGE__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_PORT_EXT_STATUS_AND_CHANGE__Anonymous_e__Struct {
    public var PortStatusChange: USB_PORT_STATUS_AND_CHANGE = USB_PORT_STATUS_AND_CHANGE()
    public var PortExtStatus: USB_PORT_EXT_STATUS = USB_PORT_EXT_STATUS()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_PORT_PROPERTIES {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func ul(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_ul(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_PORT_PROPERTIES__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PORT_PROPERTIES__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_PORT_PROPERTIES__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PORT_PROPERTIES__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_PORT_PROPERTIES__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_PORT_STATUS {
    public var _raw: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)

    public func asUshort16(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt16(base, 0)
        }
    }

    public mut func set_asUshort16(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt16(base, 0, val)
            _raw = rawStorage
        }
    }

    public func usb20PortStatus(): USB_20_PORT_STATUS {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_20_PORT_STATUS>(p)
            return ptr.read()
        }
    }

    public mut func set_usb20PortStatus(val: USB_20_PORT_STATUS): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_20_PORT_STATUS>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func usb30PortStatus(): USB_30_PORT_STATUS {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_PORT_STATUS>(p)
            return ptr.read()
        }
    }

    public mut func set_usb30PortStatus(val: USB_30_PORT_STATUS): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_30_PORT_STATUS>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_PORT_STATUS_AND_CHANGE {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func asUlong32(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_asUlong32(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_PORT_STATUS_AND_CHANGE__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PORT_STATUS_AND_CHANGE__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_PORT_STATUS_AND_CHANGE__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PORT_STATUS_AND_CHANGE__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_PORT_STATUS_AND_CHANGE__Anonymous_e__Struct {
    public var PortStatus: USB_PORT_STATUS = USB_PORT_STATUS()
    public var PortChange: USB_PORT_CHANGE = USB_PORT_CHANGE()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_POWER_INFO {
    public var _raw: VArray<UInt8, $34> = VArray<UInt8, $34>(repeat: 0u8)

    // packed field `SystemState`: type `WDMUSB_POWER_STATE` not supported by polyfill; skipped

    // packed field `HcDevicePowerState`: type `WDMUSB_POWER_STATE` not supported by polyfill; skipped

    // packed field `HcDeviceWake`: type `WDMUSB_POWER_STATE` not supported by polyfill; skipped

    // packed field `HcSystemWake`: type `WDMUSB_POWER_STATE` not supported by polyfill; skipped

    // packed field `RhDevicePowerState`: type `WDMUSB_POWER_STATE` not supported by polyfill; skipped

    // packed field `RhDeviceWake`: type `WDMUSB_POWER_STATE` not supported by polyfill; skipped

    // packed field `RhSystemWake`: type `WDMUSB_POWER_STATE` not supported by polyfill; skipped

    // packed field `LastSystemSleepState`: type `WDMUSB_POWER_STATE` not supported by polyfill; skipped

    // packed field `CanWakeup`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped

    // packed field `IsPowered`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_PROTOCOLS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public func ul(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt32(base, 0)
        }
    }

    public mut func set_ul(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func anonymous(): USB_PROTOCOLS__Anonymous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PROTOCOLS__Anonymous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_anonymous(val: USB_PROTOCOLS__Anonymous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_PROTOCOLS__Anonymous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_PROTOCOLS__Anonymous_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func _bitfield(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set__bitfield(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_ROOT_HUB_NAME {
    public var _raw: VArray<UInt8, $6> = VArray<UInt8, $6>(repeat: 0u8)

    public func actualLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_actualLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `RootHubName`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_SEND_RAW_COMMAND_PARAMETERS {
    public var _raw: VArray<UInt8, $28> = VArray<UInt8, $28>(repeat: 0u8)

    public func usb_bmRequest(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_usb_bmRequest(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func usb_bRequest(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_usb_bRequest(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func usb_wVlaue(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_usb_wVlaue(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func usb_wIndex(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_usb_wIndex(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func usb_wLength(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 6)
        }
    }

    public mut func set_usb_wLength(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 6, val)
            _raw = rawStorage
        }
    }

    public func deviceAddress(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 8)
        }
    }

    public mut func set_deviceAddress(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 8, val)
            _raw = rawStorage
        }
    }

    public func maximumPacketSize(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 10)
        }
    }

    public mut func set_maximumPacketSize(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 10, val)
            _raw = rawStorage
        }
    }

    public func timeout(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 12)
        }
    }

    public mut func set_timeout(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 12, val)
            _raw = rawStorage
        }
    }

    public func dataLength(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 16)
        }
    }

    public mut func set_dataLength(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 16, val)
            _raw = rawStorage
        }
    }

    public func usbdStatusCode(): Int32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadInt32(base, 20)
        }
    }

    public mut func set_usbdStatusCode(val: Int32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteInt32(base, 20, val)
            _raw = rawStorage
        }
    }

    // packed field `Data`: type `VArray<UInt8, $4>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_START_TRACKING_FOR_TIME_SYNC_INFORMATION {
    public var _raw: VArray<UInt8, $9> = VArray<UInt8, $9>(repeat: 0u8)

    // packed field `TimeTrackingHandle`: type `CPointer<Unit>` not supported by polyfill; skipped

    // packed field `IsStartupDelayTolerable`: type `Win32_Foundation.BOOLEAN` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_STOP_TRACKING_FOR_TIME_SYNC_INFORMATION {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    // packed field `TimeTrackingHandle`: type `CPointer<Unit>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_STRING_DESCRIPTOR {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    // packed field `bString`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_SUPERSPEEDPLUS_ISOCH_ENDPOINT_COMPANION_DESCRIPTOR {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func wReserved(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_wReserved(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func dwBytesPerInterval(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_dwBytesPerInterval(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR {
    public var _raw: VArray<UInt8, $6> = VArray<UInt8, $6>(repeat: 0u8)

    public func bLength(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_bLength(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bDescriptorType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_bDescriptorType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func bMaxBurst(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 2)
        }
    }

    public mut func set_bMaxBurst(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 2, val)
            _raw = rawStorage
        }
    }

    // packed field `bmAttributes`: type `USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union` not supported by polyfill; skipped

    public func wBytesPerInterval(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_wBytesPerInterval(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
// WARNING: union simulated as byte array (size-equivalent, not type-safe)
@C
public struct USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)

    public func asUchar(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return unionReadUInt8(base, 0)
        }
    }

    public mut func set_asUchar(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            unionWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func bulk(): USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Bulk_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Bulk_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_bulk(val: USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Bulk_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Bulk_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }

    public func isochronous(): USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Isochronous_e__Struct {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Isochronous_e__Struct>(p)
            return ptr.read()
        }
    }

    public mut func set_isochronous(val: USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Isochronous_e__Struct): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            let p = base
            let ptr = CPointer<USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Isochronous_e__Struct>(p)
            ptr.write(val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Bulk_e__Struct {
    public var _bitfield: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR__bmAttributes_e__Union__Isochronous_e__Struct {
    public var _bitfield: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_TOPOLOGY_ADDRESS {
    public var PciBusNumber: UInt32 = 0
    public var PciDeviceNumber: UInt32 = 0
    public var PciFunctionNumber: UInt32 = 0
    public var Reserved: UInt32 = 0
    public var RootHubPortNumber: UInt16 = 0
    public var HubPortNumber: VArray<UInt16, $5> = VArray<UInt16, $5>(repeat: 0)
    public var Reserved2: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_TRANSPORT_CHARACTERISTICS {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public func version(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_version(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    public func transportCharacteristicsFlags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 4)
        }
    }

    public mut func set_transportCharacteristicsFlags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 4, val)
            _raw = rawStorage
        }
    }

    public func currentRoundtripLatencyInMilliSeconds(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 8)
        }
    }

    public mut func set_currentRoundtripLatencyInMilliSeconds(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 8, val)
            _raw = rawStorage
        }
    }

    public func maxPotentialBandwidth(): UInt64 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt64(base, 16)
        }
    }

    public mut func set_maxPotentialBandwidth(val: UInt64): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt64(base, 16, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_TRANSPORT_CHARACTERISTICS_CHANGE_NOTIFICATION {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    // packed field `Handle`: type `USB_CHANGE_REGISTRATION_HANDLE` not supported by polyfill; skipped

    // packed field `UsbTransportCharacteristics`: type `USB_TRANSPORT_CHARACTERISTICS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_TRANSPORT_CHARACTERISTICS_CHANGE_REGISTRATION {
    public var _raw: VArray<UInt8, $36> = VArray<UInt8, $36>(repeat: 0u8)

    public func changeNotificationInputFlags(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_changeNotificationInputFlags(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `Handle`: type `USB_CHANGE_REGISTRATION_HANDLE` not supported by polyfill; skipped

    // packed field `UsbTransportCharacteristics`: type `USB_TRANSPORT_CHARACTERISTICS` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_TRANSPORT_CHARACTERISTICS_CHANGE_UNREGISTRATION {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    // packed field `Handle`: type `USB_CHANGE_REGISTRATION_HANDLE` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_UNICODE_NAME {
    public var _raw: VArray<UInt8, $6> = VArray<UInt8, $6>(repeat: 0u8)

    public func length(): UInt32 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt32(base, 0)
        }
    }

    public mut func set_length(val: UInt32): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt32(base, 0, val)
            _raw = rawStorage
        }
    }

    // packed field `String`: type `VArray<UInt16, $1>` not supported by polyfill; skipped
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_USB2HW_VERSION_PARAMETERS {
    public var _raw: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0u8)

    public func usb2HwRevision(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_usb2HwRevision(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_USER_ERROR_CODE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_USER_ERROR_CODE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_USER_ERROR_CODE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserSuccess: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserNotSupported: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserInvalidRequestCode: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserFeatureDisabled: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(3)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserInvalidHeaderParameter: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(4)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserInvalidParameter: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(5)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserMiniportError: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(6)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserBufferTooSmall: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(7)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserErrorNotMapped: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(8)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserDeviceNotStarted: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(9)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbUserNoDeviceConnected: USB_USER_ERROR_CODE = USB_USER_ERROR_CODE(10)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct USB_WMI_DEVICE_NODE_TYPE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: USB_WMI_DEVICE_NODE_TYPE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: USB_WMI_DEVICE_NODE_TYPE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbDevice: USB_WMI_DEVICE_NODE_TYPE = USB_WMI_DEVICE_NODE_TYPE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let HubDevice: USB_WMI_DEVICE_NODE_TYPE = USB_WMI_DEVICE_NODE_TYPE(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let CompositeDevice: USB_WMI_DEVICE_NODE_TYPE = USB_WMI_DEVICE_NODE_TYPE(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let UsbController: USB_WMI_DEVICE_NODE_TYPE = USB_WMI_DEVICE_NODE_TYPE(3)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct WDMUSB_POWER_STATE {
    public var value: Int32 = 0

    public init() {}

    public init(value: Int32) {
        this.value = value
    }

    public operator func ==(other: WDMUSB_POWER_STATE): Bool {
        return this.value == other.value
    }
    public operator func !=(other: WDMUSB_POWER_STATE): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerNotMapped: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(0)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerSystemUnspecified: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(100)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerSystemWorking: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(101)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerSystemSleeping1: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(102)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerSystemSleeping2: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(103)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerSystemSleeping3: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(104)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerSystemHibernate: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(105)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerSystemShutdown: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(106)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerDeviceUnspecified: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(200)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerDeviceD0: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(201)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerDeviceD1: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(202)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerDeviceD2: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(203)

@When[Windows_Win32_Devices_Usb == "on"]
public let WdmUsbPowerDeviceD3: WDMUSB_POWER_STATE = WDMUSB_POWER_STATE(204)

@When[Windows_Win32_Devices_Usb == "on"]
public type WINUSB_INTERFACE_HANDLE = CPointer<Unit>

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct WINUSB_PIPE_INFORMATION {
    public var PipeType: USBD_PIPE_TYPE = USBD_PIPE_TYPE()
    public var PipeId: UInt8 = 0
    public var MaximumPacketSize: UInt16 = 0
    public var Interval: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct WINUSB_PIPE_INFORMATION_EX {
    public var PipeType: USBD_PIPE_TYPE = USBD_PIPE_TYPE()
    public var PipeId: UInt8 = 0
    public var MaximumPacketSize: UInt16 = 0
    public var Interval: UInt8 = 0
    public var MaximumBytesPerInterval: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct WINUSB_PIPE_POLICY {
    public var value: UInt32 = 0

    public init() {}

    public init(value: UInt32) {
        this.value = value
    }

    public operator func ==(other: WINUSB_PIPE_POLICY): Bool {
        return this.value == other.value
    }
    public operator func !=(other: WINUSB_PIPE_POLICY): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let SHORT_PACKET_TERMINATE: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(1)

@When[Windows_Win32_Devices_Usb == "on"]
public let AUTO_CLEAR_STALL: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(2)

@When[Windows_Win32_Devices_Usb == "on"]
public let PIPE_TRANSFER_TIMEOUT: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(3)

@When[Windows_Win32_Devices_Usb == "on"]
public let IGNORE_SHORT_PACKETS: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(4)

@When[Windows_Win32_Devices_Usb == "on"]
public let ALLOW_PARTIAL_READS: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(5)

@When[Windows_Win32_Devices_Usb == "on"]
public let AUTO_FLUSH: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(6)

@When[Windows_Win32_Devices_Usb == "on"]
public let RAW_IO: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(7)

@When[Windows_Win32_Devices_Usb == "on"]
public let MAXIMUM_TRANSFER_SIZE: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(8)

@When[Windows_Win32_Devices_Usb == "on"]
public let RESET_PIPE_ON_RESUME: WINUSB_PIPE_POLICY = WINUSB_PIPE_POLICY(9)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct WINUSB_POWER_POLICY {
    public var value: UInt32 = 0

    public init() {}

    public init(value: UInt32) {
        this.value = value
    }

    public operator func ==(other: WINUSB_POWER_POLICY): Bool {
        return this.value == other.value
    }
    public operator func !=(other: WINUSB_POWER_POLICY): Bool {
        return this.value != other.value
    }
}

@When[Windows_Win32_Devices_Usb == "on"]
public let AUTO_SUSPEND: WINUSB_POWER_POLICY = WINUSB_POWER_POLICY(129)

@When[Windows_Win32_Devices_Usb == "on"]
public let SUSPEND_DELAY: WINUSB_POWER_POLICY = WINUSB_POWER_POLICY(131)

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct WINUSB_SETUP_PACKET {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    public func requestType(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 0)
        }
    }

    public mut func set_requestType(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 0, val)
            _raw = rawStorage
        }
    }

    public func request(): UInt8 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt8(base, 1)
        }
    }

    public mut func set_request(val: UInt8): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt8(base, 1, val)
            _raw = rawStorage
        }
    }

    public func value(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 2)
        }
    }

    public mut func set_value(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 2, val)
            _raw = rawStorage
        }
    }

    public func index(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 4)
        }
    }

    public mut func set_index(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 4, val)
            _raw = rawStorage
        }
    }

    public func length(): UInt16 {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            return packedReadUInt16(base, 6)
        }
    }

    public mut func set_length(val: UInt16): Unit {
        unsafe {
            var rawStorage = _raw
            let base = CPointer<UInt8>(inout rawStorage)
            packedWriteUInt16(base, 6, val)
            _raw = rawStorage
        }
    }
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_BULK_OR_INTERRUPT_TRANSFER {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var TransferFlags: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_CONTROL_DESCRIPTOR_REQUEST {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
    public var Reserved0: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var Reserved1: UInt16 = 0
    public var Index: UInt8 = 0
    public var DescriptorType: UInt8 = 0
    public var LanguageId: UInt16 = 0
    public var Reserved2: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_CONTROL_FEATURE_REQUEST {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
    public var Reserved2: UInt32 = 0
    public var Reserved3: UInt32 = 0
    public var Reserved4: CPointer<Unit> = CPointer<Unit>()
    public var Reserved5: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var Reserved0: UInt16 = 0
    public var FeatureSelector: UInt16 = 0
    public var Index: UInt16 = 0
    public var Reserved1: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_CONTROL_GET_CONFIGURATION_REQUEST {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
    public var Reserved0: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var Reserved1: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_CONTROL_GET_INTERFACE_REQUEST {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
    public var Reserved0: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var Reserved1: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)
    public var Interface: UInt16 = 0
    public var Reserved2: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_CONTROL_GET_STATUS_REQUEST {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
    public var Reserved0: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var Reserved1: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)
    public var Index: UInt16 = 0
    public var Reserved2: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_CONTROL_TRANSFER {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var TransferFlags: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var SetupPacket: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_CONTROL_TRANSFER_EX {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var TransferFlags: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var Timeout: UInt32 = 0
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var SetupPacket: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_CONTROL_VENDOR_OR_CLASS_REQUEST {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
    public var TransferFlags: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var RequestTypeReservedBits: UInt8 = 0
    public var Request: UInt8 = 0
    public var Value: UInt16 = 0
    public var Index: UInt16 = 0
    public var Reserved1: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_FRAME_LENGTH_CONTROL {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_GET_CURRENT_FRAME_NUMBER {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var FrameNumber: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_GET_FRAME_LENGTH {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var FrameLength: UInt32 = 0
    public var FrameNumber: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_GET_ISOCH_PIPE_TRANSFER_PATH_DELAYS {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var MaximumSendPathDelayInMilliSeconds: UInt32 = 0
    public var MaximumCompletionPathDelayInMilliSeconds: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_HCD_AREA {
    public var Reserved8: VArray<CPointer<Unit>, $8> = VArray<CPointer<Unit>, $8>(repeat: CPointer<Unit>())
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_HEADER {
    public var Length: UInt16 = 0
    public var Function: UInt16 = 0
    public var Status: Int32 = 0
    public var UsbdDeviceHandle: CPointer<Unit> = CPointer<Unit>()
    public var UsbdFlags: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_ISOCH_TRANSFER {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var TransferFlags: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var StartFrame: UInt32 = 0
    public var NumberOfPackets: UInt32 = 0
    public var ErrorCount: UInt32 = 0
    public var IsoPacket: VArray<USBD_ISO_PACKET_DESCRIPTOR, $1> = VArray<USBD_ISO_PACKET_DESCRIPTOR, $1>(repeat: USBD_ISO_PACKET_DESCRIPTOR())
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_OPEN_STATIC_STREAMS {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var NumberOfStreams: UInt32 = 0
    public var StreamInfoVersion: UInt16 = 0
    public var StreamInfoSize: UInt16 = 0
    public var Streams: CPointer<USBD_STREAM_INFORMATION> = CPointer<USBD_STREAM_INFORMATION>()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_OS_FEATURE_DESCRIPTOR_REQUEST {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var Reserved: CPointer<Unit> = CPointer<Unit>()
    public var Reserved0: UInt32 = 0
    public var TransferBufferLength: UInt32 = 0
    public var TransferBuffer: CPointer<Unit> = CPointer<Unit>()
    public var TransferBufferMDL: CPointer<Unit> = CPointer<Unit>()
    public var UrbLink: CPointer<URB> = CPointer<URB>()
    public var hca: _URB_HCD_AREA = _URB_HCD_AREA()
    public var _bitfield: UInt8 = 0
    public var Reserved2: UInt8 = 0
    public var InterfaceNumber: UInt8 = 0
    public var MS_PageIndex: UInt8 = 0
    public var MS_FeatureDescriptorIndex: UInt16 = 0
    public var Reserved3: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_PIPE_REQUEST {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var PipeHandle: CPointer<Unit> = CPointer<Unit>()
    public var Reserved: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_SELECT_CONFIGURATION {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var ConfigurationDescriptor: CPointer<USB_CONFIGURATION_DESCRIPTOR> = CPointer<USB_CONFIGURATION_DESCRIPTOR>()
    public var ConfigurationHandle: CPointer<Unit> = CPointer<Unit>()
    public var Interface: USBD_INTERFACE_INFORMATION = USBD_INTERFACE_INFORMATION()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_SELECT_INTERFACE {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var ConfigurationHandle: CPointer<Unit> = CPointer<Unit>()
    public var Interface: USBD_INTERFACE_INFORMATION = USBD_INTERFACE_INFORMATION()
    public init() {}
}

@When[Windows_Win32_Devices_Usb == "on"]
@C
public struct _URB_SET_FRAME_LENGTH {
    public var Hdr: _URB_HEADER = _URB_HEADER()
    public var FrameLengthDelta: Int32 = 0
    public init() {}
}

@When[WINUSB == "on"]
foreign {
    func WinUsb_AbortPipe(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PipeID: UInt8): Int32
    func WinUsb_ControlTransfer(InterfaceHandle: WINUSB_INTERFACE_HANDLE, SetupPacket: WINUSB_SETUP_PACKET, Buffer: CPointer<UInt8>, BufferLength: UInt32, LengthTransferred: CPointer<UInt32>, Overlapped: CPointer<Win32_System_IO.OVERLAPPED>): Int32
    func WinUsb_FlushPipe(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PipeID: UInt8): Int32
    func WinUsb_Free(InterfaceHandle: WINUSB_INTERFACE_HANDLE): Int32
    func WinUsb_GetAdjustedFrameNumber(CurrentFrameNumber: CPointer<UInt32>, TimeStamp: Int64): Int32
    func WinUsb_GetAssociatedInterface(InterfaceHandle: WINUSB_INTERFACE_HANDLE, AssociatedInterfaceIndex: UInt8, AssociatedInterfaceHandle: CPointer<WINUSB_INTERFACE_HANDLE>): Int32
    func WinUsb_GetCurrentAlternateSetting(InterfaceHandle: WINUSB_INTERFACE_HANDLE, SettingNumber: CPointer<UInt8>): Int32
    func WinUsb_GetCurrentFrameNumber(InterfaceHandle: WINUSB_INTERFACE_HANDLE, CurrentFrameNumber: CPointer<UInt32>, TimeStamp: CPointer<Int64>): Int32
    func WinUsb_GetCurrentFrameNumberAndQpc(InterfaceHandle: WINUSB_INTERFACE_HANDLE, FrameQpcInfo: CPointer<USB_FRAME_NUMBER_AND_QPC_FOR_TIME_SYNC_INFORMATION>): Int32
    func WinUsb_GetDescriptor(InterfaceHandle: WINUSB_INTERFACE_HANDLE, DescriptorType: UInt8, Index: UInt8, LanguageID: UInt16, Buffer: CPointer<UInt8>, BufferLength: UInt32, LengthTransferred: CPointer<UInt32>): Int32
    func WinUsb_GetOverlappedResult(InterfaceHandle: WINUSB_INTERFACE_HANDLE, lpOverlapped: CPointer<Win32_System_IO.OVERLAPPED>, lpNumberOfBytesTransferred: CPointer<UInt32>, bWait: Int32): Int32
    func WinUsb_GetPipePolicy(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PipeID: UInt8, PolicyType: WINUSB_PIPE_POLICY, ValueLength: CPointer<UInt32>, Value: CPointer<Unit>): Int32
    func WinUsb_GetPowerPolicy(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PolicyType: WINUSB_POWER_POLICY, ValueLength: CPointer<UInt32>, Value: CPointer<Unit>): Int32
    func WinUsb_Initialize(DeviceHandle: CPointer<Unit>, InterfaceHandle: CPointer<WINUSB_INTERFACE_HANDLE>): Int32
    func WinUsb_ParseConfigurationDescriptor(ConfigurationDescriptor: CPointer<USB_CONFIGURATION_DESCRIPTOR>, StartPosition: CPointer<Unit>, InterfaceNumber: Int32, AlternateSetting: Int32, InterfaceClass: Int32, InterfaceSubClass: Int32, InterfaceProtocol: Int32): CPointer<USB_INTERFACE_DESCRIPTOR>
    func WinUsb_ParseDescriptors(DescriptorBuffer: CPointer<Unit>, TotalLength: UInt32, StartPosition: CPointer<Unit>, DescriptorType: Int32): CPointer<USB_COMMON_DESCRIPTOR>
    func WinUsb_QueryDeviceInformation(InterfaceHandle: WINUSB_INTERFACE_HANDLE, InformationType: UInt32, BufferLength: CPointer<UInt32>, Buffer: CPointer<Unit>): Int32
    func WinUsb_QueryInterfaceSettings(InterfaceHandle: WINUSB_INTERFACE_HANDLE, AlternateInterfaceNumber: UInt8, UsbAltInterfaceDescriptor: CPointer<USB_INTERFACE_DESCRIPTOR>): Int32
    func WinUsb_QueryPipe(InterfaceHandle: WINUSB_INTERFACE_HANDLE, AlternateInterfaceNumber: UInt8, PipeIndex: UInt8, PipeInformation: CPointer<WINUSB_PIPE_INFORMATION>): Int32
    func WinUsb_QueryPipeEx(InterfaceHandle: WINUSB_INTERFACE_HANDLE, AlternateSettingNumber: UInt8, PipeIndex: UInt8, PipeInformationEx: CPointer<WINUSB_PIPE_INFORMATION_EX>): Int32
    func WinUsb_ReadIsochPipe(BufferHandle: CPointer<Unit>, Offset: UInt32, Length: UInt32, FrameNumber: CPointer<UInt32>, NumberOfPackets: UInt32, IsoPacketDescriptors: CPointer<USBD_ISO_PACKET_DESCRIPTOR>, Overlapped: CPointer<Win32_System_IO.OVERLAPPED>): Int32
    func WinUsb_ReadIsochPipeAsap(BufferHandle: CPointer<Unit>, Offset: UInt32, Length: UInt32, ContinueStream: Int32, NumberOfPackets: UInt32, IsoPacketDescriptors: CPointer<USBD_ISO_PACKET_DESCRIPTOR>, Overlapped: CPointer<Win32_System_IO.OVERLAPPED>): Int32
    func WinUsb_ReadPipe(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PipeID: UInt8, Buffer: CPointer<UInt8>, BufferLength: UInt32, LengthTransferred: CPointer<UInt32>, Overlapped: CPointer<Win32_System_IO.OVERLAPPED>): Int32
    func WinUsb_RegisterIsochBuffer(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PipeID: UInt8, Buffer: CPointer<UInt8>, BufferLength: UInt32, IsochBufferHandle: CPointer<CPointer<Unit>>): Int32
    func WinUsb_ResetPipe(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PipeID: UInt8): Int32
    func WinUsb_SetCurrentAlternateSetting(InterfaceHandle: WINUSB_INTERFACE_HANDLE, SettingNumber: UInt8): Int32
    func WinUsb_SetPipePolicy(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PipeID: UInt8, PolicyType: WINUSB_PIPE_POLICY, ValueLength: UInt32, Value: CPointer<Unit>): Int32
    func WinUsb_SetPowerPolicy(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PolicyType: WINUSB_POWER_POLICY, ValueLength: UInt32, Value: CPointer<Unit>): Int32
    func WinUsb_StartTrackingForTimeSync(InterfaceHandle: WINUSB_INTERFACE_HANDLE, StartTrackingInfo: CPointer<USB_START_TRACKING_FOR_TIME_SYNC_INFORMATION>): Int32
    func WinUsb_StopTrackingForTimeSync(InterfaceHandle: WINUSB_INTERFACE_HANDLE, StopTrackingInfo: CPointer<USB_STOP_TRACKING_FOR_TIME_SYNC_INFORMATION>): Int32
    func WinUsb_UnregisterIsochBuffer(IsochBufferHandle: CPointer<Unit>): Int32
    func WinUsb_WriteIsochPipe(BufferHandle: CPointer<Unit>, Offset: UInt32, Length: UInt32, FrameNumber: CPointer<UInt32>, Overlapped: CPointer<Win32_System_IO.OVERLAPPED>): Int32
    func WinUsb_WriteIsochPipeAsap(BufferHandle: CPointer<Unit>, Offset: UInt32, Length: UInt32, ContinueStream: Int32, Overlapped: CPointer<Win32_System_IO.OVERLAPPED>): Int32
    func WinUsb_WritePipe(InterfaceHandle: WINUSB_INTERFACE_HANDLE, PipeID: UInt8, Buffer: CPointer<UInt8>, BufferLength: UInt32, LengthTransferred: CPointer<UInt32>, Overlapped: CPointer<Win32_System_IO.OVERLAPPED>): Int32
}

@When[Windows_Win32_Devices_Usb == "on"]
public let DEVICE_SPEED: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let LowSpeed: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let FullSpeed: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HighSpeed: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_TRANSFER_IN: UInt32 = 2277389u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_TRANSFER_IN_APPEND_ZERO_PKT: UInt32 = 2277393u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_TRANSFER_OUT: UInt32 = 2277398u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_CONTROL_STATUS_HANDSHAKE_IN: UInt32 = 2277400u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_CONTROL_STATUS_HANDSHAKE_OUT: UInt32 = 2277404u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_GET_CLASS_INFO: UInt32 = 2277410u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_GET_PIPE_STATE: UInt32 = 2277414u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_SET_PIPE_STATE: UInt32 = 2277417u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_ACTIVATE_USB_BUS: UInt32 = 2277420u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_DEACTIVATE_USB_BUS: UInt32 = 2277424u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_BUS_EVENT_NOTIFICATION: UInt32 = 2277430u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_GET_CLASS_INFO_EX: UInt32 = 2277434u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_GET_INTERFACE_DESCRIPTOR_SET: UInt32 = 2277438u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GENERICUSBFN_REGISTER_USB_STRING: UInt32 = 2277441u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_VERSION: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_CONTROLLER_INFO_0: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_CONTROLLER_DRIVER_KEY: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_PASS_THRU: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_POWER_STATE_MAP: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_BANDWIDTH_INFORMATION: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_BUS_STATISTICS_0: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_ROOTHUB_SYMBOLIC_NAME: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_USB_DRIVER_VERSION: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_USB2_HW_VERSION: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_USB_REFRESH_HCT_REG: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_OP_SEND_ONE_PACKET: UInt32 = 268435457u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_OP_RAW_RESET_PORT: UInt32 = 536870913u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_OP_OPEN_RAW_DEVICE: UInt32 = 536870914u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_OP_CLOSE_RAW_DEVICE: UInt32 = 536870915u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_OP_SEND_RAW_COMMAND: UInt32 = 536870916u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_SET_ROOTPORT_FEATURE: UInt32 = 536870917u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_CLEAR_ROOTPORT_FEATURE: UInt32 = 536870918u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_GET_ROOTPORT_STATUS: UInt32 = 536870919u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_INVALID_REQUEST: UInt32 = 4294967280u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_OP_MASK_DEVONLY_API: UInt32 = 268435456u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBUSER_OP_MASK_HCTEST_API: UInt32 = 536870912u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_LOW_SPEED: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_FULL_SPEED: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_HIGH_SPEED: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_ASYNC_IN: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_ASYNC_OUT: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_ISO_IN: UInt32 = 32u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_ISO_OUT: UInt32 = 64u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_SETUP: UInt32 = 128u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_TOGGLE0: UInt32 = 256u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PACKETFLAG_TOGGLE1: UInt32 = 512u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_HC_FEATURE_FLAG_PORT_POWER_SWITCHING: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_HC_FEATURE_FLAG_SEL_SUSPEND: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_HC_FEATURE_LEGACY_BIOS: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_HC_FEATURE_TIME_SYNC_API: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUBMIT_URB: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_RESET_PORT: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_ROOTHUB_PDO: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_PORT_STATUS: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENABLE_PORT: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_HUB_COUNT: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CYCLE_PORT: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_HUB_NAME: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_IDLE_NOTIFICATION: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_RECORD_FAILURE: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_BUS_INFO: UInt32 = 264u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_CONTROLLER_NAME: UInt32 = 265u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_BUSGUID_INFO: UInt32 = 266u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_PARENT_HUB_INFO: UInt32 = 267u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_DEVICE_HANDLE: UInt32 = 268u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_DEVICE_HANDLE_EX: UInt32 = 269u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_TT_DEVICE_HANDLE: UInt32 = 270u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_TOPOLOGY_ADDRESS: UInt32 = 271u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_IDLE_NOTIFICATION_EX: UInt32 = 272u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQ_GLOBAL_SUSPEND: UInt32 = 273u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQ_GLOBAL_RESUME: UInt32 = 274u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_HUB_CONFIG_INFO: UInt32 = 275u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FAIL_GET_STATUS: UInt32 = 280u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REGISTER_COMPOSITE_DEVICE: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_UNREGISTER_COMPOSITE_DEVICE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_REMOTE_WAKE_NOTIFICATION: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_GET_STATS_1: UInt32 = 255u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_DIAGNOSTIC_MODE_ON: UInt32 = 256u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_DIAGNOSTIC_MODE_OFF: UInt32 = 257u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_GET_ROOT_HUB_NAME: UInt32 = 258u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_GET_DRIVERKEY_NAME: UInt32 = 265u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_GET_STATS_2: UInt32 = 266u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_DISABLE_PORT: UInt32 = 268u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_ENABLE_PORT: UInt32 = 269u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_USER_REQUEST: UInt32 = 270u32
@When[Windows_Win32_Devices_Usb == "on"]
public let HCD_TRACE_READ_REQUEST: UInt32 = 275u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_NODE_INFORMATION: UInt32 = 258u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_NODE_CONNECTION_INFORMATION: UInt32 = 259u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_DESCRIPTOR_FROM_NODE_CONNECTION: UInt32 = 260u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_NODE_CONNECTION_NAME: UInt32 = 261u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DIAG_IGNORE_HUBS_ON: UInt32 = 262u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DIAG_IGNORE_HUBS_OFF: UInt32 = 263u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_NODE_CONNECTION_DRIVERKEY_NAME: UInt32 = 264u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_HUB_CAPABILITIES: UInt32 = 271u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_NODE_CONNECTION_ATTRIBUTES: UInt32 = 272u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_HUB_CYCLE_PORT: UInt32 = 273u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_NODE_CONNECTION_INFORMATION_EX: UInt32 = 274u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_RESET_HUB: UInt32 = 275u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_HUB_CAPABILITIES_EX: UInt32 = 276u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_HUB_INFORMATION_EX: UInt32 = 277u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_PORT_CONNECTOR_PROPERTIES: UInt32 = 278u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_NODE_CONNECTION_INFORMATION_EX_V2: UInt32 = 279u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_TRANSPORT_CHARACTERISTICS: UInt32 = 281u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REGISTER_FOR_TRANSPORT_CHARACTERISTICS_CHANGE: UInt32 = 282u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_NOTIFY_ON_TRANSPORT_CHARACTERISTICS_CHANGE: UInt32 = 283u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_UNREGISTER_FOR_TRANSPORT_CHARACTERISTICS_CHANGE: UInt32 = 284u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_START_TRACKING_FOR_TIME_SYNC: UInt32 = 285u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_FRAME_NUMBER_AND_QPC_FOR_TIME_SYNC: UInt32 = 286u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_STOP_TRACKING_FOR_TIME_SYNC: UInt32 = 287u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_DEVICE_CHARACTERISTICS: UInt32 = 288u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_NODE_CONNECTION_SUPERSPEEDPLUS_INFORMATION: UInt32 = 289u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_RESERVED_USER_BASE: UInt32 = 1024u32
@When[Windows_Win32_Devices_Usb == "on"]
public let FILE_DEVICE_USB: UInt32 = 34u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_HOST_TO_DEVICE: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_DEVICE_TO_HOST: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_STANDARD: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_CLASS: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_VENDOR: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_TO_DEVICE: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_TO_INTERFACE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_TO_ENDPOINT: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BMREQUEST_TO_OTHER: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_GET_STATUS: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_CLEAR_FEATURE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SET_FEATURE: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SET_ADDRESS: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_GET_DESCRIPTOR: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SET_DESCRIPTOR: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_GET_CONFIGURATION: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SET_CONFIGURATION: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_GET_INTERFACE: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SET_INTERFACE: UInt32 = 11u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SYNC_FRAME: UInt32 = 12u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_GET_FIRMWARE_STATUS: UInt32 = 26u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SET_FIRMWARE_STATUS: UInt32 = 27u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_FIRMWARE_ALLOWED_OR_DISALLOWED_STATE: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GET_FIRMWARE_HASH: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_FIRMWARE_HASH_LENGTH: UInt32 = 32u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DISALLOW_FIRMWARE_UPDATE: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ALLOW_FIRMWARE_UPDATE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SET_SEL: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_ISOCH_DELAY: UInt32 = 49u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_DESCRIPTOR_TYPE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CONFIGURATION_DESCRIPTOR_TYPE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_STRING_DESCRIPTOR_TYPE: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_INTERFACE_DESCRIPTOR_TYPE: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_DESCRIPTOR_TYPE: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_QUALIFIER_DESCRIPTOR_TYPE: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_OTHER_SPEED_CONFIGURATION_DESCRIPTOR_TYPE: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_INTERFACE_POWER_DESCRIPTOR_TYPE: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let EUSB2_ISOCH_ENDPOINT_COMPANION_DESCRIPTOR_TYPE: UInt32 = 18u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_OTG_DESCRIPTOR_TYPE: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEBUG_DESCRIPTOR_TYPE: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_INTERFACE_ASSOCIATION_DESCRIPTOR_TYPE: UInt32 = 11u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_BOS_DESCRIPTOR_TYPE: UInt32 = 15u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_DESCRIPTOR_TYPE: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPERSPEED_ENDPOINT_COMPANION_DESCRIPTOR_TYPE: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPERSPEEDPLUS_ISOCH_ENDPOINT_COMPANION_DESCRIPTOR_TYPE: UInt32 = 49u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_RESERVED_DESCRIPTOR_TYPE: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CONFIG_POWER_DESCRIPTOR_TYPE: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_ENDPOINT_STALL: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_REMOTE_WAKEUP: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_TEST_MODE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_FUNCTION_SUSPEND: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_U1_ENABLE: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_U2_ENABLE: UInt32 = 49u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_LTM_ENABLE: UInt32 = 50u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_LDM_ENABLE: UInt32 = 53u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_BATTERY_WAKE_MASK: UInt32 = 40u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_OS_IS_PD_AWARE: UInt32 = 41u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_POLICY_MODE: UInt32 = 42u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_CHARGING_POLICY: UInt32 = 54u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CHARGING_POLICY_DEFAULT: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CHARGING_POLICY_ICCHPF: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CHARGING_POLICY_ICCLPF: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CHARGING_POLICY_NO_POWER: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_STATUS_PORT_STATUS: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_STATUS_PD_STATUS: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_STATUS_EXT_PORT_STATUS: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GETSTATUS_SELF_POWERED: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GETSTATUS_REMOTE_WAKEUP_ENABLED: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GETSTATUS_U1_ENABLE: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GETSTATUS_U2_ENABLE: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_GETSTATUS_LTM_ENABLE: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_RESERVED: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_AUDIO: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_COMMUNICATIONS: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_HUMAN_INTERFACE: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_MONITOR: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_PHYSICAL_INTERFACE: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_POWER: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_IMAGE: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_PRINTER: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_STORAGE: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_HUB: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_CDC_DATA: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_SMART_CARD: UInt32 = 11u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_CONTENT_SECURITY: UInt32 = 13u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_VIDEO: UInt32 = 14u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_PERSONAL_HEALTHCARE: UInt32 = 15u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_AUDIO_VIDEO: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_BILLBOARD: UInt32 = 17u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_DIAGNOSTIC_DEVICE: UInt32 = 220u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_WIRELESS_CONTROLLER: UInt32 = 224u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_MISCELLANEOUS: UInt32 = 239u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_APPLICATION_SPECIFIC: UInt32 = 254u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CLASS_VENDOR_SPECIFIC: UInt32 = 255u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_WIRELESS_USB: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_USB20_EXTENSION: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_USB: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_CONTAINER_ID: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_PLATFORM: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_POWER_DELIVERY: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_BATTERY_INFO: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_PD_CONSUMER_PORT: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_PD_PROVIDER_PORT: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_USB: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_PRECISION_TIME_MEASUREMENT: UInt32 = 11u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_BILLBOARD: UInt32 = 13u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_FIRMWARE_STATUS: UInt32 = 17u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_USB20_EXTENSION_BMATTRIBUTES_RESERVED_MASK: UInt32 = 4294901985u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_BMATTRIBUTES_RESERVED_MASK: UInt32 = 253u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_BMATTRIBUTES_LTM_CAPABLE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_SPEEDS_SUPPORTED_RESERVED_MASK: UInt32 = 65520u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_SPEEDS_SUPPORTED_LOW: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_SPEEDS_SUPPORTED_FULL: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_SPEEDS_SUPPORTED_HIGH: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_SPEEDS_SUPPORTED_SUPER: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_U1_DEVICE_EXIT_MAX_VALUE: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEED_U2_DEVICE_EXIT_MAX_VALUE: UInt32 = 2047u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_MAX_U1_LATENCY: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_MAX_U2_LATENCY: UInt32 = 2047u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_LSE_BPS: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_LSE_KBPS: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_LSE_MBPS: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_LSE_GBPS: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_MODE_SYMMETRIC: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_MODE_ASYMMETRIC: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_DIR_RX: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_DIR_TX: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_PROTOCOL_SS: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CAPABILITY_SUPERSPEEDPLUS_SPEED_PROTOCOL_SSP: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CONFIG_POWERED_MASK: UInt32 = 192u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CONFIG_BUS_POWERED: UInt32 = 128u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CONFIG_SELF_POWERED: UInt32 = 64u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CONFIG_REMOTE_WAKEUP: UInt32 = 32u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_CONFIG_RESERVED: UInt32 = 31u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_DIRECTION_MASK: UInt32 = 128u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_ADDRESS_MASK: UInt32 = 15u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_MASK: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_CONTROL: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_BULK: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_INTERRUPT: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_BULK_RESERVED_MASK: UInt32 = 252u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_CONTROL_RESERVED_MASK: UInt32 = 252u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_20_ENDPOINT_TYPE_INTERRUPT_RESERVED_MASK: UInt32 = 252u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_30_ENDPOINT_TYPE_INTERRUPT_RESERVED_MASK: UInt32 = 204u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_RESERVED_MASK: UInt32 = 192u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_30_ENDPOINT_TYPE_INTERRUPT_USAGE_MASK: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_30_ENDPOINT_TYPE_INTERRUPT_USAGE_PERIODIC: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_30_ENDPOINT_TYPE_INTERRUPT_USAGE_NOTIFICATION: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_30_ENDPOINT_TYPE_INTERRUPT_USAGE_RESERVED10: UInt32 = 32u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_30_ENDPOINT_TYPE_INTERRUPT_USAGE_RESERVED11: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_SYNCHRONIZATION_MASK: UInt32 = 12u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_SYNCHRONIZATION_NO_SYNCHRONIZATION: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_SYNCHRONIZATION_ASYNCHRONOUS: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_SYNCHRONIZATION_ADAPTIVE: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_SYNCHRONIZATION_SYNCHRONOUS: UInt32 = 12u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_USAGE_MASK: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_USAGE_DATA_ENDOINT: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_USAGE_FEEDBACK_ENDPOINT: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_USAGE_IMPLICIT_FEEDBACK_DATA_ENDPOINT: UInt32 = 32u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_TYPE_ISOCHRONOUS_USAGE_RESERVED: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_SUPERSPEED_BULK_MAX_PACKET_SIZE: UInt32 = 1024u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_SUPERSPEED_CONTROL_MAX_PACKET_SIZE: UInt32 = 512u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_SUPERSPEED_ISO_MAX_PACKET_SIZE: UInt32 = 1024u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_ENDPOINT_SUPERSPEED_INTERRUPT_MAX_PACKET_SIZE: UInt32 = 1024u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MAXIMUM_USB_STRING_LENGTH: UInt32 = 255u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPERSPEED_ISOCHRONOUS_MAX_MULTIPLIER: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPERSPEEDPLUS_ISOCHRONOUS_MIN_BYTESPERINTERVAL: UInt32 = 49153u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPERSPEEDPLUS_ISOCHRONOUS_MAX_BYTESPERINTERVAL: UInt32 = 16777215u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_HIGHSPEED_EUSB2_ISOCHRONOUS_MIN_BYTESPERINTERVAL: UInt32 = 3073u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_HIGHSPEED_EUSB2_ISOCHRONOUS_MAX_BYTESPERINTERVAL: UInt32 = 6144u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_20_HUB_DESCRIPTOR_TYPE: UInt32 = 41u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_30_HUB_DESCRIPTOR_TYPE: UInt32 = 42u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_GET_STATE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_CLEAR_TT_BUFFER: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_RESET_TT: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_GET_TT_STATE: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_STOP_TT: UInt32 = 11u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_SET_HUB_DEPTH: UInt32 = 12u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REQUEST_GET_PORT_ERR_COUNT: UInt32 = 13u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORT_STATUS_CONNECT: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORT_STATUS_ENABLE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORT_STATUS_SUSPEND: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORT_STATUS_OVER_CURRENT: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORT_STATUS_RESET: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORT_STATUS_POWER: UInt32 = 256u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORT_STATUS_LOW_SPEED: UInt32 = 512u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORT_STATUS_HIGH_SPEED: UInt32 = 1024u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_U0: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_U1: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_U2: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_U3: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_DISABLED: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_RX_DETECT: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_INACTIVE: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_POLLING: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_RECOVERY: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_HOT_RESET: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_COMPLIANCE_MODE: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_LOOPBACK: UInt32 = 11u32
@When[Windows_Win32_Devices_Usb == "on"]
public let PORT_LINK_STATE_TEST_MODE: UInt32 = 11u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_INTERFACE_POWER_D0: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_INTERFACE_POWER_D1: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_INTERFACE_POWER_D2: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_FEATURE_INTERFACE_POWER_D3: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPPORT_D0_COMMAND: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPPORT_D1_COMMAND: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPPORT_D2_COMMAND: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPPORT_D3_COMMAND: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPPORT_D1_WAKEUP: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_SUPPORT_D2_WAKEUP: UInt32 = 32u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBDI_VERSION: UInt32 = 1536u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORTATTR_NO_CONNECTOR: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORTATTR_SHARED_USB2: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORTATTR_MINI_CONNECTOR: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORTATTR_OEM_CONNECTOR: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORTATTR_OWNED_BY_CC: UInt32 = 16777216u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_PORTATTR_NO_OVERCURRENT_UI: UInt32 = 33554432u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEFAULT_DEVICE_ADDRESS: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEFAULT_ENDPOINT_ADDRESS: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEFAULT_MAX_PACKET: UInt32 = 64u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SELECT_CONFIGURATION: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SELECT_INTERFACE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_ABORT_PIPE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_TAKE_FRAME_LENGTH_CONTROL: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RELEASE_FRAME_LENGTH_CONTROL: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_FRAME_LENGTH: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SET_FRAME_LENGTH: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_CURRENT_FRAME_NUMBER: UInt32 = 7u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CONTROL_TRANSFER: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_BULK_OR_INTERRUPT_TRANSFER: UInt32 = 9u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_ISOCH_TRANSFER: UInt32 = 10u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_DESCRIPTOR_FROM_DEVICE: UInt32 = 11u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SET_DESCRIPTOR_TO_DEVICE: UInt32 = 12u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SET_FEATURE_TO_DEVICE: UInt32 = 13u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SET_FEATURE_TO_INTERFACE: UInt32 = 14u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SET_FEATURE_TO_ENDPOINT: UInt32 = 15u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLEAR_FEATURE_TO_DEVICE: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLEAR_FEATURE_TO_INTERFACE: UInt32 = 17u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLEAR_FEATURE_TO_ENDPOINT: UInt32 = 18u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_STATUS_FROM_DEVICE: UInt32 = 19u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_STATUS_FROM_INTERFACE: UInt32 = 20u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_STATUS_FROM_ENDPOINT: UInt32 = 21u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVED_0X0016: UInt32 = 22u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_VENDOR_DEVICE: UInt32 = 23u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_VENDOR_INTERFACE: UInt32 = 24u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_VENDOR_ENDPOINT: UInt32 = 25u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLASS_DEVICE: UInt32 = 26u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLASS_INTERFACE: UInt32 = 27u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLASS_ENDPOINT: UInt32 = 28u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVE_0X001D: UInt32 = 29u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SYNC_RESET_PIPE_AND_CLEAR_STALL: UInt32 = 30u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLASS_OTHER: UInt32 = 31u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_VENDOR_OTHER: UInt32 = 32u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_STATUS_FROM_OTHER: UInt32 = 33u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLEAR_FEATURE_TO_OTHER: UInt32 = 34u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SET_FEATURE_TO_OTHER: UInt32 = 35u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_DESCRIPTOR_FROM_ENDPOINT: UInt32 = 36u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SET_DESCRIPTOR_TO_ENDPOINT: UInt32 = 37u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_CONFIGURATION: UInt32 = 38u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_INTERFACE: UInt32 = 39u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_DESCRIPTOR_FROM_INTERFACE: UInt32 = 40u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SET_DESCRIPTOR_TO_INTERFACE: UInt32 = 41u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVE_0X002B: UInt32 = 43u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVE_0X002C: UInt32 = 44u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVE_0X002D: UInt32 = 45u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVE_0X002E: UInt32 = 46u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVE_0X002F: UInt32 = 47u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_MS_FEATURE_DESCRIPTOR: UInt32 = 42u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SYNC_RESET_PIPE: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_SYNC_CLEAR_STALL: UInt32 = 49u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CONTROL_TRANSFER_EX: UInt32 = 50u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVE_0X0033: UInt32 = 51u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESERVE_0X0034: UInt32 = 52u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_OPEN_STATIC_STREAMS: UInt32 = 53u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_CLOSE_STATIC_STREAMS: UInt32 = 54u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_BULK_OR_INTERRUPT_TRANSFER_USING_CHAINED_MDL: UInt32 = 55u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_ISOCH_TRANSFER_USING_CHAINED_MDL: UInt32 = 56u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_GET_ISOCH_PIPE_TRANSFER_PATH_DELAYS: UInt32 = 61u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_FUNCTION_RESET_PIPE: UInt32 = 30u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_SHORT_TRANSFER_OK: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_START_ISO_TRANSFER_ASAP: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_DEFAULT_PIPE_TRANSFER: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_TRANSFER_DIRECTION_OUT: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_TRANSFER_DIRECTION_IN: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_TRANSFER_DIRECTION: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_ISO_START_FRAME_RANGE: UInt32 = 1024u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_DEFAULT_MAXIMUM_TRANSFER_SIZE: UInt32 = 4294967295u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_CHANGE_MAX_PACKET: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_SHORT_PACKET_OPT: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_ENABLE_RT_THREAD_ACCESS: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_MAP_ADD_TRANSFERS: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_VIDEO_PRIORITY: UInt32 = 16u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_VOICE_PRIORITY: UInt32 = 32u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_INTERACTIVE_PRIORITY: UInt32 = 48u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_PRIORITY_MASK: UInt32 = 240u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_HANDLES_SSP_HIGH_BANDWIDTH_ISOCH: UInt32 = 256u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PF_SSP_HIGH_BANDWIDTH_ISOCH: UInt32 = 65536u32
@When[Windows_Win32_Devices_Usb == "on"]
public let OS_STRING_DESCRIPTOR_INDEX: UInt32 = 238u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MS_GENRE_DESCRIPTOR_INDEX: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MS_POWER_DESCRIPTOR_INDEX: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MS_OS_STRING_SIGNATURE: String = "MSFT100"
@When[Windows_Win32_Devices_Usb == "on"]
public let MS_OS_FLAGS_CONTAINERID: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let URB_OPEN_STATIC_STREAMS_VERSION_100: UInt32 = 256u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB4_MAX_DEPTH: UInt32 = 6u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB4_CONFIGURATION_REGISTERS_DW_LENGTH: UInt32 = 60u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB4_HRD_DEBUG_INTERFACE_REFERENCE_STRING: String = "\\DEBUGINTERFACE"
@When[Windows_Win32_Devices_Usb == "on"]
public let USB4_HRD_DEBUG_FUNCTION_READ_CONFIGURATION_SPACE: UInt32 = 1131u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB4_HRD_DEBUG_READ_CONFIGURATION_SPACE: UInt32 = 6295980u32
@When[Windows_Win32_Devices_Usb == "on"]
public let KREGUSBFNENUMPATH: String = "\\Registry\\Machine\\SYSTEM\\CurrentControlSet\\Control\\USBFN\\"
@When[Windows_Win32_Devices_Usb == "on"]
public let UREGUSBFNENUMPATH: String = "HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Control\\USBFN\\"
@When[Windows_Win32_Devices_Usb == "on"]
public let KREGMANUSBFNENUMPATH: String = "\\Registry\\Machine\\SYSTEM\\CurrentControlSet\\Control\\ManufacturingMode\\Current\\USBFN\\"
@When[Windows_Win32_Devices_Usb == "on"]
public let UREGMANUSBFNENUMPATH: String = "HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Control\\ManufacturingMode\\Current\\USBFN\\"
@When[Windows_Win32_Devices_Usb == "on"]
public let MAX_NUM_USBFN_ENDPOINTS: UInt32 = 15u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MAX_CONFIGURATION_NAME_LENGTH: UInt32 = 40u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MAX_USB_STRING_LENGTH: UInt32 = 255u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MAX_SUPPORTED_CONFIGURATIONS: UInt32 = 12u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBFN_INTERRUPT_ENDPOINT_SIZE_NOT_UPDATEABLE_MASK: UInt32 = 128u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_TEST_MODE_TEST_J: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_TEST_MODE_TEST_K: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_TEST_MODE_TEST_SE0_NAK: UInt32 = 3u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_TEST_MODE_TEST_PACKET: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_TEST_MODE_TEST_FORCE_ENABLE: UInt32 = 5u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MAX_INTERFACE_NAME_LENGTH: UInt32 = 40u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MAX_ALTERNATE_NAME_LENGTH: UInt32 = 40u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MAX_ASSOCIATION_NAME_LENGTH: UInt32 = 40u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_SUBMIT_URB: UInt32 = 2228227u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_RESET_PORT: UInt32 = 2228231u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_ROOTHUB_PDO: UInt32 = 2228239u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PORT_ENABLED: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USBD_PORT_CONNECTED: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_PORT_STATUS: UInt32 = 2228243u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_ENABLE_PORT: UInt32 = 2228247u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_HUB_COUNT: UInt32 = 2228251u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_CYCLE_PORT: UInt32 = 2228255u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_HUB_NAME: UInt32 = 2228256u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_BUS_INFO: UInt32 = 2229280u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_CONTROLLER_NAME: UInt32 = 2229284u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_BUSGUID_INFO: UInt32 = 2229288u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_PARENT_HUB_INFO: UInt32 = 2229292u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_SUBMIT_IDLE_NOTIFICATION: UInt32 = 2228263u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_DEVICE_HANDLE: UInt32 = 2229299u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_NOTIFY_IDLE_READY: UInt32 = 2229315u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_REQ_GLOBAL_SUSPEND: UInt32 = 2229319u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_REQ_GLOBAL_RESUME: UInt32 = 2229323u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_RECORD_FAILURE: UInt32 = 2228267u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_DEVICE_HANDLE_EX: UInt32 = 2229303u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_TT_DEVICE_HANDLE: UInt32 = 2229307u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_TOPOLOGY_ADDRESS: UInt32 = 2229311u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_GET_DEVICE_CONFIG_INFO: UInt32 = 2229327u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_REGISTER_COMPOSITE_DEVICE: UInt32 = 4784131u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_UNREGISTER_COMPOSITE_DEVICE: UInt32 = 4784135u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_REQUEST_REMOTE_WAKE_NOTIFICATION: UInt32 = 4784139u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INTERNAL_USB_FAIL_GET_STATUS_FROM_DEVICE: UInt32 = 2229347u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_HCD_GET_STATS_1: UInt32 = 2229244u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_HCD_GET_STATS_2: UInt32 = 2229288u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_HCD_DISABLE_PORT: UInt32 = 2229296u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_HCD_ENABLE_PORT: UInt32 = 2229300u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_DIAGNOSTIC_MODE_ON: UInt32 = 2229248u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_DIAGNOSTIC_MODE_OFF: UInt32 = 2229252u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_ROOT_HUB_NAME: UInt32 = 2229256u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GET_HCD_DRIVERKEY_NAME: UInt32 = 2229284u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_NODE_INFORMATION: UInt32 = 2229256u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_NODE_CONNECTION_INFORMATION: UInt32 = 2229260u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_DESCRIPTOR_FROM_NODE_CONNECTION: UInt32 = 2229264u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_NODE_CONNECTION_NAME: UInt32 = 2229268u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_DIAG_IGNORE_HUBS_ON: UInt32 = 2229272u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_DIAG_IGNORE_HUBS_OFF: UInt32 = 2229276u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_NODE_CONNECTION_DRIVERKEY_NAME: UInt32 = 2229280u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_HUB_CAPABILITIES: UInt32 = 2229308u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_HUB_CYCLE_PORT: UInt32 = 2229316u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_NODE_CONNECTION_ATTRIBUTES: UInt32 = 2229312u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_NODE_CONNECTION_INFORMATION_EX: UInt32 = 2229320u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_RESET_HUB: UInt32 = 2229324u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_HUB_CAPABILITIES_EX: UInt32 = 2229328u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_HUB_INFORMATION_EX: UInt32 = 2229332u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_PORT_CONNECTOR_PROPERTIES: UInt32 = 2229336u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_NODE_CONNECTION_INFORMATION_EX_V2: UInt32 = 2229340u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_TRANSPORT_CHARACTERISTICS: UInt32 = 2229348u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_REGISTER_FOR_TRANSPORT_CHARACTERISTICS_CHANGE: UInt32 = 2229352u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_NOTIFY_ON_TRANSPORT_CHARACTERISTICS_CHANGE: UInt32 = 2229356u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_UNREGISTER_FOR_TRANSPORT_CHARACTERISTICS_CHANGE: UInt32 = 2229360u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_START_TRACKING_FOR_TIME_SYNC: UInt32 = 2229364u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_FRAME_NUMBER_AND_QPC_FOR_TIME_SYNC: UInt32 = 2229368u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_STOP_TRACKING_FOR_TIME_SYNC: UInt32 = 2229372u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_DEVICE_CHARACTERISTICS: UInt32 = 2229376u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_USB_GET_NODE_CONNECTION_SUPERSPEEDPLUS_INFORMATION: UInt32 = 2229380u32
@When[Windows_Win32_Devices_Usb == "on"]
public let WMI_USB_DRIVER_INFORMATION: UInt32 = 0u32
@When[Windows_Win32_Devices_Usb == "on"]
public let WMI_USB_DRIVER_NOTIFICATION: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let WMI_USB_POWER_DEVICE_ENABLE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let WMI_USB_HUB_NODE_INFORMATION: UInt32 = 4u32
@When[Windows_Win32_Devices_Usb == "on"]
public let WMI_USB_PERFORMANCE_INFORMATION: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let WMI_USB_DEVICE_NODE_INFORMATION: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_TRANSPORT_CHARACTERISTICS_VERSION_1: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_TRANSPORT_CHARACTERISTICS_LATENCY_AVAILABLE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_TRANSPORT_CHARACTERISTICS_BANDWIDTH_AVAILABLE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REGISTER_FOR_TRANSPORT_LATENCY_CHANGE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_REGISTER_FOR_TRANSPORT_BANDWIDTH_CHANGE: UInt32 = 2u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CHARACTERISTICS_VERSION_1: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let USB_DEVICE_CHARACTERISTICS_MAXIMUM_PATH_DELAYS_AVAILABLE: UInt32 = 1u32
@When[Windows_Win32_Devices_Usb == "on"]
public let MAX_NUM_PIPES: UInt32 = 8u32
@When[Windows_Win32_Devices_Usb == "on"]
public let BULKIN_FLAG: UInt32 = 128u32
@When[Windows_Win32_Devices_Usb == "on"]
public let FILE_DEVICE_USB_SCAN: UInt32 = 32768u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_INDEX: UInt32 = 2048u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GET_VERSION: UInt32 = 2147491840u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_CANCEL_IO: UInt32 = 2147491844u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_WAIT_ON_DEVICE_EVENT: UInt32 = 2147491848u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_READ_REGISTERS: UInt32 = 2147491852u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_WRITE_REGISTERS: UInt32 = 2147491856u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GET_CHANNEL_ALIGN_RQST: UInt32 = 2147491860u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GET_DEVICE_DESCRIPTOR: UInt32 = 2147491864u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_RESET_PIPE: UInt32 = 2147491868u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GET_USB_DESCRIPTOR: UInt32 = 2147491872u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_SEND_USB_REQUEST: UInt32 = 2147491876u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_GET_PIPE_CONFIGURATION: UInt32 = 2147491880u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_SET_TIMEOUT: UInt32 = 2147491884u32
@When[Windows_Win32_Devices_Usb == "on"]
public let IOCTL_ABORT_PIPE: UInt32 = 2147491844u32
