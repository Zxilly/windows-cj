// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.Win32._impl_l1

import windows_interface.{ComInterface, GUID, InterfaceDescriptor, InterfaceWrapperBase}
import windows_interface as windows_interface
import windows_polyfill.{packedReadInt32, packedReadInt64, packedReadUInt16, packedReadUInt32, packedReadUInt64, packedReadUInt8, packedReadValue, packedWriteInt32, packedWriteInt64, packedWriteUInt16, packedWriteUInt32, packedWriteUInt64, packedWriteUInt8, packedWriteValue, unionReadInt32, unionReadUInt16, unionReadUInt32, unionReadUInt64, unionReadUInt8, unionWriteInt32, unionWriteUInt16, unionWriteUInt32, unionWriteUInt64, unionWriteUInt8}
import windows_polyfill as windows_polyfill
@When[Windows_Win32_System_SystemInformation == "on"]
import windows_sys.Win32.System.SystemInformation as Win32_System_SystemInformation
@When[Windows_Win32_System_Time == "on"]
import windows_sys.Win32.System.Time as Win32_System_Time
import windows_sys.Win32._impl_l0 as Win32__impl_l0


@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct ADDRESS64 {
    public var Offset: UInt64 = 0
    public var Segment: UInt16 = 0
    public var Mode: Int32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
@C
public struct ARM64_NT_CONTEXT {
    public var ContextFlags: UInt32 = 0
    public var Cpsr: UInt32 = 0
    public var Anonymous: ARM64_NT_CONTEXT__Anonymous_e__Union = ARM64_NT_CONTEXT__Anonymous_e__Union()
    public var Sp: UInt64 = 0
    public var Pc: UInt64 = 0
    public var V: VArray<Win32__impl_l0.ARM64_NT_NEON128, $32> = VArray<Win32__impl_l0.ARM64_NT_NEON128, $32>(repeat: Win32__impl_l0.ARM64_NT_NEON128())
    public var Fpcr: UInt32 = 0
    public var Fpsr: UInt32 = 0
    public var Bcr: VArray<UInt32, $8> = VArray<UInt32, $8>(repeat: 0)
    public var Bvr: VArray<UInt64, $8> = VArray<UInt64, $8>(repeat: 0)
    public var Wcr: VArray<UInt32, $2> = VArray<UInt32, $2>(repeat: 0)
    public var Wvr: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct ARM64_NT_CONTEXT__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $31> = VArray<UInt64, $31>(repeat: 0)

    public mut prop anonymous: ARM64_NT_CONTEXT__Anonymous_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<ARM64_NT_CONTEXT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<ARM64_NT_CONTEXT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop x: VArray<UInt64, $31> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt64, $31>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt64, $31>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
@C
public struct ARM64_NT_CONTEXT__Anonymous_e__Union__Anonymous_e__Struct {
    public var X0: UInt64 = 0
    public var X1: UInt64 = 0
    public var X2: UInt64 = 0
    public var X3: UInt64 = 0
    public var X4: UInt64 = 0
    public var X5: UInt64 = 0
    public var X6: UInt64 = 0
    public var X7: UInt64 = 0
    public var X8: UInt64 = 0
    public var X9: UInt64 = 0
    public var X10: UInt64 = 0
    public var X11: UInt64 = 0
    public var X12: UInt64 = 0
    public var X13: UInt64 = 0
    public var X14: UInt64 = 0
    public var X15: UInt64 = 0
    public var X16: UInt64 = 0
    public var X17: UInt64 = 0
    public var X18: UInt64 = 0
    public var X19: UInt64 = 0
    public var X20: UInt64 = 0
    public var X21: UInt64 = 0
    public var X22: UInt64 = 0
    public var X23: UInt64 = 0
    public var X24: UInt64 = 0
    public var X25: UInt64 = 0
    public var X26: UInt64 = 0
    public var X27: UInt64 = 0
    public var X28: UInt64 = 0
    public var Fp: UInt64 = 0
    public var Lr: UInt64 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "aarch64"]
@C
public struct CONTEXT {
    public var ContextFlags: UInt32 = 0
    public var Cpsr: UInt32 = 0
    public var Anonymous: CONTEXT__Anonymous_e__Union = CONTEXT__Anonymous_e__Union()
    public var Sp: UInt64 = 0
    public var Pc: UInt64 = 0
    public var V: VArray<Win32__impl_l0.ARM64_NT_NEON128, $32> = VArray<Win32__impl_l0.ARM64_NT_NEON128, $32>(repeat: Win32__impl_l0.ARM64_NT_NEON128())
    public var Fpcr: UInt32 = 0
    public var Fpsr: UInt32 = 0
    public var Bcr: VArray<UInt32, $8> = VArray<UInt32, $8>(repeat: 0)
    public var Bvr: VArray<UInt64, $8> = VArray<UInt64, $8>(repeat: 0)
    public var Wcr: VArray<UInt32, $2> = VArray<UInt32, $2>(repeat: 0)
    public var Wvr: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "aarch64"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct CONTEXT__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $31> = VArray<UInt64, $31>(repeat: 0)

    public mut prop anonymous: CONTEXT__Anonymous_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CONTEXT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CONTEXT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop x: VArray<UInt64, $31> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt64, $31>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt64, $31>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "aarch64"]
@C
public struct CONTEXT__Anonymous_e__Union__Anonymous_e__Struct {
    public var X0: UInt64 = 0
    public var X1: UInt64 = 0
    public var X2: UInt64 = 0
    public var X3: UInt64 = 0
    public var X4: UInt64 = 0
    public var X5: UInt64 = 0
    public var X6: UInt64 = 0
    public var X7: UInt64 = 0
    public var X8: UInt64 = 0
    public var X9: UInt64 = 0
    public var X10: UInt64 = 0
    public var X11: UInt64 = 0
    public var X12: UInt64 = 0
    public var X13: UInt64 = 0
    public var X14: UInt64 = 0
    public var X15: UInt64 = 0
    public var X16: UInt64 = 0
    public var X17: UInt64 = 0
    public var X18: UInt64 = 0
    public var X19: UInt64 = 0
    public var X20: UInt64 = 0
    public var X21: UInt64 = 0
    public var X22: UInt64 = 0
    public var X23: UInt64 = 0
    public var X24: UInt64 = 0
    public var X25: UInt64 = 0
    public var X26: UInt64 = 0
    public var X27: UInt64 = 0
    public var X28: UInt64 = 0
    public var Fp: UInt64 = 0
    public var Lr: UInt64 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && (arch == "x86_64" && (arch == "x86_64" || arch == "aarch64"))]
@C
public struct CONTEXT {
    public var P1Home: UInt64 = 0
    public var P2Home: UInt64 = 0
    public var P3Home: UInt64 = 0
    public var P4Home: UInt64 = 0
    public var P5Home: UInt64 = 0
    public var P6Home: UInt64 = 0
    public var ContextFlags: UInt32 = 0
    public var MxCsr: UInt32 = 0
    public var SegCs: UInt16 = 0
    public var SegDs: UInt16 = 0
    public var SegEs: UInt16 = 0
    public var SegFs: UInt16 = 0
    public var SegGs: UInt16 = 0
    public var SegSs: UInt16 = 0
    public var EFlags: UInt32 = 0
    public var Dr0: UInt64 = 0
    public var Dr1: UInt64 = 0
    public var Dr2: UInt64 = 0
    public var Dr3: UInt64 = 0
    public var Dr6: UInt64 = 0
    public var Dr7: UInt64 = 0
    public var Rax: UInt64 = 0
    public var Rcx: UInt64 = 0
    public var Rdx: UInt64 = 0
    public var Rbx: UInt64 = 0
    public var Rsp: UInt64 = 0
    public var Rbp: UInt64 = 0
    public var Rsi: UInt64 = 0
    public var Rdi: UInt64 = 0
    public var R8: UInt64 = 0
    public var R9: UInt64 = 0
    public var R10: UInt64 = 0
    public var R11: UInt64 = 0
    public var R12: UInt64 = 0
    public var R13: UInt64 = 0
    public var R14: UInt64 = 0
    public var R15: UInt64 = 0
    public var Rip: UInt64 = 0
    public var Anonymous: CONTEXT__Anonymous_e__Union = CONTEXT__Anonymous_e__Union()
    public var VectorRegister: VArray<Win32__impl_l0.M128A, $26> = VArray<Win32__impl_l0.M128A, $26>(repeat: Win32__impl_l0.M128A())
    public var VectorControl: UInt64 = 0
    public var DebugControl: UInt64 = 0
    public var LastBranchToRip: UInt64 = 0
    public var LastBranchFromRip: UInt64 = 0
    public var LastExceptionToRip: UInt64 = 0
    public var LastExceptionFromRip: UInt64 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && (arch == "x86_64" && (arch == "x86_64" || arch == "aarch64") && arch == "x86_64")]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct CONTEXT__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $64> = VArray<UInt64, $64>(repeat: 0)

    @When[arch == "x86_64" || arch == "aarch64"]
    public mut prop fltSave: XSAVE_FORMAT {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<XSAVE_FORMAT>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<XSAVE_FORMAT>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: CONTEXT__Anonymous_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CONTEXT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CONTEXT__Anonymous_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && (arch == "x86_64" && (arch == "x86_64" || arch == "aarch64") && arch == "x86_64" && arch == "x86_64")]
@C
public struct CONTEXT__Anonymous_e__Union__Anonymous_e__Struct {
    public var Header: VArray<Win32__impl_l0.M128A, $2> = VArray<Win32__impl_l0.M128A, $2>(repeat: Win32__impl_l0.M128A())
    public var Legacy: VArray<Win32__impl_l0.M128A, $8> = VArray<Win32__impl_l0.M128A, $8>(repeat: Win32__impl_l0.M128A())
    public var Xmm0: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm1: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm2: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm3: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm4: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm5: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm6: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm7: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm8: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm9: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm10: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm11: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm12: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm13: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm14: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public var Xmm15: Win32__impl_l0.M128A = Win32__impl_l0.M128A()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Threading == "on"]
@C
public struct CREATE_PROCESS_DEBUG_INFO {
    public var hFile: CPointer<Unit> = CPointer<Unit>()
    public var hProcess: CPointer<Unit> = CPointer<Unit>()
    public var hThread: CPointer<Unit> = CPointer<Unit>()
    public var lpBaseOfImage: CPointer<Unit> = CPointer<Unit>()
    public var dwDebugInfoFileOffset: UInt32 = 0
    public var nDebugInfoSize: UInt32 = 0
    public var lpThreadLocalBase: CPointer<Unit> = CPointer<Unit>()
    public var lpStartAddress: Win32__impl_l0.LPTHREAD_START_ROUTINE = CFunc<(CPointer<Unit>) -> UInt32>(CPointer<Unit>())
    public var lpImageName: CPointer<Unit> = CPointer<Unit>()
    public var fUnicode: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Threading == "on"]
@C
public struct CREATE_THREAD_DEBUG_INFO {
    public var hThread: CPointer<Unit> = CPointer<Unit>()
    public var lpThreadLocalBase: CPointer<Unit> = CPointer<Unit>()
    public var lpStartAddress: Win32__impl_l0.LPTHREAD_START_ROUTINE = CFunc<(CPointer<Unit>) -> UInt32>(CPointer<Unit>())
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Threading == "on"]
@C
public struct DEBUG_EVENT {
    public var dwDebugEventCode: UInt32 = 0
    public var dwProcessId: UInt32 = 0
    public var dwThreadId: UInt32 = 0
    public var u: DEBUG_EVENT__u_e__Union = DEBUG_EVENT__u_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Threading == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct DEBUG_EVENT__u_e__Union {
    public var _raw: VArray<UInt64, $20> = VArray<UInt64, $20>(repeat: 0)

    public mut prop exception: EXCEPTION_DEBUG_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<EXCEPTION_DEBUG_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<EXCEPTION_DEBUG_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop createThread: CREATE_THREAD_DEBUG_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CREATE_THREAD_DEBUG_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CREATE_THREAD_DEBUG_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop createProcessInfo: CREATE_PROCESS_DEBUG_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CREATE_PROCESS_DEBUG_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CREATE_PROCESS_DEBUG_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop exitThread: Win32__impl_l0.EXIT_THREAD_DEBUG_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.EXIT_THREAD_DEBUG_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.EXIT_THREAD_DEBUG_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop exitProcess: Win32__impl_l0.EXIT_PROCESS_DEBUG_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.EXIT_PROCESS_DEBUG_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.EXIT_PROCESS_DEBUG_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop loadDll: Win32__impl_l0.LOAD_DLL_DEBUG_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.LOAD_DLL_DEBUG_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.LOAD_DLL_DEBUG_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop unloadDll: Win32__impl_l0.UNLOAD_DLL_DEBUG_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.UNLOAD_DLL_DEBUG_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.UNLOAD_DLL_DEBUG_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop debugString: Win32__impl_l0.OUTPUT_DEBUG_STRING_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.OUTPUT_DEBUG_STRING_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.OUTPUT_DEBUG_STRING_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ripInfo: RIP_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<RIP_INFO>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<RIP_INFO>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct DIMM_ADDR_VALID_BITS {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0)

    public mut prop vb_DDR4: Win32__impl_l0.DIMM_ADDR_VALID_BITS_DDR4 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.DIMM_ADDR_VALID_BITS_DDR4>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.DIMM_ADDR_VALID_BITS_DDR4>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop vb_DDR5: Win32__impl_l0.DIMM_ADDR_VALID_BITS_DDR5 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.DIMM_ADDR_VALID_BITS_DDR5>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.DIMM_ADDR_VALID_BITS_DDR5>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop asUINT32: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct DIMM_INFO {
    public var DimmAddress: Win32__impl_l0.DIMM_ADDRESS = Win32__impl_l0.DIMM_ADDRESS()
    public var ValidBits: DIMM_ADDR_VALID_BITS = DIMM_ADDR_VALID_BITS()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct DUMP_HEADER32 {
    public var Signature: UInt32 = 0
    public var ValidDump: UInt32 = 0
    public var MajorVersion: UInt32 = 0
    public var MinorVersion: UInt32 = 0
    public var DirectoryTableBase: UInt32 = 0
    public var PfnDataBase: UInt32 = 0
    public var PsLoadedModuleList: UInt32 = 0
    public var PsActiveProcessHead: UInt32 = 0
    public var MachineImageType: UInt32 = 0
    public var NumberProcessors: UInt32 = 0
    public var BugCheckCode: UInt32 = 0
    public var BugCheckParameter1: UInt32 = 0
    public var BugCheckParameter2: UInt32 = 0
    public var BugCheckParameter3: UInt32 = 0
    public var BugCheckParameter4: UInt32 = 0
    public var VersionUser: VArray<Int8, $32> = VArray<Int8, $32>(repeat: 0)
    public var PaeEnabled: UInt8 = 0
    public var KdSecondaryVersion: UInt8 = 0
    public var Spare3: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)
    public var KdDebuggerDataBlock: UInt32 = 0
    public var Anonymous: DUMP_HEADER32__Anonymous_e__Union = DUMP_HEADER32__Anonymous_e__Union()
    public var ContextRecord: VArray<UInt8, $1200> = VArray<UInt8, $1200>(repeat: 0)
    public var Exception: Win32__impl_l0.EXCEPTION_RECORD32 = Win32__impl_l0.EXCEPTION_RECORD32()
    public var Comment: VArray<Int8, $128> = VArray<Int8, $128>(repeat: 0)
    public var Attributes: Win32__impl_l0.DUMP_FILE_ATTRIBUTES = Win32__impl_l0.DUMP_FILE_ATTRIBUTES()
    public var BootId: UInt32 = 0
    public var _reserved0: VArray<UInt8, $1760> = VArray<UInt8, $1760>(repeat: 0)
    public var DumpType: UInt32 = 0
    public var MiniDumpFields: UInt32 = 0
    public var SecondaryDataState: UInt32 = 0
    public var ProductType: UInt32 = 0
    public var SuiteMask: UInt32 = 0
    public var WriterStatus: UInt32 = 0
    public var RequiredDumpSpace: Int64 = 0
    public var _reserved2: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0)
    public var SystemUpTime: Int64 = 0
    public var SystemTime: Int64 = 0
    public var _reserved3: VArray<UInt8, $56> = VArray<UInt8, $56>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct DUMP_HEADER32__Anonymous_e__Union {
    public var _raw: VArray<UInt32, $175> = VArray<UInt32, $175>(repeat: 0)

    public mut prop physicalMemoryBlock: PHYSICAL_MEMORY_DESCRIPTOR32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<PHYSICAL_MEMORY_DESCRIPTOR32>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<PHYSICAL_MEMORY_DESCRIPTOR32>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop physicalMemoryBlockBuffer: VArray<UInt8, $700> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt8, $700>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt8, $700>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct DUMP_HEADER64 {
    public var Signature: UInt32 = 0
    public var ValidDump: UInt32 = 0
    public var MajorVersion: UInt32 = 0
    public var MinorVersion: UInt32 = 0
    public var DirectoryTableBase: UInt64 = 0
    public var PfnDataBase: UInt64 = 0
    public var PsLoadedModuleList: UInt64 = 0
    public var PsActiveProcessHead: UInt64 = 0
    public var MachineImageType: UInt32 = 0
    public var NumberProcessors: UInt32 = 0
    public var BugCheckCode: UInt32 = 0
    public var BugCheckParameter1: UInt64 = 0
    public var BugCheckParameter2: UInt64 = 0
    public var BugCheckParameter3: UInt64 = 0
    public var BugCheckParameter4: UInt64 = 0
    public var VersionUser: VArray<Int8, $32> = VArray<Int8, $32>(repeat: 0)
    public var KdDebuggerDataBlock: UInt64 = 0
    public var Anonymous: DUMP_HEADER64__Anonymous_e__Union = DUMP_HEADER64__Anonymous_e__Union()
    public var ContextRecord: VArray<UInt8, $3000> = VArray<UInt8, $3000>(repeat: 0)
    public var Exception: Win32__impl_l0.EXCEPTION_RECORD64 = Win32__impl_l0.EXCEPTION_RECORD64()
    public var DumpType: UInt32 = 0
    public var RequiredDumpSpace: Int64 = 0
    public var SystemTime: Int64 = 0
    public var Comment: VArray<Int8, $128> = VArray<Int8, $128>(repeat: 0)
    public var SystemUpTime: Int64 = 0
    public var MiniDumpFields: UInt32 = 0
    public var SecondaryDataState: UInt32 = 0
    public var ProductType: UInt32 = 0
    public var SuiteMask: UInt32 = 0
    public var WriterStatus: UInt32 = 0
    public var Unused1: UInt8 = 0
    public var KdSecondaryVersion: UInt8 = 0
    public var Unused: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)
    public var Attributes: Win32__impl_l0.DUMP_FILE_ATTRIBUTES = Win32__impl_l0.DUMP_FILE_ATTRIBUTES()
    public var BootId: UInt32 = 0
    public var _reserved0: VArray<UInt8, $4008> = VArray<UInt8, $4008>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct DUMP_HEADER64__Anonymous_e__Union {
    public var _raw: VArray<UInt32, $175> = VArray<UInt32, $175>(repeat: 0)

    public mut prop physicalMemoryBlock: PHYSICAL_MEMORY_DESCRIPTOR64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<PHYSICAL_MEMORY_DESCRIPTOR64>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<PHYSICAL_MEMORY_DESCRIPTOR64>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop physicalMemoryBlockBuffer: VArray<UInt8, $700> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt8, $700>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<UInt8, $700>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct EXCEPTION_DEBUG_INFO {
    public var ExceptionRecord: Win32__impl_l0.EXCEPTION_RECORD = Win32__impl_l0.EXCEPTION_RECORD()
    public var dwFirstChance: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IDebugPropertyEnumType_AllVtbl {
    public var base_: windows_interface.IUnknownVtbl = windows_interface.IUnknownVtbl()
    public var GetName: CFunc<(CPointer<Unit>, CPointer<CPointer<UInt16>>) -> Int32> = { _, _ => 0 }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func takeIDebugPropertyEnumType_AllFromAbi(raw: CPointer<Unit>): IDebugPropertyEnumType_All {
    IDebugPropertyEnumType_All.fromAbiTake(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func viewIDebugPropertyEnumType_All(raw: CPointer<Unit>): IDebugPropertyEnumType_All {
    IDebugPropertyEnumType_All.viewOf(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public class IDebugPropertyEnumType_All <: InterfaceWrapperBase & ComInterface {
    public init(raw: CPointer<Unit>) {
        super(raw)
    }

    protected init(raw: CPointer<Unit>, takeOwnership!: Bool, allowManualRelease!: Bool) {
        super(raw, takeOwnership: takeOwnership, allowManualRelease: allowManualRelease)
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IDebugPropertyEnumType_All {
        IDebugPropertyEnumType_All(raw, takeOwnership: true, allowManualRelease: false)
    }

    public static func viewOf(raw: CPointer<Unit>): IDebugPropertyEnumType_All {
        IDebugPropertyEnumType_All(raw, takeOwnership: false, allowManualRelease: false)
    }

    public static func iid(): GUID {
        return GUID(0x51973C55, 0xCB0C, 0x11D0, 0xB5, 0xC9, 0x00, 0xA0, 0x24, 0x4A, 0x0E, 0x7A)
    }

    public static func descriptor(): InterfaceDescriptor<IDebugPropertyEnumType_All> {
        return InterfaceDescriptor<IDebugPropertyEnumType_All>("IDebugPropertyEnumType_All", IDebugPropertyEnumType_All.iid(), takeIDebugPropertyEnumType_AllFromAbi, viewIDebugPropertyEnumType_All)
    }

    public unsafe func vtbl(): IDebugPropertyEnumType_AllVtbl {
        readVtbl<IDebugPropertyEnumType_AllVtbl>(IDebugPropertyEnumType_AllVtbl())
    }

    public unsafe func GetName(__MIDL__IDebugPropertyEnumType_All0000: CPointer<CPointer<UInt16>>): Int32 {
        let __vt = vtbl()
        let __liveRaw = asRaw()
        return __vt.GetName(__liveRaw, __MIDL__IDebugPropertyEnumType_All0000)
    }
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IDebugPropertyEnumType_ArgumentsVtbl {
    public var base_: IDebugPropertyEnumType_AllVtbl = IDebugPropertyEnumType_AllVtbl()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func takeIDebugPropertyEnumType_ArgumentsFromAbi(raw: CPointer<Unit>): IDebugPropertyEnumType_Arguments {
    IDebugPropertyEnumType_Arguments.fromAbiTake(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func viewIDebugPropertyEnumType_Arguments(raw: CPointer<Unit>): IDebugPropertyEnumType_Arguments {
    IDebugPropertyEnumType_Arguments.viewOf(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public class IDebugPropertyEnumType_Arguments <: InterfaceWrapperBase & ComInterface {
    public init(raw: CPointer<Unit>) {
        super(raw)
    }

    protected init(raw: CPointer<Unit>, takeOwnership!: Bool, allowManualRelease!: Bool) {
        super(raw, takeOwnership: takeOwnership, allowManualRelease: allowManualRelease)
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IDebugPropertyEnumType_Arguments {
        IDebugPropertyEnumType_Arguments(raw, takeOwnership: true, allowManualRelease: false)
    }

    public static func viewOf(raw: CPointer<Unit>): IDebugPropertyEnumType_Arguments {
        IDebugPropertyEnumType_Arguments(raw, takeOwnership: false, allowManualRelease: false)
    }

    public static func iid(): GUID {
        return GUID(0x51973C57, 0xCB0C, 0x11D0, 0xB5, 0xC9, 0x00, 0xA0, 0x24, 0x4A, 0x0E, 0x7A)
    }

    public static func descriptor(): InterfaceDescriptor<IDebugPropertyEnumType_Arguments> {
        return InterfaceDescriptor<IDebugPropertyEnumType_Arguments>("IDebugPropertyEnumType_Arguments", IDebugPropertyEnumType_Arguments.iid(), IDebugPropertyEnumType_All.iid(), takeIDebugPropertyEnumType_ArgumentsFromAbi, viewIDebugPropertyEnumType_Arguments)
    }

    public unsafe func vtbl(): IDebugPropertyEnumType_ArgumentsVtbl {
        readVtbl<IDebugPropertyEnumType_ArgumentsVtbl>(IDebugPropertyEnumType_ArgumentsVtbl())
    }
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IDebugPropertyEnumType_LocalsVtbl {
    public var base_: IDebugPropertyEnumType_AllVtbl = IDebugPropertyEnumType_AllVtbl()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func takeIDebugPropertyEnumType_LocalsFromAbi(raw: CPointer<Unit>): IDebugPropertyEnumType_Locals {
    IDebugPropertyEnumType_Locals.fromAbiTake(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func viewIDebugPropertyEnumType_Locals(raw: CPointer<Unit>): IDebugPropertyEnumType_Locals {
    IDebugPropertyEnumType_Locals.viewOf(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public class IDebugPropertyEnumType_Locals <: InterfaceWrapperBase & ComInterface {
    public init(raw: CPointer<Unit>) {
        super(raw)
    }

    protected init(raw: CPointer<Unit>, takeOwnership!: Bool, allowManualRelease!: Bool) {
        super(raw, takeOwnership: takeOwnership, allowManualRelease: allowManualRelease)
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IDebugPropertyEnumType_Locals {
        IDebugPropertyEnumType_Locals(raw, takeOwnership: true, allowManualRelease: false)
    }

    public static func viewOf(raw: CPointer<Unit>): IDebugPropertyEnumType_Locals {
        IDebugPropertyEnumType_Locals(raw, takeOwnership: false, allowManualRelease: false)
    }

    public static func iid(): GUID {
        return GUID(0x51973C56, 0xCB0C, 0x11D0, 0xB5, 0xC9, 0x00, 0xA0, 0x24, 0x4A, 0x0E, 0x7A)
    }

    public static func descriptor(): InterfaceDescriptor<IDebugPropertyEnumType_Locals> {
        return InterfaceDescriptor<IDebugPropertyEnumType_Locals>("IDebugPropertyEnumType_Locals", IDebugPropertyEnumType_Locals.iid(), IDebugPropertyEnumType_All.iid(), takeIDebugPropertyEnumType_LocalsFromAbi, viewIDebugPropertyEnumType_Locals)
    }

    public unsafe func vtbl(): IDebugPropertyEnumType_LocalsVtbl {
        readVtbl<IDebugPropertyEnumType_LocalsVtbl>(IDebugPropertyEnumType_LocalsVtbl())
    }
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IDebugPropertyEnumType_LocalsPlusArgsVtbl {
    public var base_: IDebugPropertyEnumType_AllVtbl = IDebugPropertyEnumType_AllVtbl()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func takeIDebugPropertyEnumType_LocalsPlusArgsFromAbi(raw: CPointer<Unit>): IDebugPropertyEnumType_LocalsPlusArgs {
    IDebugPropertyEnumType_LocalsPlusArgs.fromAbiTake(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func viewIDebugPropertyEnumType_LocalsPlusArgs(raw: CPointer<Unit>): IDebugPropertyEnumType_LocalsPlusArgs {
    IDebugPropertyEnumType_LocalsPlusArgs.viewOf(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public class IDebugPropertyEnumType_LocalsPlusArgs <: InterfaceWrapperBase & ComInterface {
    public init(raw: CPointer<Unit>) {
        super(raw)
    }

    protected init(raw: CPointer<Unit>, takeOwnership!: Bool, allowManualRelease!: Bool) {
        super(raw, takeOwnership: takeOwnership, allowManualRelease: allowManualRelease)
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IDebugPropertyEnumType_LocalsPlusArgs {
        IDebugPropertyEnumType_LocalsPlusArgs(raw, takeOwnership: true, allowManualRelease: false)
    }

    public static func viewOf(raw: CPointer<Unit>): IDebugPropertyEnumType_LocalsPlusArgs {
        IDebugPropertyEnumType_LocalsPlusArgs(raw, takeOwnership: false, allowManualRelease: false)
    }

    public static func iid(): GUID {
        return GUID(0x51973C58, 0xCB0C, 0x11D0, 0xB5, 0xC9, 0x00, 0xA0, 0x24, 0x4A, 0x0E, 0x7A)
    }

    public static func descriptor(): InterfaceDescriptor<IDebugPropertyEnumType_LocalsPlusArgs> {
        return InterfaceDescriptor<IDebugPropertyEnumType_LocalsPlusArgs>("IDebugPropertyEnumType_LocalsPlusArgs", IDebugPropertyEnumType_LocalsPlusArgs.iid(), IDebugPropertyEnumType_All.iid(), takeIDebugPropertyEnumType_LocalsPlusArgsFromAbi, viewIDebugPropertyEnumType_LocalsPlusArgs)
    }

    public unsafe func vtbl(): IDebugPropertyEnumType_LocalsPlusArgsVtbl {
        readVtbl<IDebugPropertyEnumType_LocalsPlusArgsVtbl>(IDebugPropertyEnumType_LocalsPlusArgsVtbl())
    }
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IDebugPropertyEnumType_RegistersVtbl {
    public var base_: IDebugPropertyEnumType_AllVtbl = IDebugPropertyEnumType_AllVtbl()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func takeIDebugPropertyEnumType_RegistersFromAbi(raw: CPointer<Unit>): IDebugPropertyEnumType_Registers {
    IDebugPropertyEnumType_Registers.fromAbiTake(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func viewIDebugPropertyEnumType_Registers(raw: CPointer<Unit>): IDebugPropertyEnumType_Registers {
    IDebugPropertyEnumType_Registers.viewOf(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public class IDebugPropertyEnumType_Registers <: InterfaceWrapperBase & ComInterface {
    public init(raw: CPointer<Unit>) {
        super(raw)
    }

    protected init(raw: CPointer<Unit>, takeOwnership!: Bool, allowManualRelease!: Bool) {
        super(raw, takeOwnership: takeOwnership, allowManualRelease: allowManualRelease)
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IDebugPropertyEnumType_Registers {
        IDebugPropertyEnumType_Registers(raw, takeOwnership: true, allowManualRelease: false)
    }

    public static func viewOf(raw: CPointer<Unit>): IDebugPropertyEnumType_Registers {
        IDebugPropertyEnumType_Registers(raw, takeOwnership: false, allowManualRelease: false)
    }

    public static func iid(): GUID {
        return GUID(0x51973C59, 0xCB0C, 0x11D0, 0xB5, 0xC9, 0x00, 0xA0, 0x24, 0x4A, 0x0E, 0x7A)
    }

    public static func descriptor(): InterfaceDescriptor<IDebugPropertyEnumType_Registers> {
        return InterfaceDescriptor<IDebugPropertyEnumType_Registers>("IDebugPropertyEnumType_Registers", IDebugPropertyEnumType_Registers.iid(), IDebugPropertyEnumType_All.iid(), takeIDebugPropertyEnumType_RegistersFromAbi, viewIDebugPropertyEnumType_Registers)
    }

    public unsafe func vtbl(): IDebugPropertyEnumType_RegistersVtbl {
        readVtbl<IDebugPropertyEnumType_RegistersVtbl>(IDebugPropertyEnumType_RegistersVtbl())
    }
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_CBA_EVENT {
    public var severity: UInt32 = 0
    public var code: UInt32 = 0
    public var desc: CPointer<UInt8> = CPointer<UInt8>()
    public var object: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_CBA_EVENTW {
    public var severity: UInt32 = 0
    public var code: UInt32 = 0
    public var desc: CPointer<UInt16> = CPointer<UInt16>()
    public var object: CPointer<Unit> = CPointer<Unit>()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_DUPLICATE_SYMBOL64 {
    public var SizeOfStruct: UInt32 = 0
    public var NumberOfDups: UInt32 = 0
    public var Symbol: CPointer<Win32__impl_l0.IMAGEHLP_SYMBOL64> = CPointer<Win32__impl_l0.IMAGEHLP_SYMBOL64>()
    public var SelectedSymbol: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_GET_TYPE_INFO_PARAMS {
    public var SizeOfStruct: UInt32 = 0
    public var Flags: UInt32 = 0
    public var NumIds: UInt32 = 0
    public var TypeIds: CPointer<UInt32> = CPointer<UInt32>()
    public var TagFilter: UInt64 = 0
    public var NumReqs: UInt32 = 0
    public var ReqKinds: CPointer<Int32> = CPointer<Int32>()
    public var ReqOffsets: CPointer<UIntNative> = CPointer<UIntNative>()
    public var ReqSizes: CPointer<UInt32> = CPointer<UInt32>()
    public var ReqStride: UIntNative = 0
    public var BufferSize: UIntNative = 0
    public var Buffer: CPointer<Unit> = CPointer<Unit>()
    public var EntriesMatched: UInt32 = 0
    public var EntriesFilled: UInt32 = 0
    public var TagsFound: UInt64 = 0
    public var AllReqsValid: UInt64 = 0
    public var NumReqsValid: UInt32 = 0
    public var ReqsValid: CPointer<UInt64> = CPointer<UInt64>()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_MODULE64 {
    public var SizeOfStruct: UInt32 = 0
    public var BaseOfImage: UInt64 = 0
    public var ImageSize: UInt32 = 0
    public var TimeDateStamp: UInt32 = 0
    public var CheckSum: UInt32 = 0
    public var NumSyms: UInt32 = 0
    public var SymType: Int32 = 0
    public var ModuleName: VArray<Int8, $32> = VArray<Int8, $32>(repeat: 0)
    public var ImageName: VArray<Int8, $256> = VArray<Int8, $256>(repeat: 0)
    public var LoadedImageName: VArray<Int8, $256> = VArray<Int8, $256>(repeat: 0)
    public var LoadedPdbName: VArray<Int8, $256> = VArray<Int8, $256>(repeat: 0)
    public var CVSig: UInt32 = 0
    public var CVData: VArray<Int8, $780> = VArray<Int8, $780>(repeat: 0)
    public var PdbSig: UInt32 = 0
    public var PdbSig70: GUID = GUID()
    public var PdbAge: UInt32 = 0
    public var PdbUnmatched: Int32 = 0
    public var DbgUnmatched: Int32 = 0
    public var LineNumbers: Int32 = 0
    public var GlobalSymbols: Int32 = 0
    public var TypeInfo: Int32 = 0
    public var SourceIndexed: Int32 = 0
    public var Publics: Int32 = 0
    public var MachineType: UInt32 = 0
    public var Reserved: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_MODULE64_EX {
    public var Module: IMAGEHLP_MODULE64 = IMAGEHLP_MODULE64()
    public var RegionFlags: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_MODULEW64 {
    public var SizeOfStruct: UInt32 = 0
    public var BaseOfImage: UInt64 = 0
    public var ImageSize: UInt32 = 0
    public var TimeDateStamp: UInt32 = 0
    public var CheckSum: UInt32 = 0
    public var NumSyms: UInt32 = 0
    public var SymType: Int32 = 0
    public var ModuleName: VArray<UInt16, $32> = VArray<UInt16, $32>(repeat: 0)
    public var ImageName: VArray<UInt16, $256> = VArray<UInt16, $256>(repeat: 0)
    public var LoadedImageName: VArray<UInt16, $256> = VArray<UInt16, $256>(repeat: 0)
    public var LoadedPdbName: VArray<UInt16, $256> = VArray<UInt16, $256>(repeat: 0)
    public var CVSig: UInt32 = 0
    public var CVData: VArray<UInt16, $780> = VArray<UInt16, $780>(repeat: 0)
    public var PdbSig: UInt32 = 0
    public var PdbSig70: GUID = GUID()
    public var PdbAge: UInt32 = 0
    public var PdbUnmatched: Int32 = 0
    public var DbgUnmatched: Int32 = 0
    public var LineNumbers: Int32 = 0
    public var GlobalSymbols: Int32 = 0
    public var TypeInfo: Int32 = 0
    public var SourceIndexed: Int32 = 0
    public var Publics: Int32 = 0
    public var MachineType: UInt32 = 0
    public var Reserved: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_MODULEW64_EX {
    public var Module: IMAGEHLP_MODULEW64 = IMAGEHLP_MODULEW64()
    public var RegionFlags: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_SYMBOL64_PACKAGE {
    public var sym: Win32__impl_l0.IMAGEHLP_SYMBOL64 = Win32__impl_l0.IMAGEHLP_SYMBOL64()
    public var name: VArray<Int8, $2001> = VArray<Int8, $2001>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGEHLP_SYMBOLW64_PACKAGE {
    public var sym: Win32__impl_l0.IMAGEHLP_SYMBOLW64 = Win32__impl_l0.IMAGEHLP_SYMBOLW64()
    public var name: VArray<UInt16, $2001> = VArray<UInt16, $2001>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGE_COR20_HEADER {
    public var cb: UInt32 = 0
    public var MajorRuntimeVersion: UInt16 = 0
    public var MinorRuntimeVersion: UInt16 = 0
    public var MetaData: Win32__impl_l0.IMAGE_DATA_DIRECTORY = Win32__impl_l0.IMAGE_DATA_DIRECTORY()
    public var Flags: UInt32 = 0
    public var Anonymous: IMAGE_COR20_HEADER__Anonymous_e__Union = IMAGE_COR20_HEADER__Anonymous_e__Union()
    public var Resources: Win32__impl_l0.IMAGE_DATA_DIRECTORY = Win32__impl_l0.IMAGE_DATA_DIRECTORY()
    public var StrongNameSignature: Win32__impl_l0.IMAGE_DATA_DIRECTORY = Win32__impl_l0.IMAGE_DATA_DIRECTORY()
    public var CodeManagerTable: Win32__impl_l0.IMAGE_DATA_DIRECTORY = Win32__impl_l0.IMAGE_DATA_DIRECTORY()
    public var VTableFixups: Win32__impl_l0.IMAGE_DATA_DIRECTORY = Win32__impl_l0.IMAGE_DATA_DIRECTORY()
    public var ExportAddressTableJumps: Win32__impl_l0.IMAGE_DATA_DIRECTORY = Win32__impl_l0.IMAGE_DATA_DIRECTORY()
    public var ManagedNativeHeader: Win32__impl_l0.IMAGE_DATA_DIRECTORY = Win32__impl_l0.IMAGE_DATA_DIRECTORY()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct IMAGE_COR20_HEADER__Anonymous_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public mut prop entryPointToken: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop entryPointRVA: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGE_DEBUG_DIRECTORY {
    public var Characteristics: UInt32 = 0
    public var TimeDateStamp: UInt32 = 0
    public var MajorVersion: UInt16 = 0
    public var MinorVersion: UInt16 = 0
    public var Type: UInt32 = 0
    public var SizeOfData: UInt32 = 0
    public var AddressOfRawData: UInt32 = 0
    public var PointerToRawData: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
@C
public struct IMAGE_FILE_HEADER {
    public var Machine: UInt16 = 0
    public var NumberOfSections: UInt16 = 0
    public var TimeDateStamp: UInt32 = 0
    public var PointerToSymbolTable: UInt32 = 0
    public var NumberOfSymbols: UInt32 = 0
    public var SizeOfOptionalHeader: UInt16 = 0
    public var Characteristics: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGE_LOAD_CONFIG_DIRECTORY32 {
    public var Size: UInt32 = 0
    public var TimeDateStamp: UInt32 = 0
    public var MajorVersion: UInt16 = 0
    public var MinorVersion: UInt16 = 0
    public var GlobalFlagsClear: UInt32 = 0
    public var GlobalFlagsSet: UInt32 = 0
    public var CriticalSectionDefaultTimeout: UInt32 = 0
    public var DeCommitFreeBlockThreshold: UInt32 = 0
    public var DeCommitTotalFreeThreshold: UInt32 = 0
    public var LockPrefixTable: UInt32 = 0
    public var MaximumAllocationSize: UInt32 = 0
    public var VirtualMemoryThreshold: UInt32 = 0
    public var ProcessHeapFlags: UInt32 = 0
    public var ProcessAffinityMask: UInt32 = 0
    public var CSDVersion: UInt16 = 0
    public var DependentLoadFlags: UInt16 = 0
    public var EditList: UInt32 = 0
    public var SecurityCookie: UInt32 = 0
    public var SEHandlerTable: UInt32 = 0
    public var SEHandlerCount: UInt32 = 0
    public var GuardCFCheckFunctionPointer: UInt32 = 0
    public var GuardCFDispatchFunctionPointer: UInt32 = 0
    public var GuardCFFunctionTable: UInt32 = 0
    public var GuardCFFunctionCount: UInt32 = 0
    public var GuardFlags: UInt32 = 0
    public var CodeIntegrity: Win32__impl_l0.IMAGE_LOAD_CONFIG_CODE_INTEGRITY = Win32__impl_l0.IMAGE_LOAD_CONFIG_CODE_INTEGRITY()
    public var GuardAddressTakenIatEntryTable: UInt32 = 0
    public var GuardAddressTakenIatEntryCount: UInt32 = 0
    public var GuardLongJumpTargetTable: UInt32 = 0
    public var GuardLongJumpTargetCount: UInt32 = 0
    public var DynamicValueRelocTable: UInt32 = 0
    public var CHPEMetadataPointer: UInt32 = 0
    public var GuardRFFailureRoutine: UInt32 = 0
    public var GuardRFFailureRoutineFunctionPointer: UInt32 = 0
    public var DynamicValueRelocTableOffset: UInt32 = 0
    public var DynamicValueRelocTableSection: UInt16 = 0
    public var Reserved2: UInt16 = 0
    public var GuardRFVerifyStackPointerFunctionPointer: UInt32 = 0
    public var HotPatchTableOffset: UInt32 = 0
    public var Reserved3: UInt32 = 0
    public var EnclaveConfigurationPointer: UInt32 = 0
    public var VolatileMetadataPointer: UInt32 = 0
    public var GuardEHContinuationTable: UInt32 = 0
    public var GuardEHContinuationCount: UInt32 = 0
    public var GuardXFGCheckFunctionPointer: UInt32 = 0
    public var GuardXFGDispatchFunctionPointer: UInt32 = 0
    public var GuardXFGTableDispatchFunctionPointer: UInt32 = 0
    public var CastGuardOsDeterminedFailureMode: UInt32 = 0
    public var GuardMemcpyFunctionPointer: UInt32 = 0
    public var UmaFunctionPointers: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct IMAGE_LOAD_CONFIG_DIRECTORY64 {
    public var _raw: VArray<UInt8, $328> = VArray<UInt8, $328>(repeat: 0u8)

    public mut prop size: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop timeDateStamp: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop majorVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop minorVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 10)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 10, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop globalFlagsClear: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop globalFlagsSet: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop criticalSectionDefaultTimeout: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop deCommitFreeBlockThreshold: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop deCommitTotalFreeThreshold: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 32)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 32, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop lockPrefixTable: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 40)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 40, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop maximumAllocationSize: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 48)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 48, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop virtualMemoryThreshold: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 56)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 56, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processAffinityMask: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 64)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 64, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processHeapFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 72)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 72, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop csdVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 76)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 76, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dependentLoadFlags: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 78)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 78, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop editList: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 80)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 80, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop securityCookie: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 88)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 88, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop seHandlerTable: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 96)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 96, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop seHandlerCount: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 104)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 104, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardCFCheckFunctionPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 112)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 112, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardCFDispatchFunctionPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 120)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 120, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardCFFunctionTable: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 128)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 128, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardCFFunctionCount: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 136)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 136, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 144)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 144, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop codeIntegrity: Win32__impl_l0.IMAGE_LOAD_CONFIG_CODE_INTEGRITY {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.IMAGE_LOAD_CONFIG_CODE_INTEGRITY = Win32__impl_l0.IMAGE_LOAD_CONFIG_CODE_INTEGRITY()
                return packedReadValue<Win32__impl_l0.IMAGE_LOAD_CONFIG_CODE_INTEGRITY>(base, 148, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 148, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardAddressTakenIatEntryTable: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 160)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 160, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardAddressTakenIatEntryCount: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 168)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 168, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardLongJumpTargetTable: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 176)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 176, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardLongJumpTargetCount: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 184)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 184, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dynamicValueRelocTable: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 192)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 192, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop chpeMetadataPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 200)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 200, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardRFFailureRoutine: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 208)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 208, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardRFFailureRoutineFunctionPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 216)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 216, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dynamicValueRelocTableOffset: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 224)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 224, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dynamicValueRelocTableSection: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 228)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 228, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved2: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 230)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 230, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardRFVerifyStackPointerFunctionPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 232)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 232, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hotPatchTableOffset: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 240)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 240, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved3: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 244)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 244, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enclaveConfigurationPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 248)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 248, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop volatileMetadataPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 256)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 256, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardEHContinuationTable: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 264)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 264, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardEHContinuationCount: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 272)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 272, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardXFGCheckFunctionPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 280)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 280, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardXFGDispatchFunctionPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 288)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 288, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardXFGTableDispatchFunctionPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 296)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 296, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop castGuardOsDeterminedFailureMode: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 304)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 304, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop guardMemcpyFunctionPointer: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 312)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 312, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop umaFunctionPointers: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 320)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 320, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
@C
public struct IMAGE_NT_HEADERS32 {
    public var Signature: UInt32 = 0
    public var FileHeader: IMAGE_FILE_HEADER = IMAGE_FILE_HEADER()
    public var OptionalHeader: IMAGE_OPTIONAL_HEADER32 = IMAGE_OPTIONAL_HEADER32()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
@C
public struct IMAGE_NT_HEADERS64 {
    public var Signature: UInt32 = 0
    public var FileHeader: IMAGE_FILE_HEADER = IMAGE_FILE_HEADER()
    public var OptionalHeader: IMAGE_OPTIONAL_HEADER64 = IMAGE_OPTIONAL_HEADER64()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGE_OPTIONAL_HEADER32 {
    public var Magic: UInt16 = 0
    public var MajorLinkerVersion: UInt8 = 0
    public var MinorLinkerVersion: UInt8 = 0
    public var SizeOfCode: UInt32 = 0
    public var SizeOfInitializedData: UInt32 = 0
    public var SizeOfUninitializedData: UInt32 = 0
    public var AddressOfEntryPoint: UInt32 = 0
    public var BaseOfCode: UInt32 = 0
    public var BaseOfData: UInt32 = 0
    public var ImageBase: UInt32 = 0
    public var SectionAlignment: UInt32 = 0
    public var FileAlignment: UInt32 = 0
    public var MajorOperatingSystemVersion: UInt16 = 0
    public var MinorOperatingSystemVersion: UInt16 = 0
    public var MajorImageVersion: UInt16 = 0
    public var MinorImageVersion: UInt16 = 0
    public var MajorSubsystemVersion: UInt16 = 0
    public var MinorSubsystemVersion: UInt16 = 0
    public var Win32VersionValue: UInt32 = 0
    public var SizeOfImage: UInt32 = 0
    public var SizeOfHeaders: UInt32 = 0
    public var CheckSum: UInt32 = 0
    public var Subsystem: UInt16 = 0
    public var DllCharacteristics: UInt16 = 0
    public var SizeOfStackReserve: UInt32 = 0
    public var SizeOfStackCommit: UInt32 = 0
    public var SizeOfHeapReserve: UInt32 = 0
    public var SizeOfHeapCommit: UInt32 = 0
    public var LoaderFlags: UInt32 = 0
    public var NumberOfRvaAndSizes: UInt32 = 0
    public var DataDirectory: VArray<Win32__impl_l0.IMAGE_DATA_DIRECTORY, $16> = VArray<Win32__impl_l0.IMAGE_DATA_DIRECTORY, $16>(repeat: Win32__impl_l0.IMAGE_DATA_DIRECTORY())
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct IMAGE_OPTIONAL_HEADER64 {
    public var _raw: VArray<UInt8, $240> = VArray<UInt8, $240>(repeat: 0u8)

    public mut prop magic: Win32__impl_l0.IMAGE_OPTIONAL_HEADER_MAGIC {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return Win32__impl_l0.IMAGE_OPTIONAL_HEADER_MAGIC(packedReadUInt16(base, 0))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val.value)
                _raw = rawStorage
            }
        }
    }

    public mut prop majorLinkerVersion: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop minorLinkerVersion: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfCode: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfInitializedData: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfUninitializedData: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop addressOfEntryPoint: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop baseOfCode: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop imageBase: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sectionAlignment: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 32)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 32, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fileAlignment: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 36)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 36, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop majorOperatingSystemVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 40)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 40, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop minorOperatingSystemVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 42)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 42, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop majorImageVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 44)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 44, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop minorImageVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 46)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 46, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop majorSubsystemVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 48)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 48, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop minorSubsystemVersion: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 50)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 50, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop win32VersionValue: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 52)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 52, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfImage: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 56)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 56, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfHeaders: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 60)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 60, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop checkSum: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 64)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 64, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop subsystem: Win32__impl_l0.IMAGE_SUBSYSTEM {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return Win32__impl_l0.IMAGE_SUBSYSTEM(packedReadUInt16(base, 68))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 68, val.value)
                _raw = rawStorage
            }
        }
    }

    public mut prop dllCharacteristics: Win32__impl_l0.IMAGE_DLL_CHARACTERISTICS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return Win32__impl_l0.IMAGE_DLL_CHARACTERISTICS(packedReadUInt16(base, 70))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 70, val.value)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfStackReserve: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 72)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 72, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfStackCommit: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 80)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 80, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfHeapReserve: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 88)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 88, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfHeapCommit: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 96)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 96, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop loaderFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 104)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 104, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop numberOfRvaAndSizes: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 108)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 108, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dataDirectory: VArray<Win32__impl_l0.IMAGE_DATA_DIRECTORY, $16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<Win32__impl_l0.IMAGE_DATA_DIRECTORY, $16> = VArray<Win32__impl_l0.IMAGE_DATA_DIRECTORY, $16>(repeat: Win32__impl_l0.IMAGE_DATA_DIRECTORY())
                return packedReadValue<VArray<Win32__impl_l0.IMAGE_DATA_DIRECTORY, $16>>(base, 112, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 112, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
@C
public struct IMAGE_ROM_HEADERS {
    public var FileHeader: IMAGE_FILE_HEADER = IMAGE_FILE_HEADER()
    public var OptionalHeader: Win32__impl_l0.IMAGE_ROM_OPTIONAL_HEADER = Win32__impl_l0.IMAGE_ROM_OPTIONAL_HEADER()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IMAGE_SECTION_HEADER {
    public var Name: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0)
    public var Misc: IMAGE_SECTION_HEADER__Misc_e__Union = IMAGE_SECTION_HEADER__Misc_e__Union()
    public var VirtualAddress: UInt32 = 0
    public var SizeOfRawData: UInt32 = 0
    public var PointerToRawData: UInt32 = 0
    public var PointerToRelocations: UInt32 = 0
    public var PointerToLinenumbers: UInt32 = 0
    public var NumberOfRelocations: UInt16 = 0
    public var NumberOfLinenumbers: UInt16 = 0
    public var Characteristics: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct IMAGE_SECTION_HEADER__Misc_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public mut prop physicalAddress: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop virtualSize: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IObjectSafetyVtbl {
    public var base_: windows_interface.IUnknownVtbl = windows_interface.IUnknownVtbl()
    public var GetInterfaceSafetyOptions: CFunc<(CPointer<Unit>, CPointer<GUID>, CPointer<UInt32>, CPointer<UInt32>) -> Int32> = { _, _, _, _ => 0 }
    public var SetInterfaceSafetyOptions: CFunc<(CPointer<Unit>, CPointer<GUID>, UInt32, UInt32) -> Int32> = { _, _, _, _ => 0 }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func takeIObjectSafetyFromAbi(raw: CPointer<Unit>): IObjectSafety {
    IObjectSafety.fromAbiTake(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func viewIObjectSafety(raw: CPointer<Unit>): IObjectSafety {
    IObjectSafety.viewOf(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public class IObjectSafety <: InterfaceWrapperBase & ComInterface {
    public init(raw: CPointer<Unit>) {
        super(raw)
    }

    protected init(raw: CPointer<Unit>, takeOwnership!: Bool, allowManualRelease!: Bool) {
        super(raw, takeOwnership: takeOwnership, allowManualRelease: allowManualRelease)
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IObjectSafety {
        IObjectSafety(raw, takeOwnership: true, allowManualRelease: false)
    }

    public static func viewOf(raw: CPointer<Unit>): IObjectSafety {
        IObjectSafety(raw, takeOwnership: false, allowManualRelease: false)
    }

    public static func iid(): GUID {
        return GUID(0xCB5BDC81, 0x93C1, 0x11CF, 0x8F, 0x20, 0x00, 0x80, 0x5F, 0x2C, 0xD0, 0x64)
    }

    public static func descriptor(): InterfaceDescriptor<IObjectSafety> {
        return InterfaceDescriptor<IObjectSafety>("IObjectSafety", IObjectSafety.iid(), takeIObjectSafetyFromAbi, viewIObjectSafety)
    }

    public unsafe func vtbl(): IObjectSafetyVtbl {
        readVtbl<IObjectSafetyVtbl>(IObjectSafetyVtbl())
    }

    public unsafe func GetInterfaceSafetyOptions(riid: CPointer<GUID>, pdwSupportedOptions: CPointer<UInt32>, pdwEnabledOptions: CPointer<UInt32>): Int32 {
        let __vt = vtbl()
        let __liveRaw = asRaw()
        return __vt.GetInterfaceSafetyOptions(__liveRaw, riid, pdwSupportedOptions, pdwEnabledOptions)
    }

    public unsafe func SetInterfaceSafetyOptions(riid: CPointer<GUID>, dwOptionSetMask: UInt32, dwEnabledOptions: UInt32): Int32 {
        let __vt = vtbl()
        let __liveRaw = asRaw()
        return __vt.SetInterfaceSafetyOptions(__liveRaw, riid, dwOptionSetMask, dwEnabledOptions)
    }
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct IPMI_OS_SEL_RECORD {
    public var _raw: VArray<UInt8, $21> = VArray<UInt8, $21>(repeat: 0u8)

    public mut prop signature: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop version: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop length: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop recordType: Win32__impl_l0.IPMI_OS_SEL_RECORD_TYPE {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return Win32__impl_l0.IPMI_OS_SEL_RECORD_TYPE(packedReadInt32(base, 12))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 12, val.value)
                _raw = rawStorage
            }
        }
    }

    public mut prop dataLength: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop data: VArray<UInt8, $1> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt8, $1> = VArray<UInt8, $1>(repeat: 0)
                return packedReadValue<VArray<UInt8, $1>>(base, 20, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct IPerPropertyBrowsing2Vtbl {
    public var base_: windows_interface.IUnknownVtbl = windows_interface.IUnknownVtbl()
    public var GetDisplayString: CFunc<(CPointer<Unit>, Int32, CPointer<CPointer<UInt16>>) -> Int32> = { _, _, _ => 0 }
    public var MapPropertyToPage: CFunc<(CPointer<Unit>, Int32, CPointer<GUID>) -> Int32> = { _, _, _ => 0 }
    @When[Windows_Win32_System_Ole == "on"]
    public var GetPredefinedStrings: CFunc<(CPointer<Unit>, Int32, CPointer<Win32__impl_l0.CALPOLESTR>, CPointer<Win32__impl_l0.CADWORD>) -> Int32> = { _, _, _, _ => 0 }
    public var SetPredefinedValue: CFunc<(CPointer<Unit>, Int32, UInt32) -> Int32> = { _, _, _ => 0 }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func takeIPerPropertyBrowsing2FromAbi(raw: CPointer<Unit>): IPerPropertyBrowsing2 {
    IPerPropertyBrowsing2.fromAbiTake(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
func viewIPerPropertyBrowsing2(raw: CPointer<Unit>): IPerPropertyBrowsing2 {
    IPerPropertyBrowsing2.viewOf(raw)
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public class IPerPropertyBrowsing2 <: InterfaceWrapperBase & ComInterface {
    public init(raw: CPointer<Unit>) {
        super(raw)
    }

    protected init(raw: CPointer<Unit>, takeOwnership!: Bool, allowManualRelease!: Bool) {
        super(raw, takeOwnership: takeOwnership, allowManualRelease: allowManualRelease)
    }

    public static func fromAbiTake(raw: CPointer<Unit>): IPerPropertyBrowsing2 {
        IPerPropertyBrowsing2(raw, takeOwnership: true, allowManualRelease: false)
    }

    public static func viewOf(raw: CPointer<Unit>): IPerPropertyBrowsing2 {
        IPerPropertyBrowsing2(raw, takeOwnership: false, allowManualRelease: false)
    }

    public static func iid(): GUID {
        return GUID(0x51973C54, 0xCB0C, 0x11D0, 0xB5, 0xC9, 0x00, 0xA0, 0x24, 0x4A, 0x0E, 0x7A)
    }

    public static func descriptor(): InterfaceDescriptor<IPerPropertyBrowsing2> {
        return InterfaceDescriptor<IPerPropertyBrowsing2>("IPerPropertyBrowsing2", IPerPropertyBrowsing2.iid(), takeIPerPropertyBrowsing2FromAbi, viewIPerPropertyBrowsing2)
    }

    public unsafe func vtbl(): IPerPropertyBrowsing2Vtbl {
        readVtbl<IPerPropertyBrowsing2Vtbl>(IPerPropertyBrowsing2Vtbl())
    }

    public unsafe func GetDisplayString(dispid: Int32, pBstr: CPointer<CPointer<UInt16>>): Int32 {
        let __vt = vtbl()
        let __liveRaw = asRaw()
        return __vt.GetDisplayString(__liveRaw, dispid, pBstr)
    }

    public unsafe func MapPropertyToPage(dispid: Int32, pClsidPropPage: CPointer<GUID>): Int32 {
        let __vt = vtbl()
        let __liveRaw = asRaw()
        return __vt.MapPropertyToPage(__liveRaw, dispid, pClsidPropPage)
    }

    @When[Windows_Win32_System_Ole == "on"]
    public unsafe func GetPredefinedStrings(dispid: Int32, pCaStrings: CPointer<Win32__impl_l0.CALPOLESTR>, pCaCookies: CPointer<Win32__impl_l0.CADWORD>): Int32 {
        let __vt = vtbl()
        let __liveRaw = asRaw()
        return __vt.GetPredefinedStrings(__liveRaw, dispid, pCaStrings, pCaCookies)
    }

    public unsafe func SetPredefinedValue(dispid: Int32, dwCookie: UInt32): Int32 {
        let __vt = vtbl()
        let __liveRaw = asRaw()
        return __vt.SetPredefinedValue(__liveRaw, dispid, dwCookie)
    }
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "aarch64"]
@C
public struct KNONVOLATILE_CONTEXT_POINTERS {
    public var X19: CPointer<UInt64> = CPointer<UInt64>()
    public var X20: CPointer<UInt64> = CPointer<UInt64>()
    public var X21: CPointer<UInt64> = CPointer<UInt64>()
    public var X22: CPointer<UInt64> = CPointer<UInt64>()
    public var X23: CPointer<UInt64> = CPointer<UInt64>()
    public var X24: CPointer<UInt64> = CPointer<UInt64>()
    public var X25: CPointer<UInt64> = CPointer<UInt64>()
    public var X26: CPointer<UInt64> = CPointer<UInt64>()
    public var X27: CPointer<UInt64> = CPointer<UInt64>()
    public var X28: CPointer<UInt64> = CPointer<UInt64>()
    public var Fp: CPointer<UInt64> = CPointer<UInt64>()
    public var Lr: CPointer<UInt64> = CPointer<UInt64>()
    public var D8: CPointer<UInt64> = CPointer<UInt64>()
    public var D9: CPointer<UInt64> = CPointer<UInt64>()
    public var D10: CPointer<UInt64> = CPointer<UInt64>()
    public var D11: CPointer<UInt64> = CPointer<UInt64>()
    public var D12: CPointer<UInt64> = CPointer<UInt64>()
    public var D13: CPointer<UInt64> = CPointer<UInt64>()
    public var D14: CPointer<UInt64> = CPointer<UInt64>()
    public var D15: CPointer<UInt64> = CPointer<UInt64>()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
@C
public struct KNONVOLATILE_CONTEXT_POINTERS {
    public var Anonymous1: KNONVOLATILE_CONTEXT_POINTERS__Anonymous1_e__Union = KNONVOLATILE_CONTEXT_POINTERS__Anonymous1_e__Union()
    public var Anonymous2: KNONVOLATILE_CONTEXT_POINTERS__Anonymous2_e__Union = KNONVOLATILE_CONTEXT_POINTERS__Anonymous2_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct KNONVOLATILE_CONTEXT_POINTERS__Anonymous1_e__Union {
    public var _raw: VArray<UInt64, $16> = VArray<UInt64, $16>(repeat: 0)

    public mut prop floatingContext: VArray<CPointer<Win32__impl_l0.M128A>, $16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<CPointer<Win32__impl_l0.M128A>, $16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<CPointer<Win32__impl_l0.M128A>, $16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: KNONVOLATILE_CONTEXT_POINTERS__Anonymous1_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<KNONVOLATILE_CONTEXT_POINTERS__Anonymous1_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<KNONVOLATILE_CONTEXT_POINTERS__Anonymous1_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
@C
public struct KNONVOLATILE_CONTEXT_POINTERS__Anonymous1_e__Union__Anonymous_e__Struct {
    public var Xmm0: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm1: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm2: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm3: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm4: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm5: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm6: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm7: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm8: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm9: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm10: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm11: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm12: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm13: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm14: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public var Xmm15: CPointer<Win32__impl_l0.M128A> = CPointer<Win32__impl_l0.M128A>()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct KNONVOLATILE_CONTEXT_POINTERS__Anonymous2_e__Union {
    public var _raw: VArray<UInt64, $16> = VArray<UInt64, $16>(repeat: 0)

    public mut prop integerContext: VArray<CPointer<UInt64>, $16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<CPointer<UInt64>, $16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<VArray<CPointer<UInt64>, $16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: KNONVOLATILE_CONTEXT_POINTERS__Anonymous2_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<KNONVOLATILE_CONTEXT_POINTERS__Anonymous2_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<KNONVOLATILE_CONTEXT_POINTERS__Anonymous2_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
@C
public struct KNONVOLATILE_CONTEXT_POINTERS__Anonymous2_e__Union__Anonymous_e__Struct {
    public var Rax: CPointer<UInt64> = CPointer<UInt64>()
    public var Rcx: CPointer<UInt64> = CPointer<UInt64>()
    public var Rdx: CPointer<UInt64> = CPointer<UInt64>()
    public var Rbx: CPointer<UInt64> = CPointer<UInt64>()
    public var Rsp: CPointer<UInt64> = CPointer<UInt64>()
    public var Rbp: CPointer<UInt64> = CPointer<UInt64>()
    public var Rsi: CPointer<UInt64> = CPointer<UInt64>()
    public var Rdi: CPointer<UInt64> = CPointer<UInt64>()
    public var R8: CPointer<UInt64> = CPointer<UInt64>()
    public var R9: CPointer<UInt64> = CPointer<UInt64>()
    public var R10: CPointer<UInt64> = CPointer<UInt64>()
    public var R11: CPointer<UInt64> = CPointer<UInt64>()
    public var R12: CPointer<UInt64> = CPointer<UInt64>()
    public var R13: CPointer<UInt64> = CPointer<UInt64>()
    public var R14: CPointer<UInt64> = CPointer<UInt64>()
    public var R15: CPointer<UInt64> = CPointer<UInt64>()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Memory == "on"]
// packed(4)
@C
public struct MINIDUMP_CALLBACK_OUTPUT {
    public var _raw: VArray<UInt8, $52> = VArray<UInt8, $52>(repeat: 0u8)

    public mut prop anonymous: MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union = MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union()
                return packedReadValue<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union>(base, 0, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Memory == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union {
    public var _raw: VArray<UInt32, $13> = VArray<UInt32, $13>(repeat: 0)

    public mut prop moduleWriteFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop threadWriteFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop secondaryFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous1: MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous1_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous1_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous1_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous2: MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous2_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous2_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous2_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop handle: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous3: MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous3_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous3_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous3_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous4: MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous4_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous4_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous4_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous5: MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous5_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous5_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous5_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop status: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Memory == "on"]
// packed(4)
@C
public struct MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous1_e__Struct {
    public var _raw: VArray<UInt8, $12> = VArray<UInt8, $12>(repeat: 0u8)

    public mut prop memoryBase: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop memorySize: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Memory == "on"]
@C
public struct MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous2_e__Struct {
    public var CheckCancel: Int32 = 0
    public var Cancel: Int32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Memory == "on"]
@C
public struct MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous3_e__Struct {
    public var VmRegion: Win32__impl_l0.MINIDUMP_MEMORY_INFO = Win32__impl_l0.MINIDUMP_MEMORY_INFO()
    public var Continue: Int32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Memory == "on"]
@C
public struct MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous4_e__Struct {
    public var VmQueryStatus: Int32 = 0
    public var VmQueryResult: Win32__impl_l0.MINIDUMP_MEMORY_INFO = Win32__impl_l0.MINIDUMP_MEMORY_INFO()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Memory == "on"]
@C
public struct MINIDUMP_CALLBACK_OUTPUT__Anonymous_e__Union__Anonymous5_e__Struct {
    public var VmReadStatus: Int32 = 0
    public var VmReadBytesCompleted: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_DIRECTORY {
    public var _raw: VArray<UInt8, $12> = VArray<UInt8, $12>(repeat: 0u8)

    public mut prop streamType: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop location: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR = Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR()
                return packedReadValue<Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_EXCEPTION_STREAM {
    public var _raw: VArray<UInt8, $168> = VArray<UInt8, $168>(repeat: 0u8)

    public mut prop threadId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop __alignment: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop exceptionRecord: Win32__impl_l0.MINIDUMP_EXCEPTION {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_EXCEPTION = Win32__impl_l0.MINIDUMP_EXCEPTION()
                return packedReadValue<Win32__impl_l0.MINIDUMP_EXCEPTION>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop threadContext: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR = Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR()
                return packedReadValue<Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR>(base, 160, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 160, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_MEMORY64_LIST {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    public mut prop numberOfMemoryRanges: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop baseRva: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop memoryRanges: VArray<Win32__impl_l0.MINIDUMP_MEMORY_DESCRIPTOR64, $1> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<Win32__impl_l0.MINIDUMP_MEMORY_DESCRIPTOR64, $1> = VArray<Win32__impl_l0.MINIDUMP_MEMORY_DESCRIPTOR64, $1>(repeat: Win32__impl_l0.MINIDUMP_MEMORY_DESCRIPTOR64())
                return packedReadValue<VArray<Win32__impl_l0.MINIDUMP_MEMORY_DESCRIPTOR64, $1>>(base, 16, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 16, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_MEMORY_DESCRIPTOR {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    public mut prop startOfMemoryRange: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop memory: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR = Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR()
                return packedReadValue<Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_MEMORY_LIST {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    public mut prop numberOfMemoryRanges: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop memoryRanges: VArray<MINIDUMP_MEMORY_DESCRIPTOR, $1> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<MINIDUMP_MEMORY_DESCRIPTOR, $1> = VArray<MINIDUMP_MEMORY_DESCRIPTOR, $1>(repeat: MINIDUMP_MEMORY_DESCRIPTOR())
                return packedReadValue<VArray<MINIDUMP_MEMORY_DESCRIPTOR, $1>>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_MISC_INFO {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop sizeOfInfo: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop flags1: Win32__impl_l0.MINIDUMP_MISC_INFO_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return Win32__impl_l0.MINIDUMP_MISC_INFO_FLAGS(packedReadUInt32(base, 4))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val.value)
                _raw = rawStorage
            }
        }
    }

    public mut prop processId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processCreateTime: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processUserTime: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processKernelTime: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_Time == "on"]
// packed(4)
@C
public struct MINIDUMP_MISC_INFO_5 {
    public var _raw: VArray<UInt8, $1364> = VArray<UInt8, $1364>(repeat: 0u8)

    public mut prop sizeOfInfo: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop flags1: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processCreateTime: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processUserTime: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processKernelTime: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processorMaxMhz: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processorCurrentMhz: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 28)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processorMhzLimit: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 32)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 32, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processorMaxIdleState: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 36)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 36, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processorCurrentIdleState: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 40)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 40, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processIntegrityLevel: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 44)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 44, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processExecuteFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 48)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 48, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop protectedProcess: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 52)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 52, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop timeZoneId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 56)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 56, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop timeZone: Win32_System_Time.TIME_ZONE_INFORMATION {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32_System_Time.TIME_ZONE_INFORMATION = Win32_System_Time.TIME_ZONE_INFORMATION()
                return packedReadValue<Win32_System_Time.TIME_ZONE_INFORMATION>(base, 60, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 60, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop buildString: VArray<UInt16, $260> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt16, $260> = VArray<UInt16, $260>(repeat: 0)
                return packedReadValue<VArray<UInt16, $260>>(base, 232, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 232, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dbgBldStr: VArray<UInt16, $40> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt16, $40> = VArray<UInt16, $40>(repeat: 0)
                return packedReadValue<VArray<UInt16, $40>>(base, 752, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 752, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop xStateData: XSTATE_CONFIG_FEATURE_MSC_INFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: XSTATE_CONFIG_FEATURE_MSC_INFO = XSTATE_CONFIG_FEATURE_MSC_INFO()
                return packedReadValue<XSTATE_CONFIG_FEATURE_MSC_INFO>(base, 832, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 832, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processCookie: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 1360)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 1360, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_Storage_FileSystem == "on"]
// packed(4)
@C
public struct MINIDUMP_MODULE {
    public var _raw: VArray<UInt8, $108> = VArray<UInt8, $108>(repeat: 0u8)

    public mut prop baseOfImage: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfImage: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop checkSum: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop timeDateStamp: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop moduleNameRva: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop versionInfo: VS_FIXEDFILEINFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VS_FIXEDFILEINFO = VS_FIXEDFILEINFO()
                return packedReadValue<VS_FIXEDFILEINFO>(base, 24, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cvRecord: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR = Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR()
                return packedReadValue<Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR>(base, 76, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 76, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop miscRecord: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR = Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR()
                return packedReadValue<Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR>(base, 84, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 84, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved0: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 92)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 92, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved1: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 100)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 100, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_Storage_FileSystem == "on"]
// packed(4)
@C
public struct MINIDUMP_MODULE_CALLBACK {
    public var _raw: VArray<UInt8, $104> = VArray<UInt8, $104>(repeat: 0u8)

    public mut prop fullPath: CPointer<UInt16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: CPointer<UInt16> = CPointer<UInt16>()
                return packedReadValue<CPointer<UInt16>>(base, 0, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop baseOfImage: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfImage: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop checkSum: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop timeDateStamp: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop versionInfo: VS_FIXEDFILEINFO {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VS_FIXEDFILEINFO = VS_FIXEDFILEINFO()
                return packedReadValue<VS_FIXEDFILEINFO>(base, 28, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cvRecord: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: CPointer<Unit> = CPointer<Unit>()
                return packedReadValue<CPointer<Unit>>(base, 80, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 80, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfCvRecord: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 88)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 88, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop miscRecord: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: CPointer<Unit> = CPointer<Unit>()
                return packedReadValue<CPointer<Unit>>(base, 92, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 92, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sizeOfMiscRecord: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 100)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 100, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
// packed(4)
@C
public struct MINIDUMP_SYSTEM_INFO {
    public var _raw: VArray<UInt8, $56> = VArray<UInt8, $56>(repeat: 0u8)

    public mut prop processorArchitecture: Win32_System_SystemInformation.PROCESSOR_ARCHITECTURE {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return Win32_System_SystemInformation.PROCESSOR_ARCHITECTURE(packedReadUInt16(base, 0))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val.value)
                _raw = rawStorage
            }
        }
    }

    public mut prop processorLevel: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop processorRevision: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous1: MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union = MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union()
                return packedReadValue<MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union>(base, 6, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 6, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop majorVersion: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop minorVersion: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop buildNumber: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop platformId: Win32__impl_l0.VER_PLATFORM {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return Win32__impl_l0.VER_PLATFORM(packedReadUInt32(base, 20))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val.value)
                _raw = rawStorage
            }
        }
    }

    public mut prop csdVersionRva: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous2: MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union = MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union()
                return packedReadValue<MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union>(base, 28, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop cpu: Win32__impl_l0.CPU_INFORMATION {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.CPU_INFORMATION = Win32__impl_l0.CPU_INFORMATION()
                return packedReadValue<Win32__impl_l0.CPU_INFORMATION>(base, 32, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 32, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union {
    public var _raw: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0)

    public mut prop reserved0: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
@C
public struct MINIDUMP_SYSTEM_INFO__Anonymous1_e__Union__Anonymous_e__Struct {
    public var NumberOfProcessors: UInt8 = 0
    public var ProductType: UInt8 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public mut prop reserved1: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && Windows_Win32_System_SystemInformation == "on"]
@C
public struct MINIDUMP_SYSTEM_INFO__Anonymous2_e__Union__Anonymous_e__Struct {
    public var SuiteMask: UInt16 = 0
    public var Reserved2: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_SYSTEM_MEMORY_INFO_1 {
    public var _raw: VArray<UInt8, $492> = VArray<UInt8, $492>(repeat: 0u8)

    public mut prop revision: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop flags: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop basicInfo: Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_INFORMATION {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_INFORMATION = Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_INFORMATION()
                return packedReadValue<Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_INFORMATION>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fileCacheInfo: Win32__impl_l0.MINIDUMP_SYSTEM_FILECACHE_INFORMATION {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_SYSTEM_FILECACHE_INFORMATION = Win32__impl_l0.MINIDUMP_SYSTEM_FILECACHE_INFORMATION()
                return packedReadValue<Win32__impl_l0.MINIDUMP_SYSTEM_FILECACHE_INFORMATION>(base, 56, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 56, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop basicPerfInfo: Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_PERFORMANCE_INFORMATION {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_PERFORMANCE_INFORMATION = Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_PERFORMANCE_INFORMATION()
                return packedReadValue<Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_PERFORMANCE_INFORMATION>(base, 116, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 116, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop perfInfo: Win32__impl_l0.MINIDUMP_SYSTEM_PERFORMANCE_INFORMATION {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_SYSTEM_PERFORMANCE_INFORMATION = Win32__impl_l0.MINIDUMP_SYSTEM_PERFORMANCE_INFORMATION()
                return packedReadValue<Win32__impl_l0.MINIDUMP_SYSTEM_PERFORMANCE_INFORMATION>(base, 148, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 148, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct MINIDUMP_SYSTEM_MEMORY_INFO_2 {
    public var Revision: UInt16 = 0
    public var Flags: UInt16 = 0
    public var BasicInfo: Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_INFORMATION = Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_INFORMATION()
    public var FileCacheInfo: Win32__impl_l0.MINIDUMP_SYSTEM_FILECACHE_INFORMATION = Win32__impl_l0.MINIDUMP_SYSTEM_FILECACHE_INFORMATION()
    public var BasicPerfInfo: Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_PERFORMANCE_INFORMATION = Win32__impl_l0.MINIDUMP_SYSTEM_BASIC_PERFORMANCE_INFORMATION()
    public var PerfInfo: Win32__impl_l0.MINIDUMP_SYSTEM_PERFORMANCE_INFORMATION_2 = Win32__impl_l0.MINIDUMP_SYSTEM_PERFORMANCE_INFORMATION_2()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_THREAD {
    public var _raw: VArray<UInt8, $48> = VArray<UInt8, $48>(repeat: 0u8)

    public mut prop threadId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop suspendCount: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop priorityClass: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop priority: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop teb: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop stack: MINIDUMP_MEMORY_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: MINIDUMP_MEMORY_DESCRIPTOR = MINIDUMP_MEMORY_DESCRIPTOR()
                return packedReadValue<MINIDUMP_MEMORY_DESCRIPTOR>(base, 24, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop threadContext: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR = Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR()
                return packedReadValue<Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR>(base, 40, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 40, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_THREAD_EX {
    public var _raw: VArray<UInt8, $64> = VArray<UInt8, $64>(repeat: 0u8)

    public mut prop threadId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop suspendCount: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop priorityClass: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop priority: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop teb: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop stack: MINIDUMP_MEMORY_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: MINIDUMP_MEMORY_DESCRIPTOR = MINIDUMP_MEMORY_DESCRIPTOR()
                return packedReadValue<MINIDUMP_MEMORY_DESCRIPTOR>(base, 24, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop threadContext: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR = Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR()
                return packedReadValue<Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR>(base, 40, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 40, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop backingStore: MINIDUMP_MEMORY_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: MINIDUMP_MEMORY_DESCRIPTOR = MINIDUMP_MEMORY_DESCRIPTOR()
                return packedReadValue<MINIDUMP_MEMORY_DESCRIPTOR>(base, 48, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 48, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_THREAD_INFO {
    public var _raw: VArray<UInt8, $64> = VArray<UInt8, $64>(repeat: 0u8)

    public mut prop threadId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dumpFlags: Win32__impl_l0.MINIDUMP_THREAD_INFO_DUMP_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return Win32__impl_l0.MINIDUMP_THREAD_INFO_DUMP_FLAGS(packedReadUInt32(base, 4))
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val.value)
                _raw = rawStorage
            }
        }
    }

    public mut prop dumpError: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop exitStatus: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop createTime: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop exitTime: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop kernelTime: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 32)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 32, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop userTime: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 40)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 40, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop startAddress: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 48)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 48, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop affinity: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 56)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 56, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_THREAD_NAME_LIST {
    public var _raw: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0u8)

    public mut prop numberOfThreadNames: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop threadNames: VArray<Win32__impl_l0.MINIDUMP_THREAD_NAME, $1> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<Win32__impl_l0.MINIDUMP_THREAD_NAME, $1> = VArray<Win32__impl_l0.MINIDUMP_THREAD_NAME, $1>(repeat: Win32__impl_l0.MINIDUMP_THREAD_NAME())
                return packedReadValue<VArray<Win32__impl_l0.MINIDUMP_THREAD_NAME, $1>>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct MINIDUMP_USER_RECORD {
    public var _raw: VArray<UInt8, $12> = VArray<UInt8, $12>(repeat: 0u8)

    public mut prop type_: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop memory: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR = Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR()
                return packedReadValue<Win32__impl_l0.MINIDUMP_LOCATION_DESCRIPTOR>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && (arch == "x86_64" && (arch == "x86_64" || arch == "aarch64") || arch == "aarch64" && (arch == "x86_64" || arch == "aarch64"))]
// packed(4)
@C
public struct MINIDUMP_USER_STREAM_INFORMATION {
    public var _raw: VArray<UInt8, $12> = VArray<UInt8, $12>(repeat: 0u8)

    public mut prop userStreamCount: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop userStreamArray: CPointer<Win32__impl_l0.MINIDUMP_USER_STREAM> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: CPointer<Win32__impl_l0.MINIDUMP_USER_STREAM> = CPointer<Win32__impl_l0.MINIDUMP_USER_STREAM>()
                return packedReadValue<CPointer<Win32__impl_l0.MINIDUMP_USER_STREAM>>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct MODLOAD_DATA {
    public var ssize: UInt32 = 0
    public var ssig: UInt32 = 0
    public var data: CPointer<Unit> = CPointer<Unit>()
    public var size: UInt32 = 0
    public var flags: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && (arch == "aarch64" || arch == "x86_64")]
public type PGET_RUNTIME_FUNCTION_CALLBACK = CFunc<(UInt64, CPointer<Unit>) -> CPointer<Unit>>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct PHYSICAL_MEMORY_DESCRIPTOR32 {
    public var NumberOfRuns: UInt32 = 0
    public var NumberOfPages: UInt32 = 0
    public var Run: VArray<Win32__impl_l0.PHYSICAL_MEMORY_RUN32, $1> = VArray<Win32__impl_l0.PHYSICAL_MEMORY_RUN32, $1>(repeat: Win32__impl_l0.PHYSICAL_MEMORY_RUN32())
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct PHYSICAL_MEMORY_DESCRIPTOR64 {
    public var NumberOfRuns: UInt32 = 0
    public var NumberOfPages: UInt64 = 0
    public var Run: VArray<Win32__impl_l0.PHYSICAL_MEMORY_RUN64, $1> = VArray<Win32__impl_l0.PHYSICAL_MEMORY_RUN64, $1>(repeat: Win32__impl_l0.PHYSICAL_MEMORY_RUN64())
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PIMAGEHLP_STATUS_ROUTINE = CFunc<(Int32, CPointer<UInt8>, CPointer<UInt8>, UIntNative, UIntNative) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PIMAGEHLP_STATUS_ROUTINE32 = CFunc<(Int32, CPointer<UInt8>, CPointer<UInt8>, UInt32, UIntNative) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PIMAGEHLP_STATUS_ROUTINE64 = CFunc<(Int32, CPointer<UInt8>, CPointer<UInt8>, UInt64, UIntNative) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PSYMBOLSERVERGETVERSION = CFunc<(CPointer<Win32__impl_l0.API_VERSION>) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PSYMBOLSERVERWEXPROC = CFunc<(CPointer<UInt16>, CPointer<UInt16>, CPointer<Unit>, UInt32, UInt32, CPointer<UInt16>, CPointer<Win32__impl_l0.SYMSRV_EXTENDED_OUTPUT_DATA>) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PSYM_ENUMERATESYMBOLS_CALLBACK = CFunc<(CPointer<SYMBOL_INFO>, UInt32, CPointer<Unit>) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PSYM_ENUMERATESYMBOLS_CALLBACKW = CFunc<(CPointer<SYMBOL_INFOW>, UInt32, CPointer<Unit>) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PSYM_ENUMLINES_CALLBACK = CFunc<(CPointer<Win32__impl_l0.SRCCODEINFO>, CPointer<Unit>) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PSYM_ENUMLINES_CALLBACKW = CFunc<(CPointer<Win32__impl_l0.SRCCODEINFOW>, CPointer<Unit>) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PSYM_ENUMSOURCEFILES_CALLBACK = CFunc<(CPointer<Win32__impl_l0.SOURCEFILE>, CPointer<Unit>) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PSYM_ENUMSOURCEFILES_CALLBACKW = CFunc<(CPointer<Win32__impl_l0.SOURCEFILEW>, CPointer<Unit>) -> Int32>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PTRANSLATE_ADDRESS_ROUTINE64 = CFunc<(CPointer<Unit>, CPointer<Unit>, CPointer<ADDRESS64>) -> UInt64>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
public type PWAITCHAINCALLBACK = CFunc<(CPointer<Unit>, UIntNative, UInt32, CPointer<UInt32>, CPointer<WAITCHAIN_NODE_INFO>, CPointer<Int32>) -> Unit>

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct RIP_INFO {
    public var dwError: UInt32 = 0
    public var dwType: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct STACKFRAME64 {
    public var AddrPC: ADDRESS64 = ADDRESS64()
    public var AddrReturn: ADDRESS64 = ADDRESS64()
    public var AddrFrame: ADDRESS64 = ADDRESS64()
    public var AddrStack: ADDRESS64 = ADDRESS64()
    public var AddrBStore: ADDRESS64 = ADDRESS64()
    public var FuncTableEntry: CPointer<Unit> = CPointer<Unit>()
    public var Params: VArray<UInt64, $4> = VArray<UInt64, $4>(repeat: 0)
    public var Far: Int32 = 0
    public var Virtual: Int32 = 0
    public var Reserved: VArray<UInt64, $3> = VArray<UInt64, $3>(repeat: 0)
    public var KdHelp: Win32__impl_l0.KDHELP64 = Win32__impl_l0.KDHELP64()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct STACKFRAME_EX {
    public var AddrPC: ADDRESS64 = ADDRESS64()
    public var AddrReturn: ADDRESS64 = ADDRESS64()
    public var AddrFrame: ADDRESS64 = ADDRESS64()
    public var AddrStack: ADDRESS64 = ADDRESS64()
    public var AddrBStore: ADDRESS64 = ADDRESS64()
    public var FuncTableEntry: CPointer<Unit> = CPointer<Unit>()
    public var Params: VArray<UInt64, $4> = VArray<UInt64, $4>(repeat: 0)
    public var Far: Int32 = 0
    public var Virtual: Int32 = 0
    public var Reserved: VArray<UInt64, $3> = VArray<UInt64, $3>(repeat: 0)
    public var KdHelp: Win32__impl_l0.KDHELP64 = Win32__impl_l0.KDHELP64()
    public var StackFrameSize: UInt32 = 0
    public var InlineFrameContext: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct SYMBOL_INFO {
    public var SizeOfStruct: UInt32 = 0
    public var TypeIndex: UInt32 = 0
    public var Reserved: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0)
    public var Index: UInt32 = 0
    public var Size: UInt32 = 0
    public var ModBase: UInt64 = 0
    public var Flags: UInt32 = 0
    public var Value: UInt64 = 0
    public var Address: UInt64 = 0
    public var Register: UInt32 = 0
    public var Scope: UInt32 = 0
    public var Tag: UInt32 = 0
    public var NameLen: UInt32 = 0
    public var MaxNameLen: UInt32 = 0
    public var Name: VArray<Int8, $1> = VArray<Int8, $1>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct SYMBOL_INFOW {
    public var SizeOfStruct: UInt32 = 0
    public var TypeIndex: UInt32 = 0
    public var Reserved: VArray<UInt64, $2> = VArray<UInt64, $2>(repeat: 0)
    public var Index: UInt32 = 0
    public var Size: UInt32 = 0
    public var ModBase: UInt64 = 0
    public var Flags: UInt32 = 0
    public var Value: UInt64 = 0
    public var Address: UInt64 = 0
    public var Register: UInt32 = 0
    public var Scope: UInt32 = 0
    public var Tag: UInt32 = 0
    public var NameLen: UInt32 = 0
    public var MaxNameLen: UInt32 = 0
    public var Name: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct SYMBOL_INFO_PACKAGE {
    public var si: SYMBOL_INFO = SYMBOL_INFO()
    public var name: VArray<Int8, $2001> = VArray<Int8, $2001>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct SYMBOL_INFO_PACKAGEW {
    public var si: SYMBOL_INFOW = SYMBOL_INFOW()
    public var name: VArray<UInt16, $2001> = VArray<UInt16, $2001>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct TI_GET_DISCRIMINATEDUNION_TAG_RANGES_PARAMS {
    public var Count: UInt32 = 0
    public var Start: UInt32 = 0
    public var Range: VArray<Win32__impl_l0.DISCRIMINATEDUNION_TAG_VALUE, $1> = VArray<Win32__impl_l0.DISCRIMINATEDUNION_TAG_VALUE, $1>(repeat: Win32__impl_l0.DISCRIMINATEDUNION_TAG_VALUE())
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && (arch == "x86_64" && (arch == "aarch64" || arch == "x86_64") || arch == "aarch64" && (arch == "aarch64" || arch == "x86_64"))]
@C
public struct UNWIND_HISTORY_TABLE {
    public var Count: UInt32 = 0
    public var LocalHint: UInt8 = 0
    public var GlobalHint: UInt8 = 0
    public var Search: UInt8 = 0
    public var Once: UInt8 = 0
    public var LowAddress: UIntNative = 0
    public var HighAddress: UIntNative = 0
    public var Entry: VArray<UNWIND_HISTORY_TABLE_ENTRY, $12> = VArray<UNWIND_HISTORY_TABLE_ENTRY, $12>(repeat: UNWIND_HISTORY_TABLE_ENTRY())
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "aarch64"]
@C
public struct UNWIND_HISTORY_TABLE_ENTRY {
    public var ImageBase: UIntNative = 0
    public var FunctionEntry: CPointer<Win32__impl_l0.IMAGE_ARM64_RUNTIME_FUNCTION_ENTRY> = CPointer<Win32__impl_l0.IMAGE_ARM64_RUNTIME_FUNCTION_ENTRY>()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && arch == "x86_64"]
@C
public struct UNWIND_HISTORY_TABLE_ENTRY {
    public var ImageBase: UIntNative = 0
    public var FunctionEntry: CPointer<Win32__impl_l0.IMAGE_RUNTIME_FUNCTION_ENTRY> = CPointer<Win32__impl_l0.IMAGE_RUNTIME_FUNCTION_ENTRY>()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct WAITCHAIN_NODE_INFO {
    public var ObjectType: Int32 = 0
    public var ObjectStatus: Int32 = 0
    public var Anonymous: WAITCHAIN_NODE_INFO__Anonymous_e__Union = WAITCHAIN_NODE_INFO__Anonymous_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct WAITCHAIN_NODE_INFO__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $34> = VArray<UInt64, $34>(repeat: 0)

    public mut prop lockObject: WAITCHAIN_NODE_INFO__Anonymous_e__Union__LockObject_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WAITCHAIN_NODE_INFO__Anonymous_e__Union__LockObject_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WAITCHAIN_NODE_INFO__Anonymous_e__Union__LockObject_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop threadObject: WAITCHAIN_NODE_INFO__Anonymous_e__Union__ThreadObject_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WAITCHAIN_NODE_INFO__Anonymous_e__Union__ThreadObject_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WAITCHAIN_NODE_INFO__Anonymous_e__Union__ThreadObject_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct WAITCHAIN_NODE_INFO__Anonymous_e__Union__LockObject_e__Struct {
    public var ObjectName: VArray<UInt16, $128> = VArray<UInt16, $128>(repeat: 0)
    public var Timeout: Int64 = 0
    public var Alertable: Int32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct WAITCHAIN_NODE_INFO__Anonymous_e__Union__ThreadObject_e__Struct {
    public var ProcessId: UInt32 = 0
    public var ThreadId: UInt32 = 0
    public var WaitTime: UInt32 = 0
    public var ContextSwitches: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_AER_BRIDGE_DESCRIPTOR {
    public var _raw: VArray<UInt8, $44> = VArray<UInt8, $44>(repeat: 0u8)

    public mut prop type_: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabled: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop busNumber: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop slot: Win32__impl_l0.WHEA_PCI_SLOT_NUMBER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_PCI_SLOT_NUMBER = Win32__impl_l0.WHEA_PCI_SLOT_NUMBER()
                return packedReadValue<Win32__impl_l0.WHEA_PCI_SLOT_NUMBER>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop deviceControl: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop flags: Win32__impl_l0.AER_BRIDGE_DESCRIPTOR_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.AER_BRIDGE_DESCRIPTOR_FLAGS = Win32__impl_l0.AER_BRIDGE_DESCRIPTOR_FLAGS()
                return packedReadValue<Win32__impl_l0.AER_BRIDGE_DESCRIPTOR_FLAGS>(base, 14, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 14, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uncorrectableErrorMask: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uncorrectableErrorSeverity: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop correctableErrorMask: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop advancedCapsAndControl: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 28)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop secondaryUncorrectableErrorMask: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 32)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 32, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop secondaryUncorrectableErrorSev: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 36)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 36, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop secondaryCapsAndControl: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 40)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 40, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_AER_ENDPOINT_DESCRIPTOR {
    public var _raw: VArray<UInt8, $32> = VArray<UInt8, $32>(repeat: 0u8)

    public mut prop type_: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabled: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop busNumber: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop slot: Win32__impl_l0.WHEA_PCI_SLOT_NUMBER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_PCI_SLOT_NUMBER = Win32__impl_l0.WHEA_PCI_SLOT_NUMBER()
                return packedReadValue<Win32__impl_l0.WHEA_PCI_SLOT_NUMBER>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop deviceControl: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop flags: Win32__impl_l0.AER_ENDPOINT_DESCRIPTOR_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.AER_ENDPOINT_DESCRIPTOR_FLAGS = Win32__impl_l0.AER_ENDPOINT_DESCRIPTOR_FLAGS()
                return packedReadValue<Win32__impl_l0.AER_ENDPOINT_DESCRIPTOR_FLAGS>(base, 14, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 14, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uncorrectableErrorMask: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uncorrectableErrorSeverity: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop correctableErrorMask: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop advancedCapsAndControl: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 28)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 28, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_AER_ROOTPORT_DESCRIPTOR {
    public var _raw: VArray<UInt8, $36> = VArray<UInt8, $36>(repeat: 0u8)

    public mut prop type_: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabled: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop busNumber: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop slot: Win32__impl_l0.WHEA_PCI_SLOT_NUMBER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_PCI_SLOT_NUMBER = Win32__impl_l0.WHEA_PCI_SLOT_NUMBER()
                return packedReadValue<Win32__impl_l0.WHEA_PCI_SLOT_NUMBER>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop deviceControl: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop flags: Win32__impl_l0.AER_ROOTPORT_DESCRIPTOR_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.AER_ROOTPORT_DESCRIPTOR_FLAGS = Win32__impl_l0.AER_ROOTPORT_DESCRIPTOR_FLAGS()
                return packedReadValue<Win32__impl_l0.AER_ROOTPORT_DESCRIPTOR_FLAGS>(base, 14, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 14, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uncorrectableErrorMask: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uncorrectableErrorSeverity: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop correctableErrorMask: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop advancedCapsAndControl: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 28)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rootErrorCommand: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 32)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 32, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_DEVICE_DRIVER_DESCRIPTOR {
    public var _raw: VArray<UInt8, $116> = VArray<UInt8, $116>(repeat: 0u8)

    public mut prop type_: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabled: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sourceGuid: GUID {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: GUID = GUID()
                return packedReadValue<GUID>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop logTag: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved2: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 22)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 22, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop packetLength: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 24)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop packetCount: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 28)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop packetBuffer: CPointer<UInt8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: CPointer<UInt8> = CPointer<UInt8>()
                return packedReadValue<CPointer<UInt8>>(base, 32, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 32, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop config: WHEA_ERROR_SOURCE_CONFIGURATION_DD {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: WHEA_ERROR_SOURCE_CONFIGURATION_DD = WHEA_ERROR_SOURCE_CONFIGURATION_DD()
                return packedReadValue<WHEA_ERROR_SOURCE_CONFIGURATION_DD>(base, 40, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 40, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop creatorId: GUID {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: GUID = GUID()
                return packedReadValue<GUID>(base, 64, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 64, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop partitionId: GUID {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: GUID = GUID()
                return packedReadValue<GUID>(base, 80, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 80, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop maxSectionDataLength: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 96)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 96, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop maxSectionsPerRecord: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 100)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 100, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop packetStateBuffer: CPointer<UInt8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: CPointer<UInt8> = CPointer<UInt8>()
                return packedReadValue<CPointer<UInt8>>(base, 104, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 104, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop openHandles: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt32(base, 112)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt32(base, 112, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_ERROR_SOURCE_CONFIGURATION_DD {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop initialize: Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER = CFunc<(CPointer<Unit>, UInt32) -> Int32>(CPointer<Unit>())
                return packedReadValue<Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER>(base, 0, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uninitialize: Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER = CFunc<(CPointer<Unit>) -> Unit>(CPointer<Unit>())
                return packedReadValue<Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop correct: Win32__impl_l0.WHEA_ERROR_SOURCE_CORRECT_DEVICE_DRIVER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_ERROR_SOURCE_CORRECT_DEVICE_DRIVER = CFunc<(CPointer<Unit>, CPointer<UInt32>) -> Int32>(CPointer<Unit>())
                return packedReadValue<Win32__impl_l0.WHEA_ERROR_SOURCE_CORRECT_DEVICE_DRIVER>(base, 16, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 16, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_ERROR_SOURCE_CONFIGURATION_DEVICE_DRIVER {
    public var _raw: VArray<UInt8, $84> = VArray<UInt8, $84>(repeat: 0u8)

    public mut prop version: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sourceGuid: GUID {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: GUID = GUID()
                return packedReadValue<GUID>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop logTag: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: VArray<UInt8, $6> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt8, $6> = VArray<UInt8, $6>(repeat: 0)
                return packedReadValue<VArray<UInt8, $6>>(base, 22, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 22, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop initialize: Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER = CFunc<(CPointer<Unit>, UInt32) -> Int32>(CPointer<Unit>())
                return packedReadValue<Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER>(base, 28, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uninitialize: Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER = CFunc<(CPointer<Unit>) -> Unit>(CPointer<Unit>())
                return packedReadValue<Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER>(base, 36, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 36, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop maxSectionDataLength: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 44)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 44, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop maxSectionsPerReport: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 48)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 48, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop creatorId: GUID {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: GUID = GUID()
                return packedReadValue<GUID>(base, 52, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 52, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop partitionId: GUID {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: GUID = GUID()
                return packedReadValue<GUID>(base, 68, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 68, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_ERROR_SOURCE_CONFIGURATION_DEVICE_DRIVER_V1 {
    public var _raw: VArray<UInt8, $44> = VArray<UInt8, $44>(repeat: 0u8)

    public mut prop version: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sourceGuid: GUID {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: GUID = GUID()
                return packedReadValue<GUID>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop logTag: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 20, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: VArray<UInt8, $6> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt8, $6> = VArray<UInt8, $6>(repeat: 0)
                return packedReadValue<VArray<UInt8, $6>>(base, 22, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 22, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop initialize: Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER = CFunc<(CPointer<Unit>, UInt32) -> Int32>(CPointer<Unit>())
                return packedReadValue<Win32__impl_l0.WHEA_ERROR_SOURCE_INITIALIZE_DEVICE_DRIVER>(base, 28, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 28, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uninitialize: Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER = CFunc<(CPointer<Unit>) -> Unit>(CPointer<Unit>())
                return packedReadValue<Win32__impl_l0.WHEA_ERROR_SOURCE_UNINITIALIZE_DEVICE_DRIVER>(base, 36, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 36, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_GENERIC_ERROR_DESCRIPTOR {
    public var _raw: VArray<UInt8, $52> = VArray<UInt8, $52>(repeat: 0u8)

    public mut prop type_: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabled: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusBlockLength: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop relatedErrorSourceId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddressSpaceID: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddressBitWidth: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 13)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 13, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddressBitOffset: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 14)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 14, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddressAccessSize: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 15)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 15, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddress: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt64(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt64(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop notify: WHEA_NOTIFICATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: WHEA_NOTIFICATION_DESCRIPTOR = WHEA_NOTIFICATION_DESCRIPTOR()
                return packedReadValue<WHEA_NOTIFICATION_DESCRIPTOR>(base, 24, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 24, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_GENERIC_ERROR_DESCRIPTOR_V2 {
    public var _raw: VArray<UInt8, $80> = VArray<UInt8, $80>(repeat: 0u8)

    public mut prop type_: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabled: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusBlockLength: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop relatedErrorSourceId: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddressSpaceID: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddressBitWidth: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 13)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 13, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddressBitOffset: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 14)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 14, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddressAccessSize: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 15)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 15, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errStatusAddress: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt64(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt64(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop notify: WHEA_NOTIFICATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: WHEA_NOTIFICATION_DESCRIPTOR = WHEA_NOTIFICATION_DESCRIPTOR()
                return packedReadValue<WHEA_NOTIFICATION_DESCRIPTOR>(base, 24, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 24, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop readAckAddressSpaceID: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 52)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 52, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop readAckAddressBitWidth: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 53)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 53, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop readAckAddressBitOffset: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 54)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 54, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop readAckAddressAccessSize: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 55)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 55, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop readAckAddress: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt64(base, 56)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt64(base, 56, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop readAckPreserveMask: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 64)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 64, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop readAckWriteMask: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 72)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 72, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR {
    public var Type: UInt8 = 0
    public var Length: UInt8 = 0
    public var Flags: Win32__impl_l0.WHEA_NOTIFICATION_FLAGS = Win32__impl_l0.WHEA_NOTIFICATION_FLAGS()
    public var u: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union = WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0)

    public mut prop polled: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Polled_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Polled_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Polled_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop interrupt: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Interrupt_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Interrupt_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Interrupt_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop localInterrupt: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__LocalInterrupt_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__LocalInterrupt_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__LocalInterrupt_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sci: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sci_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sci_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sci_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop nmi: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Nmi_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Nmi_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Nmi_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sea: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sea_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sea_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sea_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop sei: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sei_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sei_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sei_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop gsiv: WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Gsiv_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Gsiv_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Gsiv_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Polled_e__Struct {
    public var _raw: VArray<UInt8, $4> = VArray<UInt8, $4>(repeat: 0u8)

    public mut prop pollInterval: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Interrupt_e__Struct {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop pollInterval: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop vector: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThresholdWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__LocalInterrupt_e__Struct {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop pollInterval: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop vector: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThresholdWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sci_e__Struct {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop pollInterval: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop vector: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThresholdWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Nmi_e__Struct {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop pollInterval: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop vector: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThresholdWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sea_e__Struct {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop pollInterval: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop vector: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThresholdWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Sei_e__Struct {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop pollInterval: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop vector: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThresholdWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_NOTIFICATION_DESCRIPTOR__u_e__Union__Gsiv_e__Struct {
    public var _raw: VArray<UInt8, $24> = VArray<UInt8, $24>(repeat: 0u8)

    public mut prop pollInterval: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop vector: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop switchToPollingWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThreshold: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop errorThresholdWindow: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_XPF_CMC_DESCRIPTOR {
    public var _raw: VArray<UInt8, $932> = VArray<UInt8, $932>(repeat: 0u8)

    public mut prop type_: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabled: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop numberOfBanks: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop reserved: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop notify: WHEA_NOTIFICATION_DESCRIPTOR {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: WHEA_NOTIFICATION_DESCRIPTOR = WHEA_NOTIFICATION_DESCRIPTOR()
                return packedReadValue<WHEA_NOTIFICATION_DESCRIPTOR>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop banks: VArray<WHEA_XPF_MC_BANK_DESCRIPTOR, $32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<WHEA_XPF_MC_BANK_DESCRIPTOR, $32> = VArray<WHEA_XPF_MC_BANK_DESCRIPTOR, $32>(repeat: WHEA_XPF_MC_BANK_DESCRIPTOR())
                return packedReadValue<VArray<WHEA_XPF_MC_BANK_DESCRIPTOR, $32>>(base, 36, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 36, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_XPF_MCE_DESCRIPTOR {
    public var _raw: VArray<UInt8, $920> = VArray<UInt8, $920>(repeat: 0u8)

    public mut prop type_: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabled: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop numberOfBanks: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 3)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop flags: Win32__impl_l0.XPF_MCE_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.XPF_MCE_FLAGS = Win32__impl_l0.XPF_MCE_FLAGS()
                return packedReadValue<Win32__impl_l0.XPF_MCE_FLAGS>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop mcg_Capability: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop mcg_GlobalControl: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop banks: VArray<WHEA_XPF_MC_BANK_DESCRIPTOR, $32> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<WHEA_XPF_MC_BANK_DESCRIPTOR, $32> = VArray<WHEA_XPF_MC_BANK_DESCRIPTOR, $32>(repeat: WHEA_XPF_MC_BANK_DESCRIPTOR())
                return packedReadValue<VArray<WHEA_XPF_MC_BANK_DESCRIPTOR, $32>>(base, 24, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 24, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(1)
@C
public struct WHEA_XPF_MC_BANK_DESCRIPTOR {
    public var _raw: VArray<UInt8, $28> = VArray<UInt8, $28>(repeat: 0u8)

    public mut prop bankNumber: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop clearOnInitialization: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 1)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 1, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop statusDataFormat: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 2)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 2, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop flags: Win32__impl_l0.XPF_MC_BANK_FLAGS {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: Win32__impl_l0.XPF_MC_BANK_FLAGS = Win32__impl_l0.XPF_MC_BANK_FLAGS()
                return packedReadValue<Win32__impl_l0.XPF_MC_BANK_FLAGS>(base, 3, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 3, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop controlMsr: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop statusMsr: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop addressMsr: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop miscMsr: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop controlData: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 20)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 20, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct WOW64_CONTEXT {
    public var ContextFlags: UInt32 = 0
    public var Dr0: UInt32 = 0
    public var Dr1: UInt32 = 0
    public var Dr2: UInt32 = 0
    public var Dr3: UInt32 = 0
    public var Dr6: UInt32 = 0
    public var Dr7: UInt32 = 0
    public var FloatSave: Win32__impl_l0.WOW64_FLOATING_SAVE_AREA = Win32__impl_l0.WOW64_FLOATING_SAVE_AREA()
    public var SegGs: UInt32 = 0
    public var SegFs: UInt32 = 0
    public var SegEs: UInt32 = 0
    public var SegDs: UInt32 = 0
    public var Edi: UInt32 = 0
    public var Esi: UInt32 = 0
    public var Ebx: UInt32 = 0
    public var Edx: UInt32 = 0
    public var Ecx: UInt32 = 0
    public var Eax: UInt32 = 0
    public var Ebp: UInt32 = 0
    public var Eip: UInt32 = 0
    public var SegCs: UInt32 = 0
    public var EFlags: UInt32 = 0
    public var Esp: UInt32 = 0
    public var SegSs: UInt32 = 0
    public var ExtendedRegisters: VArray<UInt8, $512> = VArray<UInt8, $512>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct WOW64_DESCRIPTOR_TABLE_ENTRY {
    public var Selector: UInt32 = 0
    public var Descriptor: Win32__impl_l0.WOW64_LDT_ENTRY = Win32__impl_l0.WOW64_LDT_ENTRY()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && ((arch == "x86_64" || arch == "aarch64"))]
@C
public struct XSAVE_AREA {
    public var LegacyState: XSAVE_FORMAT = XSAVE_FORMAT()
    public var Header: Win32__impl_l0.XSAVE_AREA_HEADER = Win32__impl_l0.XSAVE_AREA_HEADER()
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on" && (arch == "x86_64" || arch == "aarch64")]
@C
public struct XSAVE_FORMAT {
    public var ControlWord: UInt16 = 0
    public var StatusWord: UInt16 = 0
    public var TagWord: UInt8 = 0
    public var Reserved1: UInt8 = 0
    public var ErrorOpcode: UInt16 = 0
    public var ErrorOffset: UInt32 = 0
    public var ErrorSelector: UInt16 = 0
    public var Reserved2: UInt16 = 0
    public var DataOffset: UInt32 = 0
    public var DataSelector: UInt16 = 0
    public var Reserved3: UInt16 = 0
    public var MxCsr: UInt32 = 0
    public var MxCsr_Mask: UInt32 = 0
    public var FloatRegisters: VArray<Win32__impl_l0.M128A, $8> = VArray<Win32__impl_l0.M128A, $8>(repeat: Win32__impl_l0.M128A())
    public var XmmRegisters: VArray<Win32__impl_l0.M128A, $16> = VArray<Win32__impl_l0.M128A, $16>(repeat: Win32__impl_l0.M128A())
    public var Reserved4: VArray<UInt8, $96> = VArray<UInt8, $96>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct XSTATE_CONFIGURATION {
    public var EnabledFeatures: UInt64 = 0
    public var EnabledVolatileFeatures: UInt64 = 0
    public var Size: UInt32 = 0
    public var Anonymous: XSTATE_CONFIGURATION__Anonymous_e__Union = XSTATE_CONFIGURATION__Anonymous_e__Union()
    public var Features: VArray<Win32__impl_l0.XSTATE_FEATURE, $64> = VArray<Win32__impl_l0.XSTATE_FEATURE, $64>(repeat: Win32__impl_l0.XSTATE_FEATURE())
    public var EnabledSupervisorFeatures: UInt64 = 0
    public var AlignedFeatures: UInt64 = 0
    public var AllFeatureSize: UInt32 = 0
    public var AllFeatures: VArray<UInt32, $64> = VArray<UInt32, $64>(repeat: 0)
    public var EnabledUserVisibleSupervisorFeatures: UInt64 = 0
    public var ExtendedFeatureDisableFeatures: UInt64 = 0
    public var AllNonLargeFeatureSize: UInt32 = 0
    public var MaxSveVectorLength: UInt16 = 0
    public var Spare1: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct XSTATE_CONFIGURATION__Anonymous_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public mut prop controlFlags: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: XSTATE_CONFIGURATION__Anonymous_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<XSTATE_CONFIGURATION__Anonymous_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<XSTATE_CONFIGURATION__Anonymous_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
@C
public struct XSTATE_CONFIGURATION__Anonymous_e__Union__Anonymous_e__Struct {
    public var _bitfield: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Diagnostics_Debug == "on"]
// packed(4)
@C
public struct XSTATE_CONFIG_FEATURE_MSC_INFO {
    public var _raw: VArray<UInt8, $528> = VArray<UInt8, $528>(repeat: 0u8)

    public mut prop sizeOfInfo: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop contextSize: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop enabledFeatures: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt64(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt64(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop features: VArray<Win32__impl_l0.XSTATE_FEATURE, $64> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<Win32__impl_l0.XSTATE_FEATURE, $64> = VArray<Win32__impl_l0.XSTATE_FEATURE, $64>(repeat: Win32__impl_l0.XSTATE_FEATURE())
                return packedReadValue<VArray<Win32__impl_l0.XSTATE_FEATURE, $64>>(base, 16, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 16, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}
