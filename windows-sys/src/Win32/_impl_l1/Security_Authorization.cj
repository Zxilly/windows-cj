// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.Win32._impl_l1

import windows_interface.{ComInterface, GUID, InterfaceDescriptor, InterfaceWrapperBase}
import windows_interface as windows_interface
import windows_polyfill.{unionReadInt32, unionReadUInt32, unionWriteInt32, unionWriteUInt32}
import windows_polyfill as windows_polyfill
@When[Windows_Win32_Foundation == "on"]
import windows_sys.Win32.Foundation as Win32_Foundation
@When[Windows_Win32_Security == "on"]
import windows_sys.Win32.Security as Win32_Security
import windows_sys.Win32._impl_l0 as Win32__impl_l0


@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
@C
public struct ACTRL_ACCESS_ENTRYA {
    public var Trustee: TRUSTEE_A = TRUSTEE_A()
    public var fAccessFlags: UInt32 = 0
    public var Access: UInt32 = 0
    public var ProvSpecificAccess: UInt32 = 0
    public var Inheritance: UInt32 = 0
    public var lpInheritProperty: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
@C
public struct ACTRL_ACCESS_ENTRYW {
    public var Trustee: TRUSTEE_W = TRUSTEE_W()
    public var fAccessFlags: UInt32 = 0
    public var Access: UInt32 = 0
    public var ProvSpecificAccess: UInt32 = 0
    public var Inheritance: UInt32 = 0
    public var lpInheritProperty: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
@C
public struct AUDIT_OBJECT_TYPES {
    public var Count: UInt16 = 0
    public var Flags: UInt16 = 0
    public var pObjectTypes: CPointer<Win32__impl_l0.AUDIT_OBJECT_TYPE> = CPointer<Win32__impl_l0.AUDIT_OBJECT_TYPE>()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
@C
public struct AUDIT_PARAM {
    public var Type: Int32 = 0
    public var Length: UInt32 = 0
    public var Flags: UInt32 = 0
    public var Anonymous1: AUDIT_PARAM__Anonymous1_e__Union = AUDIT_PARAM__Anonymous1_e__Union()
    public var Anonymous2: AUDIT_PARAM__Anonymous2_e__Union = AUDIT_PARAM__Anonymous2_e__Union()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct AUDIT_PARAM__Anonymous1_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop data0: UIntNative {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<UIntNative>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<UIntNative>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop string: CPointer<UInt16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop u: UIntNative {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<UIntNative>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<UIntNative>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop psid: CPointer<Win32_Security.SID> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32_Security.SID>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32_Security.SID>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pguid: CPointer<GUID> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<GUID>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<GUID>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop logonId_LowPart: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pObjectTypes: CPointer<AUDIT_OBJECT_TYPES> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<AUDIT_OBJECT_TYPES>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<AUDIT_OBJECT_TYPES>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pIpAddress: CPointer<Win32__impl_l0.AUDIT_IP_ADDRESS> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.AUDIT_IP_ADDRESS>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.AUDIT_IP_ADDRESS>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct AUDIT_PARAM__Anonymous2_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop data1: UIntNative {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<UIntNative>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<UIntNative>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop logonId_HighPart: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
@C
public struct AUTHZ_ACCESS_REPLY {
    public var ResultListLength: UInt32 = 0
    public var GrantedAccessMask: CPointer<UInt32> = CPointer<UInt32>()
    public var SaclEvaluationResults: CPointer<UInt32> = CPointer<UInt32>()
    public var Error: CPointer<UInt32> = CPointer<UInt32>()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
@C
public struct AUTHZ_AUDIT_EVENT_TYPE_OLD {
    public var Version: UInt32 = 0
    public var dwFlags: UInt32 = 0
    public var RefCount: Int32 = 0
    public var hAudit: UIntNative = 0
    public var LinkId: Win32_Foundation.LUID = Win32_Foundation.LUID()
    public var u: AUTHZ_AUDIT_EVENT_TYPE_UNION = AUTHZ_AUDIT_EVENT_TYPE_UNION()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct AUTHZ_AUDIT_EVENT_TYPE_UNION {
    public var _raw: VArray<UInt16, $3> = VArray<UInt16, $3>(repeat: 0)

    public mut prop legacy: Win32__impl_l0.AUTHZ_AUDIT_EVENT_TYPE_LEGACY {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.AUTHZ_AUDIT_EVENT_TYPE_LEGACY>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.AUTHZ_AUDIT_EVENT_TYPE_LEGACY>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
@C
public struct AUTHZ_INIT_INFO {
    public var version: UInt16 = 0
    public var szResourceManagerName: CPointer<UInt16> = CPointer<UInt16>()
    public var pfnDynamicAccessCheck: PFN_AUTHZ_DYNAMIC_ACCESS_CHECK = CFunc<(Win32__impl_l0.AUTHZ_CLIENT_CONTEXT_HANDLE, CPointer<Win32_Security.ACE_HEADER>, CPointer<Unit>, CPointer<Int32>) -> Int32>(CPointer<Unit>())
    public var pfnComputeDynamicGroups: PFN_AUTHZ_COMPUTE_DYNAMIC_GROUPS = CFunc<(Win32__impl_l0.AUTHZ_CLIENT_CONTEXT_HANDLE, CPointer<Unit>, CPointer<CPointer<Win32_Security.SID_AND_ATTRIBUTES>>, CPointer<UInt32>, CPointer<CPointer<Win32_Security.SID_AND_ATTRIBUTES>>, CPointer<UInt32>) -> Int32>(CPointer<Unit>())
    public var pfnFreeDynamicGroups: Win32__impl_l0.PFN_AUTHZ_FREE_DYNAMIC_GROUPS = CFunc<(CPointer<Win32_Security.SID_AND_ATTRIBUTES>) -> Unit>(CPointer<Unit>())
    public var pfnGetCentralAccessPolicy: PFN_AUTHZ_GET_CENTRAL_ACCESS_POLICY = CFunc<(Win32__impl_l0.AUTHZ_CLIENT_CONTEXT_HANDLE, Win32_Security.PSID, CPointer<Unit>, CPointer<Int32>, CPointer<CPointer<Unit>>) -> Int32>(CPointer<Unit>())
    public var pfnFreeCentralAccessPolicy: Win32__impl_l0.PFN_AUTHZ_FREE_CENTRAL_ACCESS_POLICY = CFunc<(CPointer<Unit>) -> Unit>(CPointer<Unit>())
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
@C
public struct AUTHZ_SECURITY_ATTRIBUTES_INFORMATION {
    public var Version: UInt16 = 0
    public var Reserved: UInt16 = 0
    public var AttributeCount: UInt32 = 0
    public var Attribute: AUTHZ_SECURITY_ATTRIBUTES_INFORMATION__Attribute_e__Union = AUTHZ_SECURITY_ATTRIBUTES_INFORMATION__Attribute_e__Union()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct AUTHZ_SECURITY_ATTRIBUTES_INFORMATION__Attribute_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop pAttributeV1: CPointer<AUTHZ_SECURITY_ATTRIBUTE_V1> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<AUTHZ_SECURITY_ATTRIBUTE_V1>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<AUTHZ_SECURITY_ATTRIBUTE_V1>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
@C
public struct AUTHZ_SECURITY_ATTRIBUTE_V1 {
    public var pName: CPointer<UInt16> = CPointer<UInt16>()
    public var ValueType: UInt16 = 0
    public var Reserved: UInt16 = 0
    public var Flags: UInt32 = 0
    public var ValueCount: UInt32 = 0
    public var Values: AUTHZ_SECURITY_ATTRIBUTE_V1__Values_e__Union = AUTHZ_SECURITY_ATTRIBUTE_V1__Values_e__Union()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct AUTHZ_SECURITY_ATTRIBUTE_V1__Values_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop pInt64: CPointer<Int64> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int64>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Int64>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pUint64: CPointer<UInt64> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt64>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt64>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ppString: CPointer<CPointer<UInt16>> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<UInt16>>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<CPointer<UInt16>>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pFqbn: CPointer<Win32__impl_l0.AUTHZ_SECURITY_ATTRIBUTE_FQBN_VALUE> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.AUTHZ_SECURITY_ATTRIBUTE_FQBN_VALUE>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.AUTHZ_SECURITY_ATTRIBUTE_FQBN_VALUE>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pOctetString: CPointer<Win32__impl_l0.AUTHZ_SECURITY_ATTRIBUTE_OCTET_STRING_VALUE> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.AUTHZ_SECURITY_ATTRIBUTE_OCTET_STRING_VALUE>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.AUTHZ_SECURITY_ATTRIBUTE_OCTET_STRING_VALUE>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
@C
public struct AUTHZ_SOURCE_SCHEMA_REGISTRATION {
    public var dwFlags: UInt32 = 0
    public var szEventSourceName: CPointer<UInt16> = CPointer<UInt16>()
    public var szEventMessageFile: CPointer<UInt16> = CPointer<UInt16>()
    public var szEventSourceXmlSchemaFile: CPointer<UInt16> = CPointer<UInt16>()
    public var szEventAccessStringsFile: CPointer<UInt16> = CPointer<UInt16>()
    public var szExecutableImagePath: CPointer<UInt16> = CPointer<UInt16>()
    public var Anonymous: AUTHZ_SOURCE_SCHEMA_REGISTRATION__Anonymous_e__Union = AUTHZ_SOURCE_SCHEMA_REGISTRATION__Anonymous_e__Union()
    public var dwObjectTypeNameCount: UInt32 = 0
    public var ObjectTypeNames: VArray<Win32__impl_l0.AUTHZ_REGISTRATION_OBJECT_TYPE_NAME_OFFSET, $1> = VArray<Win32__impl_l0.AUTHZ_REGISTRATION_OBJECT_TYPE_NAME_OFFSET, $1>(repeat: Win32__impl_l0.AUTHZ_REGISTRATION_OBJECT_TYPE_NAME_OFFSET())
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct AUTHZ_SOURCE_SCHEMA_REGISTRATION__Anonymous_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop pReserved: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pProviderGuid: CPointer<GUID> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<GUID>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<GUID>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
@C
public struct EXPLICIT_ACCESS_A {
    public var grfAccessPermissions: UInt32 = 0
    public var grfAccessMode: Int32 = 0
    public var grfInheritance: UInt32 = 0
    public var Trustee: TRUSTEE_A = TRUSTEE_A()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
@C
public struct EXPLICIT_ACCESS_W {
    public var grfAccessPermissions: UInt32 = 0
    public var grfAccessMode: Int32 = 0
    public var grfInheritance: UInt32 = 0
    public var Trustee: TRUSTEE_W = TRUSTEE_W()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
public type FN_PROGRESS = CFunc<(CPointer<UInt16>, UInt32, CPointer<Int32>, CPointer<Unit>, Int32) -> Unit>

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
@C
public struct OBJECTS_AND_NAME_A {
    public var ObjectsPresent: UInt32 = 0
    public var ObjectType: Int32 = 0
    public var ObjectTypeName: CPointer<UInt8> = CPointer<UInt8>()
    public var InheritedObjectTypeName: CPointer<UInt8> = CPointer<UInt8>()
    public var ptstrName: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
@C
public struct OBJECTS_AND_NAME_W {
    public var ObjectsPresent: UInt32 = 0
    public var ObjectType: Int32 = 0
    public var ObjectTypeName: CPointer<UInt16> = CPointer<UInt16>()
    public var InheritedObjectTypeName: CPointer<UInt16> = CPointer<UInt16>()
    public var ptstrName: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
public type PFN_AUTHZ_COMPUTE_DYNAMIC_GROUPS = CFunc<(Win32__impl_l0.AUTHZ_CLIENT_CONTEXT_HANDLE, CPointer<Unit>, CPointer<CPointer<Win32_Security.SID_AND_ATTRIBUTES>>, CPointer<UInt32>, CPointer<CPointer<Win32_Security.SID_AND_ATTRIBUTES>>, CPointer<UInt32>) -> Int32>

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
public type PFN_AUTHZ_DYNAMIC_ACCESS_CHECK = CFunc<(Win32__impl_l0.AUTHZ_CLIENT_CONTEXT_HANDLE, CPointer<Win32_Security.ACE_HEADER>, CPointer<Unit>, CPointer<Int32>) -> Int32>

@When[Windows_Win32_Security_Authorization == "on" && Windows_Win32_Security == "on"]
public type PFN_AUTHZ_GET_CENTRAL_ACCESS_POLICY = CFunc<(Win32__impl_l0.AUTHZ_CLIENT_CONTEXT_HANDLE, Win32_Security.PSID, CPointer<Unit>, CPointer<Int32>, CPointer<CPointer<Unit>>) -> Int32>

@When[Windows_Win32_Security_Authorization == "on"]
@C
public struct TRUSTEE_A {
    public var pMultipleTrustee: CPointer<TRUSTEE_A> = CPointer<TRUSTEE_A>()
    public var MultipleTrusteeOperation: Int32 = 0
    public var TrusteeForm: Int32 = 0
    public var TrusteeType: Int32 = 0
    public var ptstrName: CPointer<UInt8> = CPointer<UInt8>()
    public init() {}
}

@When[Windows_Win32_Security_Authorization == "on"]
@C
public struct TRUSTEE_W {
    public var pMultipleTrustee: CPointer<TRUSTEE_W> = CPointer<TRUSTEE_W>()
    public var MultipleTrusteeOperation: Int32 = 0
    public var TrusteeForm: Int32 = 0
    public var TrusteeType: Int32 = 0
    public var ptstrName: CPointer<UInt16> = CPointer<UInt16>()
    public init() {}
}
