// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.Win32._impl_l1

import windows_polyfill.{packedReadUInt32, packedReadValue, packedWriteUInt32, packedWriteValue, unionReadInt8, unionReadUInt16, unionWriteInt8, unionWriteUInt16}
import windows_polyfill as windows_polyfill
import windows_sys.Win32._impl_l0 as Win32__impl_l0


@When[Windows_Win32_System_Console == "on"]
@C
public struct ALLOC_CONSOLE_OPTIONS {
    public var mode: Int32 = 0
    public var useShowWindow: Int32 = 0
    public var showWindow: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct CONSOLE_FONT_INFO {
    public var nFont: UInt32 = 0
    public var dwFontSize: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct CONSOLE_FONT_INFOEX {
    public var cbSize: UInt32 = 0
    public var nFont: UInt32 = 0
    public var dwFontSize: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public var FontFamily: UInt32 = 0
    public var FontWeight: UInt32 = 0
    public var FaceName: VArray<UInt16, $32> = VArray<UInt16, $32>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct CONSOLE_SCREEN_BUFFER_INFO {
    public var dwSize: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public var dwCursorPosition: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public var wAttributes: UInt16 = 0
    public var srWindow: Win32__impl_l0.SMALL_RECT = Win32__impl_l0.SMALL_RECT()
    public var dwMaximumWindowSize: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct CONSOLE_SCREEN_BUFFER_INFOEX {
    public var cbSize: UInt32 = 0
    public var dwSize: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public var dwCursorPosition: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public var wAttributes: UInt16 = 0
    public var srWindow: Win32__impl_l0.SMALL_RECT = Win32__impl_l0.SMALL_RECT()
    public var dwMaximumWindowSize: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public var wPopupAttributes: UInt16 = 0
    public var bFullscreenSupported: Int32 = 0
    public var ColorTable: VArray<UInt32, $16> = VArray<UInt32, $16>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct CONSOLE_SELECTION_INFO {
    public var dwFlags: UInt32 = 0
    public var dwSelectionAnchor: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public var srSelection: Win32__impl_l0.SMALL_RECT = Win32__impl_l0.SMALL_RECT()
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct ExtKeyDef {
    public var keys: VArray<Win32__impl_l0.ExtKeySubst, $3> = VArray<Win32__impl_l0.ExtKeySubst, $3>(repeat: Win32__impl_l0.ExtKeySubst())
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
// packed(2)
@C
public struct ExtKeyDefBuf {
    public var _raw: VArray<UInt8, $476> = VArray<UInt8, $476>(repeat: 0u8)

    public mut prop dwVersion: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop dwCheckSum: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop table: VArray<ExtKeyDef, $26> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<ExtKeyDef, $26> = VArray<ExtKeyDef, $26>(repeat: ExtKeyDef())
                return packedReadValue<VArray<ExtKeyDef, $26>>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct INPUT_RECORD {
    public var EventType: UInt16 = 0
    public var Event: INPUT_RECORD__Event_e__Union = INPUT_RECORD__Event_e__Union()
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct INPUT_RECORD__Event_e__Union {
    public var _raw: VArray<UInt32, $4> = VArray<UInt32, $4>(repeat: 0)

    public mut prop keyEvent: Win32__impl_l0.KEY_EVENT_RECORD {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.KEY_EVENT_RECORD>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.KEY_EVENT_RECORD>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop mouseEvent: MOUSE_EVENT_RECORD {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MOUSE_EVENT_RECORD>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<MOUSE_EVENT_RECORD>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop windowBufferSizeEvent: WINDOW_BUFFER_SIZE_RECORD {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WINDOW_BUFFER_SIZE_RECORD>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<WINDOW_BUFFER_SIZE_RECORD>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop menuEvent: Win32__impl_l0.MENU_EVENT_RECORD {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.MENU_EVENT_RECORD>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.MENU_EVENT_RECORD>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop focusEvent: Win32__impl_l0.FOCUS_EVENT_RECORD {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.FOCUS_EVENT_RECORD>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.FOCUS_EVENT_RECORD>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct MOUSE_EVENT_RECORD {
    public var dwMousePosition: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public var dwButtonState: UInt32 = 0
    public var dwControlKeyState: UInt32 = 0
    public var dwEventFlags: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_Console == "on"]
@C
public struct WINDOW_BUFFER_SIZE_RECORD {
    public var dwSize: Win32__impl_l0.COORD = Win32__impl_l0.COORD()
    public init() {}
}
