// Auto-generated by windows-bindgen. Do not edit.
package windows_sys.Win32._impl_l1

import windows_interface.{GUID}
import windows_interface as windows_interface
import windows_polyfill.{packedReadInt16, packedReadInt32, packedReadInt8, packedReadUInt16, packedReadUInt32, packedReadUInt64, packedReadUInt8, packedReadValue, packedWriteInt16, packedWriteInt32, packedWriteInt8, packedWriteUInt16, packedWriteUInt32, packedWriteUInt64, packedWriteUInt8, packedWriteValue, unionReadInt16, unionReadInt32, unionReadInt64, unionReadInt8, unionReadUInt16, unionReadUInt32, unionReadUInt64, unionReadUInt8, unionWriteInt16, unionWriteInt32, unionWriteInt64, unionWriteInt8, unionWriteUInt16, unionWriteUInt32, unionWriteUInt64, unionWriteUInt8}
import windows_polyfill as windows_polyfill
import windows_sys.Win32._impl_l0 as Win32__impl_l0


@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct DRIVER_RUNTIME_REPORT {
    public var Header: Win32__impl_l0.RUNTIME_REPORT_HEADER = Win32__impl_l0.RUNTIME_REPORT_HEADER()
    public var NumberOfDrivers: UInt16 = 0
    public var Flags: DRIVER_RUNTIME_REPORT__Flags_e__Union = DRIVER_RUNTIME_REPORT__Flags_e__Union()
    public var DriverEntries: VArray<Win32__impl_l0.DRIVER_INFO_ENTRY, $1> = VArray<Win32__impl_l0.DRIVER_INFO_ENTRY, $1>(repeat: Win32__impl_l0.DRIVER_INFO_ENTRY())
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct DRIVER_RUNTIME_REPORT__Flags_e__Union {
    public var _raw: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0)

    public mut prop anonymous: DRIVER_RUNTIME_REPORT__Flags_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DRIVER_RUNTIME_REPORT__Flags_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<DRIVER_RUNTIME_REPORT__Flags_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop asUInt16: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct DRIVER_RUNTIME_REPORT__Flags_e__Union__Anonymous_e__Struct {
    public var _bitfield: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
@C
public struct GDI_NONREMOTE {
    public var fContext: Int32 = 0
    public var u: GDI_NONREMOTE__u_e__Struct = GDI_NONREMOTE__u_e__Struct()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct GDI_NONREMOTE__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Win32__impl_l0.DWORD_BLOB> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.DWORD_BLOB>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.DWORD_BLOB>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct IMAGE_AUX_SYMBOL {
    public var _raw: VArray<UInt16, $9> = VArray<UInt16, $9>(repeat: 0)

    public mut prop sym: IMAGE_AUX_SYMBOL__Sym_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Sym_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Sym_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop file: IMAGE_AUX_SYMBOL__File_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__File_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__File_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop section: IMAGE_AUX_SYMBOL__Section_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Section_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Section_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop tokenDef: Win32__impl_l0.IMAGE_AUX_SYMBOL_TOKEN_DEF {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.IMAGE_AUX_SYMBOL_TOKEN_DEF>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<Win32__impl_l0.IMAGE_AUX_SYMBOL_TOKEN_DEF>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop crc: IMAGE_AUX_SYMBOL__CRC_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__CRC_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__CRC_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// packed(2)
@C
public struct IMAGE_AUX_SYMBOL__Sym_e__Struct {
    public var _raw: VArray<UInt8, $18> = VArray<UInt8, $18>(repeat: 0u8)

    public mut prop tagIndex: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop misc: IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union = IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union()
                return packedReadValue<IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop fcnAry: IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union = IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union()
                return packedReadValue<IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop tvIndex: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 16, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union {
    public var _raw: VArray<UInt16, $2> = VArray<UInt16, $2>(repeat: 0)

    public mut prop lnSz: IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union__LnSz_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union__LnSz_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union__LnSz_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop totalSize: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct IMAGE_AUX_SYMBOL__Sym_e__Struct__Misc_e__Union__LnSz_e__Struct {
    public var Linenumber: UInt16 = 0
    public var Size: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union {
    public var _raw: VArray<UInt16, $4> = VArray<UInt16, $4>(repeat: 0)

    public mut prop function: IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union__Function_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union__Function_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union__Function_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop array: IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union__Array_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union__Array_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union__Array_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// packed(2)
@C
public struct IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union__Function_e__Struct {
    public var _raw: VArray<UInt8, $8> = VArray<UInt8, $8>(repeat: 0u8)

    public mut prop pointerToLinenumber: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop pointerToNextFunction: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct IMAGE_AUX_SYMBOL__Sym_e__Struct__FcnAry_e__Union__Array_e__Struct {
    public var Dimension: VArray<UInt16, $4> = VArray<UInt16, $4>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct IMAGE_AUX_SYMBOL__File_e__Struct {
    public var Name: VArray<UInt8, $18> = VArray<UInt8, $18>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// packed(2)
@C
public struct IMAGE_AUX_SYMBOL__Section_e__Struct {
    public var _raw: VArray<UInt8, $18> = VArray<UInt8, $18>(repeat: 0u8)

    public mut prop length: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop numberOfRelocations: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop numberOfLinenumbers: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 6)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 6, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop checkSum: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop number: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt16(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt16(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop selection: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 14)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 14, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop bReserved: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 15)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 15, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop highNumber: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt16(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt16(base, 16, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// packed(2)
@C
public struct IMAGE_AUX_SYMBOL__CRC_e__Struct {
    public var _raw: VArray<UInt8, $18> = VArray<UInt8, $18>(repeat: 0u8)

    public mut prop crc: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rgbReserved: VArray<UInt8, $14> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt8, $14> = VArray<UInt8, $14>(repeat: 0)
                return packedReadValue<VArray<UInt8, $14>>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct IMAGE_AUX_SYMBOL_EX {
    public var _raw: VArray<UInt16, $10> = VArray<UInt16, $10>(repeat: 0)

    public mut prop sym: IMAGE_AUX_SYMBOL_EX__Sym_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__Sym_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__Sym_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop file: IMAGE_AUX_SYMBOL_EX__File_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__File_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__File_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop section: IMAGE_AUX_SYMBOL_EX__Section_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__Section_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__Section_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: IMAGE_AUX_SYMBOL_EX__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop crc: IMAGE_AUX_SYMBOL_EX__CRC_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__CRC_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<IMAGE_AUX_SYMBOL_EX__CRC_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// packed(2)
@C
public struct IMAGE_AUX_SYMBOL_EX__Sym_e__Struct {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    public mut prop weakDefaultSymIndex: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop weakSearchType: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rgbReserved: VArray<UInt8, $12> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt8, $12> = VArray<UInt8, $12>(repeat: 0)
                return packedReadValue<VArray<UInt8, $12>>(base, 8, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 8, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct IMAGE_AUX_SYMBOL_EX__File_e__Struct {
    public var Name: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// packed(2)
@C
public struct IMAGE_AUX_SYMBOL_EX__Section_e__Struct {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    public mut prop length: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop numberOfRelocations: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 4)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 4, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop numberOfLinenumbers: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt16(base, 6)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt16(base, 6, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop checkSum: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 8)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 8, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop number: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt16(base, 12)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt16(base, 12, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop selection: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 14)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 14, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop bReserved: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt8(base, 15)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt8(base, 15, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop highNumber: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadInt16(base, 16)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteInt16(base, 16, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rgbReserved: VArray<UInt8, $2> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)
                return packedReadValue<VArray<UInt8, $2>>(base, 18, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 18, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct IMAGE_AUX_SYMBOL_EX__Anonymous_e__Struct {
    public var TokenDef: Win32__impl_l0.IMAGE_AUX_SYMBOL_TOKEN_DEF = Win32__impl_l0.IMAGE_AUX_SYMBOL_TOKEN_DEF()
    public var rgbReserved: VArray<UInt8, $2> = VArray<UInt8, $2>(repeat: 0)
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// packed(2)
@C
public struct IMAGE_AUX_SYMBOL_EX__CRC_e__Struct {
    public var _raw: VArray<UInt8, $20> = VArray<UInt8, $20>(repeat: 0u8)

    public mut prop crc: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return packedReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop rgbReserved: VArray<UInt8, $16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let defaultValue: VArray<UInt8, $16> = VArray<UInt8, $16>(repeat: 0)
                return packedReadValue<VArray<UInt8, $16>>(base, 4, defaultValue)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                packedWriteValue(base, 4, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct IMAGE_POLICY_ENTRY {
    public var Type: Int32 = 0
    public var PolicyId: Int32 = 0
    public var u: IMAGE_POLICY_ENTRY__u_e__Union = IMAGE_POLICY_ENTRY__u_e__Union()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct IMAGE_POLICY_ENTRY__u_e__Union {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop none: CPointer<Unit> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Unit>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop boolValue: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt8(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt8(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop int8Value: Int8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt8(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt8(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uInt8Value: UInt8 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt8(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt8(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop int16Value: Int16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uInt16Value: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop int32Value: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uInt32Value: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop int64Value: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop uInt64Value: UInt64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop ansiStringValue: CPointer<UInt8> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt8>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop unicodeStringValue: CPointer<UInt16> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<UInt16>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct IMAGE_POLICY_METADATA {
    public var Version: UInt8 = 0
    public var Reserved0: VArray<UInt8, $7> = VArray<UInt8, $7>(repeat: 0)
    public var ApplicationId: UInt64 = 0
    public var Policies: VArray<IMAGE_POLICY_ENTRY, $1> = VArray<IMAGE_POLICY_ENTRY, $1>(repeat: IMAGE_POLICY_ENTRY())
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && (arch == "aarch64" && Windows_Win32_System_Diagnostics_Debug == "on" || arch == "x86_64" && Windows_Win32_System_Diagnostics_Debug == "on")]
public type POUT_OF_PROCESS_FUNCTION_TABLE_CALLBACK = CFunc<(CPointer<Unit>, CPointer<Unit>, CPointer<UInt32>, CPointer<CPointer<Unit>>) -> UInt32>

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct POWER_LIMIT_ATTRIBUTES {
    public var Type: Int32 = 0
    public var DomainId: UInt32 = 0
    public var MaxValue: UInt32 = 0
    public var MinValue: UInt32 = 0
    public var MinTimeParameter: UInt32 = 0
    public var MaxTimeParameter: UInt32 = 0
    public var DefaultACValue: UInt32 = 0
    public var DefaultDCValue: UInt32 = 0
    public var Flags: POWER_LIMIT_ATTRIBUTES__Flags_e__Union = POWER_LIMIT_ATTRIBUTES__Flags_e__Union()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct POWER_LIMIT_ATTRIBUTES__Flags_e__Union {
    public var _raw: VArray<UInt32, $1> = VArray<UInt32, $1>(repeat: 0)

    public mut prop anonymous: POWER_LIMIT_ATTRIBUTES__Flags_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<POWER_LIMIT_ATTRIBUTES__Flags_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<POWER_LIMIT_ATTRIBUTES__Flags_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop asUlong: UInt32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct POWER_LIMIT_ATTRIBUTES__Flags_e__Union__Anonymous_e__Struct {
    public var _bitfield: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct POWER_LIMIT_VALUE {
    public var Type: Int32 = 0
    public var DomainId: UInt32 = 0
    public var TargetValue: UInt32 = 0
    public var TimeParameter: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct PROCESSOR_IDLESTATE_POLICY {
    public var Revision: UInt16 = 0
    public var Flags: PROCESSOR_IDLESTATE_POLICY__Flags_e__Union = PROCESSOR_IDLESTATE_POLICY__Flags_e__Union()
    public var PolicyCount: UInt32 = 0
    public var Policy: VArray<Win32__impl_l0.PROCESSOR_IDLESTATE_INFO, $3> = VArray<Win32__impl_l0.PROCESSOR_IDLESTATE_INFO, $3>(repeat: Win32__impl_l0.PROCESSOR_IDLESTATE_INFO())
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct PROCESSOR_IDLESTATE_POLICY__Flags_e__Union {
    public var _raw: VArray<UInt16, $1> = VArray<UInt16, $1>(repeat: 0)

    public mut prop asWORD: UInt16 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadUInt16(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteUInt16(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop anonymous: PROCESSOR_IDLESTATE_POLICY__Flags_e__Union__Anonymous_e__Struct {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<PROCESSOR_IDLESTATE_POLICY__Flags_e__Union__Anonymous_e__Struct>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<PROCESSOR_IDLESTATE_POLICY__Flags_e__Union__Anonymous_e__Struct>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct PROCESSOR_IDLESTATE_POLICY__Flags_e__Union__Anonymous_e__Struct {
    public var _bitfield: UInt16 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
public type PUMS_SCHEDULER_ENTRY_POINT = CFunc<(Int32, UIntNative, CPointer<Unit>) -> Unit>

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct QUOTA_LIMITS_EX {
    public var PagedPoolLimit: UIntNative = 0
    public var NonPagedPoolLimit: UIntNative = 0
    public var MinimumWorkingSetSize: UIntNative = 0
    public var MaximumWorkingSetSize: UIntNative = 0
    public var PagefileLimit: UIntNative = 0
    public var TimeLimit: Int64 = 0
    public var WorkingSetLimit: UIntNative = 0
    public var Reserved2: UIntNative = 0
    public var Reserved3: UIntNative = 0
    public var Reserved4: UIntNative = 0
    public var Flags: UInt32 = 0
    public var CpuRateLimit: Win32__impl_l0.RATE_QUOTA_LIMIT = Win32__impl_l0.RATE_QUOTA_LIMIT()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct SCRUB_PARITY_EXTENT_DATA {
    public var Size: UInt16 = 0
    public var Flags: UInt16 = 0
    public var NumberOfParityExtents: UInt16 = 0
    public var MaximumNumberOfParityExtents: UInt16 = 0
    public var ParityExtents: VArray<Win32__impl_l0.SCRUB_PARITY_EXTENT, $1> = VArray<Win32__impl_l0.SCRUB_PARITY_EXTENT, $1>(repeat: Win32__impl_l0.SCRUB_PARITY_EXTENT())
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct SERVERSILO_BASIC_INFORMATION {
    public var ServiceSessionId: UInt32 = 0
    public var State: Int32 = 0
    public var ExitStatus: UInt32 = 0
    public var Reserved: UInt8 = 0
    public var ApiSetSchema: CPointer<Unit> = CPointer<Unit>()
    public var HostApiSetSchema: CPointer<Unit> = CPointer<Unit>()
    public var ContainerBuildNumber: UInt32 = 0
    public var HostBuildNumber: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct SHARED_VIRTUAL_DISK_SUPPORT {
    public var SharedVirtualDiskSupport: Int32 = 0
    public var HandleState: Int32 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Power == "on"]
@C
public struct SYSTEM_POWER_SOURCE_STATE {
    public var BatteryState: Win32__impl_l0.SYSTEM_BATTERY_STATE = Win32__impl_l0.SYSTEM_BATTERY_STATE()
    public var InstantaneousPeakPower: UInt32 = 0
    public var InstantaneousPeakPeriod: UInt32 = 0
    public var SustainablePeakPower: UInt32 = 0
    public var SustainablePeakPeriod: UInt32 = 0
    public var PeakPower: UInt32 = 0
    public var MaxOutputPower: UInt32 = 0
    public var MaxInputPower: UInt32 = 0
    public var BatteryRateInCurrent: Int32 = 0
    public var BatteryVoltage: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct TAPE_GET_DRIVE_PARAMETERS {
    public var ECC: UInt8 = 0
    public var Compression: UInt8 = 0
    public var DataPadding: UInt8 = 0
    public var ReportSetmarks: UInt8 = 0
    public var DefaultBlockSize: UInt32 = 0
    public var MaximumBlockSize: UInt32 = 0
    public var MinimumBlockSize: UInt32 = 0
    public var MaximumPartitionCount: UInt32 = 0
    public var FeaturesLow: UInt32 = 0
    public var FeaturesHigh: UInt32 = 0
    public var EOTWarningZoneSize: UInt32 = 0
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct TRANSACTION_ENLISTMENTS_INFORMATION {
    public var NumberOfEnlistments: UInt32 = 0
    public var EnlistmentPair: VArray<Win32__impl_l0.TRANSACTION_ENLISTMENT_PAIR, $1> = VArray<Win32__impl_l0.TRANSACTION_ENLISTMENT_PAIR, $1>(repeat: Win32__impl_l0.TRANSACTION_ENLISTMENT_PAIR())
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct TRANSACTION_LIST_INFORMATION {
    public var NumberOfTransactions: UInt32 = 0
    public var TransactionInformation: VArray<Win32__impl_l0.TRANSACTION_LIST_ENTRY, $1> = VArray<Win32__impl_l0.TRANSACTION_LIST_ENTRY, $1>(repeat: Win32__impl_l0.TRANSACTION_LIST_ENTRY())
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct TRANSACTION_SUPERIOR_ENLISTMENT_INFORMATION {
    public var SuperiorEnlistmentPair: Win32__impl_l0.TRANSACTION_ENLISTMENT_PAIR = Win32__impl_l0.TRANSACTION_ENLISTMENT_PAIR()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
@C
public struct remoteMETAFILEPICT {
    public var mm: Int32 = 0
    public var xExt: Int32 = 0
    public var yExt: Int32 = 0
    public var hMF: CPointer<userHMETAFILE> = CPointer<userHMETAFILE>()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
@C
public struct userHBITMAP {
    public var fContext: Int32 = 0
    public var u: userHBITMAP__u_e__Struct = userHBITMAP__u_e__Struct()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct userHBITMAP__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Win32__impl_l0.userBITMAP> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.userBITMAP>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.userBITMAP>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
@C
public struct userHENHMETAFILE {
    public var fContext: Int32 = 0
    public var u: userHENHMETAFILE__u_e__Struct = userHENHMETAFILE__u_e__Struct()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct userHENHMETAFILE__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Win32__impl_l0.BYTE_BLOB> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.BYTE_BLOB>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.BYTE_BLOB>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
@C
public struct userHGLOBAL {
    public var fContext: Int32 = 0
    public var u: userHGLOBAL__u_e__Struct = userHGLOBAL__u_e__Struct()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct userHGLOBAL__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Win32__impl_l0.FLAGGED_BYTE_BLOB> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.FLAGGED_BYTE_BLOB>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.FLAGGED_BYTE_BLOB>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
@C
public struct userHMETAFILE {
    public var fContext: Int32 = 0
    public var u: userHMETAFILE__u_e__Struct = userHMETAFILE__u_e__Struct()
    public init() {}
}

@When[Windows_Win32_System_SystemServices == "on" && Windows_Win32_System_Com == "on"]
// NOTE: union represented as size-equivalent VArray slot with typed `mut prop` accessors
// (Cangjie paradigm: `@C struct` cannot host a union variant; ABI footprint is preserved)
@C
public struct userHMETAFILE__u_e__Struct {
    public var _raw: VArray<UInt64, $1> = VArray<UInt64, $1>(repeat: 0)

    public mut prop hInproc: Int32 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt32(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt32(base, 0, val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hRemote: CPointer<Win32__impl_l0.BYTE_BLOB> {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.BYTE_BLOB>>(p)
                return ptr.read()
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                let p = base
                let ptr = CPointer<CPointer<Win32__impl_l0.BYTE_BLOB>>(p)
                ptr.write(val)
                _raw = rawStorage
            }
        }
    }

    public mut prop hInproc64: Int64 {
        get() {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                return unionReadInt64(base, 0)
            }
        }
        set(val) {
            unsafe {
                var rawStorage = _raw
                let base = CPointer<UInt8>(inout rawStorage)
                unionWriteInt64(base, 0, val)
                _raw = rawStorage
            }
        }
    }
    public init() {}
}
